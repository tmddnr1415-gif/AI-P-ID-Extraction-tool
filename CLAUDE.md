# 인수인계 — P&ID → 계기·밸브 리스트 추출

이 문서 하나로 다른 PC·다른 계정에서 이어받을 수 있게 쓴 것입니다. 코드보다 먼저
읽으세요. 상세 수치와 근거는 `README.md`, 설계 원본은 `docs/design.md` 입니다.

## 1. 지금 어디까지 와 있나

| 단계 | 상태 |
| --- | --- |
| Phase 0 검증 스파이크 (타이틀블록 · 계기 · 벤더마크 · 밸브) | 완료 |
| Phase 1 MVP (FastAPI + SQLite + 검토 UI + Excel 출력) | 완료 |
| 스코프 판정 · 출력 게이트 · 수정이력 수집 | 완료 |
| 배관 그래프 (Description 준비) | 완료했으나 **추적률 13.1%** 로 실용성 없음 |
| Description 생성 (거리 문형) | F1 74.6 · 완전일치 34 — ④(판정 불가) 618행이 이 문형을 유지 |
| **Description 판정축 (5회차)** | **혼합 적용** — 판정 성립 192행(①125 ②35 ③32)만 `FROM-TO/기기 직결` 문형, ④는 현행 유지. ④→② 전환율 **5.4%** (`docs/from_to_axis.md`) |
| **p6 육안 피드백 반영 (10회차)** | TW 계상 · 벤더 공급분 계상 · SCOPE 열 · TYPE 표기 · 라인 최근접 커넥터. **정밀도 84.1% → 70.5%** 이고 원인은 벤더 계상 하나 — 발주처 확인 대기 (`docs/round10_feedback.md`) |

**⚠ 아래 표는 9회차까지의 기준선이고, 10회차가 다섯 값을 옮겼습니다.**
10회차 값은 `docs/round10_feedback.md` §8 이 기준선이며, 그중 **정밀도 하락
13.6pp 는 전부 "벤더 공급분을 계상한다"는 요구 하나에서 나옵니다** (204행을
넣어 TP +1 · FP +136).  발주처가 계상하지 않기로 하면 config
`scope.exclusion_rules: glyph+text+box` 한 줄로 아래 표로 돌아갑니다.

| 항목 | 9회차 기준선 | 10회차 | 11회차 | 12회차 | 13회차 | 14회차 | 15회차 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `fingerprint` | 91ba3e33 | 3a57b6b2 | **69ad281f** (움직인 칸은 밸브 144행의 `scope` 뿐) | **69ad281f** (불변 — 화면만 고쳤습니다) | **69ad281f** (불변 · 편집 5칸 뒤에도 같음) | **69ad281f** (불변 — 엔진 파일 0개) | **69ad281f** (불변 · **세 경로 전부 같음** — 유도/재사용/강제유도) |
| 행 수 | 824 | **1029** | **1029** (불변) | 1029 | 1029 | 1029 | 1029 |
| Q'ty 합계 | 1595 | 1911 | 1911 | 1911 | 1911 | 1911 | 1911 |
| 등급 | 704 / 47 / 12 / 61 · SKIP 0 | **704 / 48 / 51 / 22 · SKIP 204** | 같음 | 같음 | 같음 | 같음 | 같음 |
| **정확도 — 축1 전량** | 99.2% / 84.1% | **99.3% / 70.5%** | 99.3% / 70.5% (불변) | 불변 | 불변 | 불변 | 불변 |
| **정확도 — 축2 SCT** (11회차 신설) | — | — | **99.2% / 86.8%** | 불변 | 불변 | 불변 | 불변 |
| 글리프 · Description F1 · 식별 가능성 · ④→② 전환율 | H4/M32 · 69.3 · 39.6% · 19.7% | **전부 불변** | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 |
| 테스트 | 빠른 116 | 빠른 130 | **빠른 137 · UI 21 · slow 10** | 빠른 **137** · UI 21 · slow 10 | 빠른 **150** · UI 21 · slow 10 (13회차 표에 149 로 적혀 있었으나 실측 150 — 14회차 [A] 에서 갱신) | 빠른 150 · **UI 24** · slow 10 (UI 는 **SCOPE 있는 데이터** 기준) | 빠른 **170** · UI 24 · slow 10 |
| 발주처 양식 행수 | 824 기준 | 885 / 76 / 23 | **667 / 66 / 22** (SCT 만 · CZF 25행은 양식 미수령) | 같음 | 같음 | 같음 | 같음 |

**축이 둘인 이유**: 축1(전량)은 "발주처가 안 세는 것을 얼마나 넣었나"까지 재고,
축2(SCT)는 "우리가 얼마나 잘 읽었나"를 잰다.  **어느 쪽도 혼자서는 답이 아니라
둘 다 남긴다** — 정의는 `spike/accuracy.py` 의 `AXES` 하나에 있고, 정답지 쪽
모수는 두 축에서 **같다**.

**확정 지표 (회귀 기준선 — 매 회차 재확인 필수)**

| 항목 | 값 |
| --- | --- |
| 계기 recall / precision | **97.0% / 87.0%** (검출 598 / Excel 536 / TP 520) |
| BFV recall / precision | **85.7% / 81.8%** |
| MOV recall / precision | **90.5% / 82.6%** |
| 액추에이터 | MOTOR 102 / PNEUMATIC 58 / HYDRAULIC 4 / **UNREAD 0** |
| 글리프 문자 | H 4 / M 32 |
| 행 수 | **824** (FIELD 680 / MOV 76 / PNEUMATIC 45 / BFV 23) — LS 묶음 합산 후 |
| **정확도 (교집합 도면 · 취소선 제외 · 항목 단위)** | **재현율 99.2% / 정밀도 84.1%** (사람 599 / 프로그램 706 / TP 594) |
| 분석 시간 | 58장 약 3분 40초 |
| 테스트 | 빠른 **116** · UI 20 · slow 10 (9회차 HEAD 실측 — 이 표에 오래 `95`·`113` 으로 적혀 있었습니다. 11회차 기준 **빠른 137 · UI 21**) |
| LS 다중 신호 | 34묶음 102버블 → **34행** (물리 1개). 발주처 LS 22행은 맞닿지 않아 **영향 0** |
| `fingerprint` | **7b34064e** — 5회차 혼합 적용이 옮겼습니다 (직전 a38fe8ac). 원인은 needs_review 뿐이고, 824행 전수 대조로 증명: 불변 칸 위반 0 · 움직인 행 = 적용 192행 · 움직인 칸 = description/remark/grade/needs_review 뿐 |

**Description 지표 (2026-08 회차)**

| 조각 | 값 | 비고 |
| --- | --- | --- |
| 발주처 557행 대조 | 완전일치 **34** · 부분 486 · 미달 0 · 대응행 없음 37 | 직전 34 / 484 / 0 / 39 |
| 토큰 정밀도 / 재현율 / F1 | **76.1% / 73.2% / 74.6** | 직전 76.1 / 73.1 / 74.6 |
| **주어 귀속** | 완전 **16.0%** (83/518) · 대부분 이상 **73.7%** | 직전 16.0% / 73.4% |
| 변수어 | **99.0%** (501/506) | |
| 기기명 | **77.9%** (285/366) | 변화 없음 — 도면번호는 머리 낱말이 아니라 꼬리였음 |
| 순번 | **42.0%** (113/269) | 직전 40.9% |
| 위치어 | 24.1% (38/158) — 오답 47 · 미부착 73 | 직전 23.4% |
| 중간 심볼 | **0%** (0/13) | 회수 불가 확정 — §3 참조 |
| 등급 | CONFIRMED **662** · PARTIAL 47 · LOW 12 · NONE 103 · SKIP **0** | 5회차 축 적용 행 192가 CONFIRMED (도면 근거 = 탭한 런과 끝점). 직전 610/58/12/144 |
| CONFIRMED 의 실제 일치 | 대조 가능한 **455행** 중 완전일치 27 · 주어 일치 338(74.3%) | §3 하단 — 이 등급은 "도면 근거 있음"이지 "발주처와 같음"이 아닙니다 |
| 사용자 입력 사유 표시 | **137행** | CCW 방향 104(7장) · 중간 심볼 33(10장). 검토 탭에서 사유별로 묶임 |
| 재사용 가능 범위 | 기기 라벨 **486건 중 379건(78.0%)** 이 범례 어휘만으로 잡힘 | 나머지 22%가 프로젝트 어휘·수식어 |
| 서로 다른 문장 | 523 | 발주처도 557행 중 110행(19.7%)이 같은 문장을 반복 |
| 목표 도면 p33 23행 | 재현 **13** · 부분 10 · 실패 0 | 직전 재현 12 |
| 표본 3계통 15행 | 재현 **7** · 부분 8 | 직전 재현 6 |

`fingerprint` 는 엔진 판정 전체의 해시입니다. 검출을 바꾸지 않았는데 바뀌면 원인을
반드시 규명하세요 (보통 `needs_review` 문구나 `legend` 기록이 늘어난 경우입니다).

**정확도는 다시 잴 수 있어야 회귀 항목입니다.** 표에 적어만 두면 UI 를 고치다
파이프라인이 흔들려도 알아차리지 못합니다. 절차는 `spike/accuracy.py` 에 있습니다:

```bash
python3 spike/accuracy.py                 # 파이프라인을 직접 돌려서
python3 spike/accuracy.py run.json        # 이미 있는 결과 json 으로
```

모수는 교집합 도면 · 취소선 제외 · P&ID No. 가 빈 행 제외이고, 일치 수는 칸마다
`min(사람, 프로그램)` 입니다 — 발주처 리스트에 좌표가 없고 우리 행에 발주처 NO 가
없어 **1:1 대응이 성립하지 않기 때문**입니다. `data/*.xlsx` 가 없는 기계에서는
그렇게 말하고 멈춥니다.

## 2. 진행 원칙 — 어기면 되돌려야 합니다

1. **범례 유도 우선.** 판정에 쓰는 값은 문서의 Symbol & Legend 시트에서 런타임에
   측정합니다. 코드에 치수를 박지 않습니다.
2. **임의값 금지.** "90pt 반경", "10pt 최소 런" 같은 값은 근거가 없으면 쓰지
   않습니다. 필요하면 그 문서에서 분포를 재고 백분위를 쓰되 분포를 함께 보고합니다.
3. **근거 없는 값 생성 금지.** 도면에 없는 낱말은 만들지 않습니다. 비워 두고 사유를
   남깁니다 (`NEEDS_REVIEW`, Remark, 근거 패널).
4. **회귀 확인 필수.** 위 표의 수치가 하나라도 움직이면 원인을 보고하고 되돌립니다.
5. **검출에 LLM 금지.** 심볼 검출·수량·스코프는 규칙만 씁니다. LLM 은 Description
   중간 서술 *선택*에만, 기본 꺼짐으로 존재합니다
   (`tests/test_description.py` 가 검출 모듈의 import 문을 검사해 강제).
6. **측정 정의 변경 금지.** 분모·대조 집합을 바꿔 수치를 좋게 만들지 않습니다.
7. **발주처 리스트는 검증 전용.** 실사용 입력은 PDF + 빈 양식뿐입니다
   (`PID_VERIFY_EXCEL` 로만 대조 모드가 켜집니다).

## 3. 실패한 접근과 이유 — 다시 시도하지 마세요

| 접근 | 결과 | 왜 |
| --- | --- | --- |
| 배관 그래프로 Description 근거 확보 | 추적 성공 **13.1%** | 페이지당 약 320조각으로 분절. 원인은 좌표 오차가 아니라 최소 런 16.97pt 미만 스터브가 버려지는 것. 낮추면 조각이 폭발하고 F1 개선은 +4.2pp 뿐 |
| 근접 텍스트 반경으로 중간 서술 수집 | 정밀도 57.3% → **17.6%** | 240pt 원 안에 도면번호·DN 치수·그리드 좌표·ASME 코드가 들어옴 |
| LLM 선택기로 중간 서술 선택 | 30행 중 **채택 4 / 보류 26** | 모델이 아니라 후보가 부족. 후보의 대부분이 옆 계기 태그나 배관 치수 |
| 범례 기기 형상으로 도면 기기 검출 | 펌프 **0개** 검출, 5×10 잡음 161건 | 단일 축척이 없음 (범례 40.5×38.5 ↔ p26 46.2×44.0, 비율 ×0.42~×2.00 산포) |
| 범례 STRAINER 형상으로 중간 심볼 검출 | 글자 외곽선만 매칭 | 같은 이유 |
| 기기 라벨을 노운 단위로 붙이기 (위치어를 기기 명사로 판단) | PI·TI 에 없는 SUCTION 이 붙음 | 위치어는 **기기가 아니라 계기 TYPE** 이 정함 (PIT 70.8% ↔ PI 6.3%) |
| 같은 베이스라인의 낱말 간격이 행간(11.8pt)보다 넓으면 라벨을 자르기 | 재현율 −4.3pp / 정밀도 +3.4pp, 완전일치 21→15 | `#10 ST HYDRAULIC OIL COOLER` 가 `HYDRAULIC` + `COOLER` 로 잘림. **간격이 아니라 반복 낱말로 자르면 해결됨** (§3 하단) |
| 여백(빈 띠)으로 기기 무리 경계를 잡기 | p33 펌프 열에서 20pt 이상 빈 띠가 9곳, 실제 경계(y≈600)는 두드러지지 않음 | 경계와 무관한 빈 띠가 같은 크기로 흩어져 있음 |
| 라벨이 자기 위쪽 띠를 소유한다고 보기 | 주어 완전일치 9.3%→**8.2%**, 대부분이상 51.3%→**45.1%** | 펌프 열에서는 맞지만 탱크 열에서 틀림 — 탱크 라벨은 무리 **가운데**에 인쇄됨 |
| 유클리드 최근접 / 같은 열 안 y 최근접 | 각각 대부분이상 47.8% · 43.4% | 현행 체비쇼프 최근접(51.3%)보다 나쁨. **기하학적 무리 판정은 이 도면에서 답이 아님** |
| 계통 어구를 낱말 단위로 빼기 (전체 어구 통째 제거 대신) | 완전일치 30 → **10**, 주어 완전 70 → 39 | `FUEL OIL SUPPLY` 에서 두 낱말만 빼면 `SUPPLY` 가 남는데 발주처는 그렇게 쓰지 않음. **통째로 빼는 쪽이 맞음** (두 번 측정해 확인) |
| 도면 제목 낱말로 기기 후보 순위 뒤집기 | 15건 중 **13건이 동점**, 1건은 오답 쪽 | 제목은 그 시트의 모든 기기에 똑같이 걸림 |
| 라벨 글자 크기로 주·부속 기기 구분 | 높이 분포가 14/25/29/37 — **줄 수**일 뿐 | 폰트 크기는 한 종류 |
| 판정 트리를 그래프 순회로 완성 (5회차 진단) | 무제한 순회로도 FROM·TO 짝 **10.4%** (도달 40.0%) | 선분이 엘보·버려진 스터브에서 끊김 (끝점→직교 런 간격 p50 21.4pt > min_run 16.97). 배관 그래프 13.1% · 국소 연결 4.9% 에 이은 **세 번째 실측** — §2.3 순회 금지가 옳았다는 증거. `docs/from_to_axis.md` |

**대신 통하는 것**: 기기는 **도면이 인쇄한 이름**으로 찾습니다(332건/47장). 문형과
낱말 규칙은 **발주처 557행에서 센 값**을 config 에 근거 수치와 함께 적습니다.

**이번 회차에 통한 것 — 주어 귀속 (완전 5.2% → 7.9%, 대부분이상 49.4% → 60.0%)**

1. **나란히 인쇄된 라벨을 반복 낱말로 자릅니다.** 한 베이스라인에 이미 나온 낱말이
   다시 나오면 거기서 다음 라벨이 시작합니다 (`#10 ST` 가 네 번 인쇄되면 쿨러 네
   대). 한 라벨을 여는 낱말이 다른 조각 안에 있으면 거기서 또 자릅니다. 행간은
   **문서 전체에서** 재고 (글자 높이를 하한으로) 15.3pt — 페이지별로 재면 p37 처럼
   글줄이 36개뿐인 시트에서 최빈 간격이 2.0pt 가 되어 세 줄짜리 이름이 붙지 않습니다.
   결과: `CLOSED COOLING WATER OIL COOLER OIL COOLER` → `#10 ST HYDRAULIC OIL COOLER A`.
2. **계통 접두어** (`AUX` / `GT` / `HP` / `LP` / `ST LUBE` …). 한 시트에서 꼬리가
   같고 머리만 다른 이름들만 한 묶음으로 보고, 계기가 **그 기기보다 더 가까이 있는**
   `TO` / `FROM` 문구에 그 묶음의 접두어가 **정확히 하나** 나올 때만 주어를 바꿉니다.
   전 도면 3행에서 발동하며, 목표 문장 2번(`GT ... DISCHARGE PRESSURE A`)이 그 3행
   중 하나입니다. 기기 라벨 자체의 텍스트까지 읽으면 접두어가 둘이 되어 절대
   발동하지 않습니다 — `TO`/`FROM` 문구만 읽습니다.
3. **유닛 번호는 계기 옆에 인쇄된 것**을 씁니다. 발주처 438행 중 시트 코드가 맞는
   것 232 · 가까운 표기가 맞는 것 173 · 가까운 표기가 깨뜨리는 것 31 → **순증 142**.
   단 시트 코드가 `00` 인 도면은 발주처가 접두어를 아예 안 쓰므로 덮어쓰지 않습니다.
4. **경보 접미** `LSHH` → `LEVEL HIGH HIGH` (11행) · `LSH` → `LEVEL HIGH` (11행).
   범례 p3 의 후속문자표에는 H·L 수식자가 **없습니다**. `LSL` 은 발주처 리스트에
   0행이라 `LOW` 는 **근거 없는 완성**으로 config 에 그렇게 표시해 뒀습니다.
5. **위·아래 위치어**는 주어를 고친 뒤 다시 세니 과반이 나왔습니다:
   `PIT ABOVE → DISCHARGE` 21/35, `PDIT ABOVE → SUCTION` 6/10, `PDIT LEFT → SUCTION`
   18/26. n≥10 이고 한 낱말이 과반인 칸은 이 셋뿐이고, 나머지 칸은 전부 최빈이
   "아무 낱말도 없음"(PI ABOVE 29/29)이라 아무것도 붙이지 않습니다.

**그 다음 회차에 통한 것 — 발주처가 실제로 쓰는 표기 (F1 63.6 → 71.8)**

1. **라인 문구(`TO ...`)는 LS 전용입니다.** 발주처 557행 중 라인 문구를 쓴 행은
   **36행(6.5%)** 이고 그중 **22행이 LS — LS 전체(22/22)** 입니다. 나머지 535행에서는
   14행(2.6%)뿐이고 그 14행은 시트마다 제각각이라 조건이 유도되지 않습니다
   (PDIT 0/52 · TI 0/59 · LIT 0/29 · LI 0/10 · FS 0/3). 그래서 기기를 못 찾은
   행에는 **아무 문구도 넣지 않고** 규칙만으로 된 문장(UNIT + 계통 + 변수어)을
   남깁니다 — 162행이 여기 해당하고 등급은 PARTIAL 입니다.
   `--without` 측정: **F1 69.2 → 71.8 · 완전일치 25 → 32 · 정밀도 67.1 → 73.4 ·
   재현율 71.4 → 70.3**. 위치어와 같은 판단(안 붙이는 쪽이 낫다)이 그대로 성립합니다.
2. **제목 어구의 발주처 약어.** 각 제목 어구의 머리글자를 만들어 그 도면의 발주처
   문장에서 두 형태를 세면 **두 쌍만** 나옵니다: `CLOSED COOLING WATER` → **CCW**
   (127행, 풀어 쓴 행 0), `AUX. COOLING WATER` → **ACW** (4행). 나머지 제목은 전부
   그대로 씁니다 (HP STEAM 12/12 · CRH STEAM 11/11 · CLEAN DRAIN 9/9). 반대 방향
   (약어를 풀어 쓰는 경우)은 **0건**. `--without` 측정: **F1 65.8 → 71.8**
   (정밀도 65.3 → 73.4) — 이번 회차 최대 기여.
3. **계통 자체가 갖는 위치어.** CCW 도면의 **TI 는 54행 중 51행이 RETURN** 이고
   SUPPLY 는 0행입니다 (그려진 위치와 무관). 반면 **PI 는 SUPPLY 49 / RETURN 51**
   로 정확히 반반이고 방향 칸 안에서도 반반이라(BELOW 31/30, ABOVE 15/13)
   기하로는 구분되지 않습니다 → PI 에는 붙이지 않습니다.
   `--without` 측정: **F1 71.0 → 71.8**.

**그 다음 회차에 통한 것 — 기기 어휘 확장 (F1 71.8 → 72.9, 주어 대부분이상 62.4 → 69.1)**

1. **범례 EQUIPMENT 표 밖의 낱말 세 개를 추가했습니다** — `VALVE` · `HEADER` ·
   `BOX`. 조건은 둘을 동시에 만족할 것: **발주처가 그 낱말로 기기를 부르고**
   (VALVE 로 끝나는 주어 38행 + 포함 27 · HEADER 22+10 · BOX 4+7),
   **도면이 그 낱말을 인쇄할 것** (VALVE 87회/21장 · HEADER 38회/18장 · BOX 23회/10장).
   범례에 없는 이유는 밸브는 별도 표가 있고 헤더는 배관이기 때문입니다.
2. **다만 낱말만으로는 손해였습니다.** 무제한으로 넣으면 F1 71.9 · 완전일치 30 ·
   기기명 257 로, 어휘를 안 넣은 쪽(71.8 / 32 / 287)보다 나쁩니다. `DRAIN HEADER`
   (도면 39회) 같은 배관 주석이 거리로 정답을 눌렀기 때문입니다
   (`WASTE OIL TANK LEVEL` → `FUEL OIL SUPPLY DRAIN HEADER LEVEL`).
3. **해결은 수식어 제한입니다.** 발주처가 그 낱말 앞에 실제로 쓰는 낱말만 인정합니다:
   VALVE 앞 `BYPASS` 54 · `LETDOWN` 3 · `CONTROL` 3 · `MODULATING` 2 (도면의
   `BRETHER` 42 · `OFF` 59 · `VENT` 29 · `SHUTOFF` 34 는 발주처가 **한 번도** 안 씀),
   HEADER 앞 `DISCHARGE` 16 · `STEAM` 6 · `SEAL` 4 · `RING` 3 (도면의 `DRAIN` 39 제외).
   결과: **F1 72.9 · 완전일치 33 · 주어 대부분이상 69.1% · CONFIRMED 642** 로 세 지표
   모두 어휘 추가 전보다 좋아집니다.
4. **기기 종류가 위치어를 정합니다.** 발주처는 TANK 59행 **전부**에 위치어를 안 쓰고
   (좌우 규칙은 탱크에 INLET 을 붙이고 있었습니다), VALVE 에는 `DOWNSTREAM` 30/65,
   HEADER 에는 `DISCHARGE` 16/32 를 씁니다. PUMP·COOLER 는 양쪽을 다 쓰므로
   (DISCHARGE 70/SUCTION 45, RETURN 86/SUPPLY 41) 좌우 기하에 맡깁니다.

**그 다음 회차에 통한 것 — 업계 표준 약어 층 (F1 72.9 → 74.3)**

규칙이 이제 **세 층**입니다. 새 프로젝트에 넘길 때 무엇을 가져가고 무엇을 다시 재야
하는지가 파일로 갈려 있습니다.

| 층 | 위치 | 내용 |
| --- | --- | --- |
| ① 업계 표준 | `config/plant_standard_abbr.yaml` | 발전 플랜트면 어디나 같은 약어. **방향을 고정하지 않음.** `confirmed` 만 적용되고 `candidates` 는 확인 대기 목록 |
| ② 표기 선택 | `config/project_alnouf1.yaml` `equipment_alias_choice` | 이 발주처가 ①의 어느 형태를 쓰는가 (557행에서 계수) |
| ③ 프로젝트 고유 | 같은 파일의 나머지 `description` 항목 | ①에 없는 1대1 대응 |

1. **확정된 세트는 하나뿐입니다** — `boiler_feedwater_pump`
   (`BFP` / `BOILER FEED PUMP` / `BOILER FEEDWATER PUMP` / `BOILER FEED WATER PUMP`
   / `FEEDWATER PUMP`). 실사용자가 실무 기준으로 확인해 준 것이고, 도면은 긴 형태를
   **한 번도** 인쇄하지 않고 발주처는 짧은 형태를 기기 이름으로 **한 번도** 쓰지
   않아 거리 규칙으로는 이어질 수 없었습니다.
2. **약어는 이름의 머리일 때만 바꿉니다.** `BFP A/B COOLER` 는 쿨러이고 발주처도
   거기서는 `BFP` 라고 씁니다 (수식어 24행 전부). 수식어까지 바꾸면 **F1 −1.5**
   (73.8 → 72.3) 였습니다. 머리 위치로 제한하니 74.2 가 됩니다.
3. **괄호와 `FOR` 꼬리 정리.** 캡션이 괄호를 둘 달면(`(A/B)` + `(2X50% PER HRSG)`)
   수량주기를 먼저 빼는 바람에 `PER HRSG)` 가 이름에 붙어 있었습니다. 괄호를 먼저
   통째로 빼고, `FOR ...` 꼬리도 뗍니다 (발주처 557행 중 `FOR` 를 쓴 행은 **1행**).
   이것만으로 F1 72.9 → **73.8** 이고, 목표 문장 5번
   `SLOP OIL TANK FOR FORWARDING PUMP AREA LEVEL` → **`SLOP OIL TANK LEVEL`** 로
   재현됩니다.
4. **기기를 한둘만 이름 붙인 시트에서는 거리 제한을 쓰지 않습니다.** 제한은 붐비는
   시트에서 엉뚱한 이름을 빌리는 것을 막으려는 것인데, 고를 것이 없으면 할 일이
   없습니다. p20 은 펌프 이름을 시트 맨 아래 캡션에 한 번만 인쇄하고(계기에서
   700pt) 발주처 33행이 전부 그 펌프를 주어로 씁니다. `--without` 측정:
   **F1 74.2 → 74.3 · 재현율 71.8 → 73.1 · 주어 오답 53 → 42 · CONFIRMED 642 → 673**.
   다만 해당 시트가 2장뿐이고 그중 p31 은 유일한 라벨이 낱말 `PUMP` 하나라
   0/15 이므로, config `sole_equipment_sheet_max` 로 켜고 끌 수 있게 두었습니다.

**그 다음 회차 — 표준 사전 확정 (F1 74.3 유지, 세트별 기여도 측정)**

실사용자가 18세트를 ①층 표준으로 확정하고, `CD`(이 플랜트 계통명) · `RO`(계기 태그와
충돌) 2세트를 ③층으로 내렸습니다. **머리 위치 한정 규칙을 전 세트에 적용**한 결과:

| 세트 | `--without` F1 | 판단 |
| --- | --- | --- |
| `boiler_feedwater_pump` | 73.1 → **73.9** (+0.8) | 유지 |
| `condensate_extraction_pump` | 73.9 → 73.9 (0.0) | 유지 (이 문서에서는 0회 발동) |
| `heat_exchanger` | **74.3 → 73.9 (−0.4)** | **표기 선택 철회** |

`heat_exchanger` 는 세트 자체는 표준이 맞지만 **이 발주처의 표기 선택이 손해**입니다:
발주처의 `HEX` 11건은 전부 p31 인데 그 도면은 두 낱말 다 인쇄하지 않고, 정작
`HEAT EXCHANGER` 로 끝나는 라벨이 있는 시트에서는 발주처가 풀어 씁니다(6건).
그래서 ①층에는 남기고 ②층 선택만 비웠습니다 — **세트가 틀린 게 아니라 방향이
프로젝트마다 다르다는 증거**입니다.

**확정 18세트 중 실제로 적용되는 것은 2개뿐입니다.** 나머지 16세트는 발주처가 그
낱말을 **기기 이름으로 쓴 적이 없어**(HRSG 0/29 · GT 0/57 · ST 0/34 · HRH 0/32 …
전부 수식어) 머리 위치 규칙에서 발동하지 않습니다. 사전이 커도 손해가 없다는 뜻이며,
다음 프로젝트에서 방향만 다시 고르면 됩니다.

**중간 심볼(SUCTION STRAINER)은 회수 불가로 확정했습니다.** 발주처 13행은 도면 4장에
있는데, 그중 `STRAINER` 낱말을 인쇄한 도면은 **p32 한 장(3행)** 뿐이고, 그 낱말마저
계기(x≈400)와 펌프(x≈812) 사이가 아니라 **시트 반대편 x≈1830 의 드레인 경로 주석**
입니다. 즉 낱말로 회수되는 건수는 **0/13** 이며, 형상 매칭은 이전 회차 실패 기록이
있으므로 **전량 사용자 입력 대상**입니다.

**그 다음 회차 — 공급자 구간은 당사자를 말하지, 문장을 지우지 않습니다 (SKIP 2 → 0)**

폭 0 SCT 상자를 양쪽 모서리로 다시 세우면 12행이 SKIP 으로 옮겨가는데, 그 판정을
발주처 리스트로 가렸습니다.

1. **기하는 맞습니다.** AL NOUF1 의 폭 0 상자 8개는 전부 네 변이 실제로 닫힌
   체인 파선 사각형이고(왼쪽 세로변 8/8 존재 확인), 각 상자에는 두 화살표
   표기가 붙어 있습니다 — `SCT →` 는 상자 바깥, `← SUPPLIER` 는 상자 안.
   p14 · p24 · p42 · p58 에서 렌더로 눈으로 확인했습니다. 라벨이 왼쪽에 선
   p24 는 화살표가 좌우 반전돼 있고, 그래도 **안쪽이 SUPPLIER** 인 것은 같습니다.
2. **그런데 대조 가능한 시트는 하나뿐이고, 그것이 반대를 말합니다.**
   p14 의 상자는 `#10 CLEAN DRAIN TANK` 를 감싸고 그 안에 이 시트의 LIT 2개가
   전부 들어 있는데, 그 둘이 곧 발주처 xls 136·137행(`... CLEAN DRAIN TANK LEVEL A/B`)
   입니다. p42 는 압축공기 시트 전체(12검출)를 감싸지만 발주처 행이 **0** 이고,
   상자가 없는 다른 시트 **15장도 똑같이 0** 이므로 아무 것도 증명하지 못합니다.
3. **이미 같은 결론이 절반만 적용돼 있었습니다.** `DEFAULT_DISABLED` 는 스코프
   축에서 SCT 를 끄면서 그 근거로 p20 의 RO 2개를 적어 뒀는데(엑셀이 갖고 있는 행),
   Description 축은 그 둘을 여전히 SKIP 으로 비우고 있었습니다. 발주처 xls 225·226
   행이 그 둘입니다 — **네 행 전부 발주처가 문장을 씁니다. 반대 사례 0.**
4. **그래서 상자를 넓히는 대신 축을 끊었습니다.** `supplier_interface_span.description`
   을 코드가 읽게 하고 기본값을 `keep` 으로 두었습니다 (비우는 쪽이 행위이고,
   틀린 문장은 보이지만 "타사 공급"으로 비워진 칸은 확정처럼 읽힙니다).
   측정: **fingerprint a38fe8ac 그대로 · 824행 그대로 · F1 74.6 그대로
   (재현율 73.1 → 73.2) · CONFIRMED 608 → 610 · SKIP 2 → 0**.
   `box_from_both_edges` 를 켜면 F1 74.6 → **74.5** · 대응행 없음 39 → 41 이므로
   AL NOUF1 은 켜지 않습니다 (지문은 b1c6745b 로 바뀝니다).

**그 다음 회차 — 도면 주석 제거와 등급의 정직성 (F1 74.3 → 74.6)**

1. **기기 라벨에 섞인 도면번호는 전부 off-page connector 였습니다.** 라벨 486건
   중 도면번호를 가진 것은 **23건(26회) · 13장**. 그중 4건은 p1(도면 목록 시트)이고
   나머지 19건은 전부 `<목적지 이름> / <도면번호> / <그리드 셀>` 세 줄짜리 연결
   주석입니다 — 기기 캡션 안에 도면번호가 인쇄된 사례는 **0건**. 26회 중 22회는
   **다른 시트**를 가리키고 4회는 자기 시트입니다. 발주처 557행에는 도면번호가
   **0행** (느슨한 형태·`D00P`·`M05` 전부 0) 이므로 제거가 안전합니다.
   패턴은 타이틀블록의 `formats.drawing_no` 를 앵커만 떼어 그대로 씁니다.
2. **같이 잰 다른 오염물**: 그리드 셀 **0건**(항상 괄호 안이라 `BRACKET` 이 이미
   가져감) · DN 치수 **1건**(p48) · NOTE 참조 **2라벨 6회**(p30) · ASME/ANSI/API 와
   플랜지 클래스 **0건**. 발주처 557행에서도 전부 0 이므로 넷 다 제거했고,
   **ASME 는 도면에도 발주처에도 사례가 없어 규칙을 쓰지 않았습니다** — 사례가 없는
   규칙은 추측입니다.
3. **효과는 정밀도입니다**: 문장이 바뀐 행 **30행** (도면번호 23 · NOTE 7),
   F1 74.3 → **74.6** · 정밀도 75.6 → **76.1** · 재현율 73.1 유지 · 완전일치 34 유지.
   **기기명은 285/366 그대로**입니다 — 도면번호는 이름의 머리가 아니라 꼬리라
   머리 낱말 채점에는 영향이 없습니다.
4. **CONFIRMED 를 더 좁히는 근거는 찾지 못했습니다.** 대조 가능한 455행을
   거리·2위와의 차·후보 수·방향·어휘 출처로 갈라 봤지만 **모든 칸이 63~80%** 로
   붙어 있습니다 (2위와의 차 0-10pt 74.4% ↔ 80pt 이상 75.8% — 직관과 반대이고
   평평합니다). 임계값을 만들면 그것이 곧 임의값이므로, **규칙은 그대로 두고 등급이
   무엇을 주장하는지를 고쳤습니다**: `확정` → **`도면 근거 있음`**, 설명에 실측
   일치율(주어 74.3% · 완전일치 27행)을 붙였습니다. 내역은
   완전일치 27 · 주어 정확하고 조각만 다름 41 · 주어 대부분 맞음 270 ·
   주어 일부만 겹침 76 · 주어 틀림 41.
5. **일괄 적용의 범위가 틀려 있었습니다.** 도면+TYPE 으로만 묶으면 PARTIAL 58행이
   **17묶음**이 되고(화면의 25묶음과 불일치), 더 나쁘게는 p11 TIT 에서 엔진이 이미
   채운 **7행**을, p10 TIT 에서 **10행**을 한 번의 클릭으로 덮어씁니다. **지금
   같은 문장인 행**까지 조건에 넣으니 정확히 25묶음이 되고 묶음 밖은 건드리지
   않습니다 (실제로 실행해 6행 확인).

**그 다음 회차 — 판정축 혼합 (5회차, 2026-08)**

1. **판정 트리**(⓪벤더/①기기 직결/②FROM-TO/③다분기/④불가)를
   `app/engine/describe_axis.py` 로 구현했습니다.  §2.3 경계 그대로 — 재귀·큐·
   방문 집합 없음, 탭 하나 + 모선 승계 한 번, 허용치는 전부 실측
   (join_slack 범례 · min_run 16.97 · conn_reach 70.2 · eq_reach 45.8 ·
   표준 끊김 18.5pt 는 콜리니어 간격 분포의 최빈 직후 첫 빈 칸).
2. **1부 체크포인트 ④ 75.2% → 진행 조건(≤30%) 불통과.**  무제한 순회
   진단으로 한계가 §2.3 이 아니라 도면임을 증명하고(§3 표), 사용자가
   **(나) 혼합**을 채택: 판정 성립 행만 새 문형, ④는 현행 유지.
3. **적용 192행** (①125 ②35 ③32 · LS 8 포함 · 밸브 탭 41).  현행 문장·등급은
   `evidence["axis"]["old_description"]`/`old_grade` 에 보존, Remark 는
   `판정축 {축} · 귀속 {귀속점}`.  도면 오탈자(GRUOP)는 원문 그대로.
4. **지문 a38fe8ac → 7b34064e** — 824행 전수 대조로 증명 (불변 칸 위반 0,
   움직인 행 = 적용 192행뿐).  이것이 새 기준선입니다.
5. **지표** (`spike/desc_metrics.py`, 발주처 541행 · 같은 채점기로 현행·신규
   비교): 식별 가능성 42.3% → **39.6%**, F1(참고) 73.2 → 69.3, 완전일치
   33 → 24.  **발주처 표기 기준으로는 낮아졌습니다** — 새 문형이 발주처의
   기기 주어 문형과 형태가 달라서이고, 실제 가치는 라벨링 워크북
   (`out/label_nouf1.xlsx`, 사람 O/X)이 가립니다.  F1 하락은 회귀 실패가
   아닙니다 (5회차 프롬프트 명시).
6. **④→② 전환율 5.4%** — 새 회차 지표.  ② / (② + ④).  도면 벽(선분 단절)
   개선의 측정 축입니다 (`docs/from_to_axis.md`).

**그 다음 회차 — ④ 618행의 사용자 확정 경로 (6회차)**

1. 자동 추적은 세 실측(13.1% · 4.9% · 10.4%)으로 막혔으므로, ④ 행은 **사람이
   FROM/TO 를 한 번 확정하고 리비전 사이에 재사용**한다.  근거 패널의 지정
   UI 후보는 그 도면에서 이미 읽은 텍스트 세 층뿐(커넥터 문구 · 기기 라벨 ·
   도면 텍스트)이고, 출처를 후보선택/후보선택(부분)/자유입력으로 기록한다.
2. 저장은 `projects/{프로젝트}/axis_overrides.json`, 키는 **안정 ID**.  같은 값
   재확정은 confirmed_at 을 유지해 **두 번 저장하면 같은 파일**이다.  다음
   리비전에서 같은 ID 가 매칭되면 자동 승계하고(근거 패널 "확정 승계"),
   사람이 이미 고친 행은 덮지 않는다.  같은 런의 다른 ④ 행에는 **제안만** 한다.
3. 확정은 user_values 로 저장돼 ai_values 를 덮지 않고, 감사기의 "사람이 도면
   값을 덮어쓴 칸"에 자동으로 잡힌다.  p6 실확정: `FROM HRSG#12` (커넥터 후보)
   + `HP TURBINE` (도면 텍스트 `HP TURBINE IP TURBINE` 의 일부 → 후보선택(부분))
   → `FROM HRSG#12 TO HP TURBINE TEMPERATURE TRANSMITTER`.
4. **FN 두 정의 확정** — 워크북 시트2의 22행 = 발주처 FIELD 전체 모수(행 단위),
   회귀 기준선 FN 5 = 교집합 도면 · 칸 단위 min.  22 = 교집합 밖 18 + 교집합 안
   4(기준선 FIELD 몫과 같은 칸).  검출 변화 아님 (`docs/from_to_axis.md`).
5. **경보 설정값** — CZE 양식 41열에 Alarm Set Point 계열 열이 **없다**.  접힌
   신호는 REMARK 34행과 근거 패널에 남고, 설정값을 채울 자리 자체가 없다.
6. LS 8행의 `LEVEL SWITCH` 문형은 사용자가 5회차 정정을 철회하며 **유지 확정**
   (물리 계기 하나 = 계기명, 신호명 아님).
7. 후보 잡음 필터는 승인됐고 **탈출구가 조건**이다 — 걸러낸 것을 버리지 않고
   "전체 후보 보기"로 펼친다 (p6 15건 ↔ 전체 46건).
8. **7회차 확정 사항**: `needs_review` 는 **화면에서만** 감춘다.  데이터는 그대로
   (지문 대상 · 엔진 판정 기록).  근거 패널은 `엔진 판정: 확인 필요 → 사용자
   확정됨` 으로 둘 다 보이고, 검토 카운트에서 확정 행을 미처리에서 뺀다.
   ④ 확정 경로와 기존 손입력 경로를 **함께** 고친다.

**그 다음 회차 — 한쪽만 성립하는 FROM/TO (7회차)**

1. ④ 618행을 끝점 판독으로 가르면 **출발만 9 · 도착만 69 · 양쪽 실패 540**.
   읽힌 반쪽을 쓰되 없는 반대쪽은 지어내지 않는다 — 문형 `②a FROM {출발}
   {TYPE풀네임}` · `②b TO {도착} {TYPE풀네임}`.  적용 78행 중 **42행은 현행이
   공란**이던 행(MOV 31 · BFV 4 · PNEUMATIC 3 등)이라 없던 이름이 생긴다.
2. **같은 방향 커넥터가 둘이면 고르지 않는다** (4행) — 양 끝이 다 `TO …` 인
   경우 어느 쪽인지 도면이 말하지 않으므로 ④ 유지.  이 규칙이 78 vs 82 를 가른다.
3. 지문 **7b34064e → b577bae0** (824행 전수 대조: 불변 칸 위반 0 · 움직인 행
   78 = 적용 증가분).  등급 CONFIRMED **713** · PARTIAL 42 · LOW 8 · NONE **61**.
4. **④→② 전환율 정의 확장**: (② + ②a + ②b) / (… + ④) = **17.3%**
   (옛 정의로는 6.1%, 직전 5.4%).
5. 지표는 또 내려간다 — 식별 가능성 39.6 → **36.8%** · F1 69.3 → 66.8.
   발주처가 `TO …` 문형을 6.5%에서만 쓰기 때문이고, **판정 대상 31행**(현행
   문장이 있는데 도착지 문형으로 바뀐 행)은 도착지가 드레인·블로다운에 몰려
   있다(CLEAN DRAIN TANK 15 · BD TANK 계열 15).  라벨링 결과로 가른다.

**그 다음 회차 — ②a·②b 는 현행 문장이 없을 때만 (8회차)**

1. 7회차의 78행 중 **현행 문장이 있는 36행을 되돌린다** (②a 5 · ②b 31).
   판정 기준은 **현행 문장의 유무 하나** — 도착지 어휘로 가르지 않는다(드레인·
   BD 목록은 프로젝트 종속 어휘가 된다).  읽힌 반쪽은 라인이 어디서 오고 어디로
   가는지만 말하므로 계통을 담은 현행 문장을 밀어낼 수 없고, 공란이던 행에서는
   없던 이름이 생기므로 명백히 낫다.
2. **판정은 기록하고 적용만 하지 않는다** — `evidence["axis"]["withheld"]` 에
   사유가 남고 문형 출처는 `현행유지`.  ④→② 전환율은 판정 기준이라 **17.3%
   유지**.  신규문형 270 → **234** (②a 4 · ②b 38 만 적용).
3. 되돌린 36행은 **전부 6회차 문장과 글자 그대로 같고**, 살아남은 42행은
   **전부 현행 공란이던 밸브 행**이라 FIELD 지표는 6회차로 복귀한다
   (식별 39.6% · F1 69.3 · 완전일치 24).
4. 지문 **b577bae0 → 91ba3e33** (824행 전수 대조: 불변 칸 위반 0 · 움직인 행
   36 = 되돌린 행).  등급 CONFIRMED **704** · PARTIAL 47 · LOW 12 · NONE 61.
5. **p6 은 자동으로 안 된다 — 확정 기록.**  네 번째 실측(양쪽 실패 540행)까지
   같은 답이므로 사용자 확정 경로가 유일한 길이다.  워크북의 **확정 우선순위**
   열이 p6 행을 1번으로 세운다 (`docs/from_to_axis.md`).

**그 다음 회차 — 중간 접합(T) 한 단계 읽기 (9회차)**

1. 탭한 런의 **몸통**에 붙는 가지를 한 단계 읽는다.  깊이는 늘지 않는다
   (끝에서 한 번 → 몸통에서 한 번).  코드로 보장: `_mid_branches` 는 `runs` 를
   한 번 훑는 평평한 루프이고, 가지에서 하는 일은 `read_end`(텍스트·기기만
   봄) 뿐이라 런으로 넘어가는 경로가 없다.  `test_mid_branch_does_not_step_twice`
   가 두 단계 뒤의 커넥터를 못 읽는 것을 확인한다.
2. **끝점도 못 읽고 기기에도 안 닿은 행에서만** 본다 — 이 조건이 없으면 기기
   직결(①) 11행이 ②b 로 끌려간다 (실측).
3. **갈리지 않으면 고르지 않는다** — 서로 다른 이름이 둘 이상이면 ④ 유지
   (실측 1행: p20 TIT `HRSG #11 IP EOCNOMIZER` ↔ `SPRAY FOR HP BYPASS # 11`).
4. 판정 **17행** 이동 (④→②b 15 · ④→① 2) · ④ 540 → **523** ·
   ④→② 전환율 17.3% → **19.7%**.  **적용은 0행** — 17행 모두 현행 문장이
   있어 8회차 기준으로 보류.  **지문 91ba3e33 유지 · 824행 움직인 칸 0.**
5. 8회차 기준을 **축과 무관하게** 적용한다 — 중간 접합에서 새로 읽은 이름은
   ①로 서더라도 현행 문장을 밀어내지 않는다 (실측 2행이 UNIT 접두어·위치어를
   잃었다).  보류 문장은 워크북 **보류된 신규문형** 열(53행)에 실어 사람이
   뒤집을 수 있게 한다.  p6 클라우드 TT 2행이 그 예:
   `UNIT #12 HP STEAM TEMPERATURE` ↔ `TO HRSG#12 BD TANK TEMPERATURE
   TRANSMITTER A/B` (도면 근거는 §p6 절).

**국소 연결의 실측 한계 (2회차 전에 측정)**: 리드선 → 첫 배관 런까지는 788행 전부
성공합니다. 그러나 그 런이 기기까지 닿는 경우는 **44/890 (4.9%)** 이고, 그중 최근접
기기와 다른 답을 낸 것은 **7건**, F1 변화는 **0.0** 입니다. 라벨-런 간격 허용치를
문서에서 실측한 90분위 **48.4pt** 로 넓혀 7→44 로 늘린 뒤의 값입니다. 나머지 94% 는
기기까지 가는 길에 **엘보가 한 번 이상** 있어, 그래프 순회 없이는 닿지 않습니다
(순회는 금지 사항). 즉 국소 연결은 **규격대로 구현했고, 이 문서에서의 천장이 4.9%**
입니다. 나머지는 최근접 거리 폴백입니다.

**그 다음 회차 — p6 육안 피드백 반영 (10회차)**

전문은 `docs/round10_feedback.md` 입니다.  여기에는 **다음 회차가 반드시
알아야 할 것**만 적습니다.

1. **TW 는 검출 실패가 아니라 명시적 제외였다.**  `not_field: [TW]` 하나였고
   버블 검증은 15개 전부 통과하고 있었다.  다만 **발주처 CZE 에 TW 는 0행**
   이라 15행이 전부 FP 다.  계상 여부는 발주처 판단이다.
2. **M 심볼 단독 인식은 이미 되고 있었다 — 코드를 고치지 않았다.**  밸브는
   처음부터 기하로만 잡는다(범례 p2 몸체 · p3 액추에이터).  실측: 밸브 144행
   중 **67행이 버블 태그 없이** 심볼만으로 잡힌 것이고, p6 의 MOV 8행 중 4행이
   그렇다.  범례 p3 원문 `M ROTARY MOTOR (SHOWN TYPICALLY WITH ELECTRIC
   SIGNAL)`.  MOTOR FP 는 **16 → 16**.
3. **NRV 는 두 제외 사이로 빠진다.**  `NRV` 텍스트는 VALVE 앵커라 FIELD 에서
   빠지고, 체크 밸브는 액추에이터가 없어 `deliverable_class` 첫 줄에서 빠진다.
   전 도면 CHECK 몸체 **264개 중 261개가 `actuator=NONE`**.  §12 의 "CHECK 2"
   와 **같은 문제의 반대편**이다 — 행이 된 2개는 액추에이터가 잘못 읽힌
   것이고 그중 하나가 p7 의 `NRV` 태그 몸체다.  대응 리스트가 없어(CZF 미수령)
   산출물 배분은 후보만 냈다.
4. **정밀도 −13.6pp 의 원인은 하나다.**  벤더 제외분 204행을 넣으니 TP 가
   594 → **595** (+1), FP 가 113 → **249** (+136).  발주처 CZE 는 벤더
   공급분을 계상하지 않는다 — 이보다 분명한 대조는 없다.  되돌리기는
   config 한 줄(`scope.exclusion_rules`).
5. **공급자 이름은 그 장 NOTES 에서 런타임에 읽는다.**  코드에도 config 에도
   이름이 없고, 자리 규칙(`\bBY\s+(?P<name>[^.]+?)\s*\.?\s*$`)만 있다.
   11종이 도면에서 나왔다(HRSG 40 · ST SUPPLIER 34 · SEAWATER INTAKE
   FACILITY SUPPLIER 28 …).  p52·53·54·56 은 `BY` 가 아니라 `SUPPLIED FROM
   SUMP` 라 62행이 이름 없이 `VENDOR` 로 떨어진다 — 규칙을 넓힐지는 발주처 몫.
6. **TYPE 표기는 저장하지 않는다.**  `pipeline.type_display()` 함수 하나이고
   화면 응답과 Excel 이 읽을 때만 통과한다.  §8 의 대조 단위가 TYPE 문자열이라
   저장하면 측정이 무의미해지고, 저장 필드를 두면 검토자가 TYPE 을 고쳤을 때
   둘이 갈린다.  소스 검사 테스트가 `_field_rows`·`_valve_rows`·`_axis_pass`
   ·`_apply_axis` 에 이 함수가 없음을 강제한다.
7. **라인 최근접 커넥터는 순회가 아니다 — 거리다.**  탭한 런과 그 모선·중간
   가지에서 축과 **직교**하는 거리만 conn_reach(70.2pt)로 막고, 축을
   **따라가는** 방향은 막지 않는다.  런이 조각이기 때문이다(§3 의 선분 단절).
   실측: 이 구분이 없으면 524행 중 10행만 걸린다.
   **⚠ 적용 39행의 버블→커넥터 간격이 p50 573pt · max 1429pt** 다.  "가장
   가까운"은 맞지만 "가까운" 것은 아니다 — 그 선에 다른 커넥터가 없어서 이긴
   것이다.  그래서 등급은 CONFIRMED 가 아니라 **LOW** 이고 Remark 에 거리와
   원문을 적는다.
8. **피드백 12장(p6 클라우드 TT)의 답은 `TO CLEAN DRAIN TANK` 가 아니다.**
   그 TT 가 탭한 런은 x=844.4 이고 p6 의 `TO CLEAN DRAIN TANK` 세 곳은
   x=1601~1781 이라 직교 거리가 757~937pt (반경의 10배 이상)다.  9회차의
   중간 접합이 이미 읽은 답 `TO HRSG#12 BD TANK` 가 도면이 주는 답이고,
   그것을 현행 문장 대신 쓸지가 **2차 판단**이다 (④ 112행 + 8·9회차 보류 53행).
9. **Description 지표는 하나도 안 움직였다** (F1 69.3 · 식별 39.6% ·
   완전일치 24).  찬 39행이 전부 밸브 행이고 CZH/CZI 에는 Description 열이
   없기 때문이다.

**그 다음 회차 — SCT 축과 밸브 SCOPE (11회차)**

전문은 `docs/round11_scope_axis.md` 입니다.  요지는 셋입니다.

1. **측정축을 지우지 않고 늘렸다.**  `spike/accuracy.py` 의 `AXES` 가 유일한
   정의이고 `score(rows, keep)` 는 하나다 — 축이 다른 것은 `keep` 뿐이고
   **정답지 쪽 모수는 두 축에서 같다**.  축1 은 "발주처가 안 세는 것을 얼마나
   넣었나"까지, 축2 는 "우리가 얼마나 잘 읽었나"를 잰다.
2. **벤더 별표는 밸브 몸체가 아니라 액추에이터 심볼 위에 찍힌다.**  p6 실측:
   별표 y=526 · M 원 y0=534.4(8.4pt) · 몸체 y0=563.5(37.5pt), 허용치
   `mark_above` 25.0.  몸체를 보면 전 도면에서 **1회**만 걸린다.  허용치는
   건드리지 않고 **읽는 사각형만** 바꿨다.
3. **밸브에는 `scope` 만 채우고 `vendor_supply` 는 채우지 않는다.**
   `_axis_pass` 가 `vendor_supply` 행을 ⓪로 보내 문장을 지우기 때문이다
   (실측 1행: p7 CHECK `TO HRSG#11 CRH CHECK VALVE`).  스코프 표기와 판정
   입력은 다른 칸이다.

**그 다음 회차 — 화면이 하는 말과 데이터를 맞춘다 (12회차, UI 전용)**

전문은 `docs/round12_ui.md` 입니다.  **검출·측정은 한 줄도 건드리지 않았고
지문 `69ad281f` · 1029행이 그대로입니다.**

1. **화면이 사실과 어긋난 곳이 셋이었다.**  근거 패널이 벤더 행에도
   "리스트에 포함됩니다"라고 썼고(팀원이 그 말을 믿고 Excel 을 열면 그 행이
   없다), 오버레이 범례는 **라벨과 세는 대상이 달랐고**, SCOPE 열은
   `VENDOR(SEAWATER INTAKE FACILI…` 로 잘렸다.  셋 다 11회차 캡처가 잡았다.
2. **근거 패널은 이제 세 질문을 나눠 답한다** — 공급 주체 / 추출 결과 /
   발주처 양식.  "추출 결과에 있다"와 "발주처 양식에 나간다"는 11회차부터
   서로 다른 사실이므로 한 문장으로 말할 수 없다.
3. **오버레이 색은 SCOPE 열을 읽는다** (`_layers` 의 한 곳).  색과 열이
   다른 값을 읽으면 언젠가 갈린다.  p6 실측 46행 = SCT 18 + VENDOR 28 로
   범례 숫자와 그리드가 맞는다.
4. **⚠ 캡처가 진행률의 결함을 잡았다.**  13초와 60초 화면의 md5 가 같았다
   (`772644abffe1`).  원인은 진행 신호가 아예 없는 두 구간 — `.segments()`
   **95.8초** · `derive` **129.0초** — 이고, 캐시 예열 루프를 층 안에 넣어
   장마다 신호를 내게 했다.  같은 방법(서로 다른 시점 캡처의 바이트 비교)이
   §7.2 에서도 같은 실패를 잡았었다.
5. **취소는 구조적으로 안전하다 — 새 방어 코드를 쓰지 않았다.**
   행을 쓰는 곳은 `db.store_result` 하나, 안정 ID·리비전 대장을 쓰는 곳은
   `_run_comparison` 하나이고 둘 다 `analyse` **뒤**에 온다.  즉 분석 중
   취소는 **행 0 · 안정 ID 0 · 리비전 소비 0** 이다 (실측 `status=cancelled
   rows=0`).  §7.3 의 "ID 는 Rev.A 에서 한 번만" 이 지켜진다.
6. **팬은 스크롤이지 좌표가 아니다.**  `#stage` 의 `scrollLeft/Top` 만 옮기고
   SVG 는 건드리지 않으므로 확대·선택·오버레이가 그대로다 (실측
   `[400,300] → [620,440]`, 선택 유지 True).  4px 미만은 클릭으로 넘겨
   행 선택을 뺏지 않는다.
7. **주석(D-2·D-3)은 구현하지 않고 설계안만 냈다** (`docs/round12_annotation_design.md`).
   저장 5개 항목 중 답이 서지 않은 것이 있고, 프롬프트가 그 경우
   **D-1 만 하고 회차를 닫으라**고 정해 뒀다.

**그 다음 회차 — 있는 것을 먼저 찾았다 (13회차, 프로젝트 영속 · 개정 구분)**

전문은 `docs/round13_persistence.md`, 조사는 `out/round13_survey.md` 입니다.
**지문 `69ad281f` · 1029행은 편집을 5칸 넣은 뒤에도 그대로입니다.**

1. **요구 세 가지 중 둘은 이미 서버에 있었다.**  구현 전 조사가 그것을 먼저
   밝혔다 — 재분석 없이 열기는 `rows` **0.69초**(재분석 351초의 1/500)로
   이미 됐고, 편집 영속도 `EDITABLE` 10열 전부 이미 됐고, 두 층 분리
   (`ai_json`/`user_json`)도 이미 있었다.  **없던 것은 첫 화면에서 거기로
   가는 길**이었다.  10회차 [C](M 심볼)와 같은 상황이다 —
   **없는 것을 만들기 전에 있는 것을 찾는다.**
2. **첫 화면의 단위를 job 에서 프로젝트로 바꿨다** (`GET /home`).  이전 목록은
   PDF 파일명만 말해서 같은 `pid_total.pdf` 로 올린 두 분석을 구분할 수
   없었다.  이제 프로젝트 → 리비전 → (도면 Rev · 개정 배지 · 행수 · 일시 ·
   수정 칸수).  프로젝트에 안 묶인 분석은 버리지 않고 따로 묶는다.
3. **편집을 Rev 사이로 잇는다 — 짝은 안정 ID 로 짓는다.**  §7.3 이 ID 를
   "Rev.A 에서 한 번만 부여" 로 정의해 둔 것을 **쓰는** 것이지 새로 매기지
   않는다.  세 규칙: 추출값은 안 건드린다 · 이번 리비전에서 사람이 이미 고친
   칸은 안 덮는다 · 엔진 답이 갈리면 고르지 않고 `conflict` 로 검토에 올린다
   (재분석이 하던 것과 같은 규칙).  실측 5칸 승계 · 충돌 0.
4. **작성자는 자기신고다.**  `feedback` 15개 컬럼에 사람을 가리키는 것이
   하나도 없었다.  인증을 만들 자리가 아니므로 이름 한 칸을 넣되 **확인은
   매번 하고 타자는 한 번만** 친다.  비우면 비운 채로 저장되고 화면이
   "이름 없음"·"자칭" 이라고 적는다 — **서버도 화면도 이름을 지어내지 않는다.**
5. **⚠ 문서 대표 Rev 는 도면에서 유도되지 않는다.**  이 문서는 장마다 Rev 가
   다르고(PID 53장: A11 A1:1 B18 C16 D7, 전부 신뢰도 HIGH) 세 후보가 서로 다른
   답을 낸다 — `max`→D(1장) · `mode`→B(19장) · `first_sheet`→B.  `rev_date` 는
   9장에만 있고 가장 늦은 날짜에 C 3장·D 1장이 섞여 갈라지지 않는다.  사용자가
   실무 기준으로 **`max`("한 장이라도 개정되면 문서가 개정된 것")** 를 확정했고
   config `revision.document_rule` 한 줄이다.  **대표값만 내지 않고 접기 전
   분포와 장 단위 대조를 함께 보인다.**
6. **⚠ 도면번호가 장을 유일하게 가리키지 않는다.**  같은 도면번호를 쓰는 PID
   장이 **3쌍** 있고 그 장들의 개정이 다르다 (`D00P-10LBG10-M05-0001` p12 B ↔
   p15 A · `D00P-00GHC10-M05-0001` p46 C ↔ p47 B · `D00P-00GMA10-M05-0001`
   p52 A ↔ p55 B).  한쪽을 골라 비교하면 **같은 PDF 를 두 번 넣어도 "개정 2장 ·
   역행 1장"이 나온다** — 13회차 캡처가 실제로 잡았다.  그런 도면번호는
   판정하지 않고 `ambiguous` 로 센다.  쪽 번호로 짝을 짓는 것도 안 된다 —
   개정 때 장 순서가 바뀌는 것이 개정의 흔한 모습이다.
7. **판독 자체는 결정적이다** — 같은 PDF 를 두 번 분석해 쪽 단위로 대조하니
   58쪽 중 값이 다른 쪽 **0**.  위 6번은 판독이 아니라 대조 함수의 문제였다.
8. **캡처가 결함 셋을 잡았다**: 승계된 편집이 이력에 안 남던 것 · 근거 패널이
   편집값과 엔진 근거를 구분하지 않던 것(`수량 7` ↔ `수량 근거 … x 2`) ·
   위 6번.  최근 여섯 회차 연속으로 캡처가 결함을 잡고 있다.

**그 다음 회차 — 편집과 SCT 필터의 정합 (14회차)**

전문은 `docs/round14_edit_scope.md` 입니다.  **지문 `69ad281f` · 1029행이
그대로이고 엔진 파일은 0개를 건드렸습니다.**

1. **`UI 21` 은 유효한 통과가 아니었다.**  11·12·13 회차의 그 값은 전부
   **SCOPE 가 전부 빈칸인 DB**(실 DB 의 분석 4건 = 824·824·824·75행)에서
   나온 것이다.  그 상태에서는 모든 행이 `legacy` 로 분류돼 SCT 필터가
   아무 행도 거르지 않는다 — **필터를 시험한다고 적어 둔 시험이 필터가 꺼진
   것과 같은 조건에서만 돌고 있었다.**  코드는 같고 데이터가 달랐으므로
   파일 동일성으로는 잡히지 않는다.
2. **고칠 것은 시험의 가정이었다.**  `edited` 픽스처가 FIELD 첫 다섯 행을
   순서로 집었고 그 행이 SCT 인지 보지 않았다 (새 분석에서는 첫 다섯 중
   **3행이 `VENDOR(HRSG)`**).  제품은 맞다 — API 재현으로 스냅샷 rev 3→4 ·
   `out_of_scope 249` 유지를 확인했다.  이제 **상태로 고른다**(`field_rows`).
3. **세 갈래를 모두 시험한다** — SCT(나간다) · 비SCT(안 나간다, **정상**) ·
   legacy(빈 값, **나간다**).  마지막이 뒤집힌 것처럼 보이지만 11회차가
   그렇게 정했다: 없는 판정을 타사 공급으로 읽으면 양식 네 개가 빈 파일이 된다.
4. **데이터 전제를 코드로 못박았다.**  `test_step0_the_suite_runs_against_
   scope_aware_data` 는 **건너뛰지 않고 실패한다** — 건너뛰기가 그 침묵을
   세 회차 동안 만들었다.  옛 DB 에서 실제로 실패하는 것을 확인했다.
   **UI 21 → 24, SCOPE 있는 데이터(SCT 667 / VENDOR 218)에서 전량 통과.**
5. **편집이 양식에 어떻게 닿는지 그 순간 말한다.**  12회차 근거 패널은 행을
   **누른** 사람에게만 말했다.  이제 저장 **전**(작성자 줄)과 **후**(한 줄
   안내) 두 시점에 말하고, 막지는 않는다.  판정은 `scopeFacts` **하나**를
   패널·안내가 함께 읽는다.  행수는 `GET /jobs/{id}/scope_summary` 가 주고
   그 응답은 완료 화면과 **같은 함수**(`_scope_summary` → `in_client_scope`)다.
6. **화면이 한 말이 파일과 맞는지 열어서 확인했다**: `UI-SCT-1` 있음 ·
   `UI-VEN-1` 없음 · `UI-LEG-1` 있음 · `legacy_no_scope_rows: 1`.  세 문장 전부 일치.
7. **⚠ 캡처가 또 잡았다 (일곱 회차 연속)** — 앞 편집의 "나갑니다" 가 남은
   채로 다음 행의 "나가지 않습니다" 가 떠서 **서로 반대인 두 문장이 동시에**
   있었다.  둘 다 각자의 행에는 맞는 말이라 더 나쁘다.
8. **⚠ SADARA 는 프로필이 값을 가른다** — 미지정 **82행 `0767ba79`**(기준선) ↔
   `project_sadara.yaml` 지정 **75행 `2bb1b9a2`**.  **프로필 자동 맞춤은 없다**
   (`projectconfig.DEFAULT_CONFIG` 가 alnouf1 고정).  즉 기준선은 "AL NOUF1
   규칙을 SADARA 도면에 적용한 값" 이고 교차검증으로서는 그것이 맞다.
9. **문서 드리프트 갱신을 [A] 고정 절차로 삼는다** — 이번엔 빠른 테스트
   149 → **150**.  세 회차 연속으로 드리프트가 나왔다.

**그 다음 회차 — 범례 프로필 저장과 개정본 재사용 (15회차)**

전문은 `docs/round15_legend_profile.md`, 조사는 `out/round15_survey.md` 입니다.
**지문 `69ad281f` · 1029행이 그대로이고, 세 경로(유도·재사용·강제유도)가 전부
같은 지문을 냅니다.**

1. **속도가 목적이 아니다 — 실측이 그렇게 말한다.**  `legend_rules` 는 전체
   406.2초의 **1.5%(6.10초)** 이고, 그 안에서 **범례를 읽는 몫은 1.77초(0.4%)**
   뿐이다.  나머지 3.73초는 `connector_reach` 이고 그것은 범례가 아니라
   **도면 218개**에서 잰다.  **보고서에 속도를 성과로 적지 않는다.**
2. **★ 진짜 이유는 범례 없는 PDF 가 지금 죽는다는 것이다.**  범례 4장을 뺀
   PDF 로 돌리면 `KeyError: 'min_run'` 으로 멈추고 화면은 일반 문구만 낸다
   (`derive_line_styles` 가 빈 값을 내는데 `valves.legend_fallback` 에
   `line_styles` 항목이 **없다**).  더 나쁜 것은 **죽지 않는 일곱**이다 —
   특히 `unit_multipliers` 가 7코드 → **3코드**로 줄어 유닛 12·20·21·22 가
   UNDEFINED 가 되고 **Q'ty 만 조용히 틀린다.**
3. **담는 것은 범례 장에서 나온 여덟뿐이다** (butterfly · actuator_stem ·
   pneumatic · line_styles · isa_table · equipment_symbols · component_words ·
   unit_multipliers = **91칸 · 9,654바이트**).  `connector_reach` 와 `layout`
   19건은 **도면**에서 나오므로 담지 않는다 — 담으면 다음 개정본의 도면이
   답해야 할 값이 옛 값으로 굳는다.  `equipment_vocab` 은 심볼의 순수 함수라
   따로 담지 않는다 (값을 두 곳에 두면 언젠가 갈린다).
4. **재사용이 `source` 를 바꾸지 않는다.**  지문이 `legend` 의 `source`·`note`
   ·`values` 를 해싱하므로, `PROFILE` 로 바꾸면 같은 값을 쓰고도 지문이
   달라져 **프로필이 값을 정확히 담았는지 확인할 길이 사라진다.**  재사용
   여부는 지문 밖의 `result["legend_profile"]` 이 말하고 화면이 그것을 읽는다.
5. **범례 장이 있으면 적용은 프로필로 하되 대조는 한다.**  적용(학습)과
   대조(확인)는 다른 일이다 — 대조를 빼면 재작도된 범례를 옛 규칙으로 읽고도
   아무도 모른다.  범례 p2 를 0.9배로 다시 그린 PDF 로 실측: **20칸 감지**
   (circle_diameter 6.06→5.454 · min_run 16.968→15.271 · 기기 17행 앵커 전부).
   **자동 갱신하지 않는다** — 항목별로 사람이 고른다.
6. **범례 장이 없는 개정본은 완주한다** — 54쪽 PDF 로 **1029행 · Q'ty 1911**
   그대로.  지문은 `ce085c62` 로 다른데 **쪽이 4장 빠져 행 키가 밀린 것**이고
   판정이 달라진 것이 아니다.  `legend_rules` 는 5.94 → **3.51초**.
7. **⚠ 재사용이 더 느렸다 — 그대로 적는다.**  범례 장이 있는 개정본에서는
   대조 때문에 재기 때문이다 (14.50초 ↔ 유도 5.94초).  줄어드는 유일한 경우가
   6번이고, 그것도 전체의 0.6% 다.
8. **범례도 프로필도 없으면 사유를 말하고 멈춘다** (`LegendUnavailable`).
   이 문장은 **예외를 바꿔 쓴 것이 아니라** 파이프라인이 직접 검사한 조건이다
   — `_failure_reason`(예외를 보지 않는 함수)은 *알 수 없는* 실패의 자리다.
9. **조사가 또 범위를 줄였다 (세 회차 연속).**  넷이 이미 있었다 — 프로젝트
   저장소(`revisions.project_dir`) · 프로젝트↔분석 연결(`job.project`) ·
   결정성(두 프로세스의 범례 값 **전 항목 동일**) · 그리고 캐시하면 **안 되는**
   두 가지를 가려낸 것.

## 4. 미해결 과제 (다음 단계 후보) — 우선순위 순

1. **순번은 "안 붙인 것"이 최대 원인입니다** (오답 203건: 발주처는 붙였는데 우리는
   없음 **77** · 글자가 다름(정렬축·시작점) **57** · 주어가 틀림 37 · 우리만 붙임 32).
   직전 회차의 "주어 79%" 는 주어가 73.4% 로 올라오면서 **18% 로 줄었습니다.**
   가장 손댈 만한 것은 **④ 57건(정렬축·시작점)** 으로, 기하 문제라 근거를 재기 쉽습니다.
   **천장을 재 뒀습니다** — 순번만 발주처 답으로 바꿔치기하는 오라클로
   **F1 74.6 → 77.8 (+3.2) · 완전일치 34 → 46**. 즉 순번 전체가 걸린 값이 3.2점이고,
   57건은 그 일부입니다. 오라클이므로 규칙으로 얼마를 회수할지는 별개입니다.
2. **위치어도 같습니다** (오답 259건: 미부착 **156(60%)** · 낱말 다름 46 · 과부착 30 ·
   주어 틀림 27). 미부착 156은 "과반 있는 칸에만 붙인다"는 **정책의 결과**이므로,
   칸을 넓히려면 과반 기준을 낮춰야 하고 그러면 정밀도가 깎입니다. 낱말이 다른 46건
   중 최다는 `INLET → OUTLET` 11건(좌우 반전)입니다. **천장은 +0.3 입니다**
   (오라클 F1 74.6 → 74.9). 순번과 합쳐도 **78.0 (+3.5)** 이므로, 이 둘을 다 잡아도
   80 은 넘지 않습니다 — 남는 것은 주어 자체입니다.
3. **순위 밀림은 15행뿐이고 뒤집을 근거가 없습니다** (2위 8 · 3위 7). 도면 제목으로
   가르려 했으나 15건 중 13건이 동점이고 1건은 오답 쪽을 가리킵니다(§3). TYPE↔기기
   종류 경향(LS→TANK 91% 등)은 그 시트에 어떤 계기가 있느냐의 반영이라 순환 논리입니다.
4. **위치어 158행 중 120행이 아직 비었거나 틀립니다.** 과반이 나오는 칸은 5개뿐
   (PIT ABOVE · PDIT ABOVE · PDIT LEFT · CCW TI · 펌프 좌우)이고, 특히
   **CCW 의 PI 는 SUPPLY/RETURN 이 정확히 반반**이라 기하로 구분할 수 없습니다.
   배관 라벨(공급관/환수관 번호)에서 유도할 수 있는지가 다음 후보입니다.
5. **순번 42.0%** — 라인 문구를 뺀 행이 162 → 131 로 줄면서 35.7 → 40.9 로 회복했습니다.
   남은 하락분도 과제 1과 같은 원인입니다.
6. **LS 문구 안의 유닛 번호** — 발주처는 `TO HRSG BD TANK` 라고 쓰는데 우리는
   `TO HRSG#11 BD TANK` 라고 씁니다. 문장 머리에 이미 `UNIT #11` 이 있으므로
   중복입니다. 18행. 557행에서 확인 가능.
7. **중간 심볼(SUCTION STRAINER)** — 발주처 13행, 전부 PDIT, 정확도 **0/13**.
   **회수 불가로 확정**했습니다 (§3 참조): 낱말이 인쇄된 도면은 p32 한 장뿐이고 그
   낱말도 시트 반대편의 드레인 주석입니다. 형상 매칭은 실패 기록이 있으므로
   **사용자 입력 대상**입니다. config `description.between_symbol_types: [PDIT]` 로
   PDIT 외에는 아예 제안하지 않게 막아 뒀습니다.
8. **밸브 Description 144행** — 발주처 마스터 밸브 리스트가 없어 문형 기준이 없음.
   확인된 사실: CZH(MOV)·CZI(BFV) 리스트에는 **Description 열 자체가 없고**
   SYSTEM / UNIT NO. 열만 있습니다. 그리고 우리 144행은 전부 **수동 밸브**
   (GLOBE 68 · GATE 38 · BUTTERFLY 23 · BALL 13 · CHECK 2) 로, 두 리스트 어디에도
   없습니다. 즉 **NONE 은 결함이 아니라 정답**입니다.
9. ~~다중 신호 버블 102행~~ — **수량 확정·적용 완료**. 물리 1개로 접어 34행,
   TYPE 은 발주처 리스트가 22행 전부에 쓰는 `LS`, Q'ty 는 1 × 시트 승수.
   **문형만 남았습니다**: 발주처 LS 22행은 **11쌍**이고 한 주어에
   `... LEVEL HIGH HIGH` 와 `... LEVEL HIGH` 를 **따로** 씁니다 — 한 행이 두 신호를
   말하는 문형이 **0행**입니다. 그래서 접힌 행은 첫 신호(`LEVEL HIGH HIGH`)로 두고,
   선택지를 `config multi_signal_bundle.description_signal` 에 세 개
   (`representative` 적용 중 / `all` 0행 / `none` 0행) 로 적어 뒀습니다. 접힌 신호는
   근거 패널과 **산출물 REMARK 열 34행**에 그대로 나갑니다.
10. **UNKNOWN 6종** (`out/valve_project_deps.md`) — Phase 3.
11. **Tag No.** — 도면이 `.....` 로 미부여. 발주처 규칙 필요.
12. **PI 48건 — 판별자 없음** (`out/pi_discriminator.md`). 계장 신호선 가설은
    **기각**: 이 문서의 모든 선이 `width=0` · `dashes='[] 0'` 이라 선 속성으로는
    구분되지 않고, 범례가 쓰는 기하(ELECTRIC = 토막 7.2 + 틈 3.6 등)로 국소
    탐색한 결과 파선은 계상되는 PI 24.8% ↔ 계상 안 되는 PI **0%** 로 **방향이
    반대**입니다. 확인된 구조: 갈림은 버블이 아니라 **도면 단위**입니다
    (PI 가 있는 교집합 17장 중 전부 셈 9 · 전혀 안 셈 8 · **부분 0**).
    유일하게 방향이 맞는 축은 **벤더 별표**(계상 PI 0.0% ↔ 미계상 PI 43.8%,
    PIT·TIT·TI·LIT·FIT 는 0~1%)이지만 회수율이 **30.4%**(48 중 21)라 채택하지
    않았습니다. "유닛코드 00 도면에서는 PI·RO 를 안 센다"(PI 7/7 · RO 3/3)는
    **AL NOUF1 도면번호 체계 관찰**이므로 범용 규칙이 아니고, 반례도 있습니다
    (`10PGB10-M05-0001`). SADARA 정답지 미제공으로 범용성은 **미측정**입니다.
13. **밸브 VALVE TYPE 은 채울 수 있습니다** (`out/valve_operation_mode.md`).
    HYDRAULIC→HOV(4/4) · PNEUMATIC→CV(**n=1**) 은 액추에이터로 결정되고,
    갈리는 것은 MOTOR 하나인데 도면이 `INCH` 를 인쇄합니다 — 8회 전부 MOTOR
    밸브 긴변 1배 안이고, **INCH 가 있는 도면 4장 = MOV_I 가 있는 도면 4장,
    반례 0**. TAG NUMBER 와 DESCRIPTION 은 공란이 정답입니다(§4-8 · §4-11).

## 5. 발주처 확인 대기 항목

| 문서 | 내용 |
| --- | --- |
| `out/description_question.md` | 기기 명명 규칙 · 위치 관계어 · UNIT 접두어 · A/B/C 순번 (각 항목이 몇 행을 채우는지 포함) |
| `out/ls_bundle_question.md` | 수량은 답을 받아 적용했고, **접힌 행의 Description 문형** 하나만 남음 |
| 마스터 밸브 리스트 | 밸브 Description 문형 기준 (미수령) |
| `out/valve_operation_mode.md` | PNEUMATIC → MODULATING 의 근거가 발주처 **1행**뿐입니다. 우리 PNEUMATIC 탭 46행을 그 1행으로 판정해도 되는지 |
| `out/pi_discriminator.md` | PI 를 어느 도면에서 세고 어느 도면에서 안 세는지의 기준. 도면에서 유도되지 않았습니다 |

## 5-1. 마감 현황 — 824행이 사람에게 무엇을 요구하나

| 상태 | 행 | 무엇을 하나 |
| --- | ---: | --- |
| 확인만 하면 되는 행 | **451** | 문장이 있고 사유도 없음. 눈으로 넘김 |
| 사유 확인 필요 (문장은 있음) | **159** | CCW 방향 104(7장) · 중간 심볼 33(10장) · 스코프 24(1장) · 벤더 마크 14(4장) |
| 공란이 정답 | **144** | 수동 밸브 144(발주처 리스트에 Description 열 자체가 없음). 타사 공급 2행은 문장을 갖게 됐습니다 — §3 |
| 중간 서술 입력 필요 | **58** | PARTIAL. 화면에서 **25묶음** |
| AI 제안 확인 필요 | **12** | LOW |

**사람이 실제로 누르는 횟수**: 중간 서술 **25회**(묶음당 1회 일괄 적용, 실측으로
6행짜리 묶음이 한 번에 채워지는 것을 확인) + 사유 축 처리 **최대 22회**
(CCW 7장 · 중간 심볼 9묶음 · 스코프 2묶음 · 벤더 4묶음, 축별 일괄 처리 기준).
공란이 정답인 144행과 확인만 하는 451행은 입력이 없습니다.

**남은 개선 여지의 천장** (오라클 실측): 순번 **+3.2** · 위치어 INLET/OUTLET
**+0.3** · 둘 다 **+3.5 (F1 78.0 · 완전일치 46)**. 그 위는 주어 자체이고,
주어는 기하로 더 짜낼 것이 없다는 측정이 §3 에 쌓여 있습니다.

## 6. 파일과 실행

**`data/` 규칙** (리포지터리에 없음 — `.gitignore` 대상)

| 파일 | 용도 |
| --- | --- |
| `data/pid_total.pdf` | 분석 대상 도면 |
| `data/CZE_Field_Instrument.xlsx` | FIELD 양식 + 검증 모드 대조본 |
| `data/CZI_Butterfly_Valve.xlsx` | BFV 양식 |
| `data/CZH_MOV_Gate_Globe.xlsx` | MOV 양식 |

양식은 **비어 있어도 됩니다** — 서식만 씁니다. 없으면 그 산출물은 사유와 함께
건너뜁니다.

**실행**

```bash
pip install -r requirements.txt
./run.sh                 # Linux/macOS — http://127.0.0.1:8000
start.bat                # Windows (포트/--no-reload/--verify 인자 지원)
PID_VERIFY_EXCEL=data/CZE_Field_Instrument.xlsx ./run.sh   # 검증 모드
```

**배포용 exe** (Windows)

```bat
build.bat                REM dist\PID_Extract.exe 하나. 빌드일이 함께 새겨집니다
build.bat --clean        REM PyInstaller 캐시부터 지우고
```

`build.bat` 은 의존성 확인 → `app/_build.json` 에 버전·빌드일 기록 → 빠른 테스트
→ PyInstaller 순서로 돌고, **테스트가 깨지면 exe 를 만들지 않습니다.** exe 에는
`app/static/` 과 `config/` 전부와 PyMuPDF·numpy 바이너리가 들어가고 **발주처 자료
(`data/`)는 들어가지 않습니다** (`pid_extract.spec` 에 근거를 적어 뒀습니다).

더블클릭하면 빈 포트를 골라 서버를 띄우고 브라우저를 엽니다. 분석 결과는 **exe 옆의
`pid_data/` 폴더**에 남아 다시 켜면 그대로 열리고, 오류가 나면 창이 즉시 닫히지 않고
사유와 트레이스를 보여 준 뒤 Enter 를 기다립니다. `PID_PROJECT_CONFIG` 없이도
프로필 자동 맞춤이 동작합니다 (`app/paths.py` 가 번들 안·바깥을 가릅니다).

**테스트**

```bash
pytest -q -m "not slow and not ui"    # 150건, 12초
pytest -q -m ui                       # 21건 (playwright + chromium 필요)
pytest -q -m slow                     # 전 문서 분석 포함, 각 4분
```

**회귀 스크립트**

```bash
python app/engine/detect_all.py --pdf data/pid_total.pdf --compare data/CZE_Field_Instrument.xlsx
python app/engine/detect_valves.py --report out/valve/report.md
```

## 7. 코드 지도

| 모듈 | 역할 |
| --- | --- |
| `app/engine/pidcache.py` | PDF 페이지 캐시. `segments()` 는 캐시 객체를 그대로 넘기므로 **제자리 수정 금지** |
| `app/engine/legend_rules.py` | 범례에서 치수 유도 (`Derived`: values/source/note/evidence) |
| `app/engine/detect_symbols.py` · `detect_all.py` | 계기 검출·스코프. **여기 손대면 회귀 필수** |
| `app/engine/detect_valves.py` | 밸브 몸체·액추에이터 |
| `app/engine/isa_table.py` | 범례 p3 ISA 문자표 런타임 파싱 |
| `app/engine/describe_equipment.py` | 기기 표 유도 · 도면 기기 라벨 검출 · 순번 |
| `app/engine/describe_candidates.py` | 두 축 후보 + 국소 연결 주어 판정 |
| `app/engine/describe.py` | 문형 조립 (config `description`) |
| `app/engine/describe_llm.py` | 중간 서술 선택기 (기본 꺼짐, 6개 제약 코드 강제) |
| `app/engine/describe_axis.py` | 5회차 판정축 — 탭 선정(`pick_tap`)·표준 끊김 다리(`standard_break`)·판정(`judge_row`)·문형(`sentence`). §2.3 경계는 tests/test_describe_axis.py 가 소스 검사로 강제 |
| `app/engine/describe_axis.py` 의 `nearest_conn` | 10회차 — 라인 전후단 최근접 커넥터. **축 직교 방향만** conn_reach 로 막고 축 방향은 막지 않는다 (런이 조각이라). 순회 아님 — 새 런을 찾는 코드가 함수에 없다 |
| `app/pipeline.py` 의 `type_display` | TYPE 표기(`MOV(GLOBE)`). **출력 전용 함수**이고 저장 필드가 아니다 — §8 의 대조 단위가 `values["type"]` 이기 때문 |
| `app/pipeline.py` 의 `_scope_of`·`_supplier_name` | SCOPE 열 네 규칙. 공급자 이름은 그 장 NOTES 원문에서 런타임에 읽고 코드·config 에 이름을 두지 않는다 |
| `config` `scope.exclusion_rules` | 벤더 마크를 **행 삭제**에 쓸지 **표기**에 쓸지. `none`(10회차) / `glyph+text+box`(9회차까지). 정밀도 13.6pp 가 이 한 줄에 걸려 있다 |
| `app/engine/describe_axis.py` 의 `_mid_branches` | 탭한 런의 몸통에 붙는 T 가지를 **한 번** 훑는 평평한 루프. 깊이 1 보장 — 가지에서 `read_end` 만 부르고 런으로 넘어가지 않는다 |
| `app/pipeline.py` `_apply_axis` 의 `via_mid` | 중간 접합에서 새로 읽은 이름은 축과 무관하게 8회차 기준(현행 문장 있으면 보류)을 받는다 |
| `config` `description.axis_mode` | 판정축 적용 폭 — `mixed`(AL NOUF1) / 비움(기록만). 근거 수치는 config 주석 |
| `pipeline` `_axis_pass`·`_apply_axis` | 판정 기록(모든 행) → 혼합 적용(성립 행만). 현행 문장은 `evidence["axis"]["old_description"]` 에 보존 |
| `spike/desc_metrics.py` | 식별 가능성(주)·토큰 F1(참고) 채점기 — 원본 소실로 재구현, 같은 채점기로 현행·신규를 재고 차이만 읽는다 |
| `spike/label_workbook.py` | 라벨링 워크북 `out/label_nouf1.xlsx` (추출 824 · 누락 FN · 집계, "문형 출처" 열). 결정적. 발주처 문장이 들어가므로 .gitignore |
| `app/engine/pipe_graph.py` | 배관 그래프 — **Description 경로에서는 쓰지 않습니다** |
| `app/pipeline.py` | 오케스트레이션 + 등급/Remark |
| `app/db.py` · `app/main.py` · `app/static/` | 저장·API·검토 UI |
| `config/project_alnouf1.yaml` 의 `review_axes` | 검토 사유 → 축(스코프/수량/Description/검출/자료) 배정. 미등록 코드는 `OTHER` 로 보이며 숨기지 않음 |
| `app/main.py` 의 `review_codes`·`REVIEW_LABELS` | 행별 검토 사유 코드와 한 줄 설명. `/jobs/{id}/review` 가 축별로 집계 |
| `review_state` 테이블 | 미처리/확인함/수정함/보류. 없으면 미처리 — 한 일만 저장 |
| `app/engine/describe_equipment.py` 의 `derive_aliases`·`apply_alias` | ①표준 세트 + ②표기 선택 → 라벨 별칭. 머리 위치일 때만 적용 |
| `config/plant_standard_abbr.yaml` | **①업계 표준 약어 — 프로젝트 무관, 그대로 재사용.** 18세트 확정. 방향은 여기 없음 |
| `app/engine/describe_equipment.py` 의 `DRAWING_NO`·`BORE`·`NOTE_REF`·`GRID_CELL` | 라벨에서 떼는 도면 주석. 도면번호 패턴은 `formats.drawing_no` 를 앵커만 떼어 재사용 (정의는 한 곳) |
| `app/static/app.js` 의 `sameSentence` | `일괄 적용` 의 사정거리 — 도면·TYPE·**지금 같은 문장**. 누르기 전에 몇 행인지 화면에 씁니다 |
| `app/static/app.js` 의 `cellValue`·`FILTER_COLS` | 그리드·정렬·검색·컬럼 필터가 셀을 읽는 **단 하나의 접근자**. `pid_no` 는 행이 아니라 페이지에 있습니다 |
| `app/static/app.js` 의 `zoomBy`·`ZOOM_STEP` | 커서 기준 확대. 배율 1.3 은 `+`/`-` 버튼 값 재사용이고 상·하한은 없습니다 |
| `config` `multi_signal_bundle.description_signal` | 접힌 LS 행의 문형 (`representative` / `all` / `none`). 발주처 표기는 `representative` 뿐 |
| `app/axis_overrides.py` | ④ 행 FROM/TO 확정의 저장·문형·승계. 키는 안정 ID · 정렬 고정 · 같은 값 재저장은 같은 파일 |
| `app/main.py` 의 `axis_candidates`·`confirm_axis`·`axis_override_map` | 후보 세 층(도면에서 읽은 텍스트만) · ② 문형 생성 + user_values 저장 · 행별 확정/승계 지도 |
| `app/main.py` 의 `_run_comparison` 승계 훅 | Rev.B 에서 같은 안정 ID 매칭 행에 확정 문장 승계. 사람이 고친 행은 덮지 않음 |
| `app/static/app.js` 의 `fromToPicker`·`bindFromToPicker` | ④ 행 근거 패널의 지정 UI. 같은 런 ④ 행에는 제안만 — 행마다 사람이 누른다 |
| `app/main.py` 의 `_axis_text_noise` · `texts_filtered` | 후보 잡음 필터(§3 제거 패턴 재사용)와 **탈출구**. 거른 것은 버리지 않고 "전체 후보 보기" 로 펼친다 — 정답이 주석 덩어리에 섞여 인쇄되기 때문 |
| `spike/candidate_workbook.py` | 후보 병렬 워크북 `out/candidates_nouf1.xlsx` (10회차). 축을 고르지 않고 나란히 놓는다. 조건 열의 한계값은 전부 그 문서에서 유도 |
| `spike/read_candidates.py` | 워크북의 '선택 후보'를 6회차 `axis_overrides` 로 적는다. '작성 예시' 행은 건너뛰고, 안정 ID 가 안 물리면 적지 않고 센다 |
| `docs/new_project_checklist.md` | 새 프로젝트에서 무엇이 따라오고 무엇을 다시 재는지 (필수 3 · 발주처 리스트 필요 12 · 선택 2) |
| `config/project_alnouf1.yaml` | ②표기 선택 + ③프로젝트 고유값 전부. 각 항목에 근거 수치 주석 |
| `app/static/index.html` 의 `.intake` | 첫 화면 두 단. 왼쪽(프로젝트·비교 대상)이 정해져야 오른쪽(드롭)이 열립니다. 900px 아래에서는 세로로 쌓이고 왼쪽이 위 |
| `app/static/app.js` 의 `setSetupDone`·`chooseProject`·`revSummary` | 잠금 규칙과 늘 보이는 한 줄 요약(`Rev.C vs Rev.A`). Rev.A 는 비교 상자를 숨기고, Rev.B 는 Rev.A 로 고정합니다 |
| `pipeline.analyse` 의 `say(..., sheets=)` · `db.set_progress(sheets=)` | 진행 장수. 분모는 **분석 대상 도면 수**(이 문서 52)이지 쪽수(58)가 아니고, 대상이 확정된 순간 한 번 실린 뒤 바뀌지 않습니다 |
| `app/main.py` 의 `_count_pages` | 업로드 직후 PDF 에서 센 쪽수. 58장 19MB 에서 **2.1~2.9ms**. 0 은 "0쪽"이 아니라 "못 셌다" |
| `app/static/app.js` 의 `STAGE_KO`·`stageWords` | 파이프라인 단계 이름 → 화면 말. **모르는 값은 그대로 씁니다.** 예전에는 `starting` 이 영어 그대로 나갔고 80초 표본 중 79번이 그것이었습니다 |
| `app/static/styles.css` 의 `rect.revring` | 개정은 검출 상자 **바깥 링**으로 그립니다. 예전처럼 상자의 굵기·파선을 덮어쓰면 추가된 밸브가 밸브의 파선을 잃습니다 |
| `app/static/app.js` 의 `GRADE_MARK` · `.gradge` | 등급 배지 — 모양·글자·색 셋이 같이 실립니다. 색만으로 구분하지 않습니다 |
| `app/static/img/*.svg` | 삽화 12개(18.1KB). **전부 CSS 배경**이라 DOM 에 없고 스크린리더가 읽지 않으며 클릭 대상도 아닙니다. 없으면 레이아웃 그대로 크림 단색으로 떨어집니다 (실측 확인) |
| `app/static/styles.css` 의 `:root` | 크림 토큰. `--ink` 는 **선 전용**이고 글자는 `--ink-strong`/`--ink-soft`. 어두운 판의 개정 3색은 크림 위에서 전부 미달(2.01/2.15/2.31)이라 다시 골랐습니다 |
| `app/static/index.html` 의 `header.head` | 머리줄 두 층 — 위는 지표(신원·탭·배지), 아래는 동작. 층을 합치면 15행이 13행으로 줄어듭니다 |
| `app/static/app.js` 의 `auditProblems` | 위생 통계에서 0 인 항목을 접습니다. 전부 0 이면 "이상 없음" 한 줄, 아니면 0 아닌 것만 펼칩니다 |
| `app/engine/describe_llm.py` 머리 주석 | **꺼져 있는 것이 아니라 연결돼 있지 않습니다** — `select()` 를 부르는 곳이 테스트 말고 없습니다. config 를 켜도 호출되지 않습니다 |
| `docs/ui_visual.md` | 이번 회차의 실측 대비표 · 세 색 층 규칙 · 진행 표시의 근거 · 판단 대기 후보 3건 |
| `app/paths.py` | 읽는 뿌리(번들 안, 종료 시 삭제)와 쓰는 뿌리(exe 옆 `pid_data/`)를 가릅니다. 섞으면 결과가 사라집니다. `PID_DATA_DIR` 로 쓰는 뿌리를 덮어쓸 수 있고, UI 테스트가 그것으로 사본에서 돕니다 |
| `app/main.py` 의 `_failure_reason` | 분석 실패 때 **화면에 갈 문장**. 예외를 보지 않고 PDF 를 다시 열어 쪽 수와 글자 수만 셉니다 — 예외를 바꿔 쓴 문장은 예외이기 때문입니다. 특정 못 하면 일반 문구 |
| `job.error_detail` 열 · 진단 zip 의 `error.txt` | 예외 원문이 사는 곳. 화면에도 `/jobs` 응답에도 나가지 않습니다 (`_job_public`) |
| `app/static/app.js` 의 `showFailure`·`toFirstScreen` | 실패 화면의 사유·다음 행동·첫 화면 버튼·이전 분석 목록. 이것이 없으면 `#drop` 도 `#main` 도 숨겨져 나갈 길이 URL 편집뿐입니다 |
| `spike/accuracy.py` · `spike/client_lists.py` | 정확도 회귀를 다시 재는 절차. 발주처 자료가 있는 기계에서만 돕니다 |
| `app/revisions.py` | 프로젝트 저장소 · 안정 ID(`10LBA10-001`) · 리비전 대조. **ID 는 Rev.A 에서 한 번만 부여하고 재정렬하지 않습니다** — 좌표순 재부여는 1건 추가를 "1건 추가 + 40건 수정" 으로 만듭니다 |
| `app/revisions.py` 의 `match_radius` | 매칭 반경. **그 도면 자신의 버블 긴변 중앙값**이고 코드에 상수가 없습니다 (실측 54.4/68.0/85.0/85.1pt). 못 재면 같은 TYPE 최근접의 절반 |
| `app/revisions.py` 의 `compare` | 상태 판정. 자리 이동만으로는 '수정' 이 아니고 `moved_pt` 로 기록만 합니다 — 임계값에 근거가 없기 때문. 삭제는 `DELETED_CANDIDATE` 까지이고 사람이 확정해야 굳습니다 |
| `app/audit.py` | 데이터 디렉터리를 **세기만** 합니다. 삭제 코드가 없습니다 (`docs/db_hygiene.md` 의 (가)만 승인됨). 기동 시 로그 · 진단 MANIFEST · `GET /audit` |
| `revision_state` · `deleted_candidate` 테이블 | 대조 결과와 삭제 후보. `job.project`/`revision`/`compared_with` 와 함께 `_ADDED_COLUMNS` 로 가산 마이그레이션 |
| `docs/revision_provision.md` | 확정 사항 + 이번 회차 구현 + **미구현 목록** + 확인 대기 3건 |
| `docs/db_hygiene.md` | DB 위생 방침 후보와 실측 (고아 출력 43 · DB 168MB 중 91%가 죽은 페이지) |
| `app/version.py` · `app/_build.json` | 버전은 손으로, 빌드일은 `build.bat` 이 새깁니다. 커밋하지 않습니다 |
| `app/desktop.py` | exe 진입점. 빈 포트 선택 · 브라우저 열기 · **오류 시 창을 붙잡고 사유 출력** |
| `pid_extract.spec` · `build.bat` | 무엇을 넣고 무엇을 뺐는지가 주석에 있습니다 (`data/` 는 절대 안 들어감) |
| `app/pipeline.py` 의 `_layers` 색 갈래 | 오버레이 색을 정하는 **단 하나의 곳**. SCOPE 열과 같은 값(`r.scope`)을 읽는다 — 색과 열이 서로 다른 값을 읽으면 언젠가 갈린다 |
| `app/static/app.js` 의 `SCOPE` 배열 | 범례 네 갈래의 라벨·색. **라벨과 세는 대상이 같아야 한다** (11회차 캡처가 어긋난 것을 잡았다) |
| `app/static/app.js` 의 근거 패널 세 줄 | 공급 주체 / 추출 결과 / 발주처 양식. 11회차부터 "추출됨"과 "양식에 나감"은 서로 다른 사실이라 한 문장으로 못 쓴다 |
| `app/static/styles.css` 의 `td.col-scope` | SCOPE 열만 폭 330px · 12px · 자간 −0.01em. **행 높이는 안 건드린다** — §7.2 밀도(29.0px / 15행) |
| `pipeline.analyse` 의 캐시 예열 루프 | `layout` 층 안에서 장마다 `pc.segments()` 를 한 번 부른다. 없으면 진행 신호가 없는 구간이 95.8초 + 129.0초 생기고, 서로 다른 시점의 캡처가 **바이트까지 같아진다** |
| `app/main.py` 의 `Cancelled`·`_cancel_requested`·`POST /jobs/{id}/cancel` | 취소. 행을 쓰는 곳은 `db.store_result` 하나, 안정 ID 를 쓰는 곳은 `_run_comparison` 하나이고 둘 다 `analyse` 뒤라 **분석 중 취소는 아무것도 쓰지 않는다** |
| `app/main.py` 의 `_scope_summary` | 완료 화면 숫자(추출 / 양식에 나감 / 빠짐)의 출처. 화면과 Excel 이 같은 판정(`excel_out.in_client_scope`)을 읽는다 |
| `app/static/app.js` 의 `_pan`·`PAN_SLOP` | 드래그 팬. `#stage` 의 스크롤만 옮기고 SVG 좌표는 안 건드리므로 확대·선택·오버레이가 그대로다. 4px 이하 이동은 클릭으로 넘긴다 |
| `app/main.py` 의 `GET /home` | 첫 화면이 읽는 것 **하나**. 프로젝트 → 리비전, 그리고 프로젝트에 안 묶인 분석. 단위가 job 이 아니라 프로젝트다 (13회차) |
| `app/main.py` 의 `_carry_edits` | 이전 리비전의 편집을 잇는다. 짝은 **안정 ID** — §7.3 의 ID 를 쓰는 것이지 새로 매기지 않는다. 추출값 불변 · 이미 고친 칸 안 덮음 · 갈리면 conflict |
| `app/db.py` 의 `carry_user_values`·`flag_carry_conflicts` | 승계의 두 반쪽. 빈 자리만 채우고, 엔진 답이 갈린 칸은 값을 고르지 않고 둘 다 들고 검토로 올린다 |
| `app/revisions.py` 의 `document_revision` | 장별 Rev 를 문서 하나로 접는다. **규칙은 도면에서 유도되지 않아** config `revision.document_rule` 에 있고 세 후보의 실측 답이 주석에 있다. 접은 값과 분포를 **함께** 돌려준다 |
| `app/revisions.py` 의 `compare_sheet_revisions` | 장 단위 대조. **한 도면번호에 장이 여럿이면 판정하지 않는다**(`ambiguous`) — 이 문서에 그런 도면번호가 3개 있고, 고르면 같은 PDF 가 개정으로 읽힌다 |
| `pid_page.rev` · `job.doc_rev` | 도면이 스스로 말한 개정. `extract_titleblocks` 가 처음부터 읽고 있었는데 저장하는 곳이 없었다. **판정에 안 쓰이고 지문에도 안 들어간다** |
| `feedback.author` · `db.row_edit_history` | 누가 · 언제 · 무엇에서 무엇으로. 자기신고이고 화면이 "자칭"이라고 적는다. 승계된 편집은 원래 이름을 잇고 사유에 `Rev.A 에서 이어받음` |
| `app/static/app.js` 의 `askAuthor` | 편집마다 뜨는 이름 확인 줄. **확인은 매번, 타자는 한 번** — 지난 이름이 채워져 있고 Enter 로 넘긴다. 비우면 비운 채로 저장된다 |
| `app/static/app.js` 의 `mark(field, value)` | 근거 패널이 편집값과 엔진 근거를 **구분해서** 말한다 — `수량 9 ✎ 사람이 고침 (도면 근거는 2)`. 값을 바꾸지 않고 출처만 붙인다 |
| `docs/round13_persistence.md` · `out/round13_survey.md` | 13회차 측정 전문 · 구현 전 실태 조사 7문항 |
| `docs/round12_ui.md` · `docs/round12_annotation_design.md` | 12회차 측정 전문 · 주석 저장 설계안(미구현) |
| `app/static/app.js` 의 `scopeFacts` | SCOPE 한 값이 무엇을 뜻하는지 — **화면에서 이 판정을 하는 곳은 여기 하나**. 근거 패널 · 저장 전 안내 · 저장 후 안내가 같이 읽는다. 서버 쪽 한 벌은 `excel_out.in_client_scope` |
| `app/static/app.js` 의 `editNotice`·`noticeAfterEdit` | 편집이 발주처 양식에 어떻게 닿는지 한 줄. **막지 않고 알리기만** 한다. 한 번에 한 줄만 둔다 — 앞 행의 안내가 남으면 반대 문장이 동시에 보인다 (14회차 캡처) |
| `app/main.py` 의 `scope_summary_now` | 지금 양식에 나가는 행 수 (편집 반영). 완료 화면이 쓰는 `_scope_summary` 를 그대로 부르고 삭제 행은 뺀다 — `write_all` 이 그 행을 먼저 거르므로 |
| `tests/test_ui_edits.py` 의 `field_rows`·`test_step0_...scope_aware_data` | UI 스위트가 **어떤 데이터에서 도는지**를 시험이 먼저 말한다. 행은 순서가 아니라 SCOPE 상태로 고른다. 전제가 깨지면 **건너뛰지 않고 실패**한다 (건너뛰기가 세 회차 동안 침묵을 만들었다) |
| `config/project_sadara.yaml` | SADARA 교차검증의 기준선(82행)은 이 파일을 **안 쓴** 값이다 — 프로필 자동 맞춤은 없고 미지정이면 alnouf1 이다. 지정하면 75행 |
| `app/legend_profile.py` | 15회차 — 프로젝트 범례 프로필의 자리·담기·되쓰기·대조. **범례 장에서 나온 여덟 항목만** 담는다. 엔진 모듈은 `_engine()` 으로 **맨 이름**으로 가져온다 (`app.engine.x` 로 가져오면 `Derived` 가 두 클래스가 된다) |
| `legend_profile.capture` 의 JSON 정규화 | 저장한 것과 다시 읽은 것이 같아야 한다. JSON 은 튜플을 배열로, dict 키를 문자열로 바꾸므로(`dash_histogram` 7.2 → "7.2") 그 변환을 담을 때 한 번 겪게 한다 |
| `pipeline.analyse` 의 `measure_legend` | 재는가 안 재는가. **프로필이 없으면 반드시 잰다** · 프로필이 있어도 범례 장이 있으면 **대조를 위해** 잰다(적용은 프로필) · 둘 다 없을 때만 안 잰다 |
| `pipeline.LegendUnavailable` | 범례도 프로필도 없을 때. **예외를 바꿔 쓴 문장이 아니라** 파이프라인이 직접 검사한 조건이고 문장도 거기서 쓴다 — `_failure_reason` 은 *알 수 없는* 실패의 자리다. 검사는 `derive_connector_reach` **앞**에 서야 한다 (뒤에 세우면 `KeyError` 가 먼저 난다) |
| `detect_valves.derive_layout(..., derived=)` | 15회차에 는 인자 하나. 프로필에서 되살린 유도 결과를 **그대로 받는** 자리이고, 측정 코드는 한 줄도 바뀌지 않았다 — `kw` 조립은 두 경우 모두 같은 코드가 한다 |
| `app/main.py` 의 `_legend_facts` | 이 결과가 어느 범례로 나왔나 — **화면에서 이 판정을 하는 곳은 여기 하나**. 근거 띠·완료 화면·갱신 선택이 같이 읽는다. 옛 분석은 `mode: unknown` 으로 말한다 (없는 것을 "유도했음" 으로 읽지 않는다) |
| `app/main.py` 의 `adopt_legend_profile` | 범례가 달라진 곳을 **항목 단위로** 프로필에 반영. 사람이 눌렀을 때만 돌고, 안 고른 항목은 옛 값 그대로 남는다. 이번 분석 결과는 바뀌지 않는다 — 다시 분석해야 반영된다 |
| `app/static/app.js` 의 `loadLegendProfile`·`legendChangeBlock` | 결과 화면 머리 아래 띠. 서버가 쓴 문장을 놓기만 하고 판정을 다시 하지 않는다. 달라진 칸은 **옛값 → 새값**으로 그대로 보이고 **자동 갱신은 없다** |
| `config` `valves.legend_fallback` | `butterfly`·`actuator_stem`·`pneumatic` **셋뿐이고 `line_styles` 가 없다.** 그래서 범례 없는 PDF 가 죽었다 (15회차 [B]4). 셋의 숫자가 범례 값과 **같아서** p3 가 빠져도 값으로는 안 잡히고 `source` 로만 잡힌다 |
| `report` 테이블 · `app/main.py` 의 `_capture_row` | 오류 신고. 사람이 적는 것은 **무엇이 틀렸나 + 한 줄** 둘뿐이고 나머지는 서버가 담습니다 |
| `_diagnostic_zip` | 진단 내보내기. 담긴 것과 **뺀 것**을 MANIFEST 에 적습니다 (원본 PDF·발주처 Excel 제외) |

## 8. 다음 프롬프트를 쓸 때

- 목표 문장(발주처 실제 표기)을 먼저 제시하고, 조각별 현재 정확도를 근거로
  개선 대상을 지정하세요. 집계 지표만으로는 실무 가치가 보이지 않습니다.
- 새 규칙은 **범례에서 유도**하거나 **발주처 557행에서 센 값**이어야 합니다.
  어느 쪽도 아니면 "유도 불가"로 보고하게 하세요.
- 회귀 항목(2절 표)을 프롬프트에 그대로 넣어 매번 확인시키세요.
- 발주처 557행에서 이번 회차에 센 값(다시 세지 마세요): 도면번호 **0행** ·
  느슨한 하이픈 코드 **0행** · 그리드 셀 **0행** · DN 치수 **0행** · NOTE **0행** ·
  ASME/ANSI/API **0행** · 플랜지 클래스 **0행**. LS 22행 = **11쌍**이고 한 행이 두
  신호를 말하는 문형은 **0행**.
- **등급 이름은 근거를 말하지 판정을 말하지 않습니다.** `CONFIRMED` 는 "도면 근거
  있음"이며 발주처와 같다는 뜻이 아닙니다 (대조 455행 중 완전일치 27 · 주어 74.3%).
  더 좁은 등급을 만들 근거는 찾지 못했습니다 — 모든 갈래가 63~80% 로 붙어 있습니다.
- 발주처 557행에서 이미 센 값(다시 세지 마세요):
  구두점 `.`/`(` **0회** · 위치어 부착률 PIT 70.8 / PDIT 67.3 / FS 100 / FIT 41.9 /
  FE 38.9 / TIT 32.0 / TI 11.9 / PI 6.3 / LIT·LS·LI 0 ·
  끝 순번 PIT 44 / TIT 36 / FIT 26 / PDIT 6 / LIT 5 / 나머지 0 ·
  같은 도면 안 문장 반복 110행(19.7%) · 중간 심볼 13행(전부 PDIT `SUCTION STRAINER`).
- 계기가 기기의 **위·아래**에 있을 때 발주처가 쓰는 낱말은 유도되지 않습니다:
  위 = 없음 60 / DISCHARGE 24 / SUCTION 12 / OUTLET 2, 아래 = 없음 115 / OUTLET 9 /
  INLET 6 / DISCHARGE 5. 다수는 **아무 낱말도 쓰지 않는 것**이므로 붙이지 않습니다.
- 발주처 557행에서 이번 회차에 센 값:
  라인 문구 `TO`/`FROM` **36행(6.5%)** — LS 22/22 · 나머지 전부 합쳐 14/535 ·
  제목 약어는 `CLOSED COOLING WATER`→CCW(127행)·`AUX. COOLING WATER`→ACW(4행) **둘뿐** ·
  CCW 도면 TI→RETURN 51/54 · CCW 도면 PI→SUPPLY 49 / RETURN 51 (**구분 불가**).
- 이번 회차에 센 값: 라인 문구를 안 쓴 행에 어휘를 넣을 때는 **수식어까지 세세요** —
  낱말만 넣으면 배관 주석이 거리로 정답을 누릅니다 (§3 표 참조).
  발주처가 기기 앞에 쓰는 수식어: VALVE ← BYPASS 54 / LETDOWN 3 / CONTROL 3 /
  MODULATING 2, HEADER ← DISCHARGE 16 / STEAM 6 / SEAL 4 / RING 3, BOX ← WATER 6 /
  FLASH 5. 기기별 위치어: TANK 없음 59/59 · VALVE DOWNSTREAM 30/65 ·
  HEADER DISCHARGE 16/32 · PUMP·COOLER 는 양쪽(기하에 맡김).
- **규칙을 어느 층에 둘지 먼저 정하세요.** 발전 플랜트면 어디나 같은 것은
  `config/plant_standard_abbr.yaml` 에, 이 발주처의 습관은 `project_*.yaml` 에
  둡니다. 표준 파일에 프로젝트 고유 표기를 섞으면 다음 프로젝트에서 그대로 못
  씁니다. 표준 파일은 **방향을 고정하지 않습니다** — 어느 형태를 쓸지는 ②층입니다.
- **판정축 회귀**: ④→② 전환율 5.4% · 적용 192행 · 지문 7b34064e 이 기준선입니다.
  판정 트리 규칙을 바꾸면 824행 전수 대조(불변 11칸)와 `pytest -q
  tests/test_describe_axis.py` 를 다시 돌리세요.  무제한 순회는 금지이며
  그 근거 실측(10.4%)이 §3 과 `docs/from_to_axis.md` 에 있습니다.
- **화면 회귀는 "서로 다른 시점의 캡처가 바이트로 달라야 한다"로 잽니다.**
  같은 방법이 두 회차(§7.2 · 12회차)에서 같은 실패를 잡았습니다 — 진행 신호가
  없는 구간이 있으면 13초 화면과 60초 화면의 md5 가 같습니다.
- **열 잘림 캡처는 화면 비율로 자르지 말고 그 열의 bounding box 를 읽어
  자르세요.** 어림해 자르면 옆 열이 찍히고, 가로 스크롤을 끝까지 밀면 이번에는
  그 열의 왼쪽이 컨테이너 밖으로 나갑니다 (12회차에서 두 번 다 겪었습니다).
- **없는 것을 만들기 전에 있는 것을 찾으세요.** 13회차는 요구 세 가지 중 둘이
  이미 서버에 있었고(재분석 없는 로드 0.69초 · 편집 영속 10열 · 두 층 분리),
  없던 것은 화면에서 거기로 가는 길이었습니다. 10회차 M 심볼과 같습니다.
  **구현 전 실태 조사를 한 회차의 첫 단계로 두면 범위가 줄어듭니다.**
- **어떤 값을 하나로 접을 때는 접기 전 분포를 함께 내세요.** 문서 대표 Rev 는
  세 후보가 서로 다른 답을 내고(`max`→D · `mode`→B · `first_sheet`→B) 도면이
  답을 주지 않습니다. 규칙은 config 에, 근거는 주석에, 분포는 화면에 둡니다.
- **식별자가 정말 유일한지 재세요.** 도면번호는 장을 유일하게 가리키지 않습니다
  — 같은 번호를 쓰는 PID 장이 3쌍이고 개정이 서로 다릅니다. 짝이 여럿이면
  판정하지 않는 쪽이 맞습니다 (고르면 같은 PDF 가 개정으로 읽힙니다).
- **화면이 데이터에 대해 하는 말은 데이터와 같은 접근자에서 나와야 합니다.**
  오버레이 색·범례 라벨·근거 패널 문장·SCOPE 열이 서로 다른 값을 읽으면
  언젠가 갈립니다 (11회차 캡처가 셋을 한꺼번에 잡았습니다).
- **시험이 어떤 데이터에서 도는지 시험이 말하게 하세요.** 14회차에 `UI 21` 이
  세 회차 동안 **필터가 꺼진 것과 같은 데이터**(SCOPE 전부 빈칸)에서만 돌고
  있었습니다. 코드는 같고 데이터가 달랐으므로 파일 동일성으로는 안 잡힙니다.
  전제가 깨지면 **건너뛰지 말고 실패**시키세요 — 건너뛰기가 그 침묵을 만듭니다.
- **시험이 깨지면 먼저 "제품이 틀렸나 가정이 틀렸나" 를 API 로 가르세요.**
  14회차의 두 실패는 필터가 설계대로 동작한 결과였고, 고칠 것은 픽스처였습니다.
- **화면이 편집에 대해 하는 말도 데이터와 같은 접근자에서 나와야 합니다.**
  근거 패널만 고치면 **행을 누른 사람에게만** 말하게 됩니다 — 칸을 바로 고치는
  사람은 아무것도 못 봅니다 (12회차가 그렇게 절반만 고쳐져 있었습니다).
- **SADARA 회귀는 `PID_PROJECT_CONFIG` 를 지정하지 않고 잽니다** (82행
  `0767ba79`). 지정하면 75행이고 그것은 기준선이 아닙니다. 프로필 자동 맞춤은
  없습니다 — 미지정이면 alnouf1 고정입니다.
- **캐시해도 되는 값과 캐시하면 안 되는 값을 먼저 가르세요.** 15회차의
  `legend_rules` 단계는 열 가지를 유도하는데 **범례 장에서 나오는 것은 여덟**
  이고, `connector_reach`(도면 218개)와 `layout` 19건(도면 58장)은 다음
  개정본의 **도면**이 답해야 합니다. 한 단계 안에 있다는 이유로 같이 담으면
  용지가 바뀐 개정본에서 조용히 틀립니다.
- **"저장했다가 다시 쓴다" 는 `source` 를 바꾸는 일이 아닙니다.** 지문이
  `source`·`note`·`values` 를 해싱하므로 재사용이 `PROFILE` 이라고 적으면
  같은 값을 쓰고도 지문이 달라지고, **프로필이 값을 정확히 담았는지 확인할
  길이 사라집니다.** 재사용 여부는 지문 **밖**에 적으세요.
- **적용(학습)과 대조(확인)는 다른 일입니다.** "다시 학습하지 않는다" 가
  "다시 읽지 않는다" 는 아닙니다 — 개정본에 범례가 있으면 재서 **대조만**
  하세요. 대조를 빼면 재작도된 범례를 옛 규칙으로 읽고도 아무도 모릅니다
  (지문은 정상으로 나오고 행 수도 그럴듯합니다).
- **폴백이 조용한지 시끄러운지 항목마다 다릅니다.** 15회차 실측: 범례가 없으면
  `line_styles` 는 `KeyError` 로 죽고, `unit_multipliers` 는 7코드 → 3코드로
  **조용히** 줄어 Q'ty 만 틀립니다. **죽는 쪽이 안전한 쪽입니다** — 조용한
  폴백을 먼저 찾으세요.
- **속도 개선처럼 보이는 요구가 속도 문제가 아닐 수 있습니다.** 15회차의
  `legend_rules` 는 전체의 1.5%, 범례 몫은 0.4% 였고, 실제 값어치는 **범례
  없는 개정본이 완주하는 것**과 **범례가 달라진 것을 잡는 것**이었습니다.
  먼저 재고, 성과를 재지 않은 축으로 적지 마세요.
- **TIT 의 위치어는 유도되지 않습니다** (다시 재봤습니다): 방향별 최다가 전부
  "낱말 없음"(ABOVE 7/11 · LEFT 14/22 · RIGHT 13/22)이고, 기기 종류로 갈라도
  VALVE 89% 없음 · HEADER·TANK·BOX 100% 없음 · PUMP 는 n=10 에 SUCTION 3 /
  DISCHARGE 3 / OUTLET 2 로 과반이 없습니다. 목표 문장 3번은 여기서 막혀 있습니다.
