# 인수인계 — P&ID → 계기·밸브 리스트 추출

이 문서 하나로 다른 PC·다른 계정에서 이어받을 수 있게 쓴 것입니다. 코드보다 먼저
읽으세요. 상세 수치와 근거는 `README.md`, 설계 원본은 `docs/design.md` 입니다.

> **★ 새 세션은 `docs/state_and_roadmap.md` 를 먼저 읽으세요.**
> 이 문서는 **회차별 기록**이라 길고 시간순입니다.  그 문서는 **지금 상태 ·
> 절대 불변식 · 반복되는 실패 유형 열 가지 · 다음에 무엇을 할지(로드맵)** 만
> 담고 있어 방향을 세우는 데 쓰입니다.  이 문서는 그 근거를 찾을 때 봅니다.

## 1. 지금 어디까지 와 있나

| 단계 | 상태 |
| --- | --- |
| Phase 0 검증 스파이크 (타이틀블록 · 계기 · 벤더마크 · 밸브) | 완료 |
| Phase 1 MVP (FastAPI + SQLite + 검토 UI + Excel 출력) | 완료 |
| 스코프 판정 · 출력 게이트 · 수정이력 수집 | 완료 |
| 배관 그래프 (Description 준비) | 완료했으나 **추적률 13.1%** 로 실용성 없음 |
| Description 생성 (거리 문형) | F1 74.6 · 완전일치 34 — ④(판정 불가) 618행이 이 문형을 유지 |
| **Description 판정축 (5회차)** | **혼합 적용** — 판정 성립 192행(①125 ②35 ③32)만 `FROM-TO/기기 직결` 문형, ④는 현행 유지. ④→② 전환율 **5.4%** (`docs/from_to_axis.md`) |
| **p6 육안 피드백 반영 (10회차)** | TW 계상 · 벤더 공급분 계상 · SCOPE 열 · TYPE 표기 · 라인 최근접 커넥터. **정밀도 84.1% → 70.5%** 이고 원인은 벤더 계상 하나 — 발주처 확인 대기 (`docs/round10_feedback.md`) |

**⚠ 아래 표는 9회차까지의 기준선이고, 10회차가 다섯 값을 옮겼습니다.**
10회차 값은 `docs/round10_feedback.md` §8 이 기준선이며, 그중 **정밀도 하락
13.6pp 는 전부 "벤더 공급분을 계상한다"는 요구 하나에서 나옵니다** (204행을
넣어 TP +1 · FP +136).  발주처가 계상하지 않기로 하면 config
`scope.exclusion_rules: glyph+text+box` 한 줄로 아래 표로 돌아갑니다.

| 항목 | 9회차 기준선 | 10회차 | 11회차 | 12회차 | 13회차 | 14회차 | 15회차 | 16회차 | 17회차 | 18회차 | 19회차 | 20회차 | 21회차 | 22회차 | 23회차 | 24회차 | 25회차 | 26회차 | 27회차 | 28·29회차 | 30회차 | 31회차 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `fingerprint` | 91ba3e33 | 3a57b6b2 | **69ad281f** (움직인 칸은 밸브 144행의 `scope` 뿐) | **69ad281f** (불변 — 화면만 고쳤습니다) | **69ad281f** (불변 · 편집 5칸 뒤에도 같음) | **69ad281f** (불변 — 엔진 파일 0개) | **69ad281f** (불변 · **세 경로 전부 같음** — 유도/재사용/강제유도) | **caf36dc3** (SCOPE 14행이 움직였습니다 — 그 열이 지문 대상입니다) | **caf36dc3** (불변 — **`description` 은 지문 대상이 아닙니다**) | **015e34b3** ([C] SCOPE 5행 · [E] ANGLE 8행) | **fb85b039** (SCOPE 55행 — 원인 넷) | **fb85b039** (불변 — 엔진 코드 0줄) | **fb85b039** (불변 — 최종 코드로 전량 재측정) | **fb85b039** (불변 — **혼용 뒤에도** · 1037행 전수 대조 움직인 칸 0) | **fb85b039** (불변 — 엔진 코드 0줄) | **fb85b039** (불변 — AL NOUF1 은 유도값을 안 씁니다) | **fb85b039** (불변 — TC2 만 `c46b10fa`→`a94bb2ad`) | **fb85b039** (불변 — TC2 만 `a94bb2ad`→`536d3baa`) | **fb85b039** (불변 — TC2 만 `536d3baa`→`e909cf7c`) | **fb85b039** (불변 — TC2 만 `e909cf7c`→`c0a2d29d` · [B] 에서 칸 단위 규명 · **UAD 신규 `31231bd7`**) | **fb85b039** (불변 — UAD 만 `31231bd7`→`db1a77d1` · SCOPE 18행) | **fb85b039** (불변 — 사람 값이 없으면 네 프로젝트 전부 불변 · 넣어도 AL NOUF1 은 움직이지 않습니다) |
| 행 수 | 824 | **1029** | **1029** (불변) | 1029 | 1029 | 1029 | 1029 | 1029 | 1029 | **1037** | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변) | 1037 (불변 — UAD 149 신규) | 1037 (불변) | 1037 (불변) |
| Q'ty 합계 | 1595 | 1911 | 1911 | 1911 | 1911 | 1911 | 1911 | 1911 | 1911 | **1931** | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변) | 1931 (불변 — TC2 만 345 → **3047**) | 1931 (불변 — TC2 3047 · UAD 149) | 1931 (불변) | 1931 (불변 — AL NOUF1 은 승수 대상이 **0행**입니다) |
| 등급 | 704 / 47 / 12 / 61 · SKIP 0 | **704 / 48 / 51 / 22 · SKIP 204** | 같음 | 같음 | 같음 | 같음 | 같음 | **698 / 42 / 51 / 22 · SKIP 216** (벤더가 된 12행이 ⓪로) | 같음 | **700 / 42 / 52 / 22 · SKIP 221** | **670 / 30 / 52 / 22 · SKIP 263** (벤더가 된 42행이 ⓪로) | 같음 | 같음 | 같음 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 |
| **정확도 — 축1 전량** | 99.2% / 84.1% | **99.3% / 70.5%** | 99.3% / 70.5% (불변) | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 | 불변 (99.3 / 70.5) | 불변 |
| **정확도 — 축2 SCT** (11회차 신설) | — | — | **99.2% / 86.8%** | 불변 | 불변 | 불변 | 불변 | **99.3% / 88.4%** (FP 90→78 · FN 5→4) | 불변 (FP 78 · FN 4) | **99.3% / 89.1%** (FP 78→73 · FN 4) | **99.3% / 94.4%** (FP 73→**35** · FN 4) | 불변 (FP 35 내역을 전수로 갈랐습니다 — §20회차) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) | 불변 (FP 35 · FN 4) |
| 글리프 · Description F1 · 식별 가능성 · ④→② 전환율 | H4/M32 · 69.3 · 39.6% · 19.7% | **전부 불변** | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 | H4/M32 · **69.4** · 39.6% · 19.7% | H4/M32 · **68.7** · **41.6%** · 19.7% (F1 은 내리고 식별은 오릅니다 — §17회차) | 전부 불변 (밸브 8행만 늘어 FIELD 채점에 안 닿습니다) | H4/M32 · **68.8** · **40.5%** · 19.7% (식별은 내립니다 — 벤더가 된 42행의 문장이 지워집니다) | 전부 불변 | 전부 불변 | 전부 불변 | 불변 | 전부 불변 (40.5% · 68.8 · 19.7%) | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 | 전부 불변 |
| 테스트 | 빠른 116 | 빠른 130 | **빠른 137 · UI 21 · slow 10** | 빠른 **137** · UI 21 · slow 10 | 빠른 **150** · UI 21 · slow 10 (13회차 표에 149 로 적혀 있었으나 실측 150 — 14회차 [A] 에서 갱신) | 빠른 150 · **UI 24** · slow 10 (UI 는 **SCOPE 있는 데이터** 기준) | 빠른 **170** · UI 24 · slow 10 | 빠른 170 · UI 24 · slow 10 | 빠른 **173** · UI 24 · slow 10 | 빠른 **188** · UI 24 · slow **12** | 빠른 188 · UI 24 · slow **14** | 같음 (전부 통과) | 빠른 **207** · UI 24 · slow 14 (전부 통과) | 빠른 **213** · UI 24 · slow 14 | 빠른 213 · UI 24 · slow 14 (전부 통과) | 빠른 **215** · UI 24 · slow 14 (전부 통과) | 빠른 **219** · UI 24 · slow 14 | 빠른 **223** · UI 24 · slow **16** | 빠른 **241** · UI 24 · slow 16 | 빠른 **241** · UI 24 · slow 16 | 빠른 **247** · UI 24 · slow 16 | 빠른 **262** · UI 24 · slow 16 |
| 발주처 양식 행수 | 824 기준 | 885 / 76 / 23 | **667 / 66 / 22** (SCT 만 · CZF 25행은 양식 미수령) | 같음 | 같음 | 같음 | 같음 | **656 / 66 / 22** (SCT→VENDOR 11행이 빠졌습니다) | 같음 | **885 / 76 / 23** (전량 — [B] · SCT 로 두면 651/66/22) | **885 / 76 / 23** (불변 — SCT 로 두면 **607/63/18**) | 같음 (**꾸러미에서 실제 xlsx 를 열어 셌습니다**) | 같음 (885 / 76 / 23 — 실제 xlsx 의 NO 열을 셈) | 같음 (885 / 76 / 23) | 불변 | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) | 불변 (885 / 76 / 23) |

**축이 둘인 이유**: 축1(전량)은 "발주처가 안 세는 것을 얼마나 넣었나"까지 재고,
축2(SCT)는 "우리가 얼마나 잘 읽었나"를 잰다.  **어느 쪽도 혼자서는 답이 아니라
둘 다 남긴다** — 정의는 `spike/accuracy.py` 의 `AXES` 하나에 있고, 정답지 쪽
모수는 두 축에서 **같다**.

**확정 지표 (회귀 기준선 — 매 회차 재확인 필수)**

| 항목 | 값 |
| --- | --- |
| 계기 recall / precision | **97.0% / 87.0%** (검출 598 / Excel 536 / TP 520) |
| BFV recall / precision | **85.7% / 81.8%** |
| MOV recall / precision | **90.5% / 82.6%** |
| 액추에이터 | MOTOR 102 / PNEUMATIC 58 / HYDRAULIC 4 / **UNREAD 0** |
| 글리프 문자 | H 4 / M 32 |
| 행 수 | **824** (FIELD 680 / MOV 76 / PNEUMATIC 45 / BFV 23) — LS 묶음 합산 후 |
| **정확도 (교집합 도면 · 취소선 제외 · 항목 단위)** | **재현율 99.2% / 정밀도 84.1%** (사람 599 / 프로그램 706 / TP 594) |
| 분석 시간 | 58장 약 3분 40초 |
| 테스트 | 빠른 **116** · UI 20 · slow 10 (9회차 HEAD 실측 — 이 표에 오래 `95`·`113` 으로 적혀 있었습니다. 11회차 기준 **빠른 137 · UI 21**) |
| LS 다중 신호 | 34묶음 102버블 → **34행** (물리 1개). 발주처 LS 22행은 맞닿지 않아 **영향 0** |
| `fingerprint` | **7b34064e** — 5회차 혼합 적용이 옮겼습니다 (직전 a38fe8ac). 원인은 needs_review 뿐이고, 824행 전수 대조로 증명: 불변 칸 위반 0 · 움직인 행 = 적용 192행 · 움직인 칸 = description/remark/grade/needs_review 뿐 |

**Description 지표 (2026-08 회차)**

| 조각 | 값 | 비고 |
| --- | --- | --- |
| 발주처 557행 대조 | 완전일치 **34** · 부분 486 · 미달 0 · 대응행 없음 37 | 직전 34 / 484 / 0 / 39 |
| 토큰 정밀도 / 재현율 / F1 | **76.1% / 73.2% / 74.6** | 직전 76.1 / 73.1 / 74.6 |
| **주어 귀속** | 완전 **16.0%** (83/518) · 대부분 이상 **73.7%** | 직전 16.0% / 73.4% |
| 변수어 | **99.0%** (501/506) | |
| 기기명 | **77.9%** (285/366) | 변화 없음 — 도면번호는 머리 낱말이 아니라 꼬리였음 |
| 순번 | **42.0%** (113/269) | 직전 40.9% |
| 위치어 | 24.1% (38/158) — 오답 47 · 미부착 73 | 직전 23.4% |
| 중간 심볼 | **0%** (0/13) | 회수 불가 확정 — §3 참조 |
| 등급 | CONFIRMED **662** · PARTIAL 47 · LOW 12 · NONE 103 · SKIP **0** | 5회차 축 적용 행 192가 CONFIRMED (도면 근거 = 탭한 런과 끝점). 직전 610/58/12/144 | 불변 |
| CONFIRMED 의 실제 일치 | 대조 가능한 **455행** 중 완전일치 27 · 주어 일치 338(74.3%) | §3 하단 — 이 등급은 "도면 근거 있음"이지 "발주처와 같음"이 아닙니다 |
| 사용자 입력 사유 표시 | **137행** | CCW 방향 104(7장) · 중간 심볼 33(10장). 검토 탭에서 사유별로 묶임 |
| 재사용 가능 범위 | 기기 라벨 **486건 중 379건(78.0%)** 이 범례 어휘만으로 잡힘 | 나머지 22%가 프로젝트 어휘·수식어 |
| 서로 다른 문장 | 523 | 발주처도 557행 중 110행(19.7%)이 같은 문장을 반복 |
| 목표 도면 p33 23행 | 재현 **13** · 부분 10 · 실패 0 | 직전 재현 12 |
| 표본 3계통 15행 | 재현 **7** · 부분 8 | 직전 재현 6 |

`fingerprint` 는 엔진 판정 전체의 해시입니다. 검출을 바꾸지 않았는데 바뀌면 원인을
반드시 규명하세요 (보통 `needs_review` 문구나 `legend` 기록이 늘어난 경우입니다).

**정확도는 다시 잴 수 있어야 회귀 항목입니다.** 표에 적어만 두면 UI 를 고치다
파이프라인이 흔들려도 알아차리지 못합니다. 절차는 `spike/accuracy.py` 에 있습니다:

```bash
python3 spike/accuracy.py                 # 파이프라인을 직접 돌려서
python3 spike/accuracy.py run.json        # 이미 있는 결과 json 으로
```

모수는 교집합 도면 · 취소선 제외 · P&ID No. 가 빈 행 제외이고, 일치 수는 칸마다
`min(사람, 프로그램)` 입니다 — 발주처 리스트에 좌표가 없고 우리 행에 발주처 NO 가
없어 **1:1 대응이 성립하지 않기 때문**입니다. `data/*.xlsx` 가 없는 기계에서는
그렇게 말하고 멈춥니다.

## 2. 진행 원칙 — 어기면 되돌려야 합니다

1. **범례 유도 우선.** 판정에 쓰는 값은 문서의 Symbol & Legend 시트에서 런타임에
   측정합니다. 코드에 치수를 박지 않습니다.
2. **임의값 금지.** "90pt 반경", "10pt 최소 런" 같은 값은 근거가 없으면 쓰지
   않습니다. 필요하면 그 문서에서 분포를 재고 백분위를 쓰되 분포를 함께 보고합니다.
3. **근거 없는 값 생성 금지.** 도면에 없는 낱말은 만들지 않습니다. 비워 두고 사유를
   남깁니다 (`NEEDS_REVIEW`, Remark, 근거 패널).
4. **회귀 확인 필수.** 위 표의 수치가 하나라도 움직이면 원인을 보고하고 되돌립니다.
5. **검출에 LLM 금지.** 심볼 검출·수량·스코프는 규칙만 씁니다. LLM 은 Description
   중간 서술 *선택*에만, 기본 꺼짐으로 존재합니다
   (`tests/test_description.py` 가 검출 모듈의 import 문을 검사해 강제).
6. **측정 정의 변경 금지.** 분모·대조 집합을 바꿔 수치를 좋게 만들지 않습니다.
7. **발주처 리스트는 검증 전용.** 실사용 입력은 PDF + 빈 양식뿐입니다
   (`PID_VERIFY_EXCEL` 로만 대조 모드가 켜집니다).

## 3. 실패한 접근과 이유 — 다시 시도하지 마세요

| 접근 | 결과 | 왜 |
| --- | --- | --- |
| 배관 그래프로 Description 근거 확보 | 추적 성공 **13.1%** | 페이지당 약 320조각으로 분절. 원인은 좌표 오차가 아니라 최소 런 16.97pt 미만 스터브가 버려지는 것. 낮추면 조각이 폭발하고 F1 개선은 +4.2pp 뿐 |
| 근접 텍스트 반경으로 중간 서술 수집 | 정밀도 57.3% → **17.6%** | 240pt 원 안에 도면번호·DN 치수·그리드 좌표·ASME 코드가 들어옴 |
| LLM 선택기로 중간 서술 선택 | 30행 중 **채택 4 / 보류 26** | 모델이 아니라 후보가 부족. 후보의 대부분이 옆 계기 태그나 배관 치수 |
| 범례 기기 형상으로 도면 기기 검출 | 펌프 **0개** 검출, 5×10 잡음 161건 | 단일 축척이 없음 (범례 40.5×38.5 ↔ p26 46.2×44.0, 비율 ×0.42~×2.00 산포) |
| 범례 STRAINER 형상으로 중간 심볼 검출 | 글자 외곽선만 매칭 | 같은 이유 |
| 기기 라벨을 노운 단위로 붙이기 (위치어를 기기 명사로 판단) | PI·TI 에 없는 SUCTION 이 붙음 | 위치어는 **기기가 아니라 계기 TYPE** 이 정함 (PIT 70.8% ↔ PI 6.3%) |
| 같은 베이스라인의 낱말 간격이 행간(11.8pt)보다 넓으면 라벨을 자르기 | 재현율 −4.3pp / 정밀도 +3.4pp, 완전일치 21→15 | `#10 ST HYDRAULIC OIL COOLER` 가 `HYDRAULIC` + `COOLER` 로 잘림. **간격이 아니라 반복 낱말로 자르면 해결됨** (§3 하단) |
| 여백(빈 띠)으로 기기 무리 경계를 잡기 | p33 펌프 열에서 20pt 이상 빈 띠가 9곳, 실제 경계(y≈600)는 두드러지지 않음 | 경계와 무관한 빈 띠가 같은 크기로 흩어져 있음 |
| 라벨이 자기 위쪽 띠를 소유한다고 보기 | 주어 완전일치 9.3%→**8.2%**, 대부분이상 51.3%→**45.1%** | 펌프 열에서는 맞지만 탱크 열에서 틀림 — 탱크 라벨은 무리 **가운데**에 인쇄됨 |
| 유클리드 최근접 / 같은 열 안 y 최근접 | 각각 대부분이상 47.8% · 43.4% | 현행 체비쇼프 최근접(51.3%)보다 나쁨. **기하학적 무리 판정은 이 도면에서 답이 아님** |
| 계통 어구를 낱말 단위로 빼기 (전체 어구 통째 제거 대신) | 완전일치 30 → **10**, 주어 완전 70 → 39 | `FUEL OIL SUPPLY` 에서 두 낱말만 빼면 `SUPPLY` 가 남는데 발주처는 그렇게 쓰지 않음. **통째로 빼는 쪽이 맞음** (두 번 측정해 확인) |
| 도면 제목 낱말로 기기 후보 순위 뒤집기 | 15건 중 **13건이 동점**, 1건은 오답 쪽 | 제목은 그 시트의 모든 기기에 똑같이 걸림 |
| 라벨 글자 크기로 주·부속 기기 구분 | 높이 분포가 14/25/29/37 — **줄 수**일 뿐 | 폰트 크기는 한 종류 |
| 판정 트리를 그래프 순회로 완성 (5회차 진단) | 무제한 순회로도 FROM·TO 짝 **10.4%** (도달 40.0%) | 선분이 엘보·버려진 스터브에서 끊김 (끝점→직교 런 간격 p50 21.4pt > min_run 16.97). 배관 그래프 13.1% · 국소 연결 4.9% 에 이은 **세 번째 실측** — §2.3 순회 금지가 옳았다는 증거. `docs/from_to_axis.md` |

**대신 통하는 것**: 기기는 **도면이 인쇄한 이름**으로 찾습니다(332건/47장). 문형과
낱말 규칙은 **발주처 557행에서 센 값**을 config 에 근거 수치와 함께 적습니다.

**이번 회차에 통한 것 — 주어 귀속 (완전 5.2% → 7.9%, 대부분이상 49.4% → 60.0%)**

1. **나란히 인쇄된 라벨을 반복 낱말로 자릅니다.** 한 베이스라인에 이미 나온 낱말이
   다시 나오면 거기서 다음 라벨이 시작합니다 (`#10 ST` 가 네 번 인쇄되면 쿨러 네
   대). 한 라벨을 여는 낱말이 다른 조각 안에 있으면 거기서 또 자릅니다. 행간은
   **문서 전체에서** 재고 (글자 높이를 하한으로) 15.3pt — 페이지별로 재면 p37 처럼
   글줄이 36개뿐인 시트에서 최빈 간격이 2.0pt 가 되어 세 줄짜리 이름이 붙지 않습니다.
   결과: `CLOSED COOLING WATER OIL COOLER OIL COOLER` → `#10 ST HYDRAULIC OIL COOLER A`.
2. **계통 접두어** (`AUX` / `GT` / `HP` / `LP` / `ST LUBE` …). 한 시트에서 꼬리가
   같고 머리만 다른 이름들만 한 묶음으로 보고, 계기가 **그 기기보다 더 가까이 있는**
   `TO` / `FROM` 문구에 그 묶음의 접두어가 **정확히 하나** 나올 때만 주어를 바꿉니다.
   전 도면 3행에서 발동하며, 목표 문장 2번(`GT ... DISCHARGE PRESSURE A`)이 그 3행
   중 하나입니다. 기기 라벨 자체의 텍스트까지 읽으면 접두어가 둘이 되어 절대
   발동하지 않습니다 — `TO`/`FROM` 문구만 읽습니다.
3. **유닛 번호는 계기 옆에 인쇄된 것**을 씁니다. 발주처 438행 중 시트 코드가 맞는
   것 232 · 가까운 표기가 맞는 것 173 · 가까운 표기가 깨뜨리는 것 31 → **순증 142**.
   단 시트 코드가 `00` 인 도면은 발주처가 접두어를 아예 안 쓰므로 덮어쓰지 않습니다.
4. **경보 접미** `LSHH` → `LEVEL HIGH HIGH` (11행) · `LSH` → `LEVEL HIGH` (11행).
   범례 p3 의 후속문자표에는 H·L 수식자가 **없습니다**. `LSL` 은 발주처 리스트에
   0행이라 `LOW` 는 **근거 없는 완성**으로 config 에 그렇게 표시해 뒀습니다.
5. **위·아래 위치어**는 주어를 고친 뒤 다시 세니 과반이 나왔습니다:
   `PIT ABOVE → DISCHARGE` 21/35, `PDIT ABOVE → SUCTION` 6/10, `PDIT LEFT → SUCTION`
   18/26. n≥10 이고 한 낱말이 과반인 칸은 이 셋뿐이고, 나머지 칸은 전부 최빈이
   "아무 낱말도 없음"(PI ABOVE 29/29)이라 아무것도 붙이지 않습니다.

**그 다음 회차에 통한 것 — 발주처가 실제로 쓰는 표기 (F1 63.6 → 71.8)**

1. **라인 문구(`TO ...`)는 LS 전용입니다.** 발주처 557행 중 라인 문구를 쓴 행은
   **36행(6.5%)** 이고 그중 **22행이 LS — LS 전체(22/22)** 입니다. 나머지 535행에서는
   14행(2.6%)뿐이고 그 14행은 시트마다 제각각이라 조건이 유도되지 않습니다
   (PDIT 0/52 · TI 0/59 · LIT 0/29 · LI 0/10 · FS 0/3). 그래서 기기를 못 찾은
   행에는 **아무 문구도 넣지 않고** 규칙만으로 된 문장(UNIT + 계통 + 변수어)을
   남깁니다 — 162행이 여기 해당하고 등급은 PARTIAL 입니다.
   `--without` 측정: **F1 69.2 → 71.8 · 완전일치 25 → 32 · 정밀도 67.1 → 73.4 ·
   재현율 71.4 → 70.3**. 위치어와 같은 판단(안 붙이는 쪽이 낫다)이 그대로 성립합니다.
2. **제목 어구의 발주처 약어.** 각 제목 어구의 머리글자를 만들어 그 도면의 발주처
   문장에서 두 형태를 세면 **두 쌍만** 나옵니다: `CLOSED COOLING WATER` → **CCW**
   (127행, 풀어 쓴 행 0), `AUX. COOLING WATER` → **ACW** (4행). 나머지 제목은 전부
   그대로 씁니다 (HP STEAM 12/12 · CRH STEAM 11/11 · CLEAN DRAIN 9/9). 반대 방향
   (약어를 풀어 쓰는 경우)은 **0건**. `--without` 측정: **F1 65.8 → 71.8**
   (정밀도 65.3 → 73.4) — 이번 회차 최대 기여.
3. **계통 자체가 갖는 위치어.** CCW 도면의 **TI 는 54행 중 51행이 RETURN** 이고
   SUPPLY 는 0행입니다 (그려진 위치와 무관). 반면 **PI 는 SUPPLY 49 / RETURN 51**
   로 정확히 반반이고 방향 칸 안에서도 반반이라(BELOW 31/30, ABOVE 15/13)
   기하로는 구분되지 않습니다 → PI 에는 붙이지 않습니다.
   `--without` 측정: **F1 71.0 → 71.8**.

**그 다음 회차에 통한 것 — 기기 어휘 확장 (F1 71.8 → 72.9, 주어 대부분이상 62.4 → 69.1)**

1. **범례 EQUIPMENT 표 밖의 낱말 세 개를 추가했습니다** — `VALVE` · `HEADER` ·
   `BOX`. 조건은 둘을 동시에 만족할 것: **발주처가 그 낱말로 기기를 부르고**
   (VALVE 로 끝나는 주어 38행 + 포함 27 · HEADER 22+10 · BOX 4+7),
   **도면이 그 낱말을 인쇄할 것** (VALVE 87회/21장 · HEADER 38회/18장 · BOX 23회/10장).
   범례에 없는 이유는 밸브는 별도 표가 있고 헤더는 배관이기 때문입니다.
2. **다만 낱말만으로는 손해였습니다.** 무제한으로 넣으면 F1 71.9 · 완전일치 30 ·
   기기명 257 로, 어휘를 안 넣은 쪽(71.8 / 32 / 287)보다 나쁩니다. `DRAIN HEADER`
   (도면 39회) 같은 배관 주석이 거리로 정답을 눌렀기 때문입니다
   (`WASTE OIL TANK LEVEL` → `FUEL OIL SUPPLY DRAIN HEADER LEVEL`).
3. **해결은 수식어 제한입니다.** 발주처가 그 낱말 앞에 실제로 쓰는 낱말만 인정합니다:
   VALVE 앞 `BYPASS` 54 · `LETDOWN` 3 · `CONTROL` 3 · `MODULATING` 2 (도면의
   `BRETHER` 42 · `OFF` 59 · `VENT` 29 · `SHUTOFF` 34 는 발주처가 **한 번도** 안 씀),
   HEADER 앞 `DISCHARGE` 16 · `STEAM` 6 · `SEAL` 4 · `RING` 3 (도면의 `DRAIN` 39 제외).
   결과: **F1 72.9 · 완전일치 33 · 주어 대부분이상 69.1% · CONFIRMED 642** 로 세 지표
   모두 어휘 추가 전보다 좋아집니다.
4. **기기 종류가 위치어를 정합니다.** 발주처는 TANK 59행 **전부**에 위치어를 안 쓰고
   (좌우 규칙은 탱크에 INLET 을 붙이고 있었습니다), VALVE 에는 `DOWNSTREAM` 30/65,
   HEADER 에는 `DISCHARGE` 16/32 를 씁니다. PUMP·COOLER 는 양쪽을 다 쓰므로
   (DISCHARGE 70/SUCTION 45, RETURN 86/SUPPLY 41) 좌우 기하에 맡깁니다.

**그 다음 회차에 통한 것 — 업계 표준 약어 층 (F1 72.9 → 74.3)**

규칙이 이제 **세 층**입니다. 새 프로젝트에 넘길 때 무엇을 가져가고 무엇을 다시 재야
하는지가 파일로 갈려 있습니다.

| 층 | 위치 | 내용 |
| --- | --- | --- |
| ① 업계 표준 | `config/plant_standard_abbr.yaml` | 발전 플랜트면 어디나 같은 약어. **방향을 고정하지 않음.** `confirmed` 만 적용되고 `candidates` 는 확인 대기 목록 |
| ② 표기 선택 | `config/project_alnouf1.yaml` `equipment_alias_choice` | 이 발주처가 ①의 어느 형태를 쓰는가 (557행에서 계수) |
| ③ 프로젝트 고유 | 같은 파일의 나머지 `description` 항목 | ①에 없는 1대1 대응 |

1. **확정된 세트는 하나뿐입니다** — `boiler_feedwater_pump`
   (`BFP` / `BOILER FEED PUMP` / `BOILER FEEDWATER PUMP` / `BOILER FEED WATER PUMP`
   / `FEEDWATER PUMP`). 실사용자가 실무 기준으로 확인해 준 것이고, 도면은 긴 형태를
   **한 번도** 인쇄하지 않고 발주처는 짧은 형태를 기기 이름으로 **한 번도** 쓰지
   않아 거리 규칙으로는 이어질 수 없었습니다.
2. **약어는 이름의 머리일 때만 바꿉니다.** `BFP A/B COOLER` 는 쿨러이고 발주처도
   거기서는 `BFP` 라고 씁니다 (수식어 24행 전부). 수식어까지 바꾸면 **F1 −1.5**
   (73.8 → 72.3) 였습니다. 머리 위치로 제한하니 74.2 가 됩니다.
3. **괄호와 `FOR` 꼬리 정리.** 캡션이 괄호를 둘 달면(`(A/B)` + `(2X50% PER HRSG)`)
   수량주기를 먼저 빼는 바람에 `PER HRSG)` 가 이름에 붙어 있었습니다. 괄호를 먼저
   통째로 빼고, `FOR ...` 꼬리도 뗍니다 (발주처 557행 중 `FOR` 를 쓴 행은 **1행**).
   이것만으로 F1 72.9 → **73.8** 이고, 목표 문장 5번
   `SLOP OIL TANK FOR FORWARDING PUMP AREA LEVEL` → **`SLOP OIL TANK LEVEL`** 로
   재현됩니다.
4. **기기를 한둘만 이름 붙인 시트에서는 거리 제한을 쓰지 않습니다.** 제한은 붐비는
   시트에서 엉뚱한 이름을 빌리는 것을 막으려는 것인데, 고를 것이 없으면 할 일이
   없습니다. p20 은 펌프 이름을 시트 맨 아래 캡션에 한 번만 인쇄하고(계기에서
   700pt) 발주처 33행이 전부 그 펌프를 주어로 씁니다. `--without` 측정:
   **F1 74.2 → 74.3 · 재현율 71.8 → 73.1 · 주어 오답 53 → 42 · CONFIRMED 642 → 673**.
   다만 해당 시트가 2장뿐이고 그중 p31 은 유일한 라벨이 낱말 `PUMP` 하나라
   0/15 이므로, config `sole_equipment_sheet_max` 로 켜고 끌 수 있게 두었습니다.

**그 다음 회차 — 표준 사전 확정 (F1 74.3 유지, 세트별 기여도 측정)**

실사용자가 18세트를 ①층 표준으로 확정하고, `CD`(이 플랜트 계통명) · `RO`(계기 태그와
충돌) 2세트를 ③층으로 내렸습니다. **머리 위치 한정 규칙을 전 세트에 적용**한 결과:

| 세트 | `--without` F1 | 판단 |
| --- | --- | --- |
| `boiler_feedwater_pump` | 73.1 → **73.9** (+0.8) | 유지 |
| `condensate_extraction_pump` | 73.9 → 73.9 (0.0) | 유지 (이 문서에서는 0회 발동) |
| `heat_exchanger` | **74.3 → 73.9 (−0.4)** | **표기 선택 철회** |

`heat_exchanger` 는 세트 자체는 표준이 맞지만 **이 발주처의 표기 선택이 손해**입니다:
발주처의 `HEX` 11건은 전부 p31 인데 그 도면은 두 낱말 다 인쇄하지 않고, 정작
`HEAT EXCHANGER` 로 끝나는 라벨이 있는 시트에서는 발주처가 풀어 씁니다(6건).
그래서 ①층에는 남기고 ②층 선택만 비웠습니다 — **세트가 틀린 게 아니라 방향이
프로젝트마다 다르다는 증거**입니다.

**확정 18세트 중 실제로 적용되는 것은 2개뿐입니다.** 나머지 16세트는 발주처가 그
낱말을 **기기 이름으로 쓴 적이 없어**(HRSG 0/29 · GT 0/57 · ST 0/34 · HRH 0/32 …
전부 수식어) 머리 위치 규칙에서 발동하지 않습니다. 사전이 커도 손해가 없다는 뜻이며,
다음 프로젝트에서 방향만 다시 고르면 됩니다.

**중간 심볼(SUCTION STRAINER)은 회수 불가로 확정했습니다.** 발주처 13행은 도면 4장에
있는데, 그중 `STRAINER` 낱말을 인쇄한 도면은 **p32 한 장(3행)** 뿐이고, 그 낱말마저
계기(x≈400)와 펌프(x≈812) 사이가 아니라 **시트 반대편 x≈1830 의 드레인 경로 주석**
입니다. 즉 낱말로 회수되는 건수는 **0/13** 이며, 형상 매칭은 이전 회차 실패 기록이
있으므로 **전량 사용자 입력 대상**입니다.

**그 다음 회차 — 공급자 구간은 당사자를 말하지, 문장을 지우지 않습니다 (SKIP 2 → 0)**

폭 0 SCT 상자를 양쪽 모서리로 다시 세우면 12행이 SKIP 으로 옮겨가는데, 그 판정을
발주처 리스트로 가렸습니다.

1. **기하는 맞습니다.** AL NOUF1 의 폭 0 상자 8개는 전부 네 변이 실제로 닫힌
   체인 파선 사각형이고(왼쪽 세로변 8/8 존재 확인), 각 상자에는 두 화살표
   표기가 붙어 있습니다 — `SCT →` 는 상자 바깥, `← SUPPLIER` 는 상자 안.
   p14 · p24 · p42 · p58 에서 렌더로 눈으로 확인했습니다. 라벨이 왼쪽에 선
   p24 는 화살표가 좌우 반전돼 있고, 그래도 **안쪽이 SUPPLIER** 인 것은 같습니다.
2. **그런데 대조 가능한 시트는 하나뿐이고, 그것이 반대를 말합니다.**
   p14 의 상자는 `#10 CLEAN DRAIN TANK` 를 감싸고 그 안에 이 시트의 LIT 2개가
   전부 들어 있는데, 그 둘이 곧 발주처 xls 136·137행(`... CLEAN DRAIN TANK LEVEL A/B`)
   입니다. p42 는 압축공기 시트 전체(12검출)를 감싸지만 발주처 행이 **0** 이고,
   상자가 없는 다른 시트 **15장도 똑같이 0** 이므로 아무 것도 증명하지 못합니다.
3. **이미 같은 결론이 절반만 적용돼 있었습니다.** `DEFAULT_DISABLED` 는 스코프
   축에서 SCT 를 끄면서 그 근거로 p20 의 RO 2개를 적어 뒀는데(엑셀이 갖고 있는 행),
   Description 축은 그 둘을 여전히 SKIP 으로 비우고 있었습니다. 발주처 xls 225·226
   행이 그 둘입니다 — **네 행 전부 발주처가 문장을 씁니다. 반대 사례 0.**
4. **그래서 상자를 넓히는 대신 축을 끊었습니다.** `supplier_interface_span.description`
   을 코드가 읽게 하고 기본값을 `keep` 으로 두었습니다 (비우는 쪽이 행위이고,
   틀린 문장은 보이지만 "타사 공급"으로 비워진 칸은 확정처럼 읽힙니다).
   측정: **fingerprint a38fe8ac 그대로 · 824행 그대로 · F1 74.6 그대로
   (재현율 73.1 → 73.2) · CONFIRMED 608 → 610 · SKIP 2 → 0**.
   `box_from_both_edges` 를 켜면 F1 74.6 → **74.5** · 대응행 없음 39 → 41 이므로
   AL NOUF1 은 켜지 않습니다 (지문은 b1c6745b 로 바뀝니다).

**그 다음 회차 — 도면 주석 제거와 등급의 정직성 (F1 74.3 → 74.6)**

1. **기기 라벨에 섞인 도면번호는 전부 off-page connector 였습니다.** 라벨 486건
   중 도면번호를 가진 것은 **23건(26회) · 13장**. 그중 4건은 p1(도면 목록 시트)이고
   나머지 19건은 전부 `<목적지 이름> / <도면번호> / <그리드 셀>` 세 줄짜리 연결
   주석입니다 — 기기 캡션 안에 도면번호가 인쇄된 사례는 **0건**. 26회 중 22회는
   **다른 시트**를 가리키고 4회는 자기 시트입니다. 발주처 557행에는 도면번호가
   **0행** (느슨한 형태·`D00P`·`M05` 전부 0) 이므로 제거가 안전합니다.
   패턴은 타이틀블록의 `formats.drawing_no` 를 앵커만 떼어 그대로 씁니다.
2. **같이 잰 다른 오염물**: 그리드 셀 **0건**(항상 괄호 안이라 `BRACKET` 이 이미
   가져감) · DN 치수 **1건**(p48) · NOTE 참조 **2라벨 6회**(p30) · ASME/ANSI/API 와
   플랜지 클래스 **0건**. 발주처 557행에서도 전부 0 이므로 넷 다 제거했고,
   **ASME 는 도면에도 발주처에도 사례가 없어 규칙을 쓰지 않았습니다** — 사례가 없는
   규칙은 추측입니다.
3. **효과는 정밀도입니다**: 문장이 바뀐 행 **30행** (도면번호 23 · NOTE 7),
   F1 74.3 → **74.6** · 정밀도 75.6 → **76.1** · 재현율 73.1 유지 · 완전일치 34 유지.
   **기기명은 285/366 그대로**입니다 — 도면번호는 이름의 머리가 아니라 꼬리라
   머리 낱말 채점에는 영향이 없습니다.
4. **CONFIRMED 를 더 좁히는 근거는 찾지 못했습니다.** 대조 가능한 455행을
   거리·2위와의 차·후보 수·방향·어휘 출처로 갈라 봤지만 **모든 칸이 63~80%** 로
   붙어 있습니다 (2위와의 차 0-10pt 74.4% ↔ 80pt 이상 75.8% — 직관과 반대이고
   평평합니다). 임계값을 만들면 그것이 곧 임의값이므로, **규칙은 그대로 두고 등급이
   무엇을 주장하는지를 고쳤습니다**: `확정` → **`도면 근거 있음`**, 설명에 실측
   일치율(주어 74.3% · 완전일치 27행)을 붙였습니다. 내역은
   완전일치 27 · 주어 정확하고 조각만 다름 41 · 주어 대부분 맞음 270 ·
   주어 일부만 겹침 76 · 주어 틀림 41.
5. **일괄 적용의 범위가 틀려 있었습니다.** 도면+TYPE 으로만 묶으면 PARTIAL 58행이
   **17묶음**이 되고(화면의 25묶음과 불일치), 더 나쁘게는 p11 TIT 에서 엔진이 이미
   채운 **7행**을, p10 TIT 에서 **10행**을 한 번의 클릭으로 덮어씁니다. **지금
   같은 문장인 행**까지 조건에 넣으니 정확히 25묶음이 되고 묶음 밖은 건드리지
   않습니다 (실제로 실행해 6행 확인).

**그 다음 회차 — 판정축 혼합 (5회차, 2026-08)**

1. **판정 트리**(⓪벤더/①기기 직결/②FROM-TO/③다분기/④불가)를
   `app/engine/describe_axis.py` 로 구현했습니다.  §2.3 경계 그대로 — 재귀·큐·
   방문 집합 없음, 탭 하나 + 모선 승계 한 번, 허용치는 전부 실측
   (join_slack 범례 · min_run 16.97 · conn_reach 70.2 · eq_reach 45.8 ·
   표준 끊김 18.5pt 는 콜리니어 간격 분포의 최빈 직후 첫 빈 칸).
2. **1부 체크포인트 ④ 75.2% → 진행 조건(≤30%) 불통과.**  무제한 순회
   진단으로 한계가 §2.3 이 아니라 도면임을 증명하고(§3 표), 사용자가
   **(나) 혼합**을 채택: 판정 성립 행만 새 문형, ④는 현행 유지.
3. **적용 192행** (①125 ②35 ③32 · LS 8 포함 · 밸브 탭 41).  현행 문장·등급은
   `evidence["axis"]["old_description"]`/`old_grade` 에 보존, Remark 는
   `판정축 {축} · 귀속 {귀속점}`.  도면 오탈자(GRUOP)는 원문 그대로.
4. **지문 a38fe8ac → 7b34064e** — 824행 전수 대조로 증명 (불변 칸 위반 0,
   움직인 행 = 적용 192행뿐).  이것이 새 기준선입니다.
5. **지표** (`spike/desc_metrics.py`, 발주처 541행 · 같은 채점기로 현행·신규
   비교): 식별 가능성 42.3% → **39.6%**, F1(참고) 73.2 → 69.3, 완전일치
   33 → 24.  **발주처 표기 기준으로는 낮아졌습니다** — 새 문형이 발주처의
   기기 주어 문형과 형태가 달라서이고, 실제 가치는 라벨링 워크북
   (`out/label_nouf1.xlsx`, 사람 O/X)이 가립니다.  F1 하락은 회귀 실패가
   아닙니다 (5회차 프롬프트 명시).
6. **④→② 전환율 5.4%** — 새 회차 지표.  ② / (② + ④).  도면 벽(선분 단절)
   개선의 측정 축입니다 (`docs/from_to_axis.md`).

**그 다음 회차 — ④ 618행의 사용자 확정 경로 (6회차)**

1. 자동 추적은 세 실측(13.1% · 4.9% · 10.4%)으로 막혔으므로, ④ 행은 **사람이
   FROM/TO 를 한 번 확정하고 리비전 사이에 재사용**한다.  근거 패널의 지정
   UI 후보는 그 도면에서 이미 읽은 텍스트 세 층뿐(커넥터 문구 · 기기 라벨 ·
   도면 텍스트)이고, 출처를 후보선택/후보선택(부분)/자유입력으로 기록한다.
2. 저장은 `projects/{프로젝트}/axis_overrides.json`, 키는 **안정 ID**.  같은 값
   재확정은 confirmed_at 을 유지해 **두 번 저장하면 같은 파일**이다.  다음
   리비전에서 같은 ID 가 매칭되면 자동 승계하고(근거 패널 "확정 승계"),
   사람이 이미 고친 행은 덮지 않는다.  같은 런의 다른 ④ 행에는 **제안만** 한다.
3. 확정은 user_values 로 저장돼 ai_values 를 덮지 않고, 감사기의 "사람이 도면
   값을 덮어쓴 칸"에 자동으로 잡힌다.  p6 실확정: `FROM HRSG#12` (커넥터 후보)
   + `HP TURBINE` (도면 텍스트 `HP TURBINE IP TURBINE` 의 일부 → 후보선택(부분))
   → `FROM HRSG#12 TO HP TURBINE TEMPERATURE TRANSMITTER`.
4. **FN 두 정의 확정** — 워크북 시트2의 22행 = 발주처 FIELD 전체 모수(행 단위),
   회귀 기준선 FN 5 = 교집합 도면 · 칸 단위 min.  22 = 교집합 밖 18 + 교집합 안
   4(기준선 FIELD 몫과 같은 칸).  검출 변화 아님 (`docs/from_to_axis.md`).
5. **경보 설정값** — CZE 양식 41열에 Alarm Set Point 계열 열이 **없다**.  접힌
   신호는 REMARK 34행과 근거 패널에 남고, 설정값을 채울 자리 자체가 없다.
6. LS 8행의 `LEVEL SWITCH` 문형은 사용자가 5회차 정정을 철회하며 **유지 확정**
   (물리 계기 하나 = 계기명, 신호명 아님).
7. 후보 잡음 필터는 승인됐고 **탈출구가 조건**이다 — 걸러낸 것을 버리지 않고
   "전체 후보 보기"로 펼친다 (p6 15건 ↔ 전체 46건).
8. **7회차 확정 사항**: `needs_review` 는 **화면에서만** 감춘다.  데이터는 그대로
   (지문 대상 · 엔진 판정 기록).  근거 패널은 `엔진 판정: 확인 필요 → 사용자
   확정됨` 으로 둘 다 보이고, 검토 카운트에서 확정 행을 미처리에서 뺀다.
   ④ 확정 경로와 기존 손입력 경로를 **함께** 고친다.

**그 다음 회차 — 한쪽만 성립하는 FROM/TO (7회차)**

1. ④ 618행을 끝점 판독으로 가르면 **출발만 9 · 도착만 69 · 양쪽 실패 540**.
   읽힌 반쪽을 쓰되 없는 반대쪽은 지어내지 않는다 — 문형 `②a FROM {출발}
   {TYPE풀네임}` · `②b TO {도착} {TYPE풀네임}`.  적용 78행 중 **42행은 현행이
   공란**이던 행(MOV 31 · BFV 4 · PNEUMATIC 3 등)이라 없던 이름이 생긴다.
2. **같은 방향 커넥터가 둘이면 고르지 않는다** (4행) — 양 끝이 다 `TO …` 인
   경우 어느 쪽인지 도면이 말하지 않으므로 ④ 유지.  이 규칙이 78 vs 82 를 가른다.
3. 지문 **7b34064e → b577bae0** (824행 전수 대조: 불변 칸 위반 0 · 움직인 행
   78 = 적용 증가분).  등급 CONFIRMED **713** · PARTIAL 42 · LOW 8 · NONE **61**.
4. **④→② 전환율 정의 확장**: (② + ②a + ②b) / (… + ④) = **17.3%**
   (옛 정의로는 6.1%, 직전 5.4%).
5. 지표는 또 내려간다 — 식별 가능성 39.6 → **36.8%** · F1 69.3 → 66.8.
   발주처가 `TO …` 문형을 6.5%에서만 쓰기 때문이고, **판정 대상 31행**(현행
   문장이 있는데 도착지 문형으로 바뀐 행)은 도착지가 드레인·블로다운에 몰려
   있다(CLEAN DRAIN TANK 15 · BD TANK 계열 15).  라벨링 결과로 가른다.

**그 다음 회차 — ②a·②b 는 현행 문장이 없을 때만 (8회차)**

1. 7회차의 78행 중 **현행 문장이 있는 36행을 되돌린다** (②a 5 · ②b 31).
   판정 기준은 **현행 문장의 유무 하나** — 도착지 어휘로 가르지 않는다(드레인·
   BD 목록은 프로젝트 종속 어휘가 된다).  읽힌 반쪽은 라인이 어디서 오고 어디로
   가는지만 말하므로 계통을 담은 현행 문장을 밀어낼 수 없고, 공란이던 행에서는
   없던 이름이 생기므로 명백히 낫다.
2. **판정은 기록하고 적용만 하지 않는다** — `evidence["axis"]["withheld"]` 에
   사유가 남고 문형 출처는 `현행유지`.  ④→② 전환율은 판정 기준이라 **17.3%
   유지**.  신규문형 270 → **234** (②a 4 · ②b 38 만 적용).
3. 되돌린 36행은 **전부 6회차 문장과 글자 그대로 같고**, 살아남은 42행은
   **전부 현행 공란이던 밸브 행**이라 FIELD 지표는 6회차로 복귀한다
   (식별 39.6% · F1 69.3 · 완전일치 24).
4. 지문 **b577bae0 → 91ba3e33** (824행 전수 대조: 불변 칸 위반 0 · 움직인 행
   36 = 되돌린 행).  등급 CONFIRMED **704** · PARTIAL 47 · LOW 12 · NONE 61.
5. **p6 은 자동으로 안 된다 — 확정 기록.**  네 번째 실측(양쪽 실패 540행)까지
   같은 답이므로 사용자 확정 경로가 유일한 길이다.  워크북의 **확정 우선순위**
   열이 p6 행을 1번으로 세운다 (`docs/from_to_axis.md`).

**그 다음 회차 — 중간 접합(T) 한 단계 읽기 (9회차)**

1. 탭한 런의 **몸통**에 붙는 가지를 한 단계 읽는다.  깊이는 늘지 않는다
   (끝에서 한 번 → 몸통에서 한 번).  코드로 보장: `_mid_branches` 는 `runs` 를
   한 번 훑는 평평한 루프이고, 가지에서 하는 일은 `read_end`(텍스트·기기만
   봄) 뿐이라 런으로 넘어가는 경로가 없다.  `test_mid_branch_does_not_step_twice`
   가 두 단계 뒤의 커넥터를 못 읽는 것을 확인한다.
2. **끝점도 못 읽고 기기에도 안 닿은 행에서만** 본다 — 이 조건이 없으면 기기
   직결(①) 11행이 ②b 로 끌려간다 (실측).
3. **갈리지 않으면 고르지 않는다** — 서로 다른 이름이 둘 이상이면 ④ 유지
   (실측 1행: p20 TIT `HRSG #11 IP EOCNOMIZER` ↔ `SPRAY FOR HP BYPASS # 11`).
4. 판정 **17행** 이동 (④→②b 15 · ④→① 2) · ④ 540 → **523** ·
   ④→② 전환율 17.3% → **19.7%**.  **적용은 0행** — 17행 모두 현행 문장이
   있어 8회차 기준으로 보류.  **지문 91ba3e33 유지 · 824행 움직인 칸 0.**
5. 8회차 기준을 **축과 무관하게** 적용한다 — 중간 접합에서 새로 읽은 이름은
   ①로 서더라도 현행 문장을 밀어내지 않는다 (실측 2행이 UNIT 접두어·위치어를
   잃었다).  보류 문장은 워크북 **보류된 신규문형** 열(53행)에 실어 사람이
   뒤집을 수 있게 한다.  p6 클라우드 TT 2행이 그 예:
   `UNIT #12 HP STEAM TEMPERATURE` ↔ `TO HRSG#12 BD TANK TEMPERATURE
   TRANSMITTER A/B` (도면 근거는 §p6 절).

**국소 연결의 실측 한계 (2회차 전에 측정)**: 리드선 → 첫 배관 런까지는 788행 전부
성공합니다. 그러나 그 런이 기기까지 닿는 경우는 **44/890 (4.9%)** 이고, 그중 최근접
기기와 다른 답을 낸 것은 **7건**, F1 변화는 **0.0** 입니다. 라벨-런 간격 허용치를
문서에서 실측한 90분위 **48.4pt** 로 넓혀 7→44 로 늘린 뒤의 값입니다. 나머지 94% 는
기기까지 가는 길에 **엘보가 한 번 이상** 있어, 그래프 순회 없이는 닿지 않습니다
(순회는 금지 사항). 즉 국소 연결은 **규격대로 구현했고, 이 문서에서의 천장이 4.9%**
입니다. 나머지는 최근접 거리 폴백입니다.

**그 다음 회차 — p6 육안 피드백 반영 (10회차)**

전문은 `docs/round10_feedback.md` 입니다.  여기에는 **다음 회차가 반드시
알아야 할 것**만 적습니다.

1. **TW 는 검출 실패가 아니라 명시적 제외였다.**  `not_field: [TW]` 하나였고
   버블 검증은 15개 전부 통과하고 있었다.  다만 **발주처 CZE 에 TW 는 0행**
   이라 15행이 전부 FP 다.  계상 여부는 발주처 판단이다.
2. **M 심볼 단독 인식은 이미 되고 있었다 — 코드를 고치지 않았다.**  밸브는
   처음부터 기하로만 잡는다(범례 p2 몸체 · p3 액추에이터).  실측: 밸브 144행
   중 **67행이 버블 태그 없이** 심볼만으로 잡힌 것이고, p6 의 MOV 8행 중 4행이
   그렇다.  범례 p3 원문 `M ROTARY MOTOR (SHOWN TYPICALLY WITH ELECTRIC
   SIGNAL)`.  MOTOR FP 는 **16 → 16**.
3. **NRV 는 두 제외 사이로 빠진다.**  `NRV` 텍스트는 VALVE 앵커라 FIELD 에서
   빠지고, 체크 밸브는 액추에이터가 없어 `deliverable_class` 첫 줄에서 빠진다.
   전 도면 CHECK 몸체 **264개 중 261개가 `actuator=NONE`**.  §12 의 "CHECK 2"
   와 **같은 문제의 반대편**이다 — 행이 된 2개는 액추에이터가 잘못 읽힌
   것이고 그중 하나가 p7 의 `NRV` 태그 몸체다.  대응 리스트가 없어(CZF 미수령)
   산출물 배분은 후보만 냈다.
4. **정밀도 −13.6pp 의 원인은 하나다.**  벤더 제외분 204행을 넣으니 TP 가
   594 → **595** (+1), FP 가 113 → **249** (+136).  발주처 CZE 는 벤더
   공급분을 계상하지 않는다 — 이보다 분명한 대조는 없다.  되돌리기는
   config 한 줄(`scope.exclusion_rules`).
5. **공급자 이름은 그 장 NOTES 에서 런타임에 읽는다.**  코드에도 config 에도
   이름이 없고, 자리 규칙(`\bBY\s+(?P<name>[^.]+?)\s*\.?\s*$`)만 있다.
   11종이 도면에서 나왔다(HRSG 40 · ST SUPPLIER 34 · SEAWATER INTAKE
   FACILITY SUPPLIER 28 …).  p52·53·54·56 은 `BY` 가 아니라 `SUPPLIED FROM
   SUMP` 라 62행이 이름 없이 `VENDOR` 로 떨어진다 — 규칙을 넓힐지는 발주처 몫.
6. **TYPE 표기는 저장하지 않는다.**  `pipeline.type_display()` 함수 하나이고
   화면 응답과 Excel 이 읽을 때만 통과한다.  §8 의 대조 단위가 TYPE 문자열이라
   저장하면 측정이 무의미해지고, 저장 필드를 두면 검토자가 TYPE 을 고쳤을 때
   둘이 갈린다.  소스 검사 테스트가 `_field_rows`·`_valve_rows`·`_axis_pass`
   ·`_apply_axis` 에 이 함수가 없음을 강제한다.
7. **라인 최근접 커넥터는 순회가 아니다 — 거리다.**  탭한 런과 그 모선·중간
   가지에서 축과 **직교**하는 거리만 conn_reach(70.2pt)로 막고, 축을
   **따라가는** 방향은 막지 않는다.  런이 조각이기 때문이다(§3 의 선분 단절).
   실측: 이 구분이 없으면 524행 중 10행만 걸린다.
   **⚠ 적용 39행의 버블→커넥터 간격이 p50 573pt · max 1429pt** 다.  "가장
   가까운"은 맞지만 "가까운" 것은 아니다 — 그 선에 다른 커넥터가 없어서 이긴
   것이다.  그래서 등급은 CONFIRMED 가 아니라 **LOW** 이고 Remark 에 거리와
   원문을 적는다.
8. **피드백 12장(p6 클라우드 TT)의 답은 `TO CLEAN DRAIN TANK` 가 아니다.**
   그 TT 가 탭한 런은 x=844.4 이고 p6 의 `TO CLEAN DRAIN TANK` 세 곳은
   x=1601~1781 이라 직교 거리가 757~937pt (반경의 10배 이상)다.  9회차의
   중간 접합이 이미 읽은 답 `TO HRSG#12 BD TANK` 가 도면이 주는 답이고,
   그것을 현행 문장 대신 쓸지가 **2차 판단**이다 (④ 112행 + 8·9회차 보류 53행).
9. **Description 지표는 하나도 안 움직였다** (F1 69.3 · 식별 39.6% ·
   완전일치 24).  찬 39행이 전부 밸브 행이고 CZH/CZI 에는 Description 열이
   없기 때문이다.

**그 다음 회차 — SCT 축과 밸브 SCOPE (11회차)**

전문은 `docs/round11_scope_axis.md` 입니다.  요지는 셋입니다.

1. **측정축을 지우지 않고 늘렸다.**  `spike/accuracy.py` 의 `AXES` 가 유일한
   정의이고 `score(rows, keep)` 는 하나다 — 축이 다른 것은 `keep` 뿐이고
   **정답지 쪽 모수는 두 축에서 같다**.  축1 은 "발주처가 안 세는 것을 얼마나
   넣었나"까지, 축2 는 "우리가 얼마나 잘 읽었나"를 잰다.
2. **벤더 별표는 밸브 몸체가 아니라 액추에이터 심볼 위에 찍힌다.**  p6 실측:
   별표 y=526 · M 원 y0=534.4(8.4pt) · 몸체 y0=563.5(37.5pt), 허용치
   `mark_above` 25.0.  몸체를 보면 전 도면에서 **1회**만 걸린다.  허용치는
   건드리지 않고 **읽는 사각형만** 바꿨다.
3. **밸브에는 `scope` 만 채우고 `vendor_supply` 는 채우지 않는다.**
   `_axis_pass` 가 `vendor_supply` 행을 ⓪로 보내 문장을 지우기 때문이다
   (실측 1행: p7 CHECK `TO HRSG#11 CRH CHECK VALVE`).  스코프 표기와 판정
   입력은 다른 칸이다.

**그 다음 회차 — 화면이 하는 말과 데이터를 맞춘다 (12회차, UI 전용)**

전문은 `docs/round12_ui.md` 입니다.  **검출·측정은 한 줄도 건드리지 않았고
지문 `69ad281f` · 1029행이 그대로입니다.**

1. **화면이 사실과 어긋난 곳이 셋이었다.**  근거 패널이 벤더 행에도
   "리스트에 포함됩니다"라고 썼고(팀원이 그 말을 믿고 Excel 을 열면 그 행이
   없다), 오버레이 범례는 **라벨과 세는 대상이 달랐고**, SCOPE 열은
   `VENDOR(SEAWATER INTAKE FACILI…` 로 잘렸다.  셋 다 11회차 캡처가 잡았다.
2. **근거 패널은 이제 세 질문을 나눠 답한다** — 공급 주체 / 추출 결과 /
   발주처 양식.  "추출 결과에 있다"와 "발주처 양식에 나간다"는 11회차부터
   서로 다른 사실이므로 한 문장으로 말할 수 없다.
3. **오버레이 색은 SCOPE 열을 읽는다** (`_layers` 의 한 곳).  색과 열이
   다른 값을 읽으면 언젠가 갈린다.  p6 실측 46행 = SCT 18 + VENDOR 28 로
   범례 숫자와 그리드가 맞는다.
4. **⚠ 캡처가 진행률의 결함을 잡았다.**  13초와 60초 화면의 md5 가 같았다
   (`772644abffe1`).  원인은 진행 신호가 아예 없는 두 구간 — `.segments()`
   **95.8초** · `derive` **129.0초** — 이고, 캐시 예열 루프를 층 안에 넣어
   장마다 신호를 내게 했다.  같은 방법(서로 다른 시점 캡처의 바이트 비교)이
   §7.2 에서도 같은 실패를 잡았었다.
5. **취소는 구조적으로 안전하다 — 새 방어 코드를 쓰지 않았다.**
   행을 쓰는 곳은 `db.store_result` 하나, 안정 ID·리비전 대장을 쓰는 곳은
   `_run_comparison` 하나이고 둘 다 `analyse` **뒤**에 온다.  즉 분석 중
   취소는 **행 0 · 안정 ID 0 · 리비전 소비 0** 이다 (실측 `status=cancelled
   rows=0`).  §7.3 의 "ID 는 Rev.A 에서 한 번만" 이 지켜진다.
6. **팬은 스크롤이지 좌표가 아니다.**  `#stage` 의 `scrollLeft/Top` 만 옮기고
   SVG 는 건드리지 않으므로 확대·선택·오버레이가 그대로다 (실측
   `[400,300] → [620,440]`, 선택 유지 True).  4px 미만은 클릭으로 넘겨
   행 선택을 뺏지 않는다.
7. **주석(D-2·D-3)은 구현하지 않고 설계안만 냈다** (`docs/round12_annotation_design.md`).
   저장 5개 항목 중 답이 서지 않은 것이 있고, 프롬프트가 그 경우
   **D-1 만 하고 회차를 닫으라**고 정해 뒀다.

**그 다음 회차 — 있는 것을 먼저 찾았다 (13회차, 프로젝트 영속 · 개정 구분)**

전문은 `docs/round13_persistence.md`, 조사는 `out/round13_survey.md` 입니다.
**지문 `69ad281f` · 1029행은 편집을 5칸 넣은 뒤에도 그대로입니다.**

1. **요구 세 가지 중 둘은 이미 서버에 있었다.**  구현 전 조사가 그것을 먼저
   밝혔다 — 재분석 없이 열기는 `rows` **0.69초**(재분석 351초의 1/500)로
   이미 됐고, 편집 영속도 `EDITABLE` 10열 전부 이미 됐고, 두 층 분리
   (`ai_json`/`user_json`)도 이미 있었다.  **없던 것은 첫 화면에서 거기로
   가는 길**이었다.  10회차 [C](M 심볼)와 같은 상황이다 —
   **없는 것을 만들기 전에 있는 것을 찾는다.**
2. **첫 화면의 단위를 job 에서 프로젝트로 바꿨다** (`GET /home`).  이전 목록은
   PDF 파일명만 말해서 같은 `pid_total.pdf` 로 올린 두 분석을 구분할 수
   없었다.  이제 프로젝트 → 리비전 → (도면 Rev · 개정 배지 · 행수 · 일시 ·
   수정 칸수).  프로젝트에 안 묶인 분석은 버리지 않고 따로 묶는다.
3. **편집을 Rev 사이로 잇는다 — 짝은 안정 ID 로 짓는다.**  §7.3 이 ID 를
   "Rev.A 에서 한 번만 부여" 로 정의해 둔 것을 **쓰는** 것이지 새로 매기지
   않는다.  세 규칙: 추출값은 안 건드린다 · 이번 리비전에서 사람이 이미 고친
   칸은 안 덮는다 · 엔진 답이 갈리면 고르지 않고 `conflict` 로 검토에 올린다
   (재분석이 하던 것과 같은 규칙).  실측 5칸 승계 · 충돌 0.
4. **작성자는 자기신고다.**  `feedback` 15개 컬럼에 사람을 가리키는 것이
   하나도 없었다.  인증을 만들 자리가 아니므로 이름 한 칸을 넣되 **확인은
   매번 하고 타자는 한 번만** 친다.  비우면 비운 채로 저장되고 화면이
   "이름 없음"·"자칭" 이라고 적는다 — **서버도 화면도 이름을 지어내지 않는다.**
5. **⚠ 문서 대표 Rev 는 도면에서 유도되지 않는다.**  이 문서는 장마다 Rev 가
   다르고(PID 53장: A11 A1:1 B18 C16 D7, 전부 신뢰도 HIGH) 세 후보가 서로 다른
   답을 낸다 — `max`→D(1장) · `mode`→B(19장) · `first_sheet`→B.  `rev_date` 는
   9장에만 있고 가장 늦은 날짜에 C 3장·D 1장이 섞여 갈라지지 않는다.  사용자가
   실무 기준으로 **`max`("한 장이라도 개정되면 문서가 개정된 것")** 를 확정했고
   config `revision.document_rule` 한 줄이다.  **대표값만 내지 않고 접기 전
   분포와 장 단위 대조를 함께 보인다.**
6. **⚠ 도면번호가 장을 유일하게 가리키지 않는다.**  같은 도면번호를 쓰는 PID
   장이 **3쌍** 있고 그 장들의 개정이 다르다 (`D00P-10LBG10-M05-0001` p12 B ↔
   p15 A · `D00P-00GHC10-M05-0001` p46 C ↔ p47 B · `D00P-00GMA10-M05-0001`
   p52 A ↔ p55 B).  한쪽을 골라 비교하면 **같은 PDF 를 두 번 넣어도 "개정 2장 ·
   역행 1장"이 나온다** — 13회차 캡처가 실제로 잡았다.  그런 도면번호는
   판정하지 않고 `ambiguous` 로 센다.  쪽 번호로 짝을 짓는 것도 안 된다 —
   개정 때 장 순서가 바뀌는 것이 개정의 흔한 모습이다.
7. **판독 자체는 결정적이다** — 같은 PDF 를 두 번 분석해 쪽 단위로 대조하니
   58쪽 중 값이 다른 쪽 **0**.  위 6번은 판독이 아니라 대조 함수의 문제였다.
8. **캡처가 결함 셋을 잡았다**: 승계된 편집이 이력에 안 남던 것 · 근거 패널이
   편집값과 엔진 근거를 구분하지 않던 것(`수량 7` ↔ `수량 근거 … x 2`) ·
   위 6번.  최근 여섯 회차 연속으로 캡처가 결함을 잡고 있다.

**그 다음 회차 — 편집과 SCT 필터의 정합 (14회차)**

전문은 `docs/round14_edit_scope.md` 입니다.  **지문 `69ad281f` · 1029행이
그대로이고 엔진 파일은 0개를 건드렸습니다.**

1. **`UI 21` 은 유효한 통과가 아니었다.**  11·12·13 회차의 그 값은 전부
   **SCOPE 가 전부 빈칸인 DB**(실 DB 의 분석 4건 = 824·824·824·75행)에서
   나온 것이다.  그 상태에서는 모든 행이 `legacy` 로 분류돼 SCT 필터가
   아무 행도 거르지 않는다 — **필터를 시험한다고 적어 둔 시험이 필터가 꺼진
   것과 같은 조건에서만 돌고 있었다.**  코드는 같고 데이터가 달랐으므로
   파일 동일성으로는 잡히지 않는다.
2. **고칠 것은 시험의 가정이었다.**  `edited` 픽스처가 FIELD 첫 다섯 행을
   순서로 집었고 그 행이 SCT 인지 보지 않았다 (새 분석에서는 첫 다섯 중
   **3행이 `VENDOR(HRSG)`**).  제품은 맞다 — API 재현으로 스냅샷 rev 3→4 ·
   `out_of_scope 249` 유지를 확인했다.  이제 **상태로 고른다**(`field_rows`).
3. **세 갈래를 모두 시험한다** — SCT(나간다) · 비SCT(안 나간다, **정상**) ·
   legacy(빈 값, **나간다**).  마지막이 뒤집힌 것처럼 보이지만 11회차가
   그렇게 정했다: 없는 판정을 타사 공급으로 읽으면 양식 네 개가 빈 파일이 된다.
4. **데이터 전제를 코드로 못박았다.**  `test_step0_the_suite_runs_against_
   scope_aware_data` 는 **건너뛰지 않고 실패한다** — 건너뛰기가 그 침묵을
   세 회차 동안 만들었다.  옛 DB 에서 실제로 실패하는 것을 확인했다.
   **UI 21 → 24, SCOPE 있는 데이터(SCT 667 / VENDOR 218)에서 전량 통과.**
5. **편집이 양식에 어떻게 닿는지 그 순간 말한다.**  12회차 근거 패널은 행을
   **누른** 사람에게만 말했다.  이제 저장 **전**(작성자 줄)과 **후**(한 줄
   안내) 두 시점에 말하고, 막지는 않는다.  판정은 `scopeFacts` **하나**를
   패널·안내가 함께 읽는다.  행수는 `GET /jobs/{id}/scope_summary` 가 주고
   그 응답은 완료 화면과 **같은 함수**(`_scope_summary` → `in_client_scope`)다.
6. **화면이 한 말이 파일과 맞는지 열어서 확인했다**: `UI-SCT-1` 있음 ·
   `UI-VEN-1` 없음 · `UI-LEG-1` 있음 · `legacy_no_scope_rows: 1`.  세 문장 전부 일치.
7. **⚠ 캡처가 또 잡았다 (일곱 회차 연속)** — 앞 편집의 "나갑니다" 가 남은
   채로 다음 행의 "나가지 않습니다" 가 떠서 **서로 반대인 두 문장이 동시에**
   있었다.  둘 다 각자의 행에는 맞는 말이라 더 나쁘다.
8. **⚠ SADARA 는 프로필이 값을 가른다** — 미지정 **82행 `0767ba79`**(기준선) ↔
   `project_sadara.yaml` 지정 **75행 `2bb1b9a2`**.  **프로필 자동 맞춤은 없다**
   (`projectconfig.DEFAULT_CONFIG` 가 alnouf1 고정).  즉 기준선은 "AL NOUF1
   규칙을 SADARA 도면에 적용한 값" 이고 교차검증으로서는 그것이 맞다.
9. **문서 드리프트 갱신을 [A] 고정 절차로 삼는다** — 이번엔 빠른 테스트
   149 → **150**.  세 회차 연속으로 드리프트가 나왔다.

**그 다음 회차 — 범례 프로필 저장과 개정본 재사용 (15회차)**

전문은 `docs/round15_legend_profile.md`, 조사는 `out/round15_survey.md` 입니다.
**지문 `69ad281f` · 1029행이 그대로이고, 세 경로(유도·재사용·강제유도)가 전부
같은 지문을 냅니다.**

1. **속도가 목적이 아니다 — 실측이 그렇게 말한다.**  `legend_rules` 는 전체
   406.2초의 **1.5%(6.10초)** 이고, 그 안에서 **범례를 읽는 몫은 1.77초(0.4%)**
   뿐이다.  나머지 3.73초는 `connector_reach` 이고 그것은 범례가 아니라
   **도면 218개**에서 잰다.  **보고서에 속도를 성과로 적지 않는다.**
2. **★ 진짜 이유는 범례 없는 PDF 가 지금 죽는다는 것이다.**  범례 4장을 뺀
   PDF 로 돌리면 `KeyError: 'min_run'` 으로 멈추고 화면은 일반 문구만 낸다
   (`derive_line_styles` 가 빈 값을 내는데 `valves.legend_fallback` 에
   `line_styles` 항목이 **없다**).  더 나쁜 것은 **죽지 않는 일곱**이다 —
   특히 `unit_multipliers` 가 7코드 → **3코드**로 줄어 유닛 12·20·21·22 가
   UNDEFINED 가 되고 **Q'ty 만 조용히 틀린다.**
3. **담는 것은 범례 장에서 나온 여덟뿐이다** (butterfly · actuator_stem ·
   pneumatic · line_styles · isa_table · equipment_symbols · component_words ·
   unit_multipliers = **91칸 · 9,654바이트**).  `connector_reach` 와 `layout`
   19건은 **도면**에서 나오므로 담지 않는다 — 담으면 다음 개정본의 도면이
   답해야 할 값이 옛 값으로 굳는다.  `equipment_vocab` 은 심볼의 순수 함수라
   따로 담지 않는다 (값을 두 곳에 두면 언젠가 갈린다).
4. **재사용이 `source` 를 바꾸지 않는다.**  지문이 `legend` 의 `source`·`note`
   ·`values` 를 해싱하므로, `PROFILE` 로 바꾸면 같은 값을 쓰고도 지문이
   달라져 **프로필이 값을 정확히 담았는지 확인할 길이 사라진다.**  재사용
   여부는 지문 밖의 `result["legend_profile"]` 이 말하고 화면이 그것을 읽는다.
5. **범례 장이 있으면 적용은 프로필로 하되 대조는 한다.**  적용(학습)과
   대조(확인)는 다른 일이다 — 대조를 빼면 재작도된 범례를 옛 규칙으로 읽고도
   아무도 모른다.  범례 p2 를 0.9배로 다시 그린 PDF 로 실측: **20칸 감지**
   (circle_diameter 6.06→5.454 · min_run 16.968→15.271 · 기기 17행 앵커 전부).
   **자동 갱신하지 않는다** — 항목별로 사람이 고른다.
6. **범례 장이 없는 개정본은 완주한다** — 54쪽 PDF 로 **1029행 · Q'ty 1911**
   그대로.  지문은 `ce085c62` 로 다른데 **쪽이 4장 빠져 행 키가 밀린 것**이고
   판정이 달라진 것이 아니다.  `legend_rules` 는 5.94 → **3.51초**.
7. **⚠ 재사용이 더 느렸다 — 그대로 적는다.**  범례 장이 있는 개정본에서는
   대조 때문에 재기 때문이다 (14.50초 ↔ 유도 5.94초).  줄어드는 유일한 경우가
   6번이고, 그것도 전체의 0.6% 다.
8. **범례도 프로필도 없으면 사유를 말하고 멈춘다** (`LegendUnavailable`).
   이 문장은 **예외를 바꿔 쓴 것이 아니라** 파이프라인이 직접 검사한 조건이다
   — `_failure_reason`(예외를 보지 않는 함수)은 *알 수 없는* 실패의 자리다.
9. **조사가 또 범위를 줄였다 (세 회차 연속).**  넷이 이미 있었다 — 프로젝트
   저장소(`revisions.project_dir`) · 프로젝트↔분석 연결(`job.project`) ·
   결정성(두 프로세스의 범례 값 **전 항목 동일**) · 그리고 캐시하면 **안 되는**
   두 가지를 가려낸 것.
10. **⚠ 캡처가 결함 셋을 잡았다 (여덟 회차 연속).**  (가) `page_kind ==
   LEGEND` 로 "대조할지" 를 가른 것이 틀렸다 — 다시 그린 범례 장은 종류가
   바뀌는데 유도는 **인쇄된 머리말**로 그 장을 찾아 제대로 읽고 있었다.
   갈림을 없애고 **언제나 재되 적용은 프로필**로 바꿨다.  (나) "범례 4장"
   이라고 적던 것을 **"값을 읽은 3장"** 으로 고쳤다 (p4 는 어느 항목의
   근거도 아니다).  (다) 범례 없는 개정본의 띠가 같은 사실을 **세 번**
   말하고 있었다.  그리고 (다)를 고치는 중에 **문장을 `engine_json` 에
   저장해 두면 고칠 수 없다**는 것이 드러나 `compare_note` 를 지우고 사실
   셋(`legend_sheets`·`uncompared`·`compared`)만 남겼다.

**그 다음 회차 — SCOPE 판정 일괄 교정 (16회차)**

전문은 `docs/round16_scope.md`, 미검출 조사는 `out/round16_missing.md` 입니다.
**행 1029 · Q'ty 1911 은 그대로이고 지문만 `69ad281f` → `caf36dc3` 로
움직였습니다** (SCOPE 는 지문 대상 열입니다).

1. **피드백 7건은 세 원인이었다.**  ㉠ 체인 파선 상자가 아예 안 잡힘
   (`brk_max_mark` 기본 20.0 이 이 문서의 **끝 마크** 25.86·25.32 를 버려
   모서리가 29pt 어긋난다) · ㉡ 한 마크가 나란한 두 버블에 **둘 다** 세어짐
   (`mark_x_slack` 6.0 으로 창이 겹친다) · ㉢ **개정 클라우드의 빨간 호가
   별표로 읽힘**.
2. **★ Block 개념은 이미 있었다.**  `find_package_boxes` +
   `package_box_marks` + `read_vendor_mark` 의 `PACKAGE_BOX` 갈래가
   6회차부터 사용자 원문 두 규칙을 그대로 구현하고 있었고, 없던 것은
   **그 파선을 읽는 허용치** 하나였다.  13·15회차와 같다 — **없는 것을
   만들기 전에 있는 것을 찾는다.**
3. **허용치는 이 문서에서 쟀다**: 점 3.54 · 틈 3.54 · 반복 긴 마크 17.7
   (2,528 중 1,602) → 한 주기 **28.32** → `brk_max_mark: 28.3`.  근거는
   "끝 마크는 한 주기를 넘을 수 없다" 이고, 25.9 로 두어도 상자 수(78)와
   마크 붙은 상자(10)가 **같다** — p9 에 맞춘 값이 아니다.
4. **별표는 칠 없는 곧은 선이다** (`_is_stroke_glyph`).  근거는 범례다 —
   NOTES 정의줄이 인쇄한 별표 **38개가 38개 다** `fill=None` 이고 항목이
   전부 직선(`l`).  걸러지는 것은 밸브의 검은 점(곡선 4개로 그린 지름 4.26
   원 + 해칭)이고, 그 점이 p9 상자의 테두리 띠(오른쪽 18.9pt)에 들어가
   상자의 별을 `*`(HRSG) → `**`(ST SUPPLIER) 로 바꿔 놓고 있었다.
5. **잉크와 종이 둘뿐이다** (`_INK`·`_is_ink`, `detect_all._INK` 과 같은 값).
   빨강(1,0,0)으로 그려진 개정 클라우드·개정 삼각형은 도면의 표시가 아니다.
   실측 글리프 마크 **1,128 → 1,019**.
6. **⚠ 그 걸림이 p47 에서 예상 밖의 일을 했다.**  p47 의 NOTES 정의줄에
   겹친 빨간 클라우드 호가 **글리프로 읽혀** `read_mark_dictionary` 가
   `glyph_size (3.8, 3.7)` 을 내고 있었고, 그 크기로는 그 장의 진짜 별표가
   하나도 안 맞아 PI 3 · RO 3 이 SCT 로 나가고 있었다.  **지목되지 않은
   페이지에서 6행이 바뀐 것이 이번 회차의 발견이다.**
7. **한 마크는 버블 하나에만 붙는다** — 창을 좁히지 않고 **소유자**를
   정한다 (`_x_gap` · `read_vendor_mark(others=)`).  좁히면 자기 별표를
   잃는 버블이 생긴다.
8. **58장 전수로 목표를 셌다**: 별표가 있는데 벤더가 아닌 검출 **0** ·
   별표도 상자도 없는데 벤더인 검출 **0** · `UNDEFINED` 13(그 장에 NOTES
   정의가 아예 없는 p33·p40·p41).  SCOPE 가 바뀐 행은 **14행 · 4장**
   (p9 6 · p47 6 · p25 1 · p35 1).
9. **축2 정밀도 86.8% → 88.4%** 이고 **FN 이 5 → 4** 다.  사라진 FN 은
   p35 의 TIT — 발주처 리스트가 그 행을 세고 있으므로 "별표가 없는데
   VENDOR 였다"는 피드백을 **정답지가 독립적으로 확인해 준다.**
   축1(전량)은 99.3% / 70.5% 그대로다.
10. **중첩 상자는 있다** — 78상자 중 24쌍(전부 p26·p27 의 격자 칸).
   **그중 마크가 붙은 상자는 0개**라 이번 판정에는 영향이 없다.  겹치면
   `read_vendor_mark` 는 **첫 상자에서 멈춘다**(합산하지 않는다) — 다만
   "안쪽 우선"이 보장되지는 않으므로 마크 붙은 중첩이 실제로 나오면 그때
   규칙을 정해야 한다.
11. **[C] 미검출 6건은 조사만 했다** (`out/round16_missing.md`).
   **하나로 묶인다** — `deliverable_class` 첫 줄(`actuator NONE → EXCLUDED`)이
   100건 중 89건이고, 전 도면 밸브 몸체 **2,821개 중 2,676개(94.9%)가
   `actuator=NONE`** 이다.  p8 의 MOV 하나는 **몸체 위 빨간 개정 클라우드
   호가 다이어프램 돔으로 읽혀** `kind=DIAPHRAGM` 이 된 것이라 ㉢과 같은
   뿌리이고, **밸브 쪽에는 잉크 걸림을 넣지 않았다** (17회차 몫).
12. **⚠ 스스로 뒤집은 것 둘**: (가) [C] 첫 측정을 폐기했다 — 기본
   `ValveLayout` 으로 재면 공압 액추에이터가 0개로 나와 XV·PCV·FCV 가 전부
   미검출로 보이지만, `dv.derive_layout` 을 쓰면 **XV 12 · TCV 8 · PCV 2 ·
   FCV 2 · NRV 1 행이 이미 나가고 있다.**  (나) 잉크 걸림만으로는 p9 이 안
   고쳐졌다(별이 `**` 로 읽혔다) — 세 번째 전량 분석을 써서 4번을 세웠다.

**그 다음 회차 — 2차 피드백 잔여 반영 (17회차)**

전문은 `out/round17/1_보고서.md` · 게이트는 `3_gate_b.md` · 미구현은
`7_미구현.md`.  **지문 `caf36dc3` · 1029행 · Q'ty 1911 이 그대로이고 움직인
열은 `description` 하나 · 274칸**입니다 (`description` 은 지문 대상이 아닙니다).

1. **★ 시험이 실 DB 를 여는 길을 끊었다.**  닿는 길은 **하나**였다 —
   `app/main.py:50` 의 `CON = db.connect(DB_PATH)` 가 **import 시점에** 돌고,
   `from app import main` 하는 시험이 5자리다.  `db.connect` 는 읽기 전용이
   아니다(`journal_mode=WAL` · `executescript`).  14회차 방어는 **UI 스위트만**
   지켰고 그 다섯은 전부 빠른 스위트에 있었다.  막는 것은 뿌리 `conftest.py`
   두 겹 — `PID_DATA_DIR`(이미 있던 변수) + `db.connect` 걸림 — 이고
   **읽기는 막지 않는다**(UI 스위트는 사본을 쓴다).  세션 끝에서 sha256 을
   대조해 **막았다는 것을 증명**한다.  `docs/round17_test_isolation.md`.
2. **[B] 미검출 완화는 네 안 모두 게이트 불통과 — 코드 0줄.**  축2 정밀도가
   (a)86.1 · (b)87.4 · (c)87.1 · (c′)87.9 로 전부 88.4 아래다.  **재현율은
   네 안 다 99.3% 그대로이고 FN 도 4 그대로**인데, 정답지의 액추에이터 열에
   빈칸이 없어(CZH 63행 전부 MOTOR) 액추에이터 없이 넣은 행은 전부 FP 이기
   때문이다.  **"전부 PNEUMATIC 탭에 넣으면 안 움직인다" 는 통과가 아니다** —
   그 탭(CZF)이 미수령이라 채점되지 않을 뿐이고, 그것을 통과로 읽으면
   측정 정의를 피하는 것이다 (§2.2).
3. **★ p8 은 피드백과 정답지가 서로 다른 말을 한다.**  발주처 CZH 는
   `D00P-10LBC50-M05-0001` 에 MOTOR **5행**인데 우리는 **이미 6행**을 낸다.
   잉크 걸림을 밸브 경로에 넣으면 7행이 되어 순수 FP 다 (88.41 → 88.28).
   **실무 판단 대상** — 그 장의 MOV 를 발주처가 몇 개로 보는가.
4. **C-1 중복 문장에 접미** — 91묶음 **263행**.  묶는 단위는 **도면**이고
   (산출물이 P&ID No. 를 함께 실으므로), 정렬은 요구 원문대로 위→아래 ·
   좌→우다.  `instrument_ordinals` 는 *같은 기기의 같은 TYPE* 만 묶으므로
   주어를 못 찾은 ④ 행에 아무것도 주지 않았고, **그 행들이 여기서 처음
   구분된다.**
5. **★ 좌표 `(F:7)` 는 붙이지 않았다.**  테두리 그리드는 **인쇄돼 있지만
   텍스트가 아니고**(테두리 60pt 안 낱말 0개), 글리프 라이브러리는 개정
   문자에서 만들어져 `1 A B C D` 뿐이다 — **E~H 도 2~8 도 없다.**  칸을
   기하로 나누고 이름을 붙이면 그것이 곧 지어낸 값이다 (§2.1 ③).  사유는
   `evidence["duplicate_suffix"]["coordinate"]` 에 남는다.
6. **★ 그 접미가 §2.1 ③ 시험에 걸렸고, 시험이 맞았다.**  그 글자는 도면에
   없다.  숨기지 않고 ① `description_sources` 에 **`SUFFIX:` 사유**를 남기고
   (근거 패널이 도면 값과 같은 얼굴로 보여 주면 안 된다) ② 시험은 그 행
   자신의 한 글자만 예외로 열되 **사유가 있는지도 함께** 본다.
7. **★ C-4 를 스스로 뒤집었다 — "가장 가까운" 은 "가까운" 이 아니다.**
   1차 구현(31행)에는 분리 요구치가 없어 **1195.4pt ↔ 1213.7pt** 를 "가까운
   쪽"으로 읽고 있었다(그 계기에 실제로 붙은 기기는 112.0pt).  오프페이지
   커넥터는 시트 가장자리에 모여 있어 거리가 **공통 오프셋에 지배되고**
   18pt 차이는 정보가 아니다 — 10회차 [G] 가 같은 이유로 등급을 LOW 로 뒀다.
   **분리 요구치는 이 문서가 답했다**: 짝이 선 32행의 2위와의 차가
   **12.8~41.4 ↔ 190.8~804.4 로 갈리고 그 사이가 비어 있어** 문턱을 그 안
   어디에 두어도 같다.  `connector_reach`(런타임 실측 70.2pt)를 쓰므로
   코드에 새 숫자가 없다.  적용 **18** · 보류 **14**(지우지 않고
   `connector_position_withheld` 에 기록).
8. **C-3 은 이미 되고 있었다** — p14 가 이미
   `AUX STEAM LETDOWN VALVE #10 GLOBE VALVE` 를 판정축 ①로 낸다.  13·15·16
   회차와 같다 — **없는 것을 만들기 전에 있는 것을 찾는다.**
   C-2(Bypass 언급)는 후보 13행 전부에서 **어느 밸브를 말하는지 도면이
   답하지 않아** 구현하지 않았다 (거리로 고르면 §3 의 반경 실패 재연).
9. **TYPE 표기 AT · AIT → Analyzer 는 이 문서에서 0행이다.**  TYPE 값 28종에
   둘 다 없다(캡처가 알파벳 순 목록으로 보인다).  자리는 `type_display()`
   하나이고 이름은 config `description.type_display_names` 다.
   `type_internal` 은 건드리지 않았다.
10. **기록 삭제는 분석 단위만.**  게이트였던 안정 ID 질문의 답은
   `Registry.next_seq` 가 **`status` 를 보지 않는다**는 것 — 분석 하나를
   지워도 ID 는 회수·재부여되지 않는다.  **프로젝트 전체 삭제는 만들지
   않았다**: `id_registry.json` 이 사라지면 §7.3 이 실제로 깨진다.
   `deletion_log` 가 누가·언제·무엇을 지웠는지만 남긴다.
11. **⚠ 기존 비결정성을 하나 찾았다** — `evidence["trace"]["dead_ends"]` 의
   **순서**가 실행마다 다르다(같은 코드 두 번에 282행).  좌표 집합은 같고
   지문·출력 열·Excel(41,466칸 대조 0)에 닿지 않으며 근거 패널의 표시
   순서만 흔든다.  **이번 회차와 무관하므로 고치지 않고 다음 회차 후보**다.
   → **18회차 뒤에 고쳤다**: 원인은 `pipe_graph` 가 집합(`seen`)을 그대로
   순회한 것이고(`[:40]` 이 어느 40개를 남길지가 갈린다), 좌표 정렬 한 곳으로
   막았다.  해시 씨앗을 달리한 두 프로세스로 전량 2회 — 지문 `015e34b3` 동일 ·
   **순서까지 다른 행 0** (직전 282).
12. **⚠ 11분을 다시 쓰지 않는 법**: slow 의 §2.1 ③ 시험은 저장된 결과 json
   으로 **같은 함수를 그대로 불러** 재생할 수 있다.  회귀를 고칠 때는 그것으로
   먼저 확인하고 전량은 마지막에 한 번 돌린다.

**그 다음 회차 — 3차 피드백 반영 (18회차)**

전문 `out/round18/1_보고서.md` · 진행 기록 `out/round18_progress.md` ·
[C] `out/round18/3_scope_재검증.md` · [D] `docs/round18_global_symbols.md` ·
[F] `docs/round18_description.md` · 미구현 `out/round18/9_미구현.md`.

1. **발주처 양식이 전량을 담습니다** ([B]).  요구는 *"Instrument 를 당사가
   공급하지 않더라도 설치에 필요한 Bulk material 은 SCT 가 공급하므로 물량
   산출해야 함"* 입니다.  **11회차 필터를 지우지 않고 껐습니다** — config 한 줄
   (`client_form.scope_filter: all | sct`).  **측정축은 둘 다 그대로**이고
   ([B] 는 출력 필터이지 측정이 아닙니다), SCOPE 열도 남습니다.
   "세는 것"(`scope_state` · 공급 주체)과 "나가는 것"(`in_client_scope`)을
   **함수 둘로 갈랐습니다** — 전량 모드에서 벤더 행은 `out_of_scope` 이면서
   양식에 나갑니다.  실측 **885/76/23** ↔ SCT **651/66/22**.
2. **★ 16회차의 "전수 검사 0건" 은 자기 자신에게 물은 것이었습니다** ([C]).
   `read_vendor_mark` 가 보는 것은 `find_marks` 가 준 것뿐이고, `find_marks`
   는 **그 장 범례 별표 크기 ±0.6pt 밖 글리프를 이미 버린 뒤**였습니다.
   일반 원인은 ***범례는 뜻을 정하지 축척을 정하지 않는다*** — 27장 중 26장은
   범례 크기 = 도면 크기인데 **p35 는 4.0 ↔ 7.8** 입니다.  고친 방법은
   **그 장에서 런타임에 다시 재는 것**(`drawn_mark_sizes`)이고, 더하기만 하고
   받아들인 크기를 빼지 않습니다.  **검사 방법 자체를 검증했습니다** —
   독립 검사기(`find_marks` 를 안 부름) + **변이 시험**(검출기를 일부러 멀게
   만들면 검사가 잡는다).  결과 SCOPE 5행 이동 · 축2 정밀도 **88.4 → 89.1%**
   · FP 78 → 73 · FN 4 불변.
3. **★ 17회차 [B] 는 잘못된 원인을 재고 있었습니다** ([E]).  네 안 모두
   `actuator NONE` 완화였는데 실제 원인은 **몸체 기하**입니다 — 범례 p2 의
   `ANGLE`(직각으로 꼭짓점을 맞댄 두 중공 삼각형)이 검출기 어휘에 없어
   삼각형 하나만 CHECK 로 읽혔고, 그 rect 중심이 스템축에서 4.86pt 어긋나
   `act_offaxis`(0.83pt)가 **제대로 찾아 둔 공압 실린더를 거부**했습니다.
   **꼭짓점에서 재면 0.15pt** — 허용치가 아니라 **재는 점**이 틀렸습니다.
   **행 8개 추가 · 사라진 행 0 · 기존 행 움직인 칸 0.**  p20 의 세 회차 묵은
   "미지정 심볼"도 여기서 풀렸습니다(범례가 이미 답합니다).
   **게이트 통과는 자동입니다 — 8행이 전부 CZF(미수령) 탭입니다.**  17회차와
   다른 점은 **배분을 범례가 정한다**는 것뿐이고, 그 사실을 그대로 적습니다.
4. **전역 심볼 사전은 등록 화면까지입니다** ([D]).  조사가 또 범위를 줄였고
   (네 회차 연속) 세 갈래 중 **둘은 이미 검출기가 내고 파이프라인이 버리고
   있었습니다**.  실분석 미판정 **169건 · 19종류**.  **★ 사전이 검출에
   닿지 않습니다** — 닿게 하면 지문이 사람이 누른 것에 따라 달라져 회귀
   기준선이 성립하지 않습니다(시험이 강제).  자동 등록은 두 겹으로 막힙니다:
   `register()` 가 `author` 를 요구하고, **부르는 곳이 하나**임을 소스 검사가
   강제합니다.  등록 폼은 **전부 빈 칸**으로 엽니다.
5. **★ [E] 가 만든 결함을 [F] 가 잡았습니다.**  ANGLE 8행이
   `... ANALYSIS ELEMENT` 로 나갔습니다 — `describe_axis._VALVE_BODIES` 에
   없어 ISA 조립(A=ANALYSIS · E=ELEMENT)으로 흘렀고, **뜻이 없는데 문법이
   맞아 눈으로는 안 걸립니다**.  시험이 검출기의 갈래를 소스에서 세어 전부
   `<갈래> VALVE` 인지 봅니다 (`BOWTIE_OTHER` 만 예외 — "어느 갈래도 아님"
   에 이름을 주면 없는 밸브를 만드는 것입니다).
6. **F-1 Bypass 는 답이 바뀌었습니다** — [E] 가 몸체를 만들었기 때문입니다.
   p10·p11 의 라벨 3개가 새 ANGLE 몸체 3개에 **1:1 · 교차 없이 · 두 장에서
   같게** 섭니다 (거리 89.5pt · 2위 391~585pt).  17회차의 194~198pt 는 GLOBE
   를 가리키던 값이고 **거리가 답을 못 준 것이 아니라 답이 도면에 없었습니다.**
   적용은 **실무 판단**입니다 — 89.5 는 `equipment_reach` 45.8 의 두 배이고
   여섯 행 모두 현행 문장이 있으며, **게이트가 잴 수도 없습니다**(CZF 미수령).
7. **★ 12회차 기능이 17회차 변경으로 고장 나 있었습니다** ([I]).  C-1 의
   접미가 붙으면서 `일괄 적용` 의 묶음이 **언제나 1행**이 됐습니다 — 묶음의
   정의가 "지금 같은 문장" 인데 A·B·C 가 그 같음을 없앴습니다.  더 나쁜 것은
   **사정거리를 두 곳에서 계산하고 있었다**는 점입니다 (화면 `sameSentence`
   ↔ 동작 `setDescription` 안의 복사본).  판정하는 곳을 하나로 되돌리고
   묶음은 **접미를 뗀 문장**으로 짓습니다.  두 회차 모두 각자는 옳았고
   **겹치는 자리에 시험이 없었습니다.**
8. **UI 스위트가 어떤 데이터에서 도는지 `PID_UI_DB` 로 말합니다.**  실 DB 에
   SCOPE 있는 분석이 없는 PC 가 있는데, 그때 실 DB 를 갈아끼우면 17회차의
   "시험은 실 데이터를 안 건드린다" 가 깨집니다 — **가리키는 곳만 바꿉니다.**
9. **AT·AIT 는 여전히 0행**입니다 ([G]).  앵커를 넣으면 축2 89.1 → 88.3 로
   게이트 불통과입니다.  다만 **미판정 심볼 목록에는 있습니다**(4건 · p16) —
   [D] 화면에서 사람이 지정할 대상입니다.
10. **⚠ SADARA 교차검증은 이번 회차에 재지 못했습니다** — 이 작업 환경에
   `sadara_1A46.pdf` 가 없습니다.  82행 `0767ba79` 는 **미확인**입니다.

**그 다음 회차 — 별표 SCOPE 근본 규명 (19회차, 세 번째 반복 지적)**

전문 `out/round19/1_보고서.md` · 원인 `4_원인.md` · 검사기 `5_검사기검증.md` ·
실물 `3_실물덤프.md` · 진행 `out/round19_progress.md`.
**행 1037 · Q'ty 1931 그대로이고 지문만 `015e34b3` → `fb85b039` 로 움직였습니다**
(SCOPE 55행 · 검출은 한 칸도 안 움직였습니다).

1. **★ 원인은 넷이었고 그중 하나는 검사기 자신입니다.**  같은 지적이 세 번
   반복된 이유가 그것입니다 — **판정에 쓰는 축을 검사도 쓰고 있었습니다.**
   16회차는 크기·자리 둘 다 공유했고, 18회차는 크기 축만 독립으로 만들고
   **자리 축을 그대로 물려받았습니다.**  독립 감사로 세면 기준선에서
   **112건**이 안 붙고 있었습니다 (두 회차가 "전수 0건" 이라고 적은 자리).
2. **① 마크 창이 "버블 위" 하나뿐이었습니다.**  근거는 11회차 p6 **한 장**의
   실측(별표 y=526 · M 원 y0=534.4)이었습니다.  이 문서는 별표를 **옆에도**
   찍고(p25·p27·p30), 더 나쁘게는 `drawn_mark_sizes` 가 같은 창을 쓰므로
   옆 별표는 **크기조차 인정되지 않아** `find_marks` 에서 이미 떨어졌습니다 —
   18회차가 고친 크기 축이 자리 축에 매달려 있었습니다.
   `mark_side` **10.0** 은 이 문서가 답했습니다: 마크 646개의 가로 간격이
   0~8.58pt 에 386개, **8.58~10.59pt 가 0개**, 다음이 10.59 · 14.1 · 20.3.
   창은 **사각형 하나**입니다 — 두 갈래로 쪼갰더니 p52 에서 별표가 테두리선에
   정확히 걸쳐 중심이 y0 보다 **0.00002pt** 위라 "위"도 "옆"도 아니게 됐습니다.
3. **★ 아래는 열지 않았습니다 — 이번엔 근거가 있습니다.**  25pt 안의 아래
   마크를 **전부 렌더로** 봤더니 한 개도 그 버블의 것이 아니었습니다:
   p7(+8.4) 액추에이터 돔·M 원 · p33(+7.2) 옆 사각 심볼 · p20(+17.1) 밸브 ·
   p26·p27(+49.6) 스템 해칭.  8pt 열면 18건이 붙고 옳은 것은 **0** 입니다.
4. **③ 소유자는 유클리드로 가릅니다 — 체비쇼프는 안 됩니다.**  두 경우가
   서로 반대를 요구합니다: p25 는 세로가 5.67 로 같고 **가로**(0.09↔2.85)가,
   p9 은 가로가 5.64 로 같고 **세로**(0.0↔21.9)가 가릅니다.  `max` 는 각
   경우에서 **갈리는 축을 버려** 무승부를 만듭니다.  전 도면 UNDEFINED:
   가로만 20 · 체비쇼프 16 · **유클리드 15**.  렌더가 정답을 줍니다
   (p25 버블마다 `*` 하나 · p9 버블마다 `**`).
5. **④ 밸브는 몸체와 액추에이터를 둘 다 봅니다.**  11회차가 액추에이터
   하나로 정한 근거도 **p6 한 장**이었습니다 — ①과 같은 회차·같은 도면·같은
   함정입니다.  p27 은 별표가 **몸체 옆 8.1pt**(M 원에서는 30pt 밖)라 BFV
   3행이 별표를 갖고도 SCT 였습니다.  고친 것은 허용치가 아니라 **보는
   사각형**(`read_vendor_mark(also=)`)이고, 마크는 두 창에 다 들어와도 한 번만
   세어집니다.  BFV 정밀도 81.8% → **100.0%**.
6. **감사에 두 번째 질문을 더했습니다.**  18회차는 *알아봤는가*(`find_marks`)만
   물었고, p25·p30 은 **알아보기는 했는데 안 붙은** 경우라 그 시험이
   통과했습니다.  이제 *붙었는가*도 묻습니다.  변이 시험도 크기·**자리** 둘입니다.
7. **"전수 0건" 이라고 적지 않습니다.**  남은 **15건**을 한 건씩 렌더로 확인해
   주인을 적고 시험에 박았습니다 (p7 액추에이터 5 · p33 사각 심볼 3 ·
   p35 밸브 2 · p46·p47·p49 밸브 3 · p37 액추에이터 1 · p3 1).
8. **지목되지 않은 장에서 33행이 바뀌었습니다** (55행 중).  16회차 [C] 의 p47 과
   같은 모양입니다 — 전수 검사가 지목보다 넓게 잡습니다.
9. **⚠ Description 식별이 41.6% → 40.5% 로 내려갑니다.**  원인 하나입니다 —
   벤더가 된 42행의 문장이 ⓪ 규칙으로 지워집니다 (SKIP 221 → 263).
   Description 규칙의 회귀가 아니라 **SCOPE 판정이 바뀐 결과**이고, 같은
   42행이 축2 정밀도를 89.1 → 94.4 로 올린 행들입니다.  **그래도 지표가
   내려간 것은 사실이므로 그대로 적습니다.**  F1 68.7 → 68.8 · 완전일치 19 불변.
10. **⚠ SADARA 는 이번에도 못 쟀습니다** — 이 작업 환경에 `sadara_1A46.pdf`
   가 없습니다.  82행 `0767ba79` 는 **여전히 미확인**입니다 (18회차와 같음).
11. **실무 판단 대기 둘**: p25 의 `**` 2행(그 장 NOTES 가 `**` 를 정의하지
   않습니다 — `VENDOR`(이름 없음)로 두고 검토에 올립니다) · 발주처 양식
   필터(지금 전량 885/76/23, SCT 만이면 **607/63/18** 로 44행 줄어듭니다).

**그 다음 회차 — 범용성 확인 · 남은 오류 측정 · 회사 PC 반영 (20회차)**

전문 `out/round20/1_보고서.md` · SADARA `3_sadara.md` · FP 내역
`out/round20_fp35.md` · p12 `5_p12_pcv.md` · 적용검증
`out/round20/verify/README.md` · 진행 `out/round20_progress.md`.
**엔진 코드를 0줄 고쳤습니다 — 지문 `fb85b039` · 1037행 · 1931 그대로이고
1037행 전수 대조에서 움직인 행이 0 입니다.**

1. **★ SADARA PDF 는 있었습니다 — 18·19회차의 "없다" 가 오보입니다.**
   두 회차 모두 `data/sadara_1A46.pdf` **한 자리만** 봤고, 실제 위치는
   `app/_data/uploads/2391ee0f1ea6_sadara_1A46.pdf` 였습니다.  §8 의
   *없는 것을 만들기 전에 있는 것을 찾는다* 는 **파일 찾기에도** 적용됩니다.
   실 DB 를 건드리지 않으려고 같은 sha256 의 읽기 전용 사본으로 쟀습니다.
2. **SADARA 82행 · 지문 `0767ba79` — 기준선과 완전 일치.**  지문이 `scope` 를
   해싱하므로 16·18·19회차의 SCOPE 변경이 **한 칸도 안 움직였다**는 뜻입니다.
3. **★ "발동 안 했다" 가 아니라 "발동하는데 안 바뀐다" 입니다.**  규칙별로
   따로 셌습니다 — 별표 크기 재측정 **4개 추가**(p3·p4) · 마크 창 **15개
   추가**(p3·p4) · ANGLE 몸체 **2개**(p7).  앞의 둘은 **범례 장**이라 행에
   안 닿고, ANGLE 2개는 `actuator=NONE` 이라 CHECK 였을 때와 똑같이 빠집니다.
   **약한 곳도 적습니다**: 창 변경은 행 내는 장(p6~p9)에서 차이가 **0** 이라
   그 축은 SADARA 로 시험된 것이 아니고, SADARA 는 별표 NOTES 사전이
   **9장 어디에도 없어** 별표 축 자체가 뜻을 갖지 않습니다.
4. **⚠ SADARA 82행이 전부 `qty=None` 입니다.**  범례 p5 가 `PLANT×1` 만
   정의해 승수표에 `00` 밖에 없는데 행은 `10LBA10` 계열입니다 (15회차 [B]2 의
   "조용한 폴백" 실제 사례).  **지문이 같으므로 이전부터 있던 것**이고 이번
   범위 밖이라 고치지 않았습니다.
5. **★ FP 35 의 최대 원인은 검출이 아닙니다** — **16/35 가 "같은 도면번호를
   두 장이 쓴다"** 입니다 (13회차가 이미 찾아 둔 3쌍).  `00GHC10-M05-0001` 은
   **p47 한 장만으로 발주처 16행과 정확히 맞고**, `10LBG10-M05-0001` 은
   **p12 한 장이 발주처 13행과 글자 그대로 같습니다.**  검출은 두 장 다 옳고
   발주처가 한 번만 셉니다.  ⚠ 맞는 쪽이 **개정이 낮은 p47(Rev.B)** 이라
   **어느 장이 유효한지는 실무 판단**입니다.
6. **성격별**: (a) 도면이 답을 갖고 있다 **16** · (b) 발주처 계상 관행 **12**
   (`00PAB10` PI 10 · p33 PI 1 · p49 RO 1) · (c) 판정 불가 **7** (p32).
   **100% 까지의 산술**(같은 채점기 · 우리 쪽 행만 덜어냄):
   지금 94.4% → p46 빼면 **96.6%** → p15 도 빼면 **96.7%** →
   `00PAB10` PI 가 벤더면 **98.3%** → p33·p49 도면 **98.7%** →
   p32 FE·TIT 면 **99.3%** → p32 초과분까지면 **99.8%**.
   **재현율은 어느 단계에서도 99.3% 그대로이고 FN 4 는 줄지 않습니다.**
7. **★ §4.2(PI 계상 관행)를 다시 씁니다.**  §4.2 는 "도면에서 유도되지
   않는다 · 벤더 별표 축은 회수율 30.4% 라 채택 안 함" 이었습니다.  별표를
   고친 뒤 다시 재면 — 교집합 13장 중 **9장이 정확히 일치**(109=109), 발주처가
   **0 으로 세는 10장 중 7장을 별표가 갈랐습니다**(64건 중 **63건** VENDOR).
   **§4.2 가 적어 둔 반례(`10PGB10-M05-0001`)도 사라졌습니다** — 그 장은
   18회차 [C] 가 고쳐 지금 PI 4개가 전부 VENDOR 입니다.  남는 것은
   **`00PAB10` 세 장의 10건뿐**이고 그것이 회신 대상입니다.
8. **p12 PCV — 범례에 있습니다.**  `AUX. STEAM LETDOWN VALVE #10` 은
   **p2 `ANGLE`** + **p2 `DESUPERHEATER`** 두 항목이 나란히 그려진 것입니다.
   못 읽는 이유는 기하입니다: `_angle_figures` 는 (가) 한 객체 · 선 6개 ·
   `fill=None` (나) 꼭짓점에 끝점 **정확히 4개** 를 요구하는데, p12 는
   **여섯 객체로 흩어져 있고** 아래 삼각형이 **칠해져 있으며 두 번 그려져**
   꼭짓점에 끝점이 **8개** 모입니다.
9. **★ 그래서 고치지 않았습니다.**  흩어진 선분을 모으는 방법을 58장 전수로
   재 보니 "끝점 4 · 직각 두 쌍" 이 **2,208건**(85~95°만 529)이고 p12 에서
   걸리는 21건은 **전부 흐름 화살촉**(76°)입니다.  밸브 몸체가 2,821개인데
   후보가 2,208이면 규칙이 아니라 잡음이고, §3 의 "범례 형상으로 기기 검출"
   실패와 같으며 §2.2 형상 매칭 금지에도 걸립니다.
10. **⚠ p12 PCV 는 미판정 목록에도 없습니다.**  `unclassified_bodies` 가
   "배관 끝막대 둘" 을 요구하는데 이 몸체는 만족하지 않아, **검출도 안 되고
   등록 화면에도 안 뜹니다.**  조건 확장이 다음 회차 후보입니다.
11. **갈음 — 12단계 전부 통과.**  `aac64ba` worktree + `PID_DATA_DIR` 격리로
   회사 PC 조건을 재현하고 **꾸러미를 실제로 풀어** 확인했습니다:
   옛 코드 824행 → 편집 5칸 → 적용 → 첫 화면에 `824행 · 수정 5칸`,
   **편집 5개 전부 생존** → 눌러서 1037행 → 첫 화면 버튼 복귀 → 새 분석
   **1037 · 1931 · `fb85b039`** · 편집 **승계** → 양식 **885/76/23**(실제
   xlsx 를 열어 셈) → 축 99.3/70.5 · 99.3/94.4 → 시험 188/24/14 →
   **실 DB sha256 시작·끝 동일**.
   `out/PID_update_2026-09-07_r20.zip` (543,516 · sha256 `09fbba15…`).
12. **7단계에서 배운 것**: 결과 화면 진입 경로는 **`a.revrow` 를 누르는 것**
   하나입니다 — `?job=` 로 URL 을 직접 열면 결과가 뜨지 않습니다 (13회차 설계
   그대로).  검증 스크립트가 그것을 몰라 처음에 0행을 냈습니다.
13. **UI 24 를 이 검증이 만든 DB 로 돌렸습니다** — 824행(SCOPE 없음)과
   1037행(SCOPE 있음)이 한 DB 에 있어 14회차가 요구한 세 갈래가 전부 실제
   데이터로 걸립니다.

**그 다음 회차 — 신규 PDF 분석 실패 원인 규명 (21회차)**

전문 `out/round21/1_보고서.md` · 어디서 멈추나 `3_어디서멈추나.md` ·
원인과 대책 `5_원인과대책.md` · TC2 구성(보류) `4_tc2_구성.md` ·
캡처 `6_캡처/` · 진행 `out/round21_progress.md`.
**지문 `fb85b039` · 1037행 · Q'ty 1931 이 그대로입니다** (최종 코드로 전량
재측정 · 축·양식·SADARA 전부 불변).

1. **★ TC2 파일은 이 환경에 없고, 그래도 원인 규명은 끝났습니다.**  사용자가
   회사 PC 터미널에서 떠 준 **원본 예외**가 있었기 때문입니다 —
   `ValueError: zero-size array to reduction operation minimum`,
   `extract_titleblocks._ink_mask` 의 `a.min()`.  `get_pixmap(clip=...)` 은
   clip 을 페이지와 교집합하므로 교집합이 비면 0픽셀이 나오고, 빈 배열의
   `.min()` 은 정의되지 않습니다.
2. **★ 줄 번호가 갈래를 확정했습니다.**  `build_glyph_library` 에는
   `segment_chars` 호출이 **셋**(306 REV 칸 · **321 이력 행** · 330 SHEET 칸)
   이고 TC2 는 321 입니다.  거기 닿으려면 `history_rows` 가 비지 않아야 하고
   그 함수는 `x0<1975 · x1>2320` 인 괘선만 받으므로 **TC2 의 종이는 최소
   2320x1390pt** 입니다.  그러면 clip 의 가로 범위(1971~1988)는 반드시 종이
   안이고, 0픽셀이 될 남은 길은 **이력 행 높이 <= 2×`hist_row_inset`(2.4pt)**
   하나뿐입니다.  겹선·표 테두리 겹침·같은 선 두 번 그리기에서 흔합니다.
3. **★ 20회차 배포본에서 원본 예외를 그대로 재현했습니다.**  A1 종이 12장에
   이력 괘선을 `1210.0 / 1212.0 / 1240.0`(첫 간격 2.0pt)로만 그린 합성 PDF 를
   `ec43fbd` worktree 에 올리니 **파일·줄 번호가 사용자 예외와 같고**
   (`477 → 321 → 248 → 221`) 화면 문장도 같았습니다.
4. **AL NOUF1 은 이 갈래에 닿을 수 없습니다** — 이력 행 406개(58장)가 전부
   16.7~16.8pt (값이 두 개뿐: 16.8×250 · 16.7×156), 문턱의 약 7배입니다.
   20회차 동안 안 보인 이유입니다.
5. **★ 근본 원인 — 네 칸은 유도되고 이력 표 기하만 상수입니다.**
   (처음에 "타이틀블록은 전부 상수" 라고 적었다가 **스스로 뒤집었습니다.**
   `config/project_sadara.yaml` 의 주석이 *"The title-block cells are derived;
   these four are not"* 라고 이미 말하고 있었고 처음 읽을 때 흘렸습니다.)
   `_fit_layout` → `derive_layout.derive` 가 그 도면에서 잽니다 — SADARA 실측
   **7개**(`dwg_no_region` `[2726.7, 2224.8, 3289.1, 2280.1]` · `title_region` ·
   `project_name_region` · `rev_box` · `sheet_box` · 두 `*_min_height`).
   **그런데 `hist_rule_x0_max`·`hist_rule_x1_min`·`hist_rule_y`·`hist_rev_col`
   ·`hist_date_col`·`hist_row_inset` 여섯은 그 목록에 없습니다.**  그래서
   `history_rows` 는 다른 회사 양식에서도 **AL NOUF1 의 좌표 띠**에 걸리는 가로
   선을 이력 괘선으로 보고, 그렇게 주워 온 선들 사이 간격이 2.4pt 이하이면
   0픽셀이 됩니다.  **§11 범용성이 시험받은 자리가 여기입니다 — 칸은 유도되는데
   표는 안 됩니다.**
6. **17분의 정체**: 죽는 자리는 `titleblock_glyphs`(도면 읽기 **전**)이고
   그 앞에 `layout` **246.79초(전체 441.2초의 55.9%)** 가 있습니다.  닿기까지
   249.9초가 이미 지나갑니다.  화면이 장수를 못 말한 것도 같은 이유입니다 —
   `sheets_total` 은 `targets` 가 정해질 때(죽는 자리보다 뒤) 실립니다.
7. **고친 것은 (b) 하나입니다** — `_ink_mask` 가 0픽셀이면 `None`.
   **여기서 사유를 말하지 않습니다**(한 칸이 비는 것은 정상이고, 여기서 문장을
   쓰면 그것이 조용한 폴백입니다).
8. **⚠ 그런데 예외만 막으면 행 0개로 조용히 성공합니다** — 타이틀블록을 못
   읽으면 `page_kind` 가 한 장도 PID 가 아니고 `targets` 가 빕니다.  그래서
   `pipeline.TitleBlockUnreadable` 로 시끄럽게 멈춥니다 (`LegendUnavailable`
   과 같은 자리 · **문턱 없이 0** · 사실은 `tb.frame_report` 가 재고 문장은
   저장하지 않음).  사유 문장은 `_fit_layout` 의 `moved` 를 보고 **그 칸을 재는
   데 성공했는지**에 따라 갈립니다 — 사람이 할 일이 다르기 때문입니다.
   **⚠ 스스로 뒤집은 것**: `open_pdf` 직후에 이른 검사를 넣었다가 뺐습니다.
   근거가 *"판정을 바꾸지 않고 자리만 당긴다"* 였는데 **5번 때문에 전제가
   틀립니다** — 그 자리에서 보는 칸은 아직 그 도면의 것이 아니고, 종이가 작은
   문서에서 유도가 성공했을 것을 미리 잘라내면 될 분석을 못 하게 만듭니다.
   **빨리 실패하는 것보다 틀리지 않는 것이 먼저입니다.**  그래서
   **"빨리 실패하게 한다" 는 못 이뤘습니다** — 조밀한 60장에서는 `layout`
   (246.79초)을 다 지난 뒤에 멈춥니다.  이룬 것은 *언제·왜 멈췄는지를 말하는
   것*뿐입니다.
9. **⚠ 같이 찾은 별개 결함 — 이력 행 캐시가 문서 사이로 샜습니다.**
   `history_rows` 의 캐시가 모듈 전역 dict 이고 키가 `page_no` 하나, 지우는
   곳이 없었습니다.  실측: SADARA 9장을 혼자 읽으면 이력 행 0행인데 같은
   프로세스에서 AL NOUF1 뒤에 읽으면 **9장 전부**가 AL NOUF1 의 7행을 받습니다.
   **새 프로젝트의 결과가 "그 전에 무엇을 분석했느냐"에 따라 달라집니다.**
   캐시를 `_rects`·`_segments` 와 같은 자리(쪽 객체 자신)로 옮겼습니다.
   **보고된 실패의 원인은 아닙니다** — 그 실패는 죽는 쪽이고 이 캐시는 죽지
   않는 쪽을 만듭니다.
10. **[E] 화면은 어디서 멈췄는지 말합니다.**  단계 이름은 새로 만든 값이
   아닙니다 — 파이프라인이 `set_progress` 로 `job.message` 에 적고 있었고
   실패 처리가 그 칸을 사유로 덮어써 사라지고 있었습니다.  덮기 **전에** 읽어
   `job.stopped_stage` 에 옮겨 적고, **저장하는 것은 원문**이며 한국어는
   화면의 `stageWords()` 가 만듭니다.  예외 원문은 `GET /jobs/{id}/error_detail`
   로만 오고 **펼치기 전에는 받아 오지도 않습니다** (기본 응답은 그대로).
11. **하지 않은 것**: (a) `tb.LAYOUT` 런타임 유도 · (c) 글리프 라이브러리 없이
   진행 · 임계값 완화 · 좌표 추정.  전부 TC2 파일이 있어야 근거가 서고,
   없이 하면 지어낸 값입니다.  **양식이 세 개가 되어야 유도 규칙의 근거가
   섭니다.**
12. **⚠ 다음 회차의 시험**: TC2 가 오면 §2 의 예측(종이 2320×1390pt 이상 ·
   이력 괘선 간격 2.4pt 이하)이 맞는지 확인합니다.  틀리면 그대로 적고 다시
   봅니다.

**그 다음 회차 — 한 서버에서 두 양식을 섞어도 서로 망가뜨리지 않게 (22회차)**

전문 `docs/round22_config_isolation.md` · 새 양식 절차
`docs/new_form_checklist.md` · 현장 보고 `out/round21_field_report.md`.
**지문 `fb85b039` · 1037행 · Q'ty 1931 이 그대로입니다** (혼용 뒤에도 · 1037행
전수 대조 움직인 칸 0).

1. **★ 현장에서 AL NOUF1 이 죽었습니다.**  회사 PC 에서 TC2 를 분석한 뒤
   AL NOUF1 을 분석하니 `TitleBlockUnreadable` — 예외 문장이 도면번호 칸을
   **`[963.8, 785.9, 1162.3, 805.5]`**(TC2 값)로 적고 있었습니다.
   **AL NOUF1 을 남의 좌표로 읽고 있었습니다.**
2. **③ `_fit_layout` 이 전역 config 를 영구히 덮습니다.**  프로필이 그 문서의
   것이 아니면 잰 값을 `CFG.overlay(...)` 로 **모듈 전역 `CFG.data` 에 제자리
   기록**하고 되돌리지 않았습니다.  실측 — 낯선 문서 한 번에 **일곱 칸**:
   `regions.drawing_area` `[38,35,1960,1650]` → `[80.6,72.9,2726.7,2311.2]` ·
   `sheet.width_pt/height_pt` · `title_block` 네 칸.  다음 문서가 AL NOUF1 이면
   `same=True · states_geometry=True` 라 **다시 재지 않고** 그 값을 씁니다.
3. **★★ ④ 그런데 `CFG` 만 되돌려서는 모자랐습니다.**  되돌린 뒤 다시 재니
   죽지는 않는데 지문이 `fb85b039` → **`f21626fd`**.  전수 대조하니 **행
   1037개가 전부 같고** 커넥터를 **218 → 219**개 세던 것 하나였습니다
   (값 `connector_reach: 70.2` 는 같음).  원인은
   `pidcache.rename_projects` 의 `global PROJECT_NAME_REGION,
   PROJECT_NAME_MIN_HEIGHT` — 남으면 다음 문서의 프로젝트명을 **남의 자리**에서
   읽고 `analysis_scope` 가 달라져 `derive_connector_reach` 가 **세는 장**이
   달라집니다.  **판정이 아니라 세는 대상이 달라진 것**이라 더 알아채기
   어렵습니다.  엔진의 `global` 문을 전수로 세는 시험을 넣었습니다.
4. **되돌리는 곳은 하나입니다** — `analyse` 가 `_analyse` 를 감싸고 `finally`
   에서 되돌립니다.  되돌릴 때 쓰는 코드는 `_reconfigure` 가 쓰는 것과 **같은
   함수**(`_rebind_config`)입니다.  **실패해도 되돌립니다** (안 그러면 죽은
   양식의 잔재가 다음 분석을 흔듭니다).  한 문서만 도는 프로세스에서는
   아무것도 달라지지 않습니다 — 되돌리기는 결과를 만든 **뒤**입니다.
5. **회귀 — 세 방향**: SADARA `82/0767ba79` → AL NOUF1 **`1037/fb85b039/1931`**
   → SADARA `82/0767ba79`.  축1 99.3/70.5 · 축2 99.3/94.4 · 양식 885/76/23.
6. **★ TC2 를 위한 길 — `GET /jobs/{id}/measured_config`.**  새 양식의 좌표를
   재는 코드도(`derive_layout`) 저장도(`applied_rules.layout.items`) **이미
   있었습니다.**  없던 것은 꺼내 보는 길뿐이었습니다 — 13·15·16·18회차와
   같습니다.  `project_*.yaml` 에 붙일 수 있는 YAML 로 냅니다.
7. **재지 못하는 것은 지어내지 않습니다** — 개정 이력 표 여섯(`hist_*`)은 칸마다
   캡션이 없어 앵커가 없습니다.  `must_measure_by_hand` 로 **이름만** 내고
   사람이 잽니다.  안 채워도 분석은 되고 REV 만 빕니다 (검출에 안 쓰입니다).
8. **⚠ TC2 가 "AL NOUF1 과 같은 식별률" 이 되는지는 모릅니다** — 파일이 이
   환경에 없습니다.  이번에 보장하는 것은 둘뿐입니다: **섞어 분석해도 서로
   안 망가진다** · **TC2 양식의 좌표를 뽑아낼 수 있다.**
9. **⚠ 21회차의 원인 규명이 틀렸습니다** (`out/round21_field_report.md` §2).
   줄 번호로부터 "TC2 종이는 2320×1390pt 이상" 이라고 좁혔는데, 회사 PC 가
   보여 준 TC2 도면번호 칸이 **A3(1191×842) 안에 들어갑니다.**  진짜 원인은
   `_HIST_ROWS` 캐시가 TC2 에게 AL NOUF1 의 이력 행을 물려준 것이었고,
   21회차가 "별개 결함" 이라 적으며 곁다리로 고친 그것이 **원본 예외의 원인**
   이었습니다.

**그 다음 회차 — 범용성 기반: 목표선 정의 · 세 프로젝트 회귀 · 상수 전수 조사 (23회차)**

전문 `out/round23_report.md` · 상수 `out/round23_constants.md` · 축3 `out/round23_score.md`
· 격차 `out/round23_gap.md` · 로드맵 `out/round23_roadmap.md` · 진행 `out/round23_progress.md`.
**엔진 코드 0줄.  세 프로젝트 전부 불변** (AL NOUF1 `fb85b039`·1037·1931 · SADARA
82·`0767ba79` · TC2 607·`c46b10fa` 확정).

1. **★ 축3(식별률)을 더했다 — 정답지 없이 재는 축.**  `spike/identification.py` 하나가
   정의이고 **축1·축2 는 건드리지 않았다**.  여섯 지표(도면번호 · 개정 · 태그→행 ·
   수량 · 심볼판정 · 파라미터)에 **같은 무게 1/6** — 여섯이 파이프라인의 서로 다른
   단계를 하나씩 대표하고 순위를 매길 근거가 도면에 없기 때문이다.
   **AL NOUF1 94.8 · SADARA 74.8 · TC2 64.8 → TC2 는 AL NOUF1 대비 68%.**
2. **★ 첫 축 정의는 엔진이 아니라 채점기를 재고 있었다** (AL NOUF1 88.8 → 94.8).
   도면은 `TT`·`PT`·`LSH` 로 인쇄하고 엔진은 `anchors.type_map` 으로 `TIT`·`PIT`·`LS`
   로 바꿔 행을 내는데 글자 그대로 비교했고, `MOV`·`PSV` 를 계기·밸브 양쪽에서 세어
   계기 쪽에서 반드시 틀렸다.  **엔진이 읽는 그 사전을 채점기도 읽는다.**  고친 뒤
   셋이 **같이** 올라 순위와 격차는 바뀌지 않았다 — 축이 프로젝트를 유리하게 가른 것이
   아니라 같은 오차를 걷어낸 것이다.
3. **AL NOUF1 이 만점이 아닌 이유는 전부 알려진 것이다** — PSV 10짝(안전밸브는 발주처
   산출물 범위 밖) · 미판정 169 · `brk_max_mark` 등 2건 `UNAVAILABLE`.
   **축3 이 100 이 아니어야 정직한 축**이라 감점을 그대로 뒀다.
   축2(94.4%)와 축3(94.8점)은 **다른 것을 잰다** — 축2 의 남은 FP 35 중 28건(겹친
   도면번호 16 + 계상 관행 12)은 도면을 옳게 읽은 결과라 축3 에서 감점이 없다.
4. **회귀 하네스** `spike/regression_3p.py` — 명령 하나로 셋을 **순차**로 돌린다(약 17분).
   프로젝트마다 자식 프로세스라 메모리를 돌려받고 최대 RSS 를 따로 잰다
   (**AL NOUF1 4.8GB · SADARA 0.8GB · TC2 7.2GB**).  기준선은 `spike/baselines_3p.json`
   이고 벗어나면 실패한다.  **프로젝트 추가는 `spike/projects_3p.json` 에 한 항목**이다.
5. **★ 상수 전수** — dataclass 기본값 **71개 중 39개가 config 로도 못 바꾼다**.
   `project_alnouf1.yaml` 잎 키 **210개** 중 22회차가 그 도면에서 재는 것은 **15칸**뿐이고
   나머지는 사람이 적은 AL NOUF1 실측값이다 — **"설정 가능" 은 "유도" 가 아니다.**
   **조용한 실패 다섯 자리**: 이력 표 기하 6칸 · 도면번호 형식 · 승수표 파싱(머리말·행수)
   · 액추에이터 규칙 · 별표 크기.  여섯 번째 후보는 `brk_max_mark`(TC2 가 AL NOUF1 값
   28.3 으로 판정한다).
6. **★ 경계가 드러났다 — 비율·모양 규칙은 절반 축척(A1→A3)을 넘었고 절대 pt 규칙은
   전부 걸렸다.**  넘은 것: 버블 `cap_ratio`(TC2 34×11) · 몸체 `body_ratio`·`waist_ratio`
   (TC2 몸체 389개) · 회전 정규화(`/Rotate 270`).  걸린 것: `hist_*` · `act_box` ·
   `mark_above`.  **이것이 이 도구에서 확인된 유일한 범용화 방법이다.**
7. **격차** — 네 상수를 유도로 바꾸면 TC2 **92.6점(대비 98%)**: 이력 표 +16.7 ·
   승수표 +7.2 · 액추에이터 +2.8 · 도면번호 +1.1.  **넷 중 셋은 잴 대상이 도면에 있고
   하나(액추에이터)는 없다** — TC2 범례가 몸체 없이 그려 스템이 애초에 없다.
   ⚠ **축3 이 밸브의 무게를 과소평가한다** — ㉣가 +2.8인 것은 (장,태그) 짝으로 세기
   때문이고 행으로는 MOV 태그 58개가 0행이다.  **가중은 바꾸지 않고**(§8) 한계를 적는다.
8. **로드맵은 후보만 냈다** (§14.3).  셋 다 24회차는 **이력 표**로 같다 — 기여 최대 ·
   REV 는 지문 대상이 아니라 위험 최소 · 잴 대상이 도면에 있다.
   **프로젝트가 늘수록 좋아지는 구조**: 지금은 늘어도 안 좋아진다(AL NOUF1 값이 config
   에 박혀 있다).  축적 후보 셋 — 측정값 분포 · **무차원 비** · 유도 실패 사례.
9. **⚠ UI 스위트가 실패했고 원인은 데이터였다.**  이 PC 의 실 DB 에는 10회차 이전 분석
   넷뿐이고 **SCOPE 가 전부 빈칸**이라 14회차 방어가 설계대로 **건너뛰지 않고 실패**했다.
   `spike/build_ui_db.py` 가 저장된 결과에서 그 DB 를 세운다 — **재분석 없이**
   `db.store_result` 로 넣고 `legacy` 갈래를 위해 실 DB 를 읽어 복사한 위에 얹는다.
   ⚠ 첫 픽스처가 `pdf_path` 를 비워 둬 재분석 시험이 실패하고 뒤 넷으로 번졌다 —
   **제품이 아니라 픽스처의 결함**이었다.  고친 뒤 **UI 24 전량 통과**(613초).
10. **⚠ 대기 셸 셋이 8시간 40분 헛돌았다** (사용자가 중지).  `pgrep -f` 자기참조다 —
   §8 의 규칙 넷을 보라.  **UI 가 시작조차 안 됐는데 "도는 중" 으로 보고했고**, 판정
   근거는 로그 mtime 이었다.
11. **메모리** — TC2 최대 RSS **7.2GB**.  이 환경에서 두 분석을 겹치자 실제로 OOM 이
   났다.  단독이면 8GB 기계에서 스왑을 쓰며 돌고 **16GB 이상을 권한다.**  원인은
   60장 × 장당 도형 54,436개이고 `layout` 단계가 전체의 79.7% 를 쓴다.

**그 다음 회차 — 이력 표 유도 (범용화 첫 구현) (24회차)**

전문 `out/round24/1_보고서.md` · 실측 `3_measure.md` · 규칙 `4_rule.md` ·
검증 `5_score.md` · 진행 `out/round24_progress.md`.
**AL NOUF1 `fb85b039` · 1037행 · 1931 · 축1 · 축2 · 양식 885/76/23 ·
Description 40.5/68.8/19 가 전부 그대로입니다.**

1. **★ TC2 의 REV 가 0/60 → 60/60 이 됐습니다** (`'A'`).  원인은 하나였습니다 —
   **이력 행을 못 찾으면 글리프 사전이 비고, 사전이 비면 REV 칸을 읽어도 이름을
   붙일 수 없습니다.**  TC2 의 REV 칸에는 개정 문자가 실제로 인쇄돼 있고(연한 획
   글꼴 `A`), 이력 표를 찾자 그 문자에 이름이 붙었습니다.
   23회차 로드맵의 예측 **+16.7** 이 그대로 들어왔습니다 (축3 64.8 → **82.0**,
   나머지 +0.5 는 파라미터 지표 몫).  **TC2 는 AL NOUF1 대비 68% → 86%.**
2. **여섯 칸 중 다섯을 그 도면에서 잽니다** (`derive_layout._history_table`).
   셋 다 모양이고 절대 pt 가 없습니다 — 간격이 같은 괘선 띠 중 **촘촘한 쪽**
   (세 문서 다 그 열에 띠가 둘이고 행 높이가 2~3배 갈립니다: 16.74↔44.52 ·
   24.05↔63.81 · 8.52↔22.56) · 그 띠를 세로로 가로지르는 괘선(1열 REV · 2열
   DATE) · 오른쪽 끝은 노트 열의 **안쪽** 괘선.
3. **`hist_row_inset` 은 유도하지 않습니다 — 그리지 않은 값입니다.**  칸을 읽을
   때 안쪽으로 물리는 여백이라 도면이 그것에 대해 아무 말도 하지 않습니다.
   `notes` 에 그 사실을 적고 설정값을 씁니다.
4. **★ 허용치 2% 는 분포의 빈 띠에서 왔습니다.**  처음 5% 로 두었더니
   `REV. DATE DESCRIPTION` **머리글 행**이 띠 안에 들어왔고, 그 결과는 값이
   아니라 **이름이 밀리는 것**이었습니다 — `build_glyph_library` 가 머리글 행을
   rank 0 으로 세어 개정 문자 `A` 를 `B` 로 이름 붙였습니다 (TC2 60장 · SADARA
   9장 전수).  세 문서 전 장에서 개정 행은 **최대 1.4%** · 머리글 행은 **최소
   2.8%** 이고 그 사이가 비어 있습니다 (`spike/hist_gaps.py`).
5. **★ 승인된 안(캡션 앵커)에서 벗어났고 근거는 실측입니다.**  `derive_layout`
   의 캡션 찾기가 실제로 `history` 를 찾는 문서는 **셋 중 TC2 하나**입니다.
   AL NOUF1 은 그 캡션을 **58장 중 10장만 텍스트로** 인쇄하고(48장은 획으로
   그립니다), SADARA 는 인쇄하는데도 `_column_lines` 가 y 중심을 `round(...,1)`
   로 묶어 같은 줄의 `REV.`(1828.092)와 `DESCRIPTION`(1827.912)을 두 줄로 쪼개
   놓칩니다.  **캡션을 1차로 삼으면 셋 중 둘에서 표를 못 찾습니다.**
   0.1pt 반올림은 측정 도구에서만 고쳤습니다 — `derive_layout` 쪽을 고치면
   캡션으로 잡는 **다른 네 칸**이 SADARA 에서 움직여 82행·`0767ba79` 에 닿을 수
   있습니다 (25회차 후보).
6. **AL NOUF1 은 파이프라인에서 이 값을 쓰지 않습니다** — `_fit_layout` 은
   프로필이 그 문서의 것이고 기하를 적고 있으면 유도값을 하나도 얹지 않습니다.
   그래서 지문은 구조적으로 안전하고, **규칙이 맞는지는 따로 쟀습니다**:
   이력 칸만 유도값으로 바꿔 전 장을 다시 읽어도 글리프 사전 · 이력 행 7 ·
   REV 58/58 이 **한 칸도 다르지 않습니다** (`spike/hist_verify.py`).
7. **⚠ 사전에 문자가 하나뿐이면 신뢰도는 정보가 아닙니다.**  `classify` 는
   2위가 없으면 비율을 `inf` 로 내므로 무엇을 읽어도 HIGH 입니다.  TC2 의 `A`
   가 맞다는 근거는 신뢰도가 아니라 **도면 두 곳이 같은 말을 한다**는 것입니다
   (REV 칸의 `A` · 이력 표 개정 행의 `A` + `26.08.21` + `FOR INTERNAL USE`).
8. **⚠ 표본 둘로 정한 규칙이 하나 있습니다** — `hist_rule_x1_min` 의 근거인
   "노트 열의 안쪽 괘선" 은 AL NOUF1 · SADARA 에서만 확인되고 TC2 에는 그
   괘선이 없어 시트 가장자리로 떨어집니다.  **넷째 프로젝트 재확인 목록**:
   ① 노트 열 안쪽 괘선 ② 머리글 행이 몇 % 높은가(빈 띠 유지) ③ 이력 띠가
   정말 둘인가 ④ REV 칸에 개정 삼각형을 그리는가.
9. **⚠ SADARA·TC2 는 REV 칸에 개정 삼각형을 그립니다 — 글리프 층의 문제입니다.**
   빈 행에도 삼각형이 있어 칸 전체로 읽으면 "문자 하나"가 나옵니다.  실제
   파이프라인에서는 SADARA 가 TEXT 로 읽고 TC2 는 삼각형 안 문자가 획이라
   문제가 안 되지만, **규칙으로서는 약한 자리**입니다.  손입력 SADARA 값
   `[2740, 2755]` 은 삼각형 안쪽만 집는 좁은 띠였고 **도면은 그 좁은 띠에 대해
   아무 말도 하지 않습니다.**
10. **있는 것을 안 쓰고 있었다 — 일곱 번째** (13·15·16·18·22·24).  이력 표를
   읽는 코드도 글리프를 라벨링하는 코드도 처음부터 있었고, 없던 것은
   **그 표가 어디 있는지 그 도면에게 묻는 길**뿐이었습니다.
11. **회사 PC 갈음 꾸러미 r24** — `out/PID_update_2026-09-09_r24.zip`
   (10~24회차 누적 · 62파일 · 새로 41 · 덮어쓰기 21).  `aac64ba` worktree +
   `PID_DATA_DIR` 격리로 **13단계 적용 검증**을 했습니다
   (`out/round24/verify/README.md`): 옛 코드 824행 → 편집 5칸 → 꾸러미를 실제로
   풀어 **HEAD 와 바이트 동일** → 편집 5/5 생존 → 새 분석 **1037 · 1931 ·
   `fb85b039`** · 승계 5/5 → 양식 **885/76/23**(실제 xlsx 를 열어 셈) →
   축1 99.3/70.5 · 축2 99.3/94.4 → 시험 → **실 DB sha256 시작·끝 동일**.
   13단계는 둘을 봅니다 — 21회차(합성 PDF 가 `TitleBlockUnreadable` 로 멈추고
   행 0개로 조용히 성공하지 않는다)와 **24회차(푼 그 코드로 TC2 REV 60/60)**.
   ⚠ worktree 의 빠른 시험은 **212 통과 · 3 건너뜀**입니다 — 건너뛴 셋은
   `out/candidates_nouf1.xlsx` 가 있는 기계에서만 도는 시험이고 본 저장소에서는
   215 통과입니다.  `app/_data/` 는 **목록 시점과 zip 시점 두 번** 검사했고
   둘 다 0건입니다 (`spike/pack_handover_r24.py`).

**그 다음 회차 — 별표를 도면에서 읽는다 · §9 첫 적용 (25회차)**

전문 `out/round25/1_보고서.md` · 상수 재분류 `out/constants_by_source.md` · 실태
`out/round25_tc2_marks.md`.  **AL NOUF1 `fb85b039` · 1037 · 1931 · 축1 · 축2 · 축3 95.0
과 SADARA 가 불변이고, TC2 지문만 `c46b10fa` → `a94bb2ad` (SCOPE 607/0 → 555/52).**

1. **★ 현장 보고 — TC2 에서 별표 있는 것과 SCT 가 구분되지 않는다.**  실측: 607행
   전부 `vendor_supply=None`.  뿌리는 **TC2 가 별표의 획을 하나씩 따로 그린다**는
   것이다 (대각선 둘 · 세로획 · 가로획이 각각 path).  `_glyph_clusters` 가 **뭉치기
   전에** path 마다 "납작하면 버림 · 3~8pt" 를 걸어 가로·세로획을 잃고, 남은 대각선
   둘은 하한에 못 미쳐 **후보에도 못 들었다.**  AL NOUF1 은 한 path 에 획 여섯이라
   통과한다 — AL NOUF1 의 그리기 방식이 코드에 외워져 있었다.
2. **정의줄(§9 ①②)을 먼저 봤다.**  AL NOUF1 은 18장이 별표를 획으로 그리고 전부
   (2획 · 4.0×4.0 · 중점 퍼짐 0.0).  **TC2 는 9장 전부 텍스트** `*`/`(*)` 라 정의줄에서
   모양을 읽을 수 없다 → 진행 조건대로 본문 실물(④)로 갔다.
3. **규칙은 획의 관계다** (`star_groups` · `star_marks`): 짧은 곧은 획 중 **중점이
   한 점**(획 길이의 1/4)에 모이고 방향이 둘 이상이며 **어느 획 끝도 다른 잉크에
   닿지 않는** 무리.  상한은 그 장 버블의 짧은 변, 크기는 그 장 본문에서 두 번 이상
   (18회차 규칙), 정의줄이 획을 주면 획 수도 맞아야 한다.  **가산**이고 기존 마크
   자리는 건너뛴다.  절대 pt 를 코드에 새로 적지 않았다 (`tests/test_star_marks.py`).
4. **★ 끝점 조건이 없으면 밸브 나비가 별표다** — 두 삼각형의 대각선도 중심에서
   교차한다.  접촉 시트가 잡았다: 조건 전 TC2 더한 517개 대부분이 나비, AL NOUF1
   434개(414개가 17.0pt 2획) 가 더해져 p7 에서 별표+나비가 `**` 로 세어졌다.
   조건 뒤 TC2 **71/71 별표** · AL NOUF1 2(p55 · 지문 불변).
5. **[A] 상수 재분류** — A(찾는데 안 쓴다) **38** · B 5 · C 30 · D 92 · E 48 · ✔ 37.
   외워둔 값의 진짜 수 193.  A 부터 간다 (순서는 후보만 냈다 — §14.3).
6. **⚠ 비용** — 끝점 검사용 잉크 인덱스가 AL NOUF1 426→649초 · 4.8→**7.8GB**,
   TC2 463→608초 · 7.2→**8.9GB**.  쪽 객체 캐시로 장마다 4회→1회로 줄였지만 남는다.
   **8GB 기계에서 TC2 가 위험하다** — 다음 회차 후보.  시험도 같이 느려졌다:
   UI 24 가 588→835초 · slow 14 가 1343→**2749초** (첫 실행은 `timeout 2400` 에
   13/14 에서 잘려 60분 상한으로 다시 돌렸다).
7. **⚠ p25 · p41 은 별표를 찾았는데 행이 0개** — 도면번호 6자라 `formats.drawing_no`
   에 안 맞아 장이 빠진다 (23회차 ㉡).  별표 축이 아니라 도면번호 형식의 문제다.
8. **스스로 뒤집은 것 넷** — "모양 규칙" → "자리+비율" → "획의 관계+자유 끝점"(세 번째가
   두 문서를 넘었다) · 설정 크기 되살리기의 회복(32→68)은 틀린 마크였다 · "6~8pt 로
   흩어진 별표" 는 밸브 조각이었다 · 첫 별 규칙이 AL NOUF1 에 434개를 더했다 —
   **"불변" 이라고 적기 전에 잰 것이 잡았다.**

**그 다음 회차 — 별표 축 세 원인 · 구성이 다른 P&ID 도 구분한다 (26회차)**

전문 `out/round26/1_보고서.md` · 원인 규명 `out/round25_tc2_scope_defect.md`.
**AL NOUF1 `fb85b039` · 1037 · 1931 · 축1 · 축2 · 축3 95.0 · 양식 885/76/23 과
SADARA 가 불변이고, TC2 지문만 `a94bb2ad` → `536d3baa`** (SCOPE 열과 검토 사유만
움직였다 — 행 수 · Q'ty · 축3 여섯 지표가 그대로다).

1. **★ 25회차가 별표를 찾아 놓고 버리고 있었다.**  현장 보고("TC2 에서 Scope 구분이
   전혀 안 된다")를 재현하니 p6 은 별표를 **9개 찾고 마크는 0개**였다.  범인은
   25회차가 안전장치로 넣은 조건 — *"본문 별표의 획 수가 정의줄 별표와 같아야
   한다"* — 이고, **두 문서 다 그 전제가 거짓**이다:
   TC2 정의줄 2획 1.8pt ↔ 본문 4획 2.9pt · AL NOUF1 정의줄 2획 4.0pt ↔ 본문 4획.
   전 문서로 세면 TC2 **254개 중 174개(68.5%)** 를 그 조건이 버렸고, 하필
   **정의줄이 있는 장** — 별표의 뜻을 아는 장 — 에서만 버렸다.
   **정의줄은 뜻을 정하지 모양·획 수·크기를 정하지 않는다** (18회차의 *범례는 뜻을
   정하지 축척을 정하지 않는다* 와 같은 문장).
2. **AL NOUF1 이 멀쩡했던 것은 그 조건이 옳아서가 아니다.**  그 문서는 별표를 한
   path 에 그려 옛 경로(`_glyph_clusters`)가 이미 전부 잡고 있었고, 버려진 274개는
   **같은 별표를 획으로 다시 본 것**이라 중복 제거에 걸릴 것들이었다.  그래서 조건을
   빼도 **붙는 마크가 336 → 336** 으로 같다.
3. **정의줄 `* By SE` 를 사전으로 읽는다** (`definition_stars`).  25회차는 본문만
   획으로 읽게 고치고 정의줄 쪽을 두어, TC2 는 사전이 비고 이름 없는 `VENDOR` 가
   됐다.  상한은 **그 줄들의 글자 높이**다 (실측 4.20pt) — 정의줄의 별표는 그 줄에
   인쇄된 한 글자이지 그보다 크지 않다.  **획으로 그린 정의줄은 `glyph_size` 를 주지
   않는다** — 그 크기는 정의줄의 것이지 본문의 것이 아니다.
4. **공급자 이름은 활자를 가리지 않는다.**  자리 규칙(`BY <이름>`)은 그대로 두고
   대소문자만 무시한다 — TC2 는 `By SE` 라고 소문자로 인쇄한다.  `SUPPLIED FROM …`
   형태는 **넓히지 않았다** (10회차가 발주처 판단으로 남겼고, 넓히면 AL NOUF1 62행의
   SCOPE 문자열이 바뀐다).
5. **★ `BY SCT` 는 타사 공급이 아니다.**  TC2 p12 의 NOTES 가 그 표시를 그렇게
   정의하는데, 그대로 두면 `VENDOR(SCT)` — *"타사 공급 = 우리"* 라는 모순이 되고
   발주처 양식에서도 빠진다.  별표는 **누가 공급하는가**를 가리키는 것이지 "타사" 를
   뜻하지 않는다.  실측 TC2 2행 · AL NOUF1 0행 · SADARA 0행.
6. **★ 수량 승수가 남의 도면에서 오고 있었다** (`MULTIPLIER_FROM_CONFIG`).
   TC2 에는 `UNIT IDENTIFICATION NUMBERS` 표가 **60장 어디에도 없어**
   `derive_unit_multipliers` 가 config 폴백으로 떨어지는데, 프로필이 정해지지 않은
   문서는 **AL NOUF1 config 로 도는 것이 기본**이라(14회차 [8]) 그 폴백은 곧 남의
   도면 값이다.  유닛코드 31 은 UNDEFINED 로 시끄럽게 비었지만(262행) **`00` 은
   345행이 조용히 x1 을 받고 있었다.**  값은 두되 **출처를 말한다** — 버리면 쓸 수
   있었을 수를 잃고, 말하지 않으면 남의 값을 이 도면의 값으로 읽는다.
   AL NOUF1·SADARA 는 자기 범례에서 표를 읽으므로(`source=LEGEND`) 한 행도 안 바뀐다.
7. **TC2 의 SCOPE 가 실제로 갈렸다**: SCT 555 · VENDOR 52 (이름 0종) →
   **SCT 434 · VENDOR 135 · VENDOR(SE) 31 · VENDOR(CEP SUPPLIER) 5 ·
   VENDOR(SIEMENS) 2**.  지목된 p6 은 15행 전부 SCT → **VENDOR(SE) 8 · SCT 7**.
8. **★ 25회차는 정밀도만 쟀다 — 재현율을 묻는 검사를 넣었다.**  "더한 71개가 전부
   별표인가" 는 접촉 시트로 확인했지만, 접촉 시트는 **찾은 것만** 보여 주므로 못 찾은
   것을 드러낼 수 없다.  새 검사는 반대를 묻고(그 장이 인쇄한 별표 중 몇 개를 냈나),
   후보 채널이 검출기와 **다른 재료**다 — `star_groups` 를 안 부르고 자유 끝점·크기
   학습도 안 보고, 중점이 겹치고 **방향이 셋 이상이며 팔 길이가 같은** 무리만 센다
   (실측: 실제 별표 방향 3~4 · 길이비 1.02~1.06 ↔ 해칭 방향 1~2 또는 길이비 1.17+).
   그리고 **검사 자체를 검사한다** — 25회차 조건을 되살리면 이 검사가 실패한다.
9. **⚠ 25회차의 잉크 인덱스를 장마다 들고 있었다.**  60장이면 60개가 살아 있어
   최대 RSS 가 +3GB 였다.  쓰는 것은 **지금 보는 한 장**뿐이므로 다음 장이 만들 때
   앞 장의 것을 놓는다.  **AL NOUF1 7.85 → 4.9GB · TC2 8.86 → 7.2GB** 로
   25회차 이전 수준으로 돌아왔다.
10. **⚠ 스스로 뒤집은 것 둘**: (가) 캐시 키에서 격자 칸을 빼 "장마다 한 번" 으로
    고쳤더니 칸이 **작은 쪽**(정의줄 4.2)으로 굳어 TC2 가 11.0GB 로 **더 나빠졌다**
    (실측 p52: cell 4.2 → 격자 항목 111k · 11.4 → 74.5k · 100 → 61k).  답은 9번이었다.
    (나) 재현율 검사의 하한을 9 로 잡았다가 **7** 로 내렸다 — 9 는 해칭까지 세던
    느슨한 채널의 값이고, 채널을 좁히니 7 이 남고 **전부 잡힌다**.  검사는 하한이지
    인구조사가 아니다.
11. **남은 것**: TC2 도면번호 **56/60** (`formats.drawing_no` 가 AL NOUF1 에서 외운
    형식이라 6자 코드 p25·p41 을 안 받는다 — 그 두 장은 별표를 찾고도 행이 0이다) ·
    TC2 승수는 여전히 설정 값이고 **도면이 말하지 않으므로 사람에게 물을 자리**다.

**그 다음 회차 — 노트가 말하는 "유닛 몇 개" 를 수량에 쓴다 (27회차)**

전문 `out/round27/1_보고서.md`.  **AL NOUF1 `fb85b039` · 1037 · 1931 과 SADARA 가
불변이고, TC2 만 `536d3baa` → `e909cf7c` · Q'ty 345 → **3047** · 축3 82.0 → **89.2**
(AL NOUF1 대비 94%) · 빈칸 14 → **0**.**

1. **★ 코드가 그 노트를 이미 세고 있었고 쓰지 않았다.**  `parse_notes.py` 머리
   주석이 *"Note text is used for **cross-checking only**. It is counted, never
   interpreted … disagreement is reported rather than acted on"* 라고 적어 두었고,
   그 함수(`note_group_refs`)를 부르는 곳은 그 스파이크 CLI 하나뿐 — **파이프라인은
   한 번도 부르지 않는다.**  §9 3③ 의 **아홉 번째**다.
2. **읽는 순서를 §9 그대로 세웠다** (`pipeline._note_factor`):
   ① 그 문서의 범례 UNIT IDENTIFICATION 표 → ② **그 장의 NOTES** → ③ 프로젝트
   설정 폴백 → ④ 빈칸 + 사유.  **노트가 설정 폴백보다 세다** (설정 폴백은 그
   도면이 한 말이 아니다) · **범례표보다는 약하다** (이미 답이 있는 문서를 흔들지
   않는다).  그래서 AL NOUF1 은 한 칸도 안 움직인다.
3. **문형이 여섯이고 전부 같은 뜻이다** — `IDENTICAL` / `SIMILAR` / `SAME` 를 같게
   본다 (사용자 확인).  **세는 것이지 해석하지 않는다**: 그 문단이 열거한 유닛
   표기의 개수가 곧 배수다.  문장 모양으로 찾지 않는 이유는 여섯이 서로 다르기
   때문이다 (`THIS P&ID IS FOR …` · `… SHALL BE IDENTICAL TO UNIT 3-1 AS SHOWN IN
   THIS P&ID` · `THE P & ID IS TYPICAL FOR …` · `… IS COMMON FOR UNITS NO.3-1 & 3-2`).
4. **도면을 읽는 데 걸린 것 셋** (전부 TC2 실측).  (가) `_notes_lines` 의 **7pt
   버킷**이 줄 간격 9.3~11.2pt 인 문서에서 두 줄을 한 줄로 묶어 낱말을 섞는다
   (`5. UNIT THIS 5-2, P&ID UNIT IS FOR 6-1, …`) → **같은 y** 끼리만 묶는다.
   (나) p10·p12 는 NOTES 를 **두 번 인쇄**한다 → 같은 자리의 같은 낱말은 하나로.
   (다) 꼬리를 놓치면 절반으로 센다 — `UNIT#12,21,22` 에서 목록을 버리면 4가 2가
   되고(옛 스파이크 주석이 경고해 둔 자리), `UNITS NO.3-1` 의 `NO.` 와 낱말 없이
   이어지는 `4-1 & 4-2, 5-1 & 5-2` 도 같다.  **낱말에 걸린 표기가 `3-1` 꼴일 때만**
   같은 꼴을 문단에서 더 줍는다 — 두 자리 맨숫자까지 주우면 `20 DIAMETERS` 가
   유닛이 되고 `2026-08-21` 도 걸린다.
5. **⚠ AL NOUF1 은 노트와 범례가 어긋나는 장이 하나 있다.**  대조 가능한 24장 중
   **23장 일치**(코드 10 → 2 = 2 · 코드 11 → 4 = 4)이고 **p15** 만 노트 4 ↔ 범례 2 다.
   p15 는 13회차가 찾아 둔 *같은 도면번호를 두 장이 쓰는 세 쌍* 중 하나다.
   도면이 두 말을 하는 자리이므로 **범례가 이긴다**.
6. **TC2 결과**: Q'ty ×8 282행 · ×4 10 · ×2 2 · ×1 299(노트 없는 `00` 장) ·
   빈칸 **14행(p24 한 장)**.  검토 사유 `MULTIPLIER_UNDEFINED` 262 → **14**.
   노트에서 온 행은 **검토 사유를 달지 않는다** — 도면이 그 장에 적어 둔 값이다.
   근거 패널은 `1 symbol x 8 (NOTES: 3-1, 3-2, …)` 와 노트 원문을 적는다.
7. **읽은 사실은 지문 밖에 둔다** — `result["unit_notes"]`.  `multipliers` 에 넣으면
   그 칸이 지문 대상이라 AL NOUF1 이 움직인다.
8. **★ 어휘는 config 에 있고 자리는 이미 유도되고 있었다** (요구 추가분: *"NOUF 1,
   TC2 한정이 아니다"*).  NOTES 영역은 `derive_layout` 의 `regions.notes_area`
   ·`notes_text_x_max` 가 **그 도면에서** 잽니다 — 프로젝트 종속으로 남아 있던
   것은 **낱말** 하나였고 `qty_note` 로 냈습니다 (`same_words` · `unit_words` ·
   `range_words`).  기본값은 세 문서가 **실제로 인쇄한** 낱말뿐이고(§2.1 ③),
   넷째 프로젝트가 `BLOCK`·`PHASE` 로 부르면 **코드를 고치지 않고 한 줄** 더합니다.
9. **★ `TYPICAL` 을 더하니 TC2 6장이 더 섰다** — p23·p24·p51·p52·p54·p55.  전부
   `THIS P&ID IS APPLICABLE FOR UNIT 3-1 AND IS **TYPICAL** FOR UNITS 3-2, … 6-2`
   꼴이라 `SIMILAR`·`IDENTICAL`·`SAME` 어느 낱말도 없습니다.  **6번의 "빈칸
   14행(p24)" 이 그 중 한 장이었고 이제 0 입니다** (Q'ty ×8 308 · ×4 94 · ×2 2 ·
   ×1 203).  TC2 지문이 `fbee907d` → `e909cf7c` 로 움직인 원인은 이 여섯 장
   하나이고, AL NOUF1·SADARA 는 한 칸도 안 움직입니다.
10. **★ 범위 표기는 세지 않고 검토로 올린다** (`MULTIPLIER_NOTE_RANGE`).
   `UNIT 3-1 THRU 6-2` 에서 도면이 인쇄한 것은 **두 끝뿐**이고 사이에 무엇이 있는지
   말하지 않습니다 — 두 끝을 세면 `2` 가 되어 **틀린 수가 맞는 얼굴**을 합니다
   (§9 ④).  세 문서에 범위 표기는 **0건**이라 지금은 아무 것도 안 바꾸고, 다음
   문서를 위한 규칙입니다.
11. **⚠ `_notes_lines` 의 7pt 버킷은 수량 축에서만 비켜갔다.**  그 함수는 여전히
   별표 정의줄(`read_mark_dictionary`·`definition_stars`)과 공급자 이름을 읽습니다 —
   줄 간격이 7pt 아래인 문서가 오면 정의줄 둘이 한 줄이 됩니다.  **다음 회차 후보.**

**그 다음 회차 — 글자가 글자가 아닌 도면 (28회차, UAD 합류)**

전문 `out/round28/1_보고서.md`.  **AL NOUF1 `fb85b039` · 1037 · 1931 과 SADARA 가
불변이고, TC2 만 `e909cf7c` → `c0a2d29d` 로 움직였습니다** (29회차 [B] 에서 칸
단위로 규명 — 행은 0칸).

1. **★ UAD 가 "읽을 수 없다" 던 원인은 글자가 도형이라는 것이었습니다.**  실측
   p20 — PDF 텍스트 `Tj` **42개** ↔ 선분 **93,895개**.  AutoCAD 는 TrueType 만
   텍스트로 내보내고 **SHX(획) 글꼴은 플롯**합니다.  그런데 같은 글자가
   `AutoCAD SHX Text` 라는 제목의 **Square 주석**에 원문 그대로 들어 있고
   (이 문서 **10,729개**), 코드는 그것을 한 번도 읽지 않았습니다.
   **§9 3③ 의 열 번째** — 도면이 말해주고 있었고 읽을 길이 있었는데 안 썼습니다.
2. **사람이 남긴 메모는 안 들어옵니다** — 검토 주석은 FreeText 이고 제목이
   사람 이름(`sc.y` · `hyomi.kang`)입니다.  제목이 정확히 `AutoCAD SHX Text` 인
   것만 받습니다 (`pidcache._shx_entries`).
3. **SHX 낱말은 어구 단위입니다 — 쪼개지 않습니다.**  주석 하나가 한 어구를
   담고 사각형도 어구의 것입니다.  쪼개면 글자 자리를 지어내는 것이므로,
   **찾는 쪽이 어구 안을 낱말로 읽습니다** (`pidcache.tokens` 를
   `derive_layout`·`legend_rules`·`pipe_graph`·`isa_table` 이 씁니다).
4. **★ 어구를 낱말로 읽자 범례 머리말이 헐거워졌습니다.**  UAD p3 이
   `FIRST ISSUE` + `OF OTHER LETTER SYMBOLS)` 로 `FIRST LETTER` 를 거짓
   매칭했습니다.  고친 것은 **한 줄에 나란히 있어야 한다**는 조건입니다
   (`legend_rules._line_with` — 같은 줄에서 x 순으로 **연달아**).
5. **열 위치 판정은 낱말의 중심으로 합니다** (`derive_layout._column_lines`).
   어구 사각형은 넓어서 왼쪽 끝으로 재면 엉뚱한 열에 듭니다.
6. 결과 **UAD 149행** (FIELD 146 · PNEUMATIC 3 · 328초 · 3.2GB).

**그 다음 회차 — 증거 등급 (29회차, 네 프로젝트)**

전문 `out/round29/1_보고서.md` · [B] `3_tc2_지문.md` · [D] `4_tier_by_page.md` ·
[E-2] `5_tag_vs_geometry.md` · **[E-3] `8_tag_scope.md`** · 진행
`out/tier_progress.md` · 첨부 `out/tier_result.zip`.
**AL NOUF1 `fb85b039` · 1037 · 1931 · 축1 · 축2 · 축3 95.0 · 양식 885/76/23 ·
Tag 칸 전부 `.....` 가 전부 불변입니다.**

1. **[B] TC2 지문은 행이 아니었습니다** — 607행 중 **지문 대상 열에서 움직인 칸
   0**, `multipliers`·글리프 같음.  다른 것은 `legend.connector_reach`
   32.3 → 32.5 (표본 181 → 180) 하나이고, 원인은 TC2 p10 의 SHX `M` 한 글자가
   `_connector_labels` 의 **5pt y 버킷**에서 x=881 의 `TO CONDENSER` 와 한 줄로
   묶인 것입니다.  **일부러 안 고쳤습니다** — 버킷을 바꾸면 AL NOUF1 의
   커넥터 218/70.2 가 움직입니다.
2. **[D] 등급은 그 도면이 정합니다** (`app/engine/tags.py`).  판정 규칙:
   *코드가 한 검출에만 나오고, 그 모양이 두 검출·두 장 이상에서 되풀이될 때만
   태그로 본다.*  모양은 글자·숫자 달리기(`00GKB01CL001A` → `D2L3D2L2D3L1`)이고
   **KKS 정규식을 코드에 박지 않았습니다.**  규칙은 측정이 세 번 좁혔습니다
   (AL NOUF1 오검 9 → 2 → **0**).  등급: AL NOUF1·SADARA·TC2 **2급** ·
   UAD **1급**(128/149 = 85.9%).
3. **태그는 지문 밖입니다** — `tag_no`·`evidence_tier` 는 해싱 재료가 아닙니다.
   그래서 1급 경로가 켜져도 기준선이 구조적으로 안 움직입니다.
4. **[E-2] 태그가 거리보다 낫다는 것이 숫자로 섰습니다** — 둘 다 답한 26행 중
   **갈린 19행(73.1%)**, **태그만 답한 102행**, 거리만 답한 **0행**.
   갈린 p19 를 렌더로 확인하니 `00GHB00C*` 는 탱크가 아니라 **입구 배관**에
   붙어 있어 **태그가 맞고 거리가 틀렸습니다**.  ⚠ **적용은 아직입니다** —
   UAD 는 발주처 리스트가 없어 채점할 축이 없습니다 (실무 판단).
5. **★ [E-3] 태그로 SCOPE 는 판정할 수 없습니다.**  ㉠ 태그 문자열에 공급자
   자리가 없습니다 (일련번호 `5xx` 가 SCT 27 / VENDOR **46** · 기능코드 `CP`
   가 46 / 54).  ㉡ 계통코드 94% 일치는 **인과가 반대**입니다 — 한 계통의
   계기가 대개 같은 패키지 상자 안에 있어서이고, 갈리는 4계통에서 어느 쪽으로
   맞춰도 8행 중 4행을 틀립니다.  ㉢ **뜻을 정하는 것은 그 장 NOTES** 입니다
   (`SUPPLIED BY PUMP SUPPLIER`).  **별표가 여전히 답입니다.**
   → 등급 순서는 **칸마다 다릅니다**: 이름·순번·귀속은 1급, 공급 주체는 2급.
6. **★ 그런데 태그 감사가 결함을 잡았습니다.**  p23 `00GKB01CL001A`(SCT) ↔
   `…B`(VENDOR) 를 렌더로 열었더니 **두 버블 어디에도 별표가 없었습니다** —
   B 가 주장하는 마크 (215.58, 260.03) 는 버블 사각형 **안쪽**이고 거기 있는
   것은 **SHX 로 그린 태그 글자**입니다.  전수: UAD **2** · TC2 **10** ·
   AL NOUF1 **7** · SADARA 0.
7. **고치는 규칙은 세 후보를 재고 전부 버렸습니다.**  ㉠ 깊이 문턱 — 창 안
   마크 137개의 깊이가 −24.74 ~ +25.90 으로 **끊긴 데 없이** 이어져 빈 띠가
   없습니다.  ㉡ "낱말 위면 글자" — GLYPH 마크 80개 중 **77개**가 걸립니다
   (SHX 어구 사각형이 진짜 별표까지 삼킵니다).  ㉢ "사각형 안이면 글자" —
   **AL NOUF1 7행이 움직입니다.**  렌더로 보니 **AL NOUF1 은 별표를 버블의
   둥근 모서리에 찍고 그 자리는 바깥 사각형 안**입니다.
   **다음 회차 후보: 사각형이 아니라 그려진 윤곽에 대한 점-포함 판정**
   (새 상수가 하나도 없습니다).
8. **★ 태그 낱말 532 ↔ 태그 붙은 행 128 의 차이**: 행이 없는 장 **184**
   (p16·p17·p18 은 **계기 목록 시트**) · 행 있는 장의 서로 다른 태그 275 중
   기기·밸브(`A*`) **79** · 용기(`B*`) 24 · **계기(`C*`) 172** · 나머지 44는
   같은 태그를 버블과 신호선에 두 번 인쇄한 것과 미검출(p25 는 19행 중 4행).
9. **⚠ UAD 개정 0/32** — 축3 여섯 지표 중 개정만 0점입니다 (도면번호는 21/22).
   REV 칸·이력 표가 SHX 라 **글리프 사전이 비고, 비면 이름을 붙일 수
   없습니다** — 24회차 TC2 와 같은 모양이고 그때 +16.7점이었습니다.
   **다음 회차 최우선 후보.**
10. **⚠ 버킷 둘이 남아 있습니다** — `_connector_labels` 5pt(TC2 지문이 움직인
   원인) · `_notes_lines` 7pt(별표 정의줄·공급자 이름).  줄 간격이 그보다
   촘촘한 문서에서 서로 다른 줄을 섞습니다.

**그 다음 회차 — 사각형은 심볼이 아니다 · 개정 표기도 도면에서 읽는다 (30회차)**

전문 `out/round30/1_보고서.md` · [B] `3_scope_윤곽.md` · [C] `4_uad_격차44.md` ·
[D] `5_uad_개정.md` · 전수대조 `6_전수대조.txt` · 진행 `out/round30_progress.md`
· 첨부 `out/round30_result.zip`.
**AL NOUF1 `fb85b039` · 1037 · 1931 · 축1 · 축2 · 양식 885/76/23 · REV 58/58 ·
Tag `.....` 가 전부 불변이고, 네 문서 전수 대조에서 AL NOUF1 · SADARA · TC2 의
움직인 칸이 0 입니다.  UAD 만 `31231bd7` → `db1a77d1` 로 움직였습니다.**

1. **★ [B] 버블 안 글자를 별표로 읽던 것을 고쳤습니다 — 사각형이 아니라
   그려진 윤곽으로 가릅니다** (§10-10).  29회차가 버린 후보 셋(깊이 문턱 ·
   낱말 위 · bbox 안)은 다시 시도하지 않았습니다.  **`find_bubbles` 가 처음부터
   윤곽을 알고 있었습니다** — 버블은 *마주 본 호 캡 둘 + 곧은 옆면* 으로 찾는데
   바깥 사각형만 돌려주느라 캡을 버렸습니다 (**찾고 있는데 안 쓰던 것 —
   열한 번째**).  새 상수 0개이고 반지름을 가정하지 않습니다 (AL NOUF1 캡비
   ≈2.0 ↔ TC2 34×11 ≈3.1).
2. **고치기 전에 네 문서를 전수로 쟀습니다** (`spike/outline_probe.py`):
   윤곽 안(글자) **AL NOUF1 0 · SADARA 0 · TC2 0 · UAD 17**.  즉 움직일 수 있는
   것이 UAD 뿐임을 알고 고쳤습니다.  **윤곽을 못 찾으면 걸지 않습니다**
   (밸브 몸체는 스타디움이 아닙니다 — AL NOUF1 7건).
3. **획에만 겁니다.**  활자 `*`(`MARK_TEXT_RE`)에는 걸지 않습니다 — 그것이
   별표라는 것을 우리가 *압니다*.  획 뭉치는 *추론*입니다.
4. **⚠ 영향 범위 예측이 틀렸습니다 (스스로 뒤집음).**  "UAD 2행" 으로 예상했는데
   **18행**이 움직였습니다.  원인은 얼굴 뭉치가 **크기도 가르치고 있었다**는
   것입니다 — 그래서 `drawn_mark_sizes` 에도 걸어야 했습니다.  18행 전부 렌더로
   확인했습니다: p8·p19 는 별표가 아예 없어 SCT 가 맞고, **p30 은 모서리 별표
   하나라 `**`(뜻 없음) → `*` → `VENDOR(PUMP SUPPLIER)` 로 이름이 생깁니다** —
   **마크를 지우는 규칙이 아니라 바로 세는 규칙**입니다.
5. **버린 것은 셉니다** — `result["face_marks"]` (UAD 9장 34개 · 지문 밖).
   ⚠ 기록 자리를 한 번 옮겼습니다: `find_marks` 에 달았더니 **0개**가 기록됐고
   (크기 학습이 먼저 걸러 그 루프까지 안 옵니다) 전수가 지나는
   `drawn_mark_sizes` 로 옮겼습니다.
6. **★ [D] UAD 개정 0/32 → 23/32 · 축3 71.0 → 83.0.  29회차의 원인 가설이
   틀렸습니다.**  "SHX 라 글리프 사전이 빈다" 가 아니라 **`formats.revision`
   (`^[A-Z][0-9]?$`)이 외워둔 값**이었습니다 — UAD 는 개정을 **숫자**로 매기고
   REV 칸에 활자 `6` 이 **있는데** 그 식에 안 맞아 버려졌습니다.  28회차 SHX
   경로는 이미 들여오고 있었고 자리도 맞았습니다.
7. **개정 표기는 이력 표 REV 열에서 읽습니다** (`derive_layout._history_rev_marks`).
   머리글 행을 섞지 않는 장치는 **날짜**입니다 — 같은 행의 DATE 열이
   `formats.date` 에 맞는 행만 받습니다.  읽지 않은 모양은 더하지 않고, 못 읽으면
   유도하지 않습니다 (AL NOUF1·TC2·SADARA 는 획으로 그려 낱말 0개 → 설정값 유지).
8. **`_rebind_config` 이 타이틀블록 형식 정규식 셋을 다시 만듭니다.**  안 그러면
   유도값을 얹어도 import 때 compile 된 옛 식이 계속 쓰입니다 — 22회차가
   `pidcache` 전역에서 겪은 것과 같은 덫입니다.
9. **⚠ AL NOUF1 축3 95.0 → 95.1 · SADARA 75.3 → 75.4 — 판정이 아니라 분모입니다.**
   두 문서 다 **행 움직인 칸 0 · 지문 동일**이고, 움직인 것은 축3 ⑥(파라미터)
   **28/30 → 29/31** · **26/30 → 27/31** 뿐입니다.  `formats.revision` 이 유도
   항목이 되어 `applied_rules.layout.items` 에 **기록**되기 때문이고,
   **적용되지는 않습니다** (프로필이 그 문서의 것이라 `_fit_layout` 이 안 얹습니다).
   숨기면 그것이 정의 변경이므로 그대로 적고 기준선을 갱신했습니다.
   **실무 판단**: 분모를 "기록된 유도" 로 둘지 "적용된 유도" 로 좁힐지.
10. **★ 유도가 옳다는 것과 적용해도 된다는 것은 다릅니다.**  AL NOUF1 의 유도값은
   `^[A-Z]$` 인데 실제 REV 에는 **`A1` 이 한 장** 있습니다 (이력 표에는 A·B·C·D 만
   인쇄됩니다).  프로필이 그 장을 지켰습니다 — **이력 표는 개정 표기의 하한이지
   전부가 아닙니다.**  프로필 없는 다음 문서에서 부개정을 놓칠 수 있습니다.
11. **[C] UAD 카운팅 격차는 규명만 했습니다 (코드 0줄).**  "44" 는 47−3 이었고
   47 은 넷으로 갈립니다 — A/B 쌍의 **묶음 이름 11**(실패 아님) · **미검출 36**
   (다중신호 `CL101` **21** · 네모 상자 `CF001/002` 8 · `AIT` 2 · 기타 5).
   **★ 미검출의 58%가 한 원인입니다** — 앵커가 **낱말 통째 일치**인데 UAD 는
   경보 수식자를 붙여 인쇄합니다 (`LSA++` · `LS++,+,-,--` · `PIA+,-`).
   증거: 미판정 심볼 62건에 `LSA`·`LS` 가 **0건** — 버블 검증 실패가 아니라
   낱말이 사전에 안 걸린 것입니다.  **다음 회차 1순위.**
12. **반대 방향도 쟀습니다** — 태그가 안 붙은 21행은 **도면이 안 찍어서**입니다.
   p25 는 19행 중 15행의 버블 아래 칸이 비어 있고, 40pt 안의 코드 낱말 5건은
   전부 `EKG4`·`AA605`·`X100%)`·밸브 태그입니다.  **놓친 태그 0.**
   목록 시트 p16·p17·p18(태그 180 · 검출 0)은 분석 대상에서 빠지므로 **맞습니다**.
13. **⚠ UAD 의 사람 몫은 SCOPE 가 아니라 승수입니다** — 14장 전부 · 149행 전부에
   사유가 붙고 원인은 `MULTIPLIER_FROM_CONFIG` 149행입니다 (이 PDF 에 승수표도
   유닛 노트도 없습니다).  [B] 가 줄인 것은 `VENDOR_MARK_UNDEFINED` 30 → 24 뿐.
14. 시험 **241 → 247** (`tests/test_bubble_face.py` 6건).  실 DB sha256
   `643748dc…` 불변.  메모리 AL NOUF1 4.9G · TC2 7.2G (25회차 7.8/8.9 에서
   내려간 원인은 26회차 [9] 이고 이번에 는 것은 없습니다).

**그 다음 회차 — 승수를 사람이 한 번 지정하는 길 (31회차, 3급 경로)**

전문 `out/round31/1_보고서.md` · 조사 `3_기존구조_조사.md` · 대상 행
`4_승수_대상행.md` · 경보 접미 `5_alarm_suffix.md` · 진행 `out/round31_progress.md`.
**네 프로젝트가 전부 불변입니다** (AL NOUF1 `fb85b039`·1037·1931 · SADARA
`0767ba79`·82 · TC2 `c0a2d29d`·607 · UAD `db1a77d1`·149) — **사람 값이 없으면**
움직일 길이 구조적으로 없습니다 (회귀 하네스가 그 인자를 안 넘깁니다).

1. **★ 만든 것은 값이 아니라 묻는 자리입니다.**  승수 값은 하나도 정하지
   않았습니다 — 게이트에서 쓴 `x9`·`x4` 는 자리가 동작하는지 보려고 넣은
   시험값이고 저장소에 남아 있지 않습니다.  답은 사용자 몫입니다 (§5 대기).
2. **★ 열두 번째 — `_note_factor` 는 이미 §9 의 읽는 순서를 구현하고 있었습니다.**
   `①범례 → ②NOTES → ③설정 폴백 → ④빈칸` 중 **③ 자리에 넣을 값이 없었을
   뿐, 넣을 자리는 있었습니다.**  26회차의 `_BORROWED_MULTIPLIER` 도 이미
   *"확인 필요"* 라고 적고 있었고 — **확인해 달라고 적어 두고 확인할 자리를
   안 만든 것**이 지금까지의 상태였습니다.
3. **[B-0] 있는 것 셋을 먼저 재고 전부 버렸습니다.**  `axis_overrides` 는 키가
   **행**이라 SADARA 에서 항목 82개를 써야 하고, 전역 심볼 사전은 범위가 **전
   프로젝트**이며(승수는 그 플랜트의 사실입니다), 범례 프로필은 15회차가
   *범례가 말한 것만* 담기로 못박았습니다.  그래서 **새 파일이되 새 개념은
   없습니다** — 자리는 `axis_overrides` 에서, 규율(작성자 필수 · 읽는 곳 하나 ·
   범례가 이긴다 · `enabled` 스위치)은 전역 심볼 사전에서 반쪽씩 가져왔습니다.
4. **★ 게이트 2 가 이 회차의 핵심입니다** — AL NOUF1 에 유닛코드 셋 전부
   `x9` 를 넣고도 **지문 `fb85b039` · 1037행 · Q'ty 1931 · 움직인 칸 0** 입니다.
   "안 넣었으니 안 움직인다" 는 동어반복이므로 **넣고도 안 움직이는지**를
   쟀습니다.  §9 의 순서가 코드에서 실제로 지켜진다는 뜻입니다.
5. **AL NOUF1 은 승수 대상이 0행입니다** — 범례 p5 가 유닛코드 일곱을 전부
   정의합니다.  대상은 SADARA 82(유닛 `10`) · TC2 203(`00`) · UAD 149(`00`)
   이고, **화면은 유닛코드로 묶어 묻습니다** — 434행이 **세 번의 답**으로
   덮입니다 (행마다 묻는 화면은 자리를 만든 것이 아니라 일을 만든 것입니다).
6. **SADARA 축3 75.4 → 92.0** (로드맵 예측 92.1).  ⚠ **TC2·UAD 는 안 오릅니다**
   — 이미 설정 폴백으로 칸이 *차 있었기* 때문이고, 축3 수량 지표는 "칸이
   찼는가" 만 셉니다.  사람 값은 **틀린 값을 옳은 값으로** 바꾸는데 그 개선을
   이 축은 잴 수 없습니다.  **측정 정의는 바꾸지 않고 그대로 적습니다** (§2.2).
7. **`MULTIPLIER_BY_USER` 는 지웁니다가 아니라 남깁니다.**  "사람이 답했으니
   검토 끝" 으로 지우면 감사에서 사라집니다 — 13회차가 사람이 도면 값을 덮은
   칸을 자동으로 잡게 해 둔 것과 반대가 됩니다.  근거 패널이 누가·언제를 적습니다.
8. **[C] 경보 접미는 측정만 했습니다 — 코드 0줄.**  셋이 나왔습니다:
   (가) 어휘는 **UAD p4 ISA 문자표가 정의합니다**(`+ ++ - --`) — 지어낼 필요가
   없습니다.  (나) **★ AL NOUF1 의 위험 2건은 계기가 아니라 사람이 쓴 검토
   메모였습니다** (p17 `XV-> CV` — 노란 바탕·빨간 테두리).  그래서 이 항목은
   **두 규칙이 한 벌**입니다(접미 제거 + 리뷰 메모 제외).  후자가 없으면 축2 가
   94.4 → **94.1** 로 게이트 불통과입니다.  (다) **(b) 갈래(뒤 `A` 까지 떼기)는
   버립니다** — §5.1 다중 신호 묶음 위반입니다 (UAD p30: 태그 하나에 버블 셋).
9. 시험 **247 → 262** (`tests/test_unit_multipliers.py` 17건).  실 DB sha256
   `643748dc…` 불변.
10. **구조·성능·범용성을 따로 문서로 냈습니다** — `docs/program_overview.md`
   (네 층 구조 · 모듈 줄 수 · 네 프로젝트 시간·메모리 · 축 셋 · **비율·모양은
   축척을 넘고 절대 pt 는 못 넘는다** 는 경계 · 다섯째 프로젝트 절차).

## 4. 미해결 과제 (다음 단계 후보) — 우선순위 순

1. **순번은 "안 붙인 것"이 최대 원인입니다** (오답 203건: 발주처는 붙였는데 우리는
   없음 **77** · 글자가 다름(정렬축·시작점) **57** · 주어가 틀림 37 · 우리만 붙임 32).
   직전 회차의 "주어 79%" 는 주어가 73.4% 로 올라오면서 **18% 로 줄었습니다.**
   가장 손댈 만한 것은 **④ 57건(정렬축·시작점)** 으로, 기하 문제라 근거를 재기 쉽습니다.
   **천장을 재 뒀습니다** — 순번만 발주처 답으로 바꿔치기하는 오라클로
   **F1 74.6 → 77.8 (+3.2) · 완전일치 34 → 46**. 즉 순번 전체가 걸린 값이 3.2점이고,
   57건은 그 일부입니다. 오라클이므로 규칙으로 얼마를 회수할지는 별개입니다.
2. **위치어도 같습니다** (오답 259건: 미부착 **156(60%)** · 낱말 다름 46 · 과부착 30 ·
   주어 틀림 27). 미부착 156은 "과반 있는 칸에만 붙인다"는 **정책의 결과**이므로,
   칸을 넓히려면 과반 기준을 낮춰야 하고 그러면 정밀도가 깎입니다. 낱말이 다른 46건
   중 최다는 `INLET → OUTLET` 11건(좌우 반전)입니다. **천장은 +0.3 입니다**
   (오라클 F1 74.6 → 74.9). 순번과 합쳐도 **78.0 (+3.5)** 이므로, 이 둘을 다 잡아도
   80 은 넘지 않습니다 — 남는 것은 주어 자체입니다.
3. **순위 밀림은 15행뿐이고 뒤집을 근거가 없습니다** (2위 8 · 3위 7). 도면 제목으로
   가르려 했으나 15건 중 13건이 동점이고 1건은 오답 쪽을 가리킵니다(§3). TYPE↔기기
   종류 경향(LS→TANK 91% 등)은 그 시트에 어떤 계기가 있느냐의 반영이라 순환 논리입니다.
4. **위치어 158행 중 120행이 아직 비었거나 틀립니다.** 과반이 나오는 칸은 5개뿐
   (PIT ABOVE · PDIT ABOVE · PDIT LEFT · CCW TI · 펌프 좌우)이고, 특히
   **CCW 의 PI 는 SUPPLY/RETURN 이 정확히 반반**이라 기하로 구분할 수 없습니다.
   배관 라벨(공급관/환수관 번호)에서 유도할 수 있는지가 다음 후보입니다.
5. **순번 42.0%** — 라인 문구를 뺀 행이 162 → 131 로 줄면서 35.7 → 40.9 로 회복했습니다.
   남은 하락분도 과제 1과 같은 원인입니다.
6. **LS 문구 안의 유닛 번호** — 발주처는 `TO HRSG BD TANK` 라고 쓰는데 우리는
   `TO HRSG#11 BD TANK` 라고 씁니다. 문장 머리에 이미 `UNIT #11` 이 있으므로
   중복입니다. 18행. 557행에서 확인 가능.
7. **중간 심볼(SUCTION STRAINER)** — 발주처 13행, 전부 PDIT, 정확도 **0/13**.
   **회수 불가로 확정**했습니다 (§3 참조): 낱말이 인쇄된 도면은 p32 한 장뿐이고 그
   낱말도 시트 반대편의 드레인 주석입니다. 형상 매칭은 실패 기록이 있으므로
   **사용자 입력 대상**입니다. config `description.between_symbol_types: [PDIT]` 로
   PDIT 외에는 아예 제안하지 않게 막아 뒀습니다.
8. **밸브 Description 144행** — 발주처 마스터 밸브 리스트가 없어 문형 기준이 없음.
   확인된 사실: CZH(MOV)·CZI(BFV) 리스트에는 **Description 열 자체가 없고**
   SYSTEM / UNIT NO. 열만 있습니다. 그리고 우리 144행은 전부 **수동 밸브**
   (GLOBE 68 · GATE 38 · BUTTERFLY 23 · BALL 13 · CHECK 2) 로, 두 리스트 어디에도
   없습니다. 즉 **NONE 은 결함이 아니라 정답**입니다.
9. ~~다중 신호 버블 102행~~ — **수량 확정·적용 완료**. 물리 1개로 접어 34행,
   TYPE 은 발주처 리스트가 22행 전부에 쓰는 `LS`, Q'ty 는 1 × 시트 승수.
   **문형만 남았습니다**: 발주처 LS 22행은 **11쌍**이고 한 주어에
   `... LEVEL HIGH HIGH` 와 `... LEVEL HIGH` 를 **따로** 씁니다 — 한 행이 두 신호를
   말하는 문형이 **0행**입니다. 그래서 접힌 행은 첫 신호(`LEVEL HIGH HIGH`)로 두고,
   선택지를 `config multi_signal_bundle.description_signal` 에 세 개
   (`representative` 적용 중 / `all` 0행 / `none` 0행) 로 적어 뒀습니다. 접힌 신호는
   근거 패널과 **산출물 REMARK 열 34행**에 그대로 나갑니다.
10. **UNKNOWN 6종** (`out/valve_project_deps.md`) — Phase 3.
11. **Tag No.** — 도면이 `.....` 로 미부여. 발주처 규칙 필요.
12. **PI — §4.2 는 20회차에 다시 썼습니다** (`out/round20_fp35.md` §3).
    **별표가 갈랐습니다**: 교집합 13장 중 **9장 정확 일치**(109=109), 발주처가
    0 으로 세는 10장 중 **7장을 벤더 별표가 걸러냅니다**(64건 중 63건 VENDOR).
    아래에 적힌 "회수율 30.4%" 와 반례(`10PGB10-M05-0001`)는 **16·18·19회차가
    별표를 고치기 전 값이고 지금은 성립하지 않습니다** — 그 장은 지금 PI 4개가
    전부 VENDOR 입니다.  **남은 것은 `00PAB10` 세 장(p26·p27·p28)의 10건뿐**
    이고, 그 세 장은 해수 취수 설비라 LIT 이 전부
    `VENDOR(SEAWATER INTAKE FACILITY SUPPLIER)` 인데 **PI 에만 별표가 없습니다.**
    19회차 독립 감사가 그 세 장에서 "붙지 않은 글리프 0건" 을 냈으므로 우리가
    놓친 증거는 없고, **발주처 회신 대상**입니다.  아래는 그때의 기록입니다.

    (20회차 이전 기록) 계장 신호선 가설은
    **기각**: 이 문서의 모든 선이 `width=0` · `dashes='[] 0'` 이라 선 속성으로는
    구분되지 않고, 범례가 쓰는 기하(ELECTRIC = 토막 7.2 + 틈 3.6 등)로 국소
    탐색한 결과 파선은 계상되는 PI 24.8% ↔ 계상 안 되는 PI **0%** 로 **방향이
    반대**입니다. 확인된 구조: 갈림은 버블이 아니라 **도면 단위**입니다
    (PI 가 있는 교집합 17장 중 전부 셈 9 · 전혀 안 셈 8 · **부분 0**).
    유일하게 방향이 맞는 축은 **벤더 별표**(계상 PI 0.0% ↔ 미계상 PI 43.8%,
    PIT·TIT·TI·LIT·FIT 는 0~1%)이지만 회수율이 **30.4%**(48 중 21)라 채택하지
    않았습니다. "유닛코드 00 도면에서는 PI·RO 를 안 센다"(PI 7/7 · RO 3/3)는
    **AL NOUF1 도면번호 체계 관찰**이므로 범용 규칙이 아니고, 반례도 있습니다
    (`10PGB10-M05-0001`). SADARA 정답지 미제공으로 범용성은 **미측정**입니다.
13. **밸브 VALVE TYPE 은 채울 수 있습니다** (`out/valve_operation_mode.md`).
    HYDRAULIC→HOV(4/4) · PNEUMATIC→CV(**n=1**) 은 액추에이터로 결정되고,
    갈리는 것은 MOTOR 하나인데 도면이 `INCH` 를 인쇄합니다 — 8회 전부 MOTOR
    밸브 긴변 1배 안이고, **INCH 가 있는 도면 4장 = MOV_I 가 있는 도면 4장,
    반례 0**. TAG NUMBER 와 DESCRIPTION 은 공란이 정답입니다(§4-8 · §4-11).

## 5. 발주처 확인 대기 항목

| 문서 | 내용 |
| --- | --- |
| `out/description_question.md` | 기기 명명 규칙 · 위치 관계어 · UNIT 접두어 · A/B/C 순번 (각 항목이 몇 행을 채우는지 포함) |
| `out/ls_bundle_question.md` | 수량은 답을 받아 적용했고, **접힌 행의 Description 문형** 하나만 남음 |
| 마스터 밸브 리스트 | 밸브 Description 문형 기준 (미수령) |
| `out/valve_operation_mode.md` | PNEUMATIC → MODULATING 의 근거가 발주처 **1행**뿐입니다. 우리 PNEUMATIC 탭 46행을 그 1행으로 판정해도 되는지 |
| `out/pi_discriminator.md` | PI 를 어느 도면에서 세고 어느 도면에서 안 세는지의 기준. 도면에서 유도되지 않았습니다 |
| `config` `valves.legend_fallback` (15회차) | 이 세 항목의 기본값이 **범례 값과 같은 숫자**라, 범례 p3 가 빠진 개정본에서 값으로는 아무 것도 달라지지 않고 `source` 로만 잡힙니다. 비워서 "범례가 없으면 시끄럽게 멈추게" 할지, 지금처럼 조용히 같은 값으로 갈지는 **실무 판단**입니다 (프로필이 있는 프로젝트에서는 어느 쪽이든 프로필이 이깁니다) |

## 5-1. 마감 현황 — 824행이 사람에게 무엇을 요구하나

| 상태 | 행 | 무엇을 하나 |
| --- | ---: | --- |
| 확인만 하면 되는 행 | **451** | 문장이 있고 사유도 없음. 눈으로 넘김 |
| 사유 확인 필요 (문장은 있음) | **159** | CCW 방향 104(7장) · 중간 심볼 33(10장) · 스코프 24(1장) · 벤더 마크 14(4장) |
| 공란이 정답 | **144** | 수동 밸브 144(발주처 리스트에 Description 열 자체가 없음). 타사 공급 2행은 문장을 갖게 됐습니다 — §3 |
| 중간 서술 입력 필요 | **58** | PARTIAL. 화면에서 **25묶음** |
| AI 제안 확인 필요 | **12** | LOW |

**사람이 실제로 누르는 횟수**: 중간 서술 **25회**(묶음당 1회 일괄 적용, 실측으로
6행짜리 묶음이 한 번에 채워지는 것을 확인) + 사유 축 처리 **최대 22회**
(CCW 7장 · 중간 심볼 9묶음 · 스코프 2묶음 · 벤더 4묶음, 축별 일괄 처리 기준).
공란이 정답인 144행과 확인만 하는 451행은 입력이 없습니다.

**남은 개선 여지의 천장** (오라클 실측): 순번 **+3.2** · 위치어 INLET/OUTLET
**+0.3** · 둘 다 **+3.5 (F1 78.0 · 완전일치 46)**. 그 위는 주어 자체이고,
주어는 기하로 더 짜낼 것이 없다는 측정이 §3 에 쌓여 있습니다.

## 6. 파일과 실행

**`data/` 규칙** (리포지터리에 없음 — `.gitignore` 대상)

| 파일 | 용도 |
| --- | --- |
| `data/pid_total.pdf` | 분석 대상 도면 |
| `data/CZE_Field_Instrument.xlsx` | FIELD 양식 + 검증 모드 대조본 |
| `data/CZI_Butterfly_Valve.xlsx` | BFV 양식 |
| `data/CZH_MOV_Gate_Globe.xlsx` | MOV 양식 |

양식은 **비어 있어도 됩니다** — 서식만 씁니다. 없으면 그 산출물은 사유와 함께
건너뜁니다.

**실행**

```bash
pip install -r requirements.txt
./run.sh                 # Linux/macOS — http://127.0.0.1:8000
start.bat                # Windows (포트/--no-reload/--verify 인자 지원)
PID_VERIFY_EXCEL=data/CZE_Field_Instrument.xlsx ./run.sh   # 검증 모드
```

**배포용 exe** (Windows)

```bat
build.bat                REM dist\PID_Extract.exe 하나. 빌드일이 함께 새겨집니다
build.bat --clean        REM PyInstaller 캐시부터 지우고
```

`build.bat` 은 의존성 확인 → `app/_build.json` 에 버전·빌드일 기록 → 빠른 테스트
→ PyInstaller 순서로 돌고, **테스트가 깨지면 exe 를 만들지 않습니다.** exe 에는
`app/static/` 과 `config/` 전부와 PyMuPDF·numpy 바이너리가 들어가고 **발주처 자료
(`data/`)는 들어가지 않습니다** (`pid_extract.spec` 에 근거를 적어 뒀습니다).

더블클릭하면 빈 포트를 골라 서버를 띄우고 브라우저를 엽니다. 분석 결과는 **exe 옆의
`pid_data/` 폴더**에 남아 다시 켜면 그대로 열리고, 오류가 나면 창이 즉시 닫히지 않고
사유와 트레이스를 보여 준 뒤 Enter 를 기다립니다. `PID_PROJECT_CONFIG` 없이도
프로필 자동 맞춤이 동작합니다 (`app/paths.py` 가 번들 안·바깥을 가릅니다).

**테스트**

```bash
pytest -q -m "not slow and not ui"    # 262건, 12초
pytest -q -m ui                       # 24건 (playwright + chromium 필요 · **SCOPE 있는 데이터**에서만 뜻이 있습니다 — `PID_UI_DB` 로 가리키고, 없으면 `spike/build_ui_db.py` 로 세웁니다)
pytest -q -m slow                     # 16건 · 전 문서 분석 포함, **46분** (25회차 잉크 인덱스로 23분 → 46분 · `timeout 2400` 이면 13/14 에서 잘린다)
```

**회귀 스크립트**

```bash
python app/engine/detect_all.py --pdf data/pid_total.pdf --compare data/CZE_Field_Instrument.xlsx
python app/engine/detect_valves.py --report out/valve/report.md
python3 spike/regression_3p.py            # 세 프로젝트 회귀 (약 17분) — 23회차
python3 spike/regression_3p.py --reuse    # 이미 있는 결과로 채점만 (2초)
```

## 7. 코드 지도

| 모듈 | 역할 |
| --- | --- |
| `app/engine/pidcache.py` | PDF 페이지 캐시. `segments()` 는 캐시 객체를 그대로 넘기므로 **제자리 수정 금지** |
| `app/engine/legend_rules.py` | 범례에서 치수 유도 (`Derived`: values/source/note/evidence) |
| `app/engine/detect_symbols.py` · `detect_all.py` | 계기 검출·스코프. **여기 손대면 회귀 필수** |
| `app/engine/detect_valves.py` | 밸브 몸체·액추에이터 |
| `app/engine/isa_table.py` | 범례 p3 ISA 문자표 런타임 파싱 |
| `app/engine/describe_equipment.py` | 기기 표 유도 · 도면 기기 라벨 검출 · 순번 |
| `app/engine/describe_candidates.py` | 두 축 후보 + 국소 연결 주어 판정 |
| `app/engine/describe.py` | 문형 조립 (config `description`) |
| `app/engine/describe_llm.py` | 중간 서술 선택기 (기본 꺼짐, 6개 제약 코드 강제) |
| `app/engine/describe_axis.py` | 5회차 판정축 — 탭 선정(`pick_tap`)·표준 끊김 다리(`standard_break`)·판정(`judge_row`)·문형(`sentence`). §2.3 경계는 tests/test_describe_axis.py 가 소스 검사로 강제 |
| `app/engine/describe_axis.py` 의 `nearest_conn` | 10회차 — 라인 전후단 최근접 커넥터. **축 직교 방향만** conn_reach 로 막고 축 방향은 막지 않는다 (런이 조각이라). 순회 아님 — 새 런을 찾는 코드가 함수에 없다 |
| `app/pipeline.py` 의 `type_display` | TYPE 표기(`MOV(GLOBE)`). **출력 전용 함수**이고 저장 필드가 아니다 — §8 의 대조 단위가 `values["type"]` 이기 때문 |
| `app/pipeline.py` 의 `_scope_of`·`_supplier_name` | SCOPE 열 네 규칙. 공급자 이름은 그 장 NOTES 원문에서 런타임에 읽고 코드·config 에 이름을 두지 않는다 |
| `config` `scope.exclusion_rules` | 벤더 마크를 **행 삭제**에 쓸지 **표기**에 쓸지. `none`(10회차) / `glyph+text+box`(9회차까지). 정밀도 13.6pp 가 이 한 줄에 걸려 있다 |
| `app/engine/describe_axis.py` 의 `_mid_branches` | 탭한 런의 몸통에 붙는 T 가지를 **한 번** 훑는 평평한 루프. 깊이 1 보장 — 가지에서 `read_end` 만 부르고 런으로 넘어가지 않는다 |
| `app/pipeline.py` `_apply_axis` 의 `via_mid` | 중간 접합에서 새로 읽은 이름은 축과 무관하게 8회차 기준(현행 문장 있으면 보류)을 받는다 |
| `config` `description.axis_mode` | 판정축 적용 폭 — `mixed`(AL NOUF1) / 비움(기록만). 근거 수치는 config 주석 |
| `pipeline` `_axis_pass`·`_apply_axis` | 판정 기록(모든 행) → 혼합 적용(성립 행만). 현행 문장은 `evidence["axis"]["old_description"]` 에 보존 |
| `spike/desc_metrics.py` | 식별 가능성(주)·토큰 F1(참고) 채점기 — 원본 소실로 재구현, 같은 채점기로 현행·신규를 재고 차이만 읽는다 |
| `spike/label_workbook.py` | 라벨링 워크북 `out/label_nouf1.xlsx` (추출 824 · 누락 FN · 집계, "문형 출처" 열). 결정적. 발주처 문장이 들어가므로 .gitignore |
| `app/engine/pipe_graph.py` | 배관 그래프 — **Description 경로에서는 쓰지 않습니다** |
| `app/pipeline.py` | 오케스트레이션 + 등급/Remark |
| `app/db.py` · `app/main.py` · `app/static/` | 저장·API·검토 UI |
| `config/project_alnouf1.yaml` 의 `review_axes` | 검토 사유 → 축(스코프/수량/Description/검출/자료) 배정. 미등록 코드는 `OTHER` 로 보이며 숨기지 않음 |
| `app/main.py` 의 `review_codes`·`REVIEW_LABELS` | 행별 검토 사유 코드와 한 줄 설명. `/jobs/{id}/review` 가 축별로 집계 |
| `review_state` 테이블 | 미처리/확인함/수정함/보류. 없으면 미처리 — 한 일만 저장 |
| `app/engine/describe_equipment.py` 의 `derive_aliases`·`apply_alias` | ①표준 세트 + ②표기 선택 → 라벨 별칭. 머리 위치일 때만 적용 |
| `config/plant_standard_abbr.yaml` | **①업계 표준 약어 — 프로젝트 무관, 그대로 재사용.** 18세트 확정. 방향은 여기 없음 |
| `app/engine/describe_equipment.py` 의 `DRAWING_NO`·`BORE`·`NOTE_REF`·`GRID_CELL` | 라벨에서 떼는 도면 주석. 도면번호 패턴은 `formats.drawing_no` 를 앵커만 떼어 재사용 (정의는 한 곳) |
| `app/static/app.js` 의 `sameSentence` | `일괄 적용` 의 사정거리 — 도면·TYPE·**지금 같은 문장**. 누르기 전에 몇 행인지 화면에 씁니다 |
| `app/static/app.js` 의 `cellValue`·`FILTER_COLS` | 그리드·정렬·검색·컬럼 필터가 셀을 읽는 **단 하나의 접근자**. `pid_no` 는 행이 아니라 페이지에 있습니다 |
| `app/static/app.js` 의 `zoomBy`·`ZOOM_STEP` | 커서 기준 확대. 배율 1.3 은 `+`/`-` 버튼 값 재사용이고 상·하한은 없습니다 |
| `config` `multi_signal_bundle.description_signal` | 접힌 LS 행의 문형 (`representative` / `all` / `none`). 발주처 표기는 `representative` 뿐 |
| `app/axis_overrides.py` | ④ 행 FROM/TO 확정의 저장·문형·승계. 키는 안정 ID · 정렬 고정 · 같은 값 재저장은 같은 파일 |
| `app/main.py` 의 `axis_candidates`·`confirm_axis`·`axis_override_map` | 후보 세 층(도면에서 읽은 텍스트만) · ② 문형 생성 + user_values 저장 · 행별 확정/승계 지도 |
| `app/main.py` 의 `_run_comparison` 승계 훅 | Rev.B 에서 같은 안정 ID 매칭 행에 확정 문장 승계. 사람이 고친 행은 덮지 않음 |
| `app/static/app.js` 의 `fromToPicker`·`bindFromToPicker` | ④ 행 근거 패널의 지정 UI. 같은 런 ④ 행에는 제안만 — 행마다 사람이 누른다 |
| `app/main.py` 의 `_axis_text_noise` · `texts_filtered` | 후보 잡음 필터(§3 제거 패턴 재사용)와 **탈출구**. 거른 것은 버리지 않고 "전체 후보 보기" 로 펼친다 — 정답이 주석 덩어리에 섞여 인쇄되기 때문 |
| `spike/candidate_workbook.py` | 후보 병렬 워크북 `out/candidates_nouf1.xlsx` (10회차). 축을 고르지 않고 나란히 놓는다. 조건 열의 한계값은 전부 그 문서에서 유도 |
| `spike/read_candidates.py` | 워크북의 '선택 후보'를 6회차 `axis_overrides` 로 적는다. '작성 예시' 행은 건너뛰고, 안정 ID 가 안 물리면 적지 않고 센다 |
| `docs/new_project_checklist.md` | 새 프로젝트에서 무엇이 따라오고 무엇을 다시 재는지 (필수 3 · 발주처 리스트 필요 12 · 선택 2) |
| `config/project_alnouf1.yaml` | ②표기 선택 + ③프로젝트 고유값 전부. 각 항목에 근거 수치 주석 |
| `app/static/index.html` 의 `.intake` | 첫 화면 두 단. 왼쪽(프로젝트·비교 대상)이 정해져야 오른쪽(드롭)이 열립니다. 900px 아래에서는 세로로 쌓이고 왼쪽이 위 |
| `app/static/app.js` 의 `setSetupDone`·`chooseProject`·`revSummary` | 잠금 규칙과 늘 보이는 한 줄 요약(`Rev.C vs Rev.A`). Rev.A 는 비교 상자를 숨기고, Rev.B 는 Rev.A 로 고정합니다 |
| `pipeline.analyse` 의 `say(..., sheets=)` · `db.set_progress(sheets=)` | 진행 장수. 분모는 **분석 대상 도면 수**(이 문서 52)이지 쪽수(58)가 아니고, 대상이 확정된 순간 한 번 실린 뒤 바뀌지 않습니다 |
| `app/main.py` 의 `_count_pages` | 업로드 직후 PDF 에서 센 쪽수. 58장 19MB 에서 **2.1~2.9ms**. 0 은 "0쪽"이 아니라 "못 셌다" |
| `app/static/app.js` 의 `STAGE_KO`·`stageWords` | 파이프라인 단계 이름 → 화면 말. **모르는 값은 그대로 씁니다.** 예전에는 `starting` 이 영어 그대로 나갔고 80초 표본 중 79번이 그것이었습니다 |
| `app/static/styles.css` 의 `rect.revring` | 개정은 검출 상자 **바깥 링**으로 그립니다. 예전처럼 상자의 굵기·파선을 덮어쓰면 추가된 밸브가 밸브의 파선을 잃습니다 |
| `app/static/app.js` 의 `GRADE_MARK` · `.gradge` | 등급 배지 — 모양·글자·색 셋이 같이 실립니다. 색만으로 구분하지 않습니다 |
| `app/static/img/*.svg` | 삽화 12개(18.1KB). **전부 CSS 배경**이라 DOM 에 없고 스크린리더가 읽지 않으며 클릭 대상도 아닙니다. 없으면 레이아웃 그대로 크림 단색으로 떨어집니다 (실측 확인) |
| `app/static/styles.css` 의 `:root` | 크림 토큰. `--ink` 는 **선 전용**이고 글자는 `--ink-strong`/`--ink-soft`. 어두운 판의 개정 3색은 크림 위에서 전부 미달(2.01/2.15/2.31)이라 다시 골랐습니다 |
| `app/static/index.html` 의 `header.head` | 머리줄 두 층 — 위는 지표(신원·탭·배지), 아래는 동작. 층을 합치면 15행이 13행으로 줄어듭니다 |
| `app/static/app.js` 의 `auditProblems` | 위생 통계에서 0 인 항목을 접습니다. 전부 0 이면 "이상 없음" 한 줄, 아니면 0 아닌 것만 펼칩니다 |
| `app/engine/describe_llm.py` 머리 주석 | **꺼져 있는 것이 아니라 연결돼 있지 않습니다** — `select()` 를 부르는 곳이 테스트 말고 없습니다. config 를 켜도 호출되지 않습니다 |
| `docs/ui_visual.md` | 이번 회차의 실측 대비표 · 세 색 층 규칙 · 진행 표시의 근거 · 판단 대기 후보 3건 |
| `app/paths.py` | 읽는 뿌리(번들 안, 종료 시 삭제)와 쓰는 뿌리(exe 옆 `pid_data/`)를 가릅니다. 섞으면 결과가 사라집니다. `PID_DATA_DIR` 로 쓰는 뿌리를 덮어쓸 수 있고, UI 테스트가 그것으로 사본에서 돕니다 |
| `app/main.py` 의 `_failure_reason` | 분석 실패 때 **화면에 갈 문장**. 예외를 보지 않고 PDF 를 다시 열어 쪽 수와 글자 수만 셉니다 — 예외를 바꿔 쓴 문장은 예외이기 때문입니다. 특정 못 하면 일반 문구 |
| `job.error_detail` 열 · 진단 zip 의 `error.txt` | 예외 원문이 사는 곳. 화면에도 `/jobs` 응답에도 나가지 않습니다 (`_job_public`) |
| `app/static/app.js` 의 `showFailure`·`toFirstScreen` | 실패 화면의 사유·다음 행동·첫 화면 버튼·이전 분석 목록. 이것이 없으면 `#drop` 도 `#main` 도 숨겨져 나갈 길이 URL 편집뿐입니다 |
| `spike/accuracy.py` · `spike/client_lists.py` | 정확도 회귀를 다시 재는 절차. 발주처 자료가 있는 기계에서만 돕니다 |
| `app/revisions.py` | 프로젝트 저장소 · 안정 ID(`10LBA10-001`) · 리비전 대조. **ID 는 Rev.A 에서 한 번만 부여하고 재정렬하지 않습니다** — 좌표순 재부여는 1건 추가를 "1건 추가 + 40건 수정" 으로 만듭니다 |
| `app/revisions.py` 의 `match_radius` | 매칭 반경. **그 도면 자신의 버블 긴변 중앙값**이고 코드에 상수가 없습니다 (실측 54.4/68.0/85.0/85.1pt). 못 재면 같은 TYPE 최근접의 절반 |
| `app/revisions.py` 의 `compare` | 상태 판정. 자리 이동만으로는 '수정' 이 아니고 `moved_pt` 로 기록만 합니다 — 임계값에 근거가 없기 때문. 삭제는 `DELETED_CANDIDATE` 까지이고 사람이 확정해야 굳습니다 |
| `app/audit.py` | 데이터 디렉터리를 **세기만** 합니다. 삭제 코드가 없습니다 (`docs/db_hygiene.md` 의 (가)만 승인됨). 기동 시 로그 · 진단 MANIFEST · `GET /audit` |
| `revision_state` · `deleted_candidate` 테이블 | 대조 결과와 삭제 후보. `job.project`/`revision`/`compared_with` 와 함께 `_ADDED_COLUMNS` 로 가산 마이그레이션 |
| `docs/revision_provision.md` | 확정 사항 + 이번 회차 구현 + **미구현 목록** + 확인 대기 3건 |
| `docs/db_hygiene.md` | DB 위생 방침 후보와 실측 (고아 출력 43 · DB 168MB 중 91%가 죽은 페이지) |
| `app/version.py` · `app/_build.json` | 버전은 손으로, 빌드일은 `build.bat` 이 새깁니다. 커밋하지 않습니다 |
| `app/desktop.py` | exe 진입점. 빈 포트 선택 · 브라우저 열기 · **오류 시 창을 붙잡고 사유 출력** |
| `pid_extract.spec` · `build.bat` | 무엇을 넣고 무엇을 뺐는지가 주석에 있습니다 (`data/` 는 절대 안 들어감) |
| `app/pipeline.py` 의 `_layers` 색 갈래 | 오버레이 색을 정하는 **단 하나의 곳**. SCOPE 열과 같은 값(`r.scope`)을 읽는다 — 색과 열이 서로 다른 값을 읽으면 언젠가 갈린다 |
| `app/static/app.js` 의 `SCOPE` 배열 | 범례 네 갈래의 라벨·색. **라벨과 세는 대상이 같아야 한다** (11회차 캡처가 어긋난 것을 잡았다) |
| `app/static/app.js` 의 근거 패널 세 줄 | 공급 주체 / 추출 결과 / 발주처 양식. 11회차부터 "추출됨"과 "양식에 나감"은 서로 다른 사실이라 한 문장으로 못 쓴다 |
| `app/static/styles.css` 의 `td.col-scope` | SCOPE 열만 폭 330px · 12px · 자간 −0.01em. **행 높이는 안 건드린다** — §7.2 밀도(29.0px / 15행) |
| `pipeline.analyse` 의 캐시 예열 루프 | `layout` 층 안에서 장마다 `pc.segments()` 를 한 번 부른다. 없으면 진행 신호가 없는 구간이 95.8초 + 129.0초 생기고, 서로 다른 시점의 캡처가 **바이트까지 같아진다** |
| `app/main.py` 의 `Cancelled`·`_cancel_requested`·`POST /jobs/{id}/cancel` | 취소. 행을 쓰는 곳은 `db.store_result` 하나, 안정 ID 를 쓰는 곳은 `_run_comparison` 하나이고 둘 다 `analyse` 뒤라 **분석 중 취소는 아무것도 쓰지 않는다** |
| `app/main.py` 의 `_scope_summary` | 완료 화면 숫자(추출 / 양식에 나감 / 빠짐)의 출처. 화면과 Excel 이 같은 판정(`excel_out.in_client_scope`)을 읽는다 |
| `app/static/app.js` 의 `_pan`·`PAN_SLOP` | 드래그 팬. `#stage` 의 스크롤만 옮기고 SVG 좌표는 안 건드리므로 확대·선택·오버레이가 그대로다. 4px 이하 이동은 클릭으로 넘긴다 |
| `app/main.py` 의 `GET /home` | 첫 화면이 읽는 것 **하나**. 프로젝트 → 리비전, 그리고 프로젝트에 안 묶인 분석. 단위가 job 이 아니라 프로젝트다 (13회차) |
| `app/main.py` 의 `_carry_edits` | 이전 리비전의 편집을 잇는다. 짝은 **안정 ID** — §7.3 의 ID 를 쓰는 것이지 새로 매기지 않는다. 추출값 불변 · 이미 고친 칸 안 덮음 · 갈리면 conflict |
| `app/db.py` 의 `carry_user_values`·`flag_carry_conflicts` | 승계의 두 반쪽. 빈 자리만 채우고, 엔진 답이 갈린 칸은 값을 고르지 않고 둘 다 들고 검토로 올린다 |
| `app/revisions.py` 의 `document_revision` | 장별 Rev 를 문서 하나로 접는다. **규칙은 도면에서 유도되지 않아** config `revision.document_rule` 에 있고 세 후보의 실측 답이 주석에 있다. 접은 값과 분포를 **함께** 돌려준다 |
| `app/revisions.py` 의 `compare_sheet_revisions` | 장 단위 대조. **한 도면번호에 장이 여럿이면 판정하지 않는다**(`ambiguous`) — 이 문서에 그런 도면번호가 3개 있고, 고르면 같은 PDF 가 개정으로 읽힌다 |
| `pid_page.rev` · `job.doc_rev` | 도면이 스스로 말한 개정. `extract_titleblocks` 가 처음부터 읽고 있었는데 저장하는 곳이 없었다. **판정에 안 쓰이고 지문에도 안 들어간다** |
| `feedback.author` · `db.row_edit_history` | 누가 · 언제 · 무엇에서 무엇으로. 자기신고이고 화면이 "자칭"이라고 적는다. 승계된 편집은 원래 이름을 잇고 사유에 `Rev.A 에서 이어받음` |
| `app/static/app.js` 의 `askAuthor` | 편집마다 뜨는 이름 확인 줄. **확인은 매번, 타자는 한 번** — 지난 이름이 채워져 있고 Enter 로 넘긴다. 비우면 비운 채로 저장된다 |
| `app/static/app.js` 의 `mark(field, value)` | 근거 패널이 편집값과 엔진 근거를 **구분해서** 말한다 — `수량 9 ✎ 사람이 고침 (도면 근거는 2)`. 값을 바꾸지 않고 출처만 붙인다 |
| `docs/round13_persistence.md` · `out/round13_survey.md` | 13회차 측정 전문 · 구현 전 실태 조사 7문항 |
| `docs/round12_ui.md` · `docs/round12_annotation_design.md` | 12회차 측정 전문 · 주석 저장 설계안(미구현) |
| `app/static/app.js` 의 `scopeFacts` | SCOPE 한 값이 무엇을 뜻하는지 — **화면에서 이 판정을 하는 곳은 여기 하나**. 근거 패널 · 저장 전 안내 · 저장 후 안내가 같이 읽는다. 서버 쪽 한 벌은 `excel_out.in_client_scope` |
| `app/static/app.js` 의 `editNotice`·`noticeAfterEdit` | 편집이 발주처 양식에 어떻게 닿는지 한 줄. **막지 않고 알리기만** 한다. 한 번에 한 줄만 둔다 — 앞 행의 안내가 남으면 반대 문장이 동시에 보인다 (14회차 캡처) |
| `app/main.py` 의 `scope_summary_now` | 지금 양식에 나가는 행 수 (편집 반영). 완료 화면이 쓰는 `_scope_summary` 를 그대로 부르고 삭제 행은 뺀다 — `write_all` 이 그 행을 먼저 거르므로 |
| `tests/test_ui_edits.py` 의 `field_rows`·`test_step0_...scope_aware_data` | UI 스위트가 **어떤 데이터에서 도는지**를 시험이 먼저 말한다. 행은 순서가 아니라 SCOPE 상태로 고른다. 전제가 깨지면 **건너뛰지 않고 실패**한다 (건너뛰기가 세 회차 동안 침묵을 만들었다) |
| `config/project_sadara.yaml` | SADARA 교차검증의 기준선(82행)은 이 파일을 **안 쓴** 값이다 — 프로필 자동 맞춤은 없고 미지정이면 alnouf1 이다. 지정하면 75행 |
| `legend_profile.read_pages`·`uncompared` | "몇 장을 읽었나" 는 **읽은 쪽**이 답한다 (`page_kind` 로 세지 않는다). 못 읽은 항목은 `diff` 에서 빼고 **"모른다"** 로 말한다 — "같다" 로 세면 개정본마다 거짓 경보가 뜨고 진짜 변경이 묻힌다 |
| `app/legend_profile.py` | 15회차 — 프로젝트 범례 프로필의 자리·담기·되쓰기·대조. **범례 장에서 나온 여덟 항목만** 담는다. 엔진 모듈은 `_engine()` 으로 **맨 이름**으로 가져온다 (`app.engine.x` 로 가져오면 `Derived` 가 두 클래스가 된다) |
| `legend_profile.capture` 의 JSON 정규화 | 저장한 것과 다시 읽은 것이 같아야 한다. JSON 은 튜플을 배열로, dict 키를 문자열로 바꾸므로(`dash_histogram` 7.2 → "7.2") 그 변환을 담을 때 한 번 겪게 한다 |
| `pipeline.analyse` 의 `measure_legend` | 재는가 안 재는가. **프로필이 없으면 반드시 잰다** · 프로필이 있어도 범례 장이 있으면 **대조를 위해** 잰다(적용은 프로필) · 둘 다 없을 때만 안 잰다 |
| `pipeline.LegendUnavailable` | 범례도 프로필도 없을 때. **예외를 바꿔 쓴 문장이 아니라** 파이프라인이 직접 검사한 조건이고 문장도 거기서 쓴다 — `_failure_reason` 은 *알 수 없는* 실패의 자리다. 검사는 `derive_connector_reach` **앞**에 서야 한다 (뒤에 세우면 `KeyError` 가 먼저 난다) |
| `detect_valves.derive_layout(..., derived=)` | 15회차에 는 인자 하나. 프로필에서 되살린 유도 결과를 **그대로 받는** 자리이고, 측정 코드는 한 줄도 바뀌지 않았다 — `kw` 조립은 두 경우 모두 같은 코드가 한다 |
| `app/main.py` 의 `_legend_facts` | 이 결과가 어느 범례로 나왔나 — **화면에서 이 판정을 하는 곳은 여기 하나**. 근거 띠·완료 화면·갱신 선택이 같이 읽는다. 옛 분석은 `mode: unknown` 으로 말한다 (없는 것을 "유도했음" 으로 읽지 않는다) |
| `app/main.py` 의 `adopt_legend_profile` | 범례가 달라진 곳을 **항목 단위로** 프로필에 반영. 사람이 눌렀을 때만 돌고, 안 고른 항목은 옛 값 그대로 남는다. 이번 분석 결과는 바뀌지 않는다 — 다시 분석해야 반영된다 |
| `app/static/app.js` 의 `loadLegendProfile`·`legendChangeBlock` | 결과 화면 머리 아래 띠. 서버가 쓴 문장을 놓기만 하고 판정을 다시 하지 않는다. 달라진 칸은 **옛값 → 새값**으로 그대로 보이고 **자동 갱신은 없다** |
| `config` `valves.legend_fallback` | `butterfly`·`actuator_stem`·`pneumatic` **셋뿐이고 `line_styles` 가 없다.** 그래서 범례 없는 PDF 가 죽었다 (15회차 [B]4). 셋의 숫자가 범례 값과 **같아서** p3 가 빠져도 값으로는 안 잡히고 `source` 로만 잡힌다 |
| `detect_symbols._is_ink` · `_INK` | 잉크와 종이 둘뿐 — 그 밖의 색으로 그려진 것은 도면의 표시가 아니라 위에 덧그린 것이다 (개정 클라우드·개정 삼각형).  `detect_all._INK` 과 같은 값 |
| `detect_symbols._is_stroke_glyph` | 별표는 **칠 없는 곧은 선**.  근거는 범례 NOTES 정의줄의 별표 **38/38** 이고, 걸러지는 것은 밸브의 검은 점(곡선+칠).  크기만으로는 둘이 구분되지 않는다 |
| `detect_symbols._x_gap` · `read_vendor_mark(others=)` | 한 마크는 **더 가까운 버블 하나**에만 붙는다.  창(`mark_x_slack` 6.0)을 좁히지 않는다 — 좁히면 자기 별표를 잃는 버블이 생긴다 |
| `config` `broken_line.brk_max_mark` | 체인 파선의 **끝 마크** 상한.  이 문서의 한 주기 28.32 에서 왔고(점 3.54 · 틈 3.54 · 긴 마크 17.7), 기본 20.0 으로는 패키지 상자가 41개만 잡힌다(28.3 이면 78개) |
| `detect_symbols.package_box_marks` · `read_vendor_mark` 의 `PACKAGE_BOX` 갈래 | Block 규칙 — 상자 테두리의 별표가 **안 전체**에 걸린다.  **버블 자기 별표가 상자보다 우선**한다(`if not stars:` 뒤에서만 상자를 본다).  중첩이면 **첫 상자에서 멈춘다**(합산 안 함) |
| `conftest.py` (저장소 뿌리) | **시험은 실 데이터에 닿지 않는다** (17회차).  `PID_DATA_DIR` 을 버릴 폴더로 돌리고, 그래도 `app/_data` 밑을 열면 **예외**.  읽기(`read_bytes`)는 막지 않는다 — 막아야 하는 것은 `connect` 다.  세션 끝에서 실 DB 의 sha256 을 대조해 **막았다는 것을 증명**한다 |
| `app/pipeline.py` 의 `_disambiguate_duplicates` | 한 도면 안에서 글자 그대로 같은 문장에 A·B·C…  묶는 단위는 **도면**이고 정렬은 위→아래 · 좌→우.  **좌표는 붙이지 않는다** — 테두리 그리드가 텍스트가 아니고 글리프 사전에 E~H·2~8 이 없다.  붙인 글자는 도면에 없으므로 `description_sources` 에 `SUFFIX:` 사유를 남긴다 |
| `app/pipeline.py` 의 `_position_from_connectors` | 커넥터 **짝**이 말하는 SUPPLY / RETURN.  짝이 아니면 안 고르고, **갈리지 않아도 안 고른다** — 둘의 거리 차가 `connector_reach`(런타임 70.2pt)보다 작으면 도면이 구분한 것이 아니다.  보류는 지우지 않고 `connector_position_withheld` 에 기록 |
| `app/pipeline.py` 의 `_insert_before_variable` | 위치어를 `... <위치어> <변수어> [<순번>]` 자리에 넣는다.  **자리를 못 찾으면 아무 데도 넣지 않는다** — 끝에 붙이면 발주처가 쓰지 않는 문형이 된다 |
| `config` `description.type_display_names` | TYPE 표기 이름(AT·AIT → Analyzer).  **이 문서에서는 0행**이고 규칙은 다음 문서를 위한 것.  자리는 `type_display()` 하나이고 `type_internal` 은 안 바뀐다 |
| `app/db.py` 의 `delete_job`·`deletion_log` | 분석 하나 삭제.  여덟 표의 그 job 행 + 다른 분석이 안 쓰는 업로드 PDF 만 지우고 **`projects/…/id_registry.json` 은 안 건드린다** (§7.3).  `next_seq` 가 상태를 안 보므로 안정 ID 는 회수되지 않는다 |
| `tests/test_determinism.py` 의 `extra_allowed` | §2.1 ③ 시험의 **좁은 예외 하나** — 그 행 자신의 접미 한 글자.  위치어는 예외가 아니라 **출처 확인**(그 낱말을 말한 CONNECTOR 후보가 실제로 있는가).  접미 사유 확인은 접미가 있는 행에만 건다 |
| `config` `client_form.scope_filter` | 발주처 양식이 **전량**을 담을지 SCT 만 담을지 (18회차 [B]).  기본 `all`.  11회차 코드는 지우지 않고 껐다 — `sct` 한 낱말로 돌아간다 |
| `excel_out.scope_state` ↔ `in_client_scope` | **세는 것**(공급 주체)과 **나가는 것**(양식에 실리는가).  18회차부터 갈렸다 — 전량 모드에서 벤더 행은 `out_of_scope` 이면서 나간다.  한 함수로 묶으면 "벤더 공급분이 몇 행인가" 를 물을 수 없다 |
| `detect_symbols.in_mark_window` | 마크 자리를 판정하는 **단 하나의 곳** (19회차).  사각형 **하나**다 — 가로 `mark_side`(10.0) · 세로 `y0-mark_above` ~ `y1`.  쪼개면 p52 처럼 별표가 테두리선에 걸친 장에서 0.00002pt 차로 어느 쪽도 아니게 된다.  **아래는 안 연다** — 25pt 안의 아래 마크는 전부 액추에이터·옆 심볼·밸브의 것이었다 (렌더 확인) |
| `detect_symbols._mark_gap` | 한 마크가 누구 것인가 — **유클리드**.  가로만(16회차)이면 세로로 쌓인 p9 을, 체비쇼프면 나란한 p25 를 못 가른다.  두 경우가 서로 다른 축을 요구하고 `max` 는 그 축을 버린다 |
| `read_vendor_mark(also=)` | **같은 항목의 다른 심볼** (19회차).  작동 밸브는 몸체와 액추에이터를 따로 그리고 도면이 별표를 어느 쪽에 찍는지가 장마다 다르다 (p6 M 원 8.4pt ↔ p27 몸체 8.1pt).  `marks` 를 한 번만 훑으므로 두 창에 다 들어와도 **한 번만** 세어진다 |
| `tests/test_vendor_mark_audit.py` 의 두 질문 | **알아봤는가**(`..._silently_dropped`) 와 **붙었는가**(`..._claimed_or_explained`).  18회차는 앞만 물었고, p25·p30 은 알아보기는 했는데 안 붙은 경우라 통과했다.  변이 시험도 **크기·자리 둘**이다 |
| `detect_symbols.drawn_mark_sizes` | 벤더 마크 크기를 **그 장에서 런타임에 다시 잰다** (18회차 [C]).  버블의 마크 자리에 **두 번 이상** 나타난 크기만 받고, **이미 받아들인 크기를 빼지 않는다**.  근거: 27장 중 p35 만 범례 4.0 ↔ 도면 7.8 — *범례는 뜻을 정하지 축척을 정하지 않는다* |
| `tests/test_vendor_mark_audit.py` | **검사기를 검증하는 시험.**  독립 검사(`find_marks` 를 안 부르고 `_glyph_clusters` 를 직접 훑음) + **변이 시험**(검출기를 일부러 멀게 만들면 검사가 잡는다).  16회차 "전수 0건" 이 자기 자신에게 물은 것이었다는 증거 |
| `detect_valves._angle_figures` · `_fold_angle_bodies` | 범례 p2 의 ANGLE — 직각으로 꼭짓점을 맞댄 두 중공 삼각형.  꼭짓점은 **끝점 네 개**가 모이는 점이고, 방향은 삼각형 **무게중심**으로 가른다 (좌우·상하만 보면 밑변이 꼭짓점을 사이에 두고 뻗어 안 갈린다).  `attach_actuators` 는 rect 중심이 아니라 **꼭짓점**에서 잰다 |
| `detect_valves.unclassified_bodies` | 배관 끝막대 둘을 갖췄는데 몸체 어휘에 없는 중공 도형.  **세기만 한다 — `find_bodies` 가 부르지 않는 것을 소스 검사가 강제한다** (18회차 [D]) |
| `app/global_symbols.py` · `GET/POST /symbols/global` | 전역 심볼 사전.  **범례가 이기고**(`resolve(..., legend_has=True)` → None), 끄면 없던 때와 정확히 같아진다.  `register()` 가 `author` 를 요구하고 **부르는 곳이 `app/main.py` 하나**임을 소스 검사가 강제한다 — 자동 등록의 유일한 자물쇠 |
| `pipeline` 의 `unjudged` · `engine_json["unjudged_symbols"]` | 판정하지 못한 심볼을 **버리지 않고 모은다**.  `ds.detect` 는 `unmapped`·`unverified` 를 처음부터 냈고 파이프라인이 받아서 버렸다.  등록 화면이 `GET /jobs/{id}/unjudged` 로 읽고 **다시 세지 않는다**(58장 재독 3분) |
| `describe_axis._VALVE_BODIES` | 밸브 몸체 갈래 → `<갈래> VALVE`.  **검출기가 낼 수 있는 갈래 전부**여야 한다 — 빠지면 ISA 조립으로 흘러 `ANGLE` 이 `ANALYSIS ELEMENT` 가 된다.  낱말은 범례 p2 인쇄(x=1024.2 세로줄).  `BOWTIE_OTHER` 는 일부러 뺀다 |
| `app/static/app.js` 의 `baseSentence` | `일괄 적용` 묶음의 단위 — **접미(17회차 C-1)를 뗀 문장**.  뗀 값은 지어내지 않고 엔진이 `duplicate_suffix.sentence` 에 남긴 원문을 쓰되, 지금 문장이 실제로 그 원문 + 그 한 글자인지 확인한 뒤에만 쓴다 |
| `app/static/app.js` 의 `setDescription` | 일괄 적용의 대상은 **`sameSentence` 하나**가 정한다.  예전에는 여기에 복사본이 있어 17회차 접미가 붙자 화면(3행)과 동작(1행)이 갈렸다 |
| `tests/test_ui_edits.py` 의 `REAL_DB` · `PID_UI_DB` | 이 스위트가 **어떤 데이터에서 도는지**.  실 DB 에 SCOPE 있는 분석이 없는 PC 에서는 가리키는 곳만 바꾼다 — 실 DB 를 갈아끼우면 17회차 격리가 깨진다 |
| `extract_titleblocks._ink_mask` 의 `a.size == 0` | **0픽셀에 `.min()` 을 부르지 않는다** (21회차).  `get_pixmap(clip=...)` 은 clip 을 페이지와 교집합하므로 교집합이 비면 0픽셀이 나온다.  **여기서 사유를 말하지 않는다** — 한 칸이 비는 것은 정상이고, 여기서 문장을 쓰면 그것이 조용한 폴백이 된다 |
| `extract_titleblocks.frame_report` | 이 도면틀이 이 문서에 맞는가 — **사실만 낸다, 문장은 쓰지 않는다**.  쪽 크기 · 네 칸이 종이 밖인 장 · 안쪽 여백보다 얇은 이력 행.  문장은 `pipeline._frame_reason` 이 그때 만든다 (15회차 `compare_note`) |
| `pipeline.TitleBlockUnreadable` · `_frame_reason` | 한 장도 도면번호를 못 읽었을 때.  `LegendUnavailable` 과 같은 자리 — **문턱이 없고 0 이다.**  예외만 막으면 `targets` 가 비어 **행 0개로 조용히 성공**한다.  검사는 **`titleblocks` 뒤 한 자리**다 — `open_pdf` 직후의 이른 검사를 넣었다가 뺐다 (그 자리의 `tb.LAYOUT` 은 아직 `_fit_layout` 이 유도하기 **전** 값이라, 유도가 성공했을 문서를 미리 잘라낸다).  문장은 `moved` 를 보고 **잰 값인가 설정값인가**로 갈린다 |
| `derive_layout._history_table` · `_equal_gap_runs` | **이력 표 다섯 칸은 24회차부터 그 도면에서 잽니다** (21회차에는 AL NOUF1 상수였고 그래서 TC2 REV 가 0/60 이었습니다).  찾는 것은 셋 다 모양입니다 — 간격이 같은 괘선 띠 중 **촘촘한 쪽** · 그 띠를 가로지르는 세로 괘선(1열 REV · 2열 DATE) · 오른쪽 끝은 노트 열 **안쪽** 괘선.  허용치 2% 는 **분포의 빈 띠**(개정 행 최대 1.4% ↔ 머리글 행 최소 2.8%)에서 왔고, 5% 로 두면 머리글 행이 들어와 개정 문자 `A` 가 `B` 로 **이름이 밀립니다**.  `hist_row_inset` 은 **그리지 않은 값**이라 유도하지 않습니다 |
| `spike/hist_verify.py` · `spike/hist_gaps.py` | 유도값이 손입력값을 재현하는가(세 경로: 설정 그대로 · 이력 칸만 · 유도값 전체) · 띠 안 간격의 산포.  AL NOUF1 은 파이프라인이 유도값을 **안 얹으므로**, 규칙이 맞는지는 이 도구로만 잽니다 (이력 칸만 유도값으로 → 사전·행·REV 58/58 이 한 칸도 안 다릅니다) |
| `extract_titleblocks.history_rows` 의 `pd._hist_rows` | 캐시는 **그 쪽 객체 자신**이 든다 (21회차).  전역 dict 를 `page_no` 하나로 키 삼던 때는 **두 번째 문서가 첫 문서의 이력 행을 물려받았다** — SADARA 9장이 혼자면 0행인데 AL NOUF1 뒤에 읽으면 9장 전부 AL NOUF1 의 7행 |
| `job.stopped_stage` · `app/main.py` 의 `_stopped_stage` | 실패 직전 파이프라인이 마지막으로 말한 **단계 이름**.  새 값이 아니라 `set_progress` 가 `job.message` 에 적던 것을 **덮기 전에** 읽어 둔 것이다.  담는 것은 **원문**이고 한국어는 화면 `stageWords()` 가 만든다 |
| `GET /jobs/{id}/error_detail` · `#prog-error` | 예외 원문 — **접어 두되 버리지 않는다** (21회차).  기본 응답(`_job_public`)은 그대로이고 **펼치기 전에는 받아 오지도 않는다**.  그 전에는 진단 zip 을 풀어야만 볼 수 있었다 |
| `pipeline.analyse` 의 `_own_config` 감싸기 | **이 분석이 config 를 바꾼 것이 다음 분석으로 새지 않게 한다** (22회차).  `_fit_layout` 이 `CFG.overlay` 로 모듈 전역에 제자리 기록하고 복원이 없었다 — 낯선 문서 한 번에 일곱 칸이 바뀌고, 다음 문서가 그 좌표로 읽힌다.  **실패해도 되돌린다** · 되돌리는 코드는 `_reconfigure` 와 같은 함수(`_rebind_config`) |
| `pipeline._rebind_config` | config 에서 나오는 모듈 값을 지금 `CFG` 로 다시 만든다 (쪽을 안 본다).  `_reconfigure` 의 앞부분이자 되돌리기가 쓰는 것 — **두 벌을 두면 갈린다** |
| `pidcache.PROJECT_NAME_REGION` · `_MIN_HEIGHT` | `rename_projects` 가 `global` 로 쓰는 둘.  남으면 다음 문서의 프로젝트명을 **남의 자리**에서 읽고 `analysis_scope` 가 달라져 `derive_connector_reach` 가 세는 장이 달라진다 (실측 218→219 · **행은 전부 같은데 지문만 움직였다**).  `_rebind_config` 이 되돌린다 |
| `tests/test_config_isolation.py` 의 `..._every_module_global_...` | 엔진의 `global` 문을 **전수로 세어 못박는다**.  파이프라인 경로에 새 `global` 이 생기면 걸린다 (`detect_valves` 의 것은 CLI `main()` 안) |
| `GET /jobs/{id}/measured_config` | 새 양식의 좌표를 `project_*.yaml` 에 붙일 수 있는 YAML 로 낸다 (22회차).  **재는 코드도 저장도 이미 있었다** (`applied_rules.layout.items`) — 없던 것은 꺼내 보는 길뿐.  재지 못하는 이력 표 여섯은 지어내지 않고 `must_measure_by_hand` 로 이름만 낸다 |
| `docs/new_form_checklist.md` | 새 회사 양식을 읽히는 절차 — 한 번 올려 `measured_config` 를 받고 · 설정 파일을 만들고 · 이력 표 여섯은 손으로 재고 · 도면번호 형식과 범례를 확인한다 |
| `spike/regression_3p.py` · `spike/projects_3p.json` · `spike/baselines_3p.json` | 세 프로젝트 회귀 하네스 (23회차).  **순차**로 돈다 — 병렬은 메모리가 위험하다(TC2 최대 RSS 7.2GB).  프로젝트마다 **자식 프로세스**라 메모리를 돌려받고 최대 RSS 를 따로 잰다.  **프로젝트 추가는 `projects_3p.json` 에 한 항목** |
| `spike/identification.py` | **축3(식별률)** — 정답지 없이 "도면이 인쇄한 것 중 얼마를 읽었나".  여섯 지표 · **같은 무게 1/6**(순위 근거가 도면에 없어서).  ⚠ 채점기가 **엔진의 `anchors.type_map` 을 읽어야** 한다 — 안 읽으면 엔진이 옳게 한 일을 틀렸다고 센다(첫 정의 88.8 ↔ 고친 뒤 94.8) |
| `spike/constant_survey.py` | 상수 전수 — dataclass 기본값과 **조립 함수가 리터럴을 넣는가 식을 넣는가**로 "코드 고정 / config 가능" 을 가른다.  키 이름으로 세면 0 이 나온다 (`vendor_marks.above` → `mark_above`) |
| `spike/build_ui_db.py` | UI 스위트가 돌 **SCOPE 있는 DB** 를 저장된 결과 json 에서 세운다 — **재분석하지 않는다**(8분).  `pdf_path` 는 **실제로 열리는 PDF** 여야 한다 (비워 두면 재분석 시험이 실패하고 뒤 시험들로 번진다) |
| `detect_symbols.star_groups` · `definition_stars` · `star_marks` | **별표를 획의 관계로 읽는다** (25회차 · §9).  중점이 한 점(획 길이의 1/4)에 모이고 방향 둘 이상, **어느 획 끝도 다른 잉크에 닿지 않는다**(없으면 밸브 나비가 별표가 된다).  상한 = 그 장 버블의 짧은 변 · 크기 = 그 장에서 두 번 이상 · 정의줄이 획을 주면 획 수도.  `find_marks` 끝에 **가산**으로 붙고 기존 마크 자리는 건너뛴다 |
| `detect_symbols._ink_index` · `pc._ink_index` | 끝점 검사용 그 장 전체 선분 격자.  쪽 객체가 든다 (장마다 4회 → 1회).  ⚠ 메모리 +3GB (AL NOUF1 7.8GB · TC2 8.9GB) |
| `detect_symbols._ink_cache` · `_INK_LAST` | 잉크 인덱스는 **지금 보는 한 장**만 든다 (26회차).  다음 장이 만들 때 앞 장의 것을 놓는다 — 25회차는 60장을 다 들고 있어 최대 RSS 가 +3GB 였다 (AL NOUF1 7.85→4.9GB · TC2 8.86→7.2GB).  값을 나르는 자리가 아니라 **놓는 자리**라 22회차 격리 규칙에 걸리지 않는다 |
| `detect_symbols.star_marks` 의 없어진 조건 | **정의줄의 획 수로 본문을 거르지 않는다** (26회차).  두 문서 다 정의줄 2획 ↔ 본문 4획이고, 그 조건이 TC2 별표 254개 중 **174개**를 버렸다 — 하필 정의줄이 있는 장에서만.  *정의줄은 뜻을 정하지 모양을 정하지 않는다* |
| `detect_symbols.definition_stars` | 정의줄의 별표를 **획으로** 읽어 사전에 뜻을 준다 (`* By SE`).  상한은 **그 줄들의 글자 높이** — 정의줄의 별표는 그 줄에 인쇄된 한 글자다.  **`glyph_size` 는 주지 않는다** (정의줄 1.8pt ↔ 본문 2.9pt) |
| `pipeline._scope_of` 의 `name.upper() == COL_SCT` | 표시가 가리키는 공급자가 **우리면 우리 공급이다** (26회차 · TC2 p12 `BY SCT` 2행).  별표는 "누가 공급하는가" 를 가리키지 "타사" 를 뜻하지 않는다 |
| `pipeline._BORROWED_MULTIPLIER` · `MULTIPLIER_FROM_CONFIG` | 승수가 **이 도면의 범례가 아니라 설정에서** 왔으면 행이 그렇게 말한다 (26회차).  TC2 는 승수표가 60장 어디에도 없어 345행이 조용히 AL NOUF1 의 `00:1` 을 받고 있었다.  값은 두고 출처를 말한다 |
| `tests/test_star_marks.py` 의 `_midpoint_clusters` | **재현율**을 묻는 독립 채널 (26회차).  `star_groups` 를 안 부르고 자유 끝점·크기 학습도 안 본다 — 중점이 겹치고 **방향 셋 이상 · 팔 길이가 같은** 무리만.  25회차는 정밀도(접촉 시트)만 쟀고, 접촉 시트는 **찾은 것만** 보여 준다 |
| `spike/legend_star.py` · `spike/mark_probe.py` · `spike/mark_shape.py` | 정의줄 별표가 획인가 텍스트인가 · 별표 축이 어디서 0 이 되나(4단계) · 뭉치의 모양 특징 |
| `projectconfig.note_unit_span` · `_unit_tokens` | 그 장 NOTES 가 말하는 **"이 도면은 유닛 몇 개에 같이 쓰인다"** (27회차).  세는 것이지 해석하지 않는다 — 문단이 열거한 표기의 개수가 배수다.  줄은 **같은 y** 끼리만 묶고(7pt 버킷은 두 줄을 섞는다) 덧인쇄된 낱말은 하나로 본다.  낱말 뒤 꼬리(`UNIT#12,21,22`)와 `NO.` 와 낱말 없이 이어지는 `4-1 & 4-2` 까지 읽되, **`3-1` 꼴일 때만** 맨 표기를 더 줍는다 |
| `pipeline._note_factor` | 그 값을 **범례가 답하지 못할 때만** 쓴다 — ①범례 → ②노트 → ③설정 폴백 → ④빈칸.  노트가 설정 폴백보다 세고 범례보다 약하다.  그래서 AL NOUF1(범례가 코드 일곱을 전부 덮는다)은 한 칸도 안 움직인다 |
| `config` `qty_note` · `projectconfig.note_vocabulary` | 노트를 읽는 **낱말** (27회차).  `same_words`(IDENTICAL·SIMILAR·SAME·TYPICAL) · `unit_words`(GROUP·UNIT·TRAIN) · `range_words`(THRU·THROUGH·~).  **영어 낱말이지 프로젝트 값이 아니라서** 기본값은 코드에 있고 config 가 늘린다 — 넷째 프로젝트가 `BLOCK` 으로 부르면 한 줄 더한다.  `range_words` 는 세는 낱말이 아니라 **세지 않는** 낱말이다 |
| `pipeline._note_ambiguous` · `MULTIPLIER_NOTE_RANGE` | `UNIT 3-1 THRU 6-2` 는 **두 끝만 인쇄돼 있다** — 세면 2 가 나와 틀린 수가 맞는 얼굴을 한다.  배수를 정하지 않고 검토로 올린다.  **범례가 답한 문서에서는 사유도 안 올린다** (노트를 아예 안 쓰므로) |
| `result["unit_notes"]` | 장별로 읽은 노트 사실.  **지문 밖**이다 — `multipliers` 에 넣으면 그 칸이 지문 대상이라 이미 답이 있는 문서가 움직인다 |
| `out/constants_by_source.md` | §9 [A] — 상수 71+210 을 A~E·✔ 로.  **이후 회차 순서는 이 표의 A 부터** |
| `pidcache._shx_entries` · `_shx_words` · `SHX_AUTHOR` | 획(SHX) 글꼴로 플롯된 글자를 읽는다 (28회차).  제목이 **정확히** `AutoCAD SHX Text` 인 Square 주석만 받는다 — 검토 메모는 FreeText 이고 제목이 **사람 이름**이라 안 들어온다.  **어구 단위 사각형을 쪼개지 않는다** (쪼개면 글자 자리를 지어내는 것) |
| `pidcache.tokens` | 어구 안을 낱말로 읽는 **단 하나의 접근자**.  이름으로 찾는 쪽(`derive_layout`·`legend_rules`·`pipe_graph`·`isa_table`)이 쓴다.  ⚠ 쓰는 쪽이 헐거우면 거짓 매칭이 난다 — `legend_rules._line_with` 참조 |
| `legend_rules._line_with` | 범례 머리말은 **한 줄에 x 순으로 연달아** 있어야 한다 (28회차).  어구를 낱말로 읽자 UAD p3 이 `FIRST ISSUE` + `OF OTHER LETTER SYMBOLS)` 로 `FIRST LETTER` 를 거짓 매칭했다 |
| `app/engine/tags.py` 의 `assign`·`shape` | **증거 등급을 그 도면이 정한다** (29회차 · §10).  규칙: 코드가 **한 검출에만** 나오고 그 **모양이 두 검출·두 장 이상**에서 되풀이될 때만 태그.  모양은 글자·숫자 달리기(`D2L3D2L2D3L1`)이고 **KKS 정규식을 코드에 박지 않는다**.  측정이 세 번 좁혔다 (AL NOUF1 오검 9 → 2 → **0**) |
| `row.tag_no` · `result["evidence_tier"]` | **지문 밖이다.**  해싱 재료가 아니므로 1급 경로가 켜져도 기준선이 구조적으로 안 움직인다.  ★ 1급은 태그 있는 장에서만 켜지고 **2급(기하)은 가산으로 그대로 돈다** |
| `out/round29/8_tag_scope.md` | **태그로 SCOPE 는 판정할 수 없다** (29회차 [E-3]).  태그 문자열에 공급자 자리가 없고(일련번호 `5xx` = SCT 27 / VENDOR 46), 계통코드 94% 일치는 패키지 상자의 그림자다.  **등급 순서는 칸마다 다르다** — 이름·순번·귀속은 1급, **공급 주체는 2급(별표·NOTES)** |
| ⚠ 버블 안 글자를 별표로 읽는 자리 | `_glyph_clusters` 가 SHX 글자 한 덩이를 별표 크기 획 뭉치로 읽는다 (UAD 2행 · TC2 10행 · AL NOUF1 7행).  **고치는 규칙이 아직 없다** — 깊이 문턱은 빈 띠가 없고, "낱말 위" 는 SHX 어구 사각형이 삼키고, "사각형 안" 은 **AL NOUF1 이 별표를 버블의 둥근 모서리에 찍어** 7행이 움직인다.  다음 회차 후보는 **그려진 윤곽에 대한 점-포함 판정** |
| ⚠ `_connector_labels` 5pt 버킷 · `_notes_lines` 7pt 버킷 | 줄을 묶는 버킷이고 줄 간격이 그보다 촘촘한 문서에서 서로 다른 줄을 섞는다.  TC2 지문이 `e909cf7c`→`c0a2d29d` 로 움직인 원인이 앞의 것이다 (SHX `M` 한 글자) |
| `detect_symbols.Outline` · `bubble_outlines` · `on_bubble_face` | **사각형이 아니라 그려진 윤곽**으로 안/밖을 가른다 (30회차 · §10-10).  버블은 *마주 본 호 캡 둘 + 곧은 옆면* 이고 `find_bubbles` 는 **처음부터 캡을 알고 있었다** — 바깥 사각형만 돌려주느라 버렸다(열한 번째).  **새 상수 0개 · 반지름을 가정하지 않는다** (AL NOUF1 캡비 ≈2.0 ↔ TC2 34×11 ≈3.1).  **윤곽을 못 찾으면 걸지 않는다** (밸브 몸체는 스타디움이 아니다) |
| `find_marks`·`drawn_mark_sizes`·`star_marks` 의 얼굴 걸림 | 심볼의 얼굴 위 획 뭉치는 마크가 아니라 **그 심볼의 글자**다.  **크기 학습에도 건다** — 안 걸면 글자가 크기를 가르쳐(`n >= 2`) 그 크기의 얼룩이 다른 자리에서 마크가 된다 (실측: 그래서 UAD 가 예상 2행이 아니라 **18행** 움직였다).  **활자 `*` 에는 걸지 않는다** |
| `result["face_marks"]` | 얼굴 위라 글자로 읽은 획 뭉치 (장 → [(x, y, 갈래)]).  **지문 밖**.  세는 곳은 `drawn_mark_sizes` **하나**다 — `find_marks` 에 달면 크기 학습이 먼저 걸러 **0개**가 기록된다 (실측) |
| `derive_layout._history_rev_marks` · `_revision_pattern` | **개정 표기의 모양도 도면이 말한다** (30회차).  이력 표 REV 열의 낱말 중 **같은 행의 DATE 열이 `formats.date` 에 맞는 행**만 받는다 (머리글 `REV.`/`DATE` 를 그렇게 배제한다).  읽지 않은 모양은 더하지 않고, 못 읽으면 유도하지 않는다.  ⚠ **이력 표는 개정 표기의 하한이지 전부가 아니다** — AL NOUF1 은 유도값 `^[A-Z]$` 인데 실제 REV 에 `A1` 이 한 장 있다 (프로필이 지켰다) |
| `pipeline._rebind_config` 의 `tb.*_RE` 셋 | 타이틀블록 형식 정규식(`DWG_NO_RE`·`DATE_RE`·`REV_TEXT_RE`)을 지금 CFG 로 **다시 만든다**.  안 하면 유도값을 얹어도 import 때 compile 된 옛 식이 쓰인다 — 22회차 `pidcache` 전역과 같은 덫 |
| ⚠ 앵커는 **낱말 통째 일치**다 | UAD 는 경보 수식자를 글자에 붙여 인쇄한다 (`LSA++` · `LS++,+,-,--` · `PIA+,-`).  `LS` 가 아니므로 후보에도 못 든다 — **미판정 목록에도 안 뜬다**(62건에 `LSA`·`LS` 0건).  UAD 미검출 36 중 **21** 이 이것이다.  30회차는 규명만 했다 (검출 변경은 AL NOUF1 게이트가 필요하다) |
| `spike/outline_probe.py` | 윤곽 판정을 **엔진에 넣기 전에** 네 문서 전수로 재는 도구.  윤곽 안 AL NOUF1 0 · SADARA 0 · TC2 0 · UAD 17 — 움직일 수 있는 문서를 알고 고쳤다 |
| `app/unit_multipliers.py` · `table()` | **도면이 승수를 말하지 않을 때 사람이 한 번 답하는 자리** (31회차 · §9 ③).  `{data_dir}/projects/{프로젝트}/unit_multipliers.json` — 자리는 `axis_overrides`, 규율(작성자 필수 · **읽는 곳 하나** · 범례가 이긴다 · `enabled:false` 면 없던 때와 정확히 같아진다)은 전역 심볼 사전에서.  키는 **유닛코드**이지 행이 아니다 — 행이면 SADARA 에서 82개를 써야 한다 |
| `pipeline._note_factor(user=)` | 읽는 순서 `①범례 → ②NOTES → **③사람** → ④설정 폴백 → ⑤빈칸`.  **사람은 도면을 이기지 않는다** — AL NOUF1 에 전 유닛 `x9` 를 넣어도 지문·행·Q'ty 가 한 칸도 안 움직인다(실측).  ★ 이 함수는 27회차부터 ③ 자리를 갖고 있었고 **넣을 값이 없었을 뿐**이다 (열두 번째) |
| `MULTIPLIER_BY_USER` | 사람이 지정한 값에 붙는 사유.  **지우지 않는다** — 지우면 "사람이 도면 값을 덮은 칸" 이 감사에서 사라진다(13회차와 반대가 된다).  근거 패널이 누가·언제를 적고 `qty_basis` 가 `사람 지정 — {누가}` |
| `GET/POST/DELETE /jobs/{id}/multipliers` · `#mult-panel` | 대상 행을 **유닛코드로 묶어** 낸다 (적용 범위 · Q'ty 전/후 · 작성자 · 되돌리기).  유닛코드가 아니라 행으로 물으면 SADARA 를 82번 묻는다 — 시험이 그것을 못박는다 |
| `docs/program_overview.md` | **구조 · 성능 · 범용성** 한 자리 (31회차).  네 층 구조 · 네 프로젝트 시간·메모리 · 축 셋의 뜻 · *비율·모양은 축척을 넘고 절대 pt 는 못 넘는다* · 다섯째 프로젝트 절차 |
| `report` 테이블 · `app/main.py` 의 `_capture_row` | 오류 신고. 사람이 적는 것은 **무엇이 틀렸나 + 한 줄** 둘뿐이고 나머지는 서버가 담습니다 |
| `_diagnostic_zip` | 진단 내보내기. 담긴 것과 **뺀 것**을 MANIFEST 에 적습니다 (원본 PDF·발주처 Excel 제외) |

## 8. 다음 프롬프트를 쓸 때

- 목표 문장(발주처 실제 표기)을 먼저 제시하고, 조각별 현재 정확도를 근거로
  개선 대상을 지정하세요. 집계 지표만으로는 실무 가치가 보이지 않습니다.
- 새 규칙은 **범례에서 유도**하거나 **발주처 557행에서 센 값**이어야 합니다.
  어느 쪽도 아니면 "유도 불가"로 보고하게 하세요.
- 회귀 항목(2절 표)을 프롬프트에 그대로 넣어 매번 확인시키세요.
- 발주처 557행에서 이번 회차에 센 값(다시 세지 마세요): 도면번호 **0행** ·
  느슨한 하이픈 코드 **0행** · 그리드 셀 **0행** · DN 치수 **0행** · NOTE **0행** ·
  ASME/ANSI/API **0행** · 플랜지 클래스 **0행**. LS 22행 = **11쌍**이고 한 행이 두
  신호를 말하는 문형은 **0행**.
- **등급 이름은 근거를 말하지 판정을 말하지 않습니다.** `CONFIRMED` 는 "도면 근거
  있음"이며 발주처와 같다는 뜻이 아닙니다 (대조 455행 중 완전일치 27 · 주어 74.3%).
  더 좁은 등급을 만들 근거는 찾지 못했습니다 — 모든 갈래가 63~80% 로 붙어 있습니다.
- 발주처 557행에서 이미 센 값(다시 세지 마세요):
  구두점 `.`/`(` **0회** · 위치어 부착률 PIT 70.8 / PDIT 67.3 / FS 100 / FIT 41.9 /
  FE 38.9 / TIT 32.0 / TI 11.9 / PI 6.3 / LIT·LS·LI 0 ·
  끝 순번 PIT 44 / TIT 36 / FIT 26 / PDIT 6 / LIT 5 / 나머지 0 ·
  같은 도면 안 문장 반복 110행(19.7%) · 중간 심볼 13행(전부 PDIT `SUCTION STRAINER`).
- 계기가 기기의 **위·아래**에 있을 때 발주처가 쓰는 낱말은 유도되지 않습니다:
  위 = 없음 60 / DISCHARGE 24 / SUCTION 12 / OUTLET 2, 아래 = 없음 115 / OUTLET 9 /
  INLET 6 / DISCHARGE 5. 다수는 **아무 낱말도 쓰지 않는 것**이므로 붙이지 않습니다.
- 발주처 557행에서 이번 회차에 센 값:
  라인 문구 `TO`/`FROM` **36행(6.5%)** — LS 22/22 · 나머지 전부 합쳐 14/535 ·
  제목 약어는 `CLOSED COOLING WATER`→CCW(127행)·`AUX. COOLING WATER`→ACW(4행) **둘뿐** ·
  CCW 도면 TI→RETURN 51/54 · CCW 도면 PI→SUPPLY 49 / RETURN 51 (**구분 불가**).
- 이번 회차에 센 값: 라인 문구를 안 쓴 행에 어휘를 넣을 때는 **수식어까지 세세요** —
  낱말만 넣으면 배관 주석이 거리로 정답을 누릅니다 (§3 표 참조).
  발주처가 기기 앞에 쓰는 수식어: VALVE ← BYPASS 54 / LETDOWN 3 / CONTROL 3 /
  MODULATING 2, HEADER ← DISCHARGE 16 / STEAM 6 / SEAL 4 / RING 3, BOX ← WATER 6 /
  FLASH 5. 기기별 위치어: TANK 없음 59/59 · VALVE DOWNSTREAM 30/65 ·
  HEADER DISCHARGE 16/32 · PUMP·COOLER 는 양쪽(기하에 맡김).
- **규칙을 어느 층에 둘지 먼저 정하세요.** 발전 플랜트면 어디나 같은 것은
  `config/plant_standard_abbr.yaml` 에, 이 발주처의 습관은 `project_*.yaml` 에
  둡니다. 표준 파일에 프로젝트 고유 표기를 섞으면 다음 프로젝트에서 그대로 못
  씁니다. 표준 파일은 **방향을 고정하지 않습니다** — 어느 형태를 쓸지는 ②층입니다.
- **판정축 회귀**: ④→② 전환율 5.4% · 적용 192행 · 지문 7b34064e 이 기준선입니다.
  판정 트리 규칙을 바꾸면 824행 전수 대조(불변 11칸)와 `pytest -q
  tests/test_describe_axis.py` 를 다시 돌리세요.  무제한 순회는 금지이며
  그 근거 실측(10.4%)이 §3 과 `docs/from_to_axis.md` 에 있습니다.
- **화면 회귀는 "서로 다른 시점의 캡처가 바이트로 달라야 한다"로 잽니다.**
  같은 방법이 두 회차(§7.2 · 12회차)에서 같은 실패를 잡았습니다 — 진행 신호가
  없는 구간이 있으면 13초 화면과 60초 화면의 md5 가 같습니다.
- **열 잘림 캡처는 화면 비율로 자르지 말고 그 열의 bounding box 를 읽어
  자르세요.** 어림해 자르면 옆 열이 찍히고, 가로 스크롤을 끝까지 밀면 이번에는
  그 열의 왼쪽이 컨테이너 밖으로 나갑니다 (12회차에서 두 번 다 겪었습니다).
- **없는 것을 만들기 전에 있는 것을 찾으세요.** 13회차는 요구 세 가지 중 둘이
  이미 서버에 있었고(재분석 없는 로드 0.69초 · 편집 영속 10열 · 두 층 분리),
  없던 것은 화면에서 거기로 가는 길이었습니다. 10회차 M 심볼과 같습니다.
  **구현 전 실태 조사를 한 회차의 첫 단계로 두면 범위가 줄어듭니다.**
- **어떤 값을 하나로 접을 때는 접기 전 분포를 함께 내세요.** 문서 대표 Rev 는
  세 후보가 서로 다른 답을 내고(`max`→D · `mode`→B · `first_sheet`→B) 도면이
  답을 주지 않습니다. 규칙은 config 에, 근거는 주석에, 분포는 화면에 둡니다.
- **식별자가 정말 유일한지 재세요.** 도면번호는 장을 유일하게 가리키지 않습니다
  — 같은 번호를 쓰는 PID 장이 3쌍이고 개정이 서로 다릅니다. 짝이 여럿이면
  판정하지 않는 쪽이 맞습니다 (고르면 같은 PDF 가 개정으로 읽힙니다).
- **화면이 데이터에 대해 하는 말은 데이터와 같은 접근자에서 나와야 합니다.**
  오버레이 색·범례 라벨·근거 패널 문장·SCOPE 열이 서로 다른 값을 읽으면
  언젠가 갈립니다 (11회차 캡처가 셋을 한꺼번에 잡았습니다).
- **시험이 어떤 데이터에서 도는지 시험이 말하게 하세요.** 14회차에 `UI 21` 이
  세 회차 동안 **필터가 꺼진 것과 같은 데이터**(SCOPE 전부 빈칸)에서만 돌고
  있었습니다. 코드는 같고 데이터가 달랐으므로 파일 동일성으로는 안 잡힙니다.
  전제가 깨지면 **건너뛰지 말고 실패**시키세요 — 건너뛰기가 그 침묵을 만듭니다.
- **시험이 깨지면 먼저 "제품이 틀렸나 가정이 틀렸나" 를 API 로 가르세요.**
  14회차의 두 실패는 필터가 설계대로 동작한 결과였고, 고칠 것은 픽스처였습니다.
- **화면이 편집에 대해 하는 말도 데이터와 같은 접근자에서 나와야 합니다.**
  근거 패널만 고치면 **행을 누른 사람에게만** 말하게 됩니다 — 칸을 바로 고치는
  사람은 아무것도 못 봅니다 (12회차가 그렇게 절반만 고쳐져 있었습니다).
- **SADARA 회귀는 `PID_PROJECT_CONFIG` 를 지정하지 않고 잽니다** (82행
  `0767ba79`). 지정하면 75행이고 그것은 기준선이 아닙니다. 프로필 자동 맞춤은
  없습니다 — 미지정이면 alnouf1 고정입니다.
- **화면이 말할 문장을 데이터에 저장하지 마세요.** 15회차에 `compare_note`
  를 `engine_json` 에 넣었더니 문구를 고쳐도 옛 분석이 옛 문장을 계속
  말했습니다. 저장하는 것은 **사실**이고(`legend_sheets`·`uncompared`
  ·`compared`), 문장은 화면 쪽 판정 함수가 그때 만듭니다.
- **"무엇이 있는지" 를 파생 분류로 가르지 마세요.** `page_kind == LEGEND`
  로 "범례가 있나" 를 물었더니, 다시 그린 범례 장이 종류를 잃어 **범례가
  있는데도 대조를 건너뛰는 길**이 생겼습니다. 실제로 읽는 코드가 무엇으로
  찾는지(여기서는 인쇄된 머리말)를 그대로 쓰세요.
- **캐시해도 되는 값과 캐시하면 안 되는 값을 먼저 가르세요.** 15회차의
  `legend_rules` 단계는 열 가지를 유도하는데 **범례 장에서 나오는 것은 여덟**
  이고, `connector_reach`(도면 218개)와 `layout` 19건(도면 58장)은 다음
  개정본의 **도면**이 답해야 합니다. 한 단계 안에 있다는 이유로 같이 담으면
  용지가 바뀐 개정본에서 조용히 틀립니다.
- **"저장했다가 다시 쓴다" 는 `source` 를 바꾸는 일이 아닙니다.** 지문이
  `source`·`note`·`values` 를 해싱하므로 재사용이 `PROFILE` 이라고 적으면
  같은 값을 쓰고도 지문이 달라지고, **프로필이 값을 정확히 담았는지 확인할
  길이 사라집니다.** 재사용 여부는 지문 **밖**에 적으세요.
- **적용(학습)과 대조(확인)는 다른 일입니다.** "다시 학습하지 않는다" 가
  "다시 읽지 않는다" 는 아닙니다 — 개정본에 범례가 있으면 재서 **대조만**
  하세요. 대조를 빼면 재작도된 범례를 옛 규칙으로 읽고도 아무도 모릅니다
  (지문은 정상으로 나오고 행 수도 그럴듯합니다).
- **폴백이 조용한지 시끄러운지 항목마다 다릅니다.** 15회차 실측: 범례가 없으면
  `line_styles` 는 `KeyError` 로 죽고, `unit_multipliers` 는 7코드 → 3코드로
  **조용히** 줄어 Q'ty 만 틀립니다. **죽는 쪽이 안전한 쪽입니다** — 조용한
  폴백을 먼저 찾으세요.
- **속도 개선처럼 보이는 요구가 속도 문제가 아닐 수 있습니다.** 15회차의
  `legend_rules` 는 전체의 1.5%, 범례 몫은 0.4% 였고, 실제 값어치는 **범례
  없는 개정본이 완주하는 것**과 **범례가 달라진 것을 잡는 것**이었습니다.
  먼저 재고, 성과를 재지 않은 축으로 적지 마세요.
- **문턱을 정할 때는 분포에 빈 띠가 있는지 먼저 보세요.**  17회차 C-4 의
  "2위와의 차" 는 12.8~41.4 와 190.8~804.4 로 갈리고 그 사이가 비어 있어,
  문턱을 그 안 어디에 두어도 결과가 같습니다 — 그러면 그 값은 임의값이
  아닙니다.  16회차 `brk_max_mark` 도 25.9~28.3 에서 같은 답이었습니다.
- **"가장 가까운" 이 "가까운" 은 아닙니다.**  오프페이지 커넥터는 시트
  가장자리에 모여 있어 거리가 공통 오프셋에 지배되고, 1195pt ↔ 1213pt 의
  18pt 차이는 정보를 담지 않습니다.  10회차 [G] 가 같은 이유로 등급을
  LOW 로 두었습니다 — **거리로 이겼다고 근거가 되는 것이 아닙니다.**
- **시험이 새 규칙을 막으면 시험이 맞는지부터 보세요.**  17회차의 접미는
  §2.1 ③("도면에 없는 낱말을 만들지 않는다")을 실제로 어깁니다.  답은 예외를
  넓히는 것이 아니라 **사유를 데이터에 남기고 예외를 그 한 글자로 좁히는
  것**이었습니다.
- **전량 분석을 다시 돌리기 전에, 저장된 결과로 그 시험만 재생하세요.**
  slow 의 채점 함수를 json 하나로 그대로 부를 수 있습니다 (11분 → 2초).
- **★ 대기 루프에는 15분 상한을 걸고, 프로세스 이름이 아니라 완료 표식을 기다리세요.**
  23회차에 대기 셸 셋이 **8시간 40분** 헛돌았습니다.  원인은 `pgrep -f "pytest -q -m ui"`
  가 **자기 자신을 잡는 것**입니다 — `pgrep -f` 는 명령줄 전체를 보므로 조건문 안에 적은
  그 문자열이 자기 셸에 걸립니다 (재현: pytest 가 0개인데 자기 셸 3개가 잡힙니다).
  그래서 셋 다 자기참조로 무한이었고, 그중 하나는 통과하면 UI 를 띄울 셸이라
  **UI 스위트가 시작조차 되지 않았는데 "도는 중" 으로 보였습니다.**
  규칙 셋:
  ① `timeout 900` 을 **반드시** 붙인다 (상한 15분).
  ② 기다리는 대상은 **파일**이다 — 작업이 끝날 때 표식(`echo DONE > …done`)을 쓰게 하고
     `until [ -f …done ]` 로 기다린다.  프로세스 이름으로 기다리지 않는다.
  ③ 그래도 `pgrep` 을 써야 하면 패턴을 자기 명령줄에 **적지 않는다** (변수에 담아도
     명령줄에 남는다 — 실행 스크립트를 파일로 두고 그 파일 이름으로 찾는다).
  ④ 상한에 걸리면 **기다린 것이 아니라 실패다.**  로그의 mtime 으로 실제 실행을
     판정하고, 안 돌았으면 "안 돌았다" 고 적는다.
- **TIT 의 위치어는 유도되지 않습니다** (다시 재봤습니다): 방향별 최다가 전부
  "낱말 없음"(ABOVE 7/11 · LEFT 14/22 · RIGHT 13/22)이고, 기기 종류로 갈라도
  VALVE 89% 없음 · HEADER·TANK·BOX 100% 없음 · PUMP 는 n=10 에 SUCTION 3 /
  DISCHARGE 3 / OUTLET 2 로 과반이 없습니다. 목표 문장 3번은 여기서 막혀 있습니다.

## 9. 범용성 원칙 — 도면이 말하는 것은 도면에서 읽는다

> **도면이 말하는 것은 도면에서 읽는다. 외워두지 않는다. 도면이 말하지 않으면 사람에게 묻는다.**

도면은 자기 설명서를 항상 들고 온다 — Symbol & Legend(심볼의 모양·크기·별표가
어떻게 그려지는지) · NOTES(별표의 뜻·공급자) · 타이틀블록 캡션(도면번호·REV·이력이
어디 있는지) · 승수표(UNIT 코드의 배수).  §2.1 ② 가 처음부터 이것을 말했다.
그런데 23회차가 센 실태는 config 잎 210개 중 도면에서 읽는 것 **15개**, dataclass
71개 중 코드 고정 **39개** — **범례가 항상 있는데 코드가 매번 읽지 않고 AL NOUF1
에서 읽은 결과를 외워뒀다.**  이것이 TC2 가 안 되는 이유의 전부다.
13·15·16·18·22·24·25회차가 전부 같은 형태였다 — *도면이 말해주고 있었고, 코드가
찾기까지 했는데, 쓰지 않았다.*

1. **파라미터를 정할 때 읽는 순서**
   ① 그 도면의 Symbol & Legend ② 그 도면의 NOTES ③ 그 도면의 타이틀블록 캡션 ·
   승수표 ④ 그 도면 본문의 실물 (범례에 없을 때) ⑤ 사람에게 묻고 프로젝트 프로필에
   저장.  **★ ①~④ 에서 못 읽으면 ⑤ 다. 다른 프로젝트 값으로 폴백하지 않는다.**
2. **"외워둔 값" 판별.**  config 나 코드에 적힌 숫자·문자열 중 특정 도면에서 잰 것을
   옮겨 적은 것은 전부 "외워둔 값" 이다 (`out/constants_by_source.md` 가 그 목록).
   **★ 새 상수를 추가할 때 "이것은 어느 도면에서 읽는가" 를 주석에 적는다. 적을 수
   없으면 추가하지 않는다.**
3. **새 규칙을 만들기 전에** ① 그 정보가 이미 도면에 인쇄돼 있는가 ② 코드가 이미
   그것을 찾고 있는가 (notes · 로그 · 미사용 반환값) ③ 찾고 있는데 쓰지 않는 것인가.
   **★ 13·15·16·18·22·24·25회차 전부 ③ 이었다.**
4. **못 읽으면 멈춘다.**  `TitleBlockUnreadable` · `LegendUnavailable` 과 같은 방식으로
   시끄럽게 멈춘다.  조용히 진행하지 않는다.  사유를 화면에 말한다.
5. **세 프로젝트 회귀.**  어떤 변경이든 AL NOUF1 · TC2 · SADARA 를 전부 확인한다
   (`spike/regression_3p.py`).  **AL NOUF1 지문(`fb85b039` · 1037행 · 축2 94.4%)은
   절대 불변이다.**  넷째 프로젝트가 오면 넷을 확인한다.
6. **축척 · 프로젝트 · 크기로 분기하지 않는다.**  절대 pt 를 축척으로 나누지 않는다
   (22회차 반증: 현행 1 · 절반 0 · 실측 3).  프로젝트 이름 · 종이 크기 · 회전으로
   분기하지 않는다.  **모양 · 관계 · 비율은 축척을 넘고 절대 pt 는 못 넘는다**
   (23회차 확인).


## §10. 증거 등급 — 도면이 주는 만큼 읽는다

> **도면은 자기가 얼마나 말해주는지도 말한다.**
> **절차는 하나고, 그 첫 단계가 "이 도면이 무엇을 주는가" 를 재는 것이다.**

| 등급 | 도면이 주는 것 | 어떻게 읽는가 | 실측 |
| --- | --- | --- | --- |
| **1급** | 태그가 인쇄돼 있다 | 태그로 기기 귀속 · SCOPE · Description | **UAD** (128/149행 · 14/14장) |
| **2급** | 범례 · NOTES · 커넥터만 | 기하 · 거리 · 별표 (§3 판정 트리) | AL NOUF1 · TC2 · SADARA |
| 3급 | 둘 다 부족 | 사람이 지정 + 프로젝트 프로필 저장 | 15·18회차 구조 |

* **등급 판정은 그 도면에서 잰다** (`app/engine/tags.py` 의 `assign`).
  프로젝트 이름 · 종이 크기 · 파일명으로 분기하지 않는다.
* **1급은 태그가 있는 장에서만 켜진다.**  없는 장에서 켜지면 회귀다 —
  AL NOUF1 · SADARA · TC2 는 **한 행도** 안 붙는다 (실측 0).
* **2급은 사라지지 않는다.**  1급이 더해지는 것이다 (가산).
* **어느 등급에서 온 값인지 결과에 남긴다** — `result["evidence_tier"]`
  (지문 밖) · 행의 `evidence["tag"]`.
* **태그와 거리가 다른 답을 내면 그것이 발견이다.**  숨기지 않는다.

**★ 태그 형태를 코드에 박지 않는다.**  KKS·ISA·회사 규칙은 프로젝트마다 다르다.
대신 **쓰이는 방식**을 본다 — 28회차에 실측이 규칙을 **두 번** 좁혔다:

1. `글자+숫자 낱말` → AL NOUF1 의 `DN150`·`DN2400`(관경) **9행**이 태그가 됐다.
2. `두 검출 이상에 나오면 이름이 아니다` (두 항목이 같은 이름을 가질 수 없다)
   → 2행으로 줄었지만 **0 이 아니다** (우연히 한 검출에만 든 관경).
3. **`태그는 사고가 아니라 체계다`** — 같은 **모양**(글자·숫자 run 의 길이 열,
   `00GKB01CL001A` → `D2L3D2L2D3L1`)이 **두 검출 이상 · 두 장 이상**에서
   되풀이될 때만 태그로 본다.  → AL NOUF1 0 · SADARA 0 · TC2 0 · **UAD 128**.

실측 격차: UAD 으뜸 모양이 검출 **103개 · 14장** ↔ AL NOUF1 의 가장 흔한 모양이
**1개 · 1장**.  문턱이 아니라 구조다.

### §10-7 등급은 칸마다 따로 정한다 (29회차 [E-3])

태그는 **이름 · 순번 · 귀속**에는 1급이지만 **공급 주체에는 아무 말도 하지
않는다**.  "이 장이 1급" 이 아니라 **"이 장의 이 칸이 1급"** 이다.

| 칸 | 읽는 순서 |
| --- | --- |
| 이름 · 순번 · 기기 귀속 | 1급 태그 → 2급 기하 → 3급 사람 |
| **공급 주체(SCOPE)** | **2급 별표·NOTES → 3급 사람** (태그는 감사용) |
| 수량(승수) | 2급 승수표·NOTES → 3급 사람 |

### §10-8 높은 일치율이 인과는 아니다

계통코드 94% 일치는 **패키지 상자의 그림자**다 — 한 계통의 계기가 대개 같은
상자 안에 있어서이지 태그가 공급자를 말해서가 아니다.  갈리는 4계통에서 어느
쪽으로 맞춰도 **8행 중 4행을 틀린다**.  일치율로 규칙을 세우지 않는다.

### §10-9 글자가 글자가 아닐 수 있다 (28회차)

SHX 글꼴은 **획으로 플롯**되고 원문은 `AutoCAD SHX Text` 주석에 남는다.
`page.get_text()` 가 비었다고 도면이 글자를 안 가진 것이 아니다 (UAD p20:
텍스트 42 ↔ 선분 93,895 ↔ 주석 10,729).  **글리프 라이브러리 · 범례 ·
커넥터 · ISA 표 전부 이 경로를 봐야 한다.**

### §10-10 사각형은 심볼이 아니다 (29회차 [E-3])

둥근 버블의 **바깥 사각형 모서리는 빈 자리**이고 도면은 거기에 별표를 찍는다
(AL NOUF1 p55 렌더).  안/밖 판정은 **그려진 윤곽**으로 한다.  bbox 로 하면
모서리 별표를 "심볼 위" 로 읽어 SCOPE 를 바꾼다.
