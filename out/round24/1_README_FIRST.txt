P&ID 추출 도구 — 업데이트 꾸러미 r24
================================================================================
만든 날 2026-09-09 · 10 ~ 24 회차 누적


━━ 담긴 것
--------------------------------------------------------------------------------
    1_README_FIRST.txt        <- 먼저 읽기 (이 파일)
    2_FILE_LIST.txt           <- 파일 목록
    3_FILES_TO_OVERWRITE.zip  <- 덮어쓸 파일 (이것을 저장소 루트에 풉니다)

파일 이름은 전부 영문입니다 (r21 부터 — 알집에서 깨지지 않게).  내용은 한글입니다.
두 txt 는 UTF-8(BOM) 이라 메모장으로 바로 열립니다.


━━ 이 꾸러미는 무엇인가
--------------------------------------------------------------------------------
10회차부터 24회차까지의 변경을 한 번에 담았습니다.
앞의 꾸러미(r17 · r18 · r20 · r21 · r22)를 따로 넣을 필요 없습니다. 이것 하나면 됩니다.

바뀌는 파일 62개 — 새로 생기는 것 41개 · 덮어쓸 것 21개.
삭제하거나 이름을 바꿀 파일은 없습니다.


━━ ★ r22 에서 달라지는 것 (24회차)
--------------------------------------------------------------------------------
· ★★ 다른 회사 양식의 개정(REV) 을 읽습니다.
  개정 이력 표의 자리를 이제 **그 도면에서 잽니다** (전에는 AL NOUF1 좌표가
  코드에 박혀 있어, 종이가 다른 도면에서는 이력 표를 한 줄도 못 찾았습니다).
  실측: TC2 양식 60장의 REV 가 0장 -> **60장** 읽힙니다.

· AL NOUF1 은 **아무 것도 달라지지 않습니다.**
  1037행 · Q'ty 1931 · 지문 fb85b039 · 발주처 양식 885/76/23 ·
  정확도 축1 99.3%/70.5% · 축2 99.3%/94.4% — 전부 그대로입니다.
  (프로젝트 설정이 그 도면의 것이고 좌표를 적고 있으면, 프로그램은 잰 값을
   쓰지 않고 설정값을 그대로 씁니다.  그래서 기존 결과가 흔들리지 않습니다.)

· 새 프로젝트를 넣을 때 손으로 재야 하던 여섯 칸 중 **다섯이 없어졌습니다.**
  docs\new_form_checklist.md 의 "손으로 재야 하는 것" 이 그만큼 줄었습니다.
  남은 하나(hist_row_inset)는 도면이 그리는 값이 아니라 읽을 때 물리는
  여백이라 기본값(1.2)을 그대로 씁니다.

· 회귀 확인이 명령 하나가 됐습니다 (23회차):
      python3 spike/regression_3p.py
  세 프로젝트를 순차로 돌려 행 수 · 지문 · 정확도 · 식별률을 기준선과 견줍니다
  (약 17분).  ★ 메모리 때문에 **동시에 두 개를 돌리지 마세요** — 큰 문서 하나가
  7GB 를 씁니다 (16GB 이상 권장).


━━ r22 까지 들어 있던 것 (그대로입니다)
--------------------------------------------------------------------------------
· 서로 다른 회사 양식을 한 서버에서 섞어 분석해도 서로 망가뜨리지 않습니다 (22회차)
· 새 양식의 좌표를 도구가 뽑아 줍니다 — http://127.0.0.1:8000/jobs/<분석ID>/measured_config
· 분석이 실패하면 어디서 왜 멈췄는지 말합니다 (21회차)
· 발주처 양식이 전량(1037행)을 담습니다 — SCT 만 담으려면 config\project_alnouf1.yaml 의
  client_form.scope_filter 를 sct 로 (그러면 607/63/18)
· 첫 화면에서 저장된 프로젝트를 눌러 바로 열 수 있습니다 (재분석 불필요)
· 값을 고치면 저장되고 다음 개정본으로 이어집니다 (처음 고칠 때 이름을 묻습니다)
· SCOPE 열이 공급 주체를 구분합니다 (SCT / VENDOR(공급자 이름) / VENDOR)


━━ 넣는 법
--------------------------------------------------------------------------------
1. 서버가 떠 있으면 끕니다.
2. 3_FILES_TO_OVERWRITE.zip 을 저장소 루트(run.sh · start.bat 이 있는 폴더)에
   풀어 덮어씁니다.
3. 서버를 다시 켭니다.  기존 분석 기록은 그대로 열립니다.

★ app\_data\ 폴더(app.db · uploads)는 이 꾸러미에 **들어 있지 않습니다.**
  회사 PC 의 분석 기록과 올린 PDF 는 손대지 않습니다.


━━ 넣은 뒤 확인
--------------------------------------------------------------------------------
· 첫 화면에 기존 프로젝트가 그대로 보이는가
· 눌러서 열면 행이 그대로 보이고 고친 칸이 살아 있는가
· AL NOUF1 을 다시 분석하면 1037행 · Q'ty 1931 이 나오는가
· 시험: python -m pytest -q -m "not slow and not ui"   -> 215건 통과

문제가 있으면 결과 화면의 "진단 내보내기" 로 zip 을 만들어 보내 주세요.
