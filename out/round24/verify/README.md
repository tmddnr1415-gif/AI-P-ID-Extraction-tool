# 갈음 r24 — 적용 검증 (13단계)

`git worktree`(aac64ba) + `PID_DATA_DIR` 격리로 회사 PC 조건을 재현하고,
**꾸러미를 실제로 풀어** 확인했습니다.  ★ 실 DB(`app/_data/app.db`)는 어느
단계에서도 열지 않았습니다 — 시작·끝 sha256 동일.

| 단계 | 결과 |
|---|---|
| 1 aac64ba 격리 환경 | ✔ worktree `aac64ba` · `PID_DATA_DIR=/tmp/v24_data` |
| 2 옛 코드로 분석 | ✔ **824행** · 412.1초 · SCOPE `(빈칸) 822 · SCT 2` (회사 PC 조건) |
| 3 편집 5칸 | ✔ FIELD 5행의 description · 작성자 `검증자` |
| 4 DB 백업 | ✔ app.db · app.db-shm · app.db-wal |
| 5 꾸러미 풀기 | ✔ **62파일** · D·R 없음 · **푼 파일이 HEAD 와 바이트 동일 (다른 파일 0)** |
| 6 첫 화면 → 열기 | ✔ 프로젝트 1개 · **824행** · **편집 5/5 생존** |
| 7 첫 화면 버튼 | ✔ 눌러서 결과(1037행) → `#to-home` → `#main` 감춤=True · `#drop` 보임=True |
| 8 새 분석 | ✔ **1037행** · Q'ty **1931** · 지문 **fb85b039** · 436.7초 · 편집 **5/5 승계** |
| 9 발주처 양식 | ✔ **FIELD 885 / MOV 76 / BFV 23** — 서버가 만든 **실제 xlsx 를 열어 셈** |
| 10 두 축 | ✔ 축1 **99.3% / 70.5%** (FP 249 · FN 4) · 축2 **99.3% / 94.4%** (FP 35 · FN 4) |
| 11 시험 전량 | ✔ 빠른 **212 통과 · 3 건너뜀**(12.9초) · UI **24**(575.4초) · slow **14**(1343.5초) |
| 12 실 DB sha256 | ✔ `643748dc0efe…` — 시작·끝 **동일** |
| **13 ★ 21·24회차 수정** | **둘 다 통과 — 아래** |

## 11단계의 "3 건너뜀" 이 무엇인가

`tests/test_candidate_intake.py` 세 건입니다 — `out/candidates_nouf1.xlsx`
(발주처 문장이 들어 있어 저장소에 없는 파일)가 있는 기계에서만 도는 시험이고,
worktree 에는 그 파일이 없습니다.  **제품이 아니라 데이터의 유무**이고, 같은
코드가 본 저장소에서는 **215 통과**입니다.

## ★ 13단계 — 두 가지를 봅니다

### (a) 21회차 수정 — 이력 괘선이 촘촘한 양식에서 시끄럽게 멈추는가

이력 괘선 간격 2.0pt 짜리 합성 PDF(A1 12장, TC2 와 같은 갈래)를 올렸습니다.

| 묻는 것 | 결과 |
|---|---|
| `ValueError` 가 아니라 사유가 나오는가 | ✔ `app.pipeline.TitleBlockUnreadable` |
| 조용히 행 0개로 "성공" 하지 않는가 | ✔ `failed` · 행 **0** |
| 어디서 멈췄는지 말하는가 | ✔ `stopped_stage = reading title blocks` |
| 사유가 무엇을 하면 되는지 말하는가 | ✔ 칸 자리 · 쪽 크기 · 잰 값인가 설정값인가 · 할 일 |
| 빨리 멈추는가 | ✔ **5.0초** |

### (b) 24회차 수정 — 다른 양식의 REV 를 읽는가

worktree 에 푼 **그 코드로** TC2 를 읽었습니다 (`spike/hist_verify.py`):

```
설정값 그대로       이력 행 0 · REV 0/60
유도값 전체를 얹고  이력 행 7 · REV 60/60 'A' HIGH
```

## 꾸러미 무결성

| 파일 | 크기 | sha256 |
|---|---:|---|
| `PID_update_2026-09-09_r24.zip` | 638,709 | `1cfa7f6f73ded572d8ecb2b0e6098c65a79b417e67afb9227ce6bd2161ed7408` |
| `3_FILES_TO_OVERWRITE.zip` (안쪽) | 632,961 | `2eed9ad59404d5ae98454da596b0051dca857e8596b6bd00bf855be50cb61944` |

**`app\_data\` 검사는 두 번 했습니다** — 목록을 만들 때와 zip 에 담긴 이름을
훑을 때 (`spike/pack_handover_r24.py` 의 `forbidden()`).  둘 다 0건입니다.

## 캡처

| 파일 | 무엇 |
|---|---|
| `s7_1_home.png` | 7단계 — 첫 화면 (md5 `407485da45e4`) |
| `s7_2_rows.png` | 7단계 — 눌러서 연 결과 화면 1037행 (md5 `36fa5a5d471d`) |
| `s7_3_back.png` | 7단계 — `첫 화면` 버튼을 누른 뒤 (md5 `407485da45e4`) |

⚠ 첫째와 셋째의 md5 가 **같은 것이 통과 조건**입니다 — 첫 화면으로 정확히
돌아왔다는 뜻입니다.  가운데는 다릅니다.  (§8 의 "서로 다른 시점의 캡처는
바이트로 달라야 한다" 는 **진행 화면**의 규칙이고 여기서는 반대입니다.)
