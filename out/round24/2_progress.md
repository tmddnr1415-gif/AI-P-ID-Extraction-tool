# 24회차 진행 기록 — 이력 표 유도 (범용화 첫 구현)

## 목표선 (절대 불변)

| | 기준선 | 이번 회차 |
| --- | --- | --- |
| AL NOUF1 | `fb85b039` · 1037행 · Q'ty 1931 · 축3 94.8 · REV 58/58 | **재현** (행 1037 · `fb85b039` · 축3 94.8) |
| SADARA | 82행 · `0767ba79` · 축3 74.8 | **재현** (82 · `0767ba79` · 74.8) |
| TC2 | 607행 · `c46b10fa` · 축3 64.8 · REV 0/60 | **재현** (607 · `c46b10fa` · 64.8 · REV 0/60) |

## 단계

| 단계 | 상태 |
| --- | --- |
| [A] 기준선 3종 | **완료 — 셋 다 기준선과 같음** |
| [B] 실측 | 진행 중 — 캡션은 세 문서 다 인쇄돼 있다 |
| [C] 규칙 재작성 | |
| [D] 세 프로젝트 검증 | |
| [E] 회귀 | |
| [F] 출력 | |

## 기록

1. [B] **이력 표에는 자기 캡션이 인쇄돼 있고 `derive_layout` 이 이미 찾는다.**
   `CAPTIONS["history"] = ("REV.","DATE","DESCRIPTION")` 이 `_caption_rows` 에 있고,
   `derive()` 는 그것을 **찾았다고 `notes` 에만 적고 값으로 쓰지 않는다.**
   13·15·16·18·22회차와 같은 모양 — **없는 것을 만들기 전에 있는 것을 찾는다.**
2. ★ **그런데 그 캡션 찾기가 SADARA 에서 실패한다 — 0.1pt 반올림 때문이다.**
   `_column_lines` 는 y 중심을 `round(..., 1)` 로 묶는데 SADARA 는 같은 줄의
   `REV.` 중심 1828.092 ↔ `DESCRIPTION` 1827.912 로 **0.18pt** 어긋나 두 줄로
   쪼개지고, `words[:3] == CAPTION` 이 성립하지 않는다.  글자 높이는 10.62pt 이므로
   **같은 줄임이 분명하다.**  AL NOUF1 은 우연히 정확히 정렬돼 있어 통했다.
   허용치는 그 열 낱말 높이의 1/4 로 두고 잰다 (SADARA 2.47 · 상수 아님).
3. ★ **REV 를 읽는 갈래가 문서마다 다르다** (실측):
   AL NOUF1 GLYPH 48 + TEXT 10 · **SADARA TEXT 9/9** · **TC2 GLYPH 60/60 전부 `?`**.
   즉 SADARA 는 `rev_box` 텍스트로 읽어 **이력 표를 아예 쓰지 않는다** — 손입력
   `hist_*` 가 틀려 있어도(21회차 실측 이력 행 0행) REV 9/9 HIGH 가 나온 이유다.
   **이력 표 유도가 REV 에 닿는 문서는 AL NOUF1 과 TC2 뿐이다.**
4. ★ **글리프 사전의 표본 통로가 이력 표 하나뿐이다** (`build_glyph_library` 실측).
   문자를 배우는 길은 둘 — 타이틀블록에 **값 텍스트**가 있으면 `rev_box` 문자열이
   자기 글리프에 이름을 붙이고, 없으면 **이력 표 REV 칸의 한 글자**를 행 순서
   (아래부터 A·B·C)로 라벨한다.  덤으로 `sheet_box` 가 3글자(`1/1`)면 그 둘을 `1` 로.
   **TC2 는 유도된 `rev_box` 안에 캡션 `REV.` 만 있고 값 텍스트가 없다** (p1~p4 전수).
   `sheet_box` 도 `SCALE`·`SHEET` 캡션뿐이고 `1/1` 이 없다.  그래서 GLYPH 갈래로
   떨어지고 **라벨된 표본을 얻을 통로가 이력 표뿐**이다 —
   **이력 표 유도는 TC2 REV 의 필요조건이고, 충분조건인지는 그 칸에 획이 있는지가 정한다.**
5. ⚠ **앞 하네스가 TC2 단계에서 끊겼다.**  프로세스도 표식도 없었고 `TC2.json` 이
   23회차 것 그대로였다.  이 회차 규칙(결과 파일로 판정)이 그것을 잡았고, TC2 만
   다시 돌려 **607 · `c46b10fa` · 축3 64.8 · REV 0/60** 으로 기준선과 같음을 확인했다.
6. 캡션은 **세 문서 다 인쇄돼 있다** (허용치를 고친 뒤):
   SADARA `REV.` x2735.7~2757.9 · `DATE` 2794.2~2819.5 · `DESCRIPTION` 2938.7~3003.6
   TC2 `REV.` x967.0~974.8 · `DATE` 987.6~996.5 · `DESCRIPTION` 1038.7~1061.7

## [C]·[D]·[E] — 구현과 검증 (2026-09-09)

7. ⚠ **6번을 고친다 — 캡션은 앵커로 못 쓴다.**  `derive_layout` 의 캡션 찾기가
   실제로 `history` 를 찾는 문서는 **셋 중 TC2 하나**다 (`applied_rules.layout.notes`
   실측).  AL NOUF1 은 **58장 중 10장**만 텍스트로 인쇄하고 48장은 획으로 그린다.
   SADARA 는 인쇄하는데도 [B]2 의 0.1pt 반올림이 `derive_layout` 쪽에는 그대로라
   놓친다 (측정 도구만 고쳤다).  그래서 **모양(간격이 같은 띠)을 1차로 삼았다** —
   보정 프롬프트가 승인한 "캡션 앵커" 에서 벗어난 것이고 근거는 이 실측이다.
8. ★ **허용치 5% 는 머리글 행을 띠 안에 넣었고, 그것이 값이 아니라 이름을 밀었다.**
   `build_glyph_library` 가 머리글 행을 rank 0 으로 세어 개정 문자 `A` 를 `B` 로
   이름 붙였다 (TC2 60장 · SADARA 9장 전수).  세 문서 전 장에서 개정 행은 최대
   1.4% · 머리글 행은 최소 2.8% 이고 **그 사이가 비어 있다** (`spike/hist_gaps.py`).
   2% 로 바꾸니 AL NOUF1 의 유도 띠가 손입력 띠와 **같은 7행**이 된다.
9. **[D] 결과**: AL NOUF1 이력 칸만 유도값으로 → 글리프 사전 · 이력 행 7 · REV
   58/58 이 **한 칸도 안 다르다**.  SADARA 이력 행 0 → 7 (REV 는 TEXT 라 안 닿는다).
   **TC2 REV 0/60 → 60/60 'A' HIGH.**
   ⚠ 사전에 문자가 하나뿐이면 `classify` 의 2위가 없어 신뢰도가 언제나 HIGH 다 —
   값이 맞다는 근거는 신뢰도가 아니라 도면 두 곳(REV 칸 · 이력 행)이 같은 말을
   한다는 것이다.
10. **[E] 회귀 전량 통과** — AL NOUF1 1037·`fb85b039`·1931 · SADARA 82·`0767ba79`
   · TC2 607·`c46b10fa` · 축1 99.3/70.5 · 축2 99.3/94.4 · 양식 885/76/23 ·
   Description 40.5/68.8/19 · 빠른 **215** · UI 24 · slow 14 · 실 DB sha 불변.
   축3 은 셋 다 올랐다 (95.0 / 75.3 / **82.0**) — 예측 +16.7 대 실측 +17.2.
