P&ID 추출 도구 — 업데이트 꾸러미 r25
================================================================================
만든 날 2026-09-09 · 10 ~ 25 회차 누적 · 기준 커밋 aac64ba -> 84f90df


━━ 담긴 것
--------------------------------------------------------------------------------
    1_먼저읽기_README.txt   <- 먼저 읽기 (이 파일)
    2_파일목록.txt          <- 파일 목록 (새로 47 · 덮어쓰기 21 · 삭제 0 · 이동 0)
    3_덮어쓸파일.zip        <- 덮어쓸 파일 68개 (이것을 저장소 루트에 풉니다)

두 txt 는 UTF-8(BOM) 이라 메모장으로 바로 열립니다.
(r21 ~ r24 는 알집 때문에 영문 이름을 썼습니다.  이번 이름은 요청대로 한글입니다 —
 압축 프로그램에서 이름이 깨져 보이면 Windows 탐색기의 "압축 풀기" 로 푸세요.)


━━ 이 꾸러미는 무엇인가
--------------------------------------------------------------------------------
10 ~ 25 회차 누적.  앞의 것(r17 · r18 · r20 · r21 · r22 · r24)을 따로 넣을 필요 없습니다.
이것 하나면 됩니다.  회사 PC 는 지금 aac64ba(9회차) 이고 그 뒤 전부가 들어 있습니다.

바뀌는 파일 68개 — 새로 생기는 것 47개 · 덮어쓸 것 21개.
삭제하거나 이름을 바꿀 파일은 없습니다.


━━ ★ 크게 달라지는 것
--------------------------------------------------------------------------------
· 추출 정확도가 올랐습니다 — 당사 공급분 기준 정밀도 84.1% -> 94.4%
  (재현율 99.3% 그대로.  발주처 리스트와 대조한 값입니다.)
· 발주처 양식이 전량(1037행)을 담습니다.  벤더 공급분도 포함됩니다
  (SCT 가 Bulk material 을 공급하므로 물량 산출이 필요합니다).
  SCT 만 담으려면 config\project_alnouf1.yaml 의 client_form.scope_filter 를 sct 로
  바꿉니다 (그러면 885/76/23 -> 607/63/18).
· 첫 화면에서 저장된 프로젝트를 클릭해 바로 열 수 있습니다 (재분석 불필요, 1~3초).
· 결과 화면에서 첫 화면으로 돌아가는 버튼이 생겼습니다.
· 값을 고치면 저장되고 다른 팀원에게도 보입니다.  처음 고칠 때 이름을 묻습니다.
  고친 값은 다음 개정본 분석으로 이어집니다.
· 분석 기록을 삭제할 수 있습니다 — 되돌릴 수 없으니 자기가 만든 것만 지우세요.
· SCOPE 열이 공급 주체를 구분합니다 (SCT / VENDOR(이름) / VENDOR).
  이름은 그 장 NOTES 의 "BY <이름>" 에서 읽습니다.  NOTES 에 이름이 없으면 VENDOR 만 나옵니다.
· 분석이 실패하면 어디서 왜 멈췄는지 말합니다 (예외 원문은 "자세히" 를 눌러야 보입니다).
· 서로 다른 회사 양식을 한 서버에서 섞어 분석해도 서로 망가뜨리지 않습니다.
· 회귀 확인이 명령 하나입니다:  python spike\regression_3p.py  (세 프로젝트 순차 · 약 25분)

· AL NOUF1 의 결과는 이 꾸러미로 이렇게 됩니다 (검증에서 실측):
  1037행 · Q'ty 1931 · 지문 fb85b039 · 발주처 양식 885 / 76 / 23 ·
  정확도 축1 99.3% / 70.5% · 축2(SCT) 99.3% / 94.4%.


━━ ★ 다른 프로젝트 도면에 대해
--------------------------------------------------------------------------------
AL NOUF1 외의 도면(다른 종이 크기 등)도 분석이 시작됩니다.
다만 아직 식별률이 낮습니다 — 도면틀 치수 일부가 AL NOUF1 기준이기 때문입니다.
지금 그것을 하나씩 그 도면에서 읽도록 바꾸는 중입니다.

25회차까지 나아진 것:
· 개정(REV)을 다른 양식에서도 읽습니다 — 개정 이력 표의 자리를 그 도면에서 잽니다 (24회차).
  실측: TC2 양식 60장의 REV 가 0장 -> 60장.
· 별표(벤더 공급 표시)가 다른 형식에서도 잡힙니다 (25회차).
  TC2 는 별표의 획을 하나씩 따로 그려서 전에는 하나도 못 읽었는데, 이제 획의 관계로
  읽습니다.  실측: TC2 별표 0개 -> 71개, SCOPE 가 SCT 555 / VENDOR 52 로 갈립니다.
  (TC2 NOTES 에는 "BY <이름>" 이 없어 이름 없는 VENDOR 로 나옵니다.)
· 새 양식의 좌표를 도구가 뽑아 줍니다 — http://127.0.0.1:8000/jobs/<분석ID>/measured_config
  절차는 docs\new_form_checklist.md 입니다.


━━ ★ 메모리 — 반드시 읽으세요
--------------------------------------------------------------------------------
분석에 메모리를 많이 씁니다 (실측: AL NOUF1 7.9GB · 큰 도면 최대 9GB).
25회차의 별표 규칙이 장마다 선분 색인을 만들어서 전(4.8GB)보다 늘었습니다.
· 이 PC 메모리가 8GB 면 큰 도면은 분석이 매우 느리거나 실패할 수 있습니다.
  (이 꾸러미는 16GB 이상이라는 확인을 받고 만들었습니다.)
· 창을 여러 개 띄워 동시에 분석하지 마세요 — 프로그램은 하나씩 돌립니다.
· 분석 중에는 다른 무거운 프로그램(Excel 대용량 · 브라우저 탭 다수)을 닫아 두세요.
· 분석 시간은 AL NOUF1 58장 기준 약 12분입니다 (전 7분).


━━ 넣기 전에 — 백업 (건너뛰지 마세요)
--------------------------------------------------------------------------------
PowerShell 에서:

    cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
    $d = "$env:USERPROFILE\Desktop\pid_backup_" + (Get-Date -Format yyyyMMdd_HHmm)
    mkdir $d
    Copy-Item app\_data\app.db* $d\
    Copy-Item app\_data\uploads $d\ -Recurse
    Get-ChildItem $d

확인 — 네 가지가 다 있어야 합니다: uploads · app.db · app.db-shm · app.db-wal
(app.db-shm · app.db-wal 은 서버가 켜져 있을 때만 있을 수 있습니다.  서버를 끈 뒤에는
 app.db 하나로 합쳐지므로 그때는 uploads · app.db 둘이면 됩니다.)


━━ 넣는 순서
--------------------------------------------------------------------------------
1. 쓰는 사람이 없는지 확인 (터미널 로그에 다른 IP 가 조용한지).
2. 서버 중지 — 터미널에서 Ctrl + C.
3. 3_덮어쓸파일.zip 을 저장소 루트(run.sh · start.bat 이 있는 폴더)에 풀어 덮어씁니다.
   "모두 덮어쓰기" 를 고릅니다.  68개가 들어갑니다.
4. 삭제할 파일 없음.  이름이 바뀐 파일도 없습니다.
5. 서버 시작:
        cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
        $env:PYTHONUTF8 = "1"
        .\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
6. 브라우저에서 Ctrl + Shift + R (캐시를 버리고 다시 읽기).

★ app\_data\ 폴더(app.db · uploads)는 이 꾸러미에 들어 있지 않습니다.
  회사 PC 의 분석 기록과 올린 PDF 는 손대지 않습니다.  기존 기록은 그대로 열립니다.


━━ 넣고 나서 확인
--------------------------------------------------------------------------------
1. 첫 화면에 기존 프로젝트가 보이는가.
2. 클릭해서 열리는가 — 824행이 나옵니다 (옛 분석본이라 정상).  고친 칸도 그대로입니다.
3. 새로 분석하면 1037행 · 약 12 ~ 30분 (메모리에 따라 다릅니다).
   ※ 824 -> 1037 로 늘어나는 것이 정상입니다 (TW 15행 · 벤더 공급분 등이 들어갑니다).
   ※ 재분석해야 새 값이 적용됩니다.  옛 분석본은 옛 값 그대로입니다.
4. 시험:  .\.venv\Scripts\python.exe -m pytest -q -m "not slow and not ui"
   -> 216 통과 · 3 건너뜀 (건너뛴 셋은 out\candidates_nouf1.xlsx 가 있는 PC 에서만 돕니다)


━━ 아직 남은 것 (물어보실 수 있어 적어 둡니다)
--------------------------------------------------------------------------------
· 같은 도면번호를 두 장이 쓰는 건 16행 — 어느 장이 유효한지 실무 판단 대기
· PI 10행 (00PAB10 세 장) — 발주처 회신 대기
· p8 MOV — 우리 6행 <-> 발주처 CZH 5행
· p17 PRV · p12 PCV — 별도 회차
· AIT — 표기 준비됨.  발주처 리스트에 A 계열이 0행이라 미적용
· 미판정 심볼 169건 — 등록 화면에서 사람이 지정합니다
· 다른 형식 도면의 Q'ty · REV · 도면번호 일부 — 다음 회차들에서 순서대로
· 메모리 — 별표 색인이 쓰는 3GB 를 줄이는 회차가 후보입니다


━━ 되돌리려면
--------------------------------------------------------------------------------
1. 서버 중지 (Ctrl + C).
2. 코드를 aac64ba 로 되돌립니다 (덮어쓴 21개가 원래대로 돌아갑니다):
        cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
        git checkout aac64ba -- .
3. 새로 생긴 47개를 지웁니다 (2_파일목록.txt 의 [A] 줄이 그 목록입니다):
        Get-Content "<꾸러미를 푼 폴더>\2_파일목록.txt" -Encoding UTF8 |
          Where-Object { $_ -like "[[]A] *" } |
          ForEach-Object { $_.Substring(4) } |
          ForEach-Object { if (Test-Path $_) { Remove-Item $_ -Force } }
   (지우지 않아도 옛 코드는 그 파일을 읽지 않습니다 — 남겨 두어도 동작에는 지장이 없습니다.)
4. 백업한 app\_data 를 되돌립니다:
        Remove-Item app\_data\app.db* -Force
        Copy-Item "<백업 폴더>\app.db*" app\_data\
        Remove-Item app\_data\uploads -Recurse -Force
        Copy-Item "<백업 폴더>\uploads" app\_data\ -Recurse
   ★ 새 코드로 만든 분석 기록(1037행짜리)은 옛 코드가 열지 못할 수 있으므로,
     되돌릴 때는 백업본으로 바꾸는 것이 안전합니다.
5. 서버 시작.


━━ 문제가 생기면
--------------------------------------------------------------------------------
터미널 화면을 사진으로 찍어 둡니다.  결과 화면까지 갔으면 "진단 내보내기" 로 zip 을
만들어 함께 보내 주세요 (원본 PDF · 발주처 Excel 은 그 zip 에 들어가지 않습니다).
