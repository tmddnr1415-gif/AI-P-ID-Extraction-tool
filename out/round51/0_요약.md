# 51회차 요약 — 요구 20종 전수 · 축4 신설

**★ AL NOUF1 PDF 를 사용자가 첨부해 처음으로 실측했습니다.**
(`D00P_PID_Total_20251125.pdf` · 58쪽 · sha256 `883f75aa805f480f…` — 저장소의
`out/round41/verify/db_backup/uploads/0f7cde48ec7a_pid_total.pdf` 와 **바이트 동일**)

## 한 줄

**50회차가 보고한 1069 는 계산값이었고 실측은 1037 이었습니다.**  원인 둘을
고쳐 이제 **실측 1069** 입니다.  그리고 요구 20종 중 **일곱(TG·PG·SG·PV·LV·
TV·FV)은 네 도면 어디에도 인쇄돼 있지 않다**는 것을 세 채널로 증명했습니다.

## [A] 기준선 실측

| | 행 | 지문 | Q'ty | 축3 | 초 · RSS |
| --- | ---: | --- | ---: | ---: | --- |
| 51회차 전 (`derive_from_isa` 켬) | **1037** | `fb85b039` | 1931 | 88.6 | 599 · 4.9G |
| **51회차 (고친 뒤)** | **1069** | **`117b2d61`** | **2022** | **94.6** | 606 · 4.9G |
| `derive_from_isa: false` | 1037 | `fb85b039` | 1931 | 94.2 | 598 · 4.9G |

**50회차 계산값 1069 · 내역(PP 8 · ZSC 7 · ZT 6 · ZS 5 · AIT 4 · AT 2)은
맞았고, 그 32개가 행이 되지 않는다는 것만 몰랐습니다.**

⚠ **축1·축2 는 못 쟀습니다** — `data/*.xlsx`(발주처 리스트 셋)가 없습니다.
사용자가 정한 *"축2 85% 아래면 멈춘다"* 는 회사 PC 에서 확인해야 합니다.
⚠ SADARA·TC2·UAD 는 PDF 가 없어 못 돌렸습니다.

## [B] 요구 20종 — ㉠ 13종 · ㉡ 0종(해결) · ㉢ 7종

| 갈래 | 종 | |
| --- | ---: | --- |
| ㉠ 인쇄돼 있고 행도 있다 | **13** | PIT · TIT · TI · PI · FE · FIT · LIT · LS · MOV · XV · PCV · **AIT** · TW |
| ㉡ 인쇄돼 있는데 행이 없다 | **0** | `AIT` 가 마지막이었고 이번 회차에 섭니다 |
| ㉢ 인쇄돼 있지 않다 | **7** | **TG · PG · SG · PV · LV · TV · FV** |

**㉢ 를 세 채널로 증명했습니다** — ① 네 프로젝트 버블 안 낱말 목록 **0건** ·
② AL NOUF1 인쇄 낱말 19,322개(종류 618) 중 **0건** · ③ `PV-101` 처럼 숫자를
붙인 꼴 **0건**.  이 도면들은 게이지를 `PI`·`TI` 로, 제어 밸브를 `PCV`·`LCV`
·`TCV`·`FCV` 로 인쇄합니다.

`AIT` 는 `Analyzer` 로 나갑니다 (AL NOUF1 6 = AIT 4 + AT 2).

## [C] 축4 (완전 추출) — 신설

```
분모 = 버블이 선 계기 태그 + 그 도면이 버블에 인쇄한 밸브 태그
분자 = 그중 행이 된 것
```

| 프로젝트 | 축4-계기 | 축4-밸브태그 | 축4 |
| --- | --- | --- | --- |
| **AL NOUF1 (51회차 실측)** | **917/928 = 98.8%** | **84/186 = 45.2%** | **89.9%** |
| AL NOUF1 (51회차 전) | 885/928 = 95.4% | — | — |
| SADARA · TC2 · UAD (저장분) | 100.0% · 92.4% · 95.3% | (미측정) | — |

**밸브 쪽 미달 102 중 68이 `PSV`** 입니다 (안전밸브 — 발주처 산출물 범위 밖 ·
23회차 · 요구 20종에도 없음).  **진짜 미검출은 32건**이고 그중
`PCV` 4 · `FCV` 3 · `LCV` 3 이 **사용자가 물은 제어 밸브**입니다.

## [D] 고친 것 둘

1. **같은 질문에 답하는 곳이 둘이고 갈렸습니다.**  `detect()` 는 인쇄된
   낱말을 그대로 쓰는데, **파이프라인이 행을 만들 때 보는**
   `detect_all.excel_type_under()` 는 사전 dict 를 그대로 봐 `None` 을 냈고,
   `None` 이면 행이 되지 않습니다.  사전은 그대로 **이름 붙이기**의 권한이고,
   사전이 답하지 못할 때만 **그 검출이 들고 있는 근거**로 가릅니다 —
   낱말 목록이 아닙니다.
2. **ISA 표가 평범한 낱말을 분해합니다** (`DN` 129 · `GT` 112 · `VS` 96 ·
   `SCT` 88 · `HRSG` 84 · `LINE` 20 …).  행은 안 되지만(버블 0) 미판정 목록이
   **87 → 1164** 가 되고 축3 이 94.2 → 88.6 이 됩니다.  **유도된 낱말은 버블이
   섰을 때만 셉니다** — 표가 `DN` 을 분해한다는 것은 `DN` 이 계기라는 증거가
   아니고, **버블이 증거**입니다.

## [E] 회귀

* **움직인 칸을 칸 단위로 규명했습니다** — 기존 1037행 중 지문 대상 열에서
  움직인 것은 **`needs_review` 8개뿐**이고 전부 p11 FIT·FE 입니다.
  32행이 늘며 문서 단위 90분위 `distance_limit` 이 옮겨가 583.9pt 짜리 기기
  후보가 잘렸습니다 (40회차 [B]·48회차 [5] 의 감도 · 실무 판단 17).
* **되돌림 확인** — `derive_from_isa: false` 로 두면 **1037 · `fb85b039` ·
  1931 · 94.2** 로 정확히 돌아옵니다.
* **새 행 렌더 확인** — `AIT` p16 네 버블은 사용자 화면의 그 넷이고,
  `ZSC`/`ZSO`/`XV` 는 차단 밸브의 위치 스위치 무리입니다.
* 시험 **381 → 398** (새 6 + `data/pid_total.pdf` 가 생겨 11건이 더 돕니다).
  `tests/test_project_delete.py` 7건은 이 컨테이너에 `httpx2` 가 없어 나는
  **기존 실패**입니다.

## ★ "찾고 있는데 안 쓰던 것" 19번째

`detect()` 는 밸브 태그 낱말도 버블로 검증해 `category="VALVE"` 검출을
만들어 왔는데 **파이프라인이 받아서 버렸습니다.**  축4 의 밸브 분모가 바로
그것입니다.  18회차 `unmapped`·`unverified` 와 **같은 자리**입니다.

## 20종 중 아직 못 뽑는 것 — 0종.  다만

요구 20종은 **도면이 인쇄한 것은 전부** 나갑니다.  남은 것은 종이 아니라
**건**입니다:

| 남은 것 | 건 | 무엇이 있으면 풀리나 |
| --- | ---: | --- |
| `PSV` | 68 | **발주처 판단** — 안전밸브를 셀 탭이 없습니다 |
| 제어 밸브 미검출 (`PCV` 4 · `FCV` 3 · `LCV` 3 · `TCV` 5 · `XV` 3 · `HV` 3 · `MOV` 11) | 32 | 밸브 **몸체** 검출 (태그는 읽었는데 몸체를 못 잡습니다) |
| `ZSO` | 11 | 범례 SUCCEEDING 표에 `O` 가 없습니다 — **도면이 말하지 않습니다** |
| 버블 기하 실패 (`PIT` 15 · `MOV` 8 · `PDIT` 5 · `RO` 4 …) | 44 | 50회차 `bubbles_matched` 로 `0`/`2 이상`이 갈립니다 — 다음 회차 |
