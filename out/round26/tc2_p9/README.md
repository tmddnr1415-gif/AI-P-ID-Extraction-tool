# TC2 9쪽 (`D02J-31LBA30-M05-0001`) — 26회차 코드의 분석 화면

저장된 회귀 결과(`out/regression_3p/TC2.json`, 지문 `536d3baa`)로 화면용 DB 를
세워(재분석 없음) `PID_DATA_DIR=/tmp/tc2ui_data` 로 띄운 실제 화면입니다.
**실 DB(`app/_data/app.db`)는 열지 않았습니다** — sha256 `643748dc0efe…` 시작·끝 동일.

| 파일 | 무엇 |
| --- | --- |
| `0_첫화면.png` | 첫 화면 (프로젝트에 안 묶인 분석으로 보입니다) |
| `1_전체.png` | 쪽 9 를 고른 화면 전체 |
| `2_도면.png` · `3_표.png` | 왼쪽(도면) · 오른쪽(표)만 |
| `4_표_p9.png` | 그 도면번호로 거른 표 — SCOPE 열이 보이게 가로로 밀었습니다 |
| `5_근거.png` | VENDOR(SE) 행을 눌러 연 근거 패널 |

## 그 쪽이 낸 것

* 16행 — **SCOPE `VENDOR(SE)` 10 · `SCT` 6** (25회차까지는 16행 전부 `SCT`)
* TYPE TIT 5 · TW 4 · PIT 4 · LS 3

## ⚠ 왼쪽 도면의 색이 전부 빨강인 것은 별표와 무관합니다

오버레이 범례는 `SCT 0 · VENDOR 0 · 검토 필요 16` 입니다.  `_layers` 에서
`needs_review` 가 색 우선순위를 이기고, 그 쪽 16행은 전부 검토 필요이며 사유는
하나입니다 — 유닛코드 `31` 이 승수표에 없어 Q'ty 를 못 매깁니다.  TC2 에는
`UNIT IDENTIFICATION NUMBERS` 표가 60장 어디에도 없으므로 **도면이 답하지 않는
값**이고, 발주처 확인 대상입니다 (26회차 보고서 §3 · §9).

캡처 절차는 `spike/build_ui_db.py` 와 같은 방식이고, 이 폴더의 PNG 는
`.gitignore` 대상입니다 (도면 내용이 들어 있습니다).
