# 35회차 보고서 — 상수를 전부 지우고 AL NOUF1 을 재현한다

**시험이지 수정이 아니다.**  본선 코드는 0줄 · 본선 네 프로젝트 지문은 시작·끝 동일(§A).
엔진 수정은 전부 worktree **`/tmp/r35_strip`** (본선 `1b967ee` 에서 딴 것 · 남겨 두었다 · `out/round35/worktree.diff`).

## 한 줄

**"외워둔 값" 199개 중 61개는 지워도 AL NOUF1 이 `fb85b039 · 1037 · 1931` 그대로 나온다.  나머지 138개 중
26개는 유도가 없어 멈추고(㉣), 91개는 도면에 없는 값이며(㉣-D · 사람·발주처), 25개는 이 방법이 닿지 않는
자리(`anchors` — import 때 굳는 표 = 결함)이고, 19개는 상한 안에 개별로 재지 못했다.
㉢(유도가 대신했는데 지문이 다름)는 0 · ㉤(None 이 조용히 흘러 틀린 값)도 0.**

## A. 본선 확인 — 시작·끝

| | AL NOUF1 | SADARA | TC2 | UAD |
|---|---|---|---|---|
| 기준선 | `fb85b039` · 1037 · 1931 | `0767ba79` · 82 | `c0a2d29d` · 607 | `db1a77d1` · 149 |
| 시작 (`8_regression_4p_start.json`) | 같음 | 같음 | 같음 | 같음 |
| 끝 (`8_regression_4p.json`) | __END_ALNOUF__ | __END_SADARA__ | __END_TC2__ | __END_UAD__ |

## B. 지운 목록 — `3_strip_list.md`

`out/constants_by_source.md`(25회차) 의 A·B·C·D 전부 = dataclass 39 + config 160 = **199**, 항목 단위.  E(알고리즘·UI)와
✔(이미 유도)는 남긴다.  25회차의 193 과 다른 것은 config 잎이 210 → 216 으로 늘었고(27·31회차 `qty_note` 등) 그 표가 묶음 단위였기 때문이다.
사용자 확인: "목록은 고치지 않는다" → 그대로.  `project.code` 는 상수가 아니라 프로필 **열쇠**인데 남기면 `_fit_layout` 이
"이 문서의 프로필" 로 보고 유도값을 안 얹으므로, 표식 문자열로 바꿔 낯선 프로필 경로를 강제했다 (+1 = 200).

## C. 지우기 — sentinel · 유도 뒤에

* 지운 자리는 0 이나 빈 문자열이 아니라 **`Missing(name)`** — 값으로 쓰는 순간(`bool`·`float`·비교·순회·`getattr`) `MissingConstant` 로
  멈추고 이름을 말한다.  `==`·`hash`·`repr`·`deepcopy` 는 조용하다 (dict 순회·로그가 그것을 쓴다).
* dataclass 기본값 39 는 `_M("ds.field", 원값)` 으로, config 잎 160 은 `projectconfig.load()` 뒤 · **`_fit_layout` 이 얹은 뒤**에
  `strip_config` 로.  조립 함수의 변환기(`float/tuple/int/re.compile`)는 sentinel 을 통과시킨다.
* sentinel 패치 자체는 중립이다 — 상수를 하나도 안 지운 worktree 가 탐침 10장에서 본선과 같은 `199 · 0087c236 · 398` 을 낸다.

## D. 재현 실행 — `4_replay.md`

* **1단계(import 시점 지우기)는 버렸다** — 20회 상한을 몇 초 만에 썼고 전부 내 변환기에서 터진 것이었다.
* **2단계(유도 뒤 지우기)** — 1차 3 + 2차 20 = **23개를 되살리고도 멈춘다** (다음 차례 `qty_note.same_words`).  2차는 런타임 D 표 91개
  (description·excel·scope·… 지문에 안 닿고 잎 하나씩 읽혀 상한을 정보 없이 쓴다)를 덩이로 미리 되살렸다.
  되살린 순서: `tb.zoom · formats.drawing_no · formats.date · ds.cap_span · ds.side_slack · vendor_marks.blob_span · cluster_gap ·
  glyph_span · note_line_gap · ds.mark_side · vendor_marks.above · broken_line.brk_max_mark · ds.brk_min_marks · brk_max_gap ·
  brk_bridge · brk_min_span · package_box.edge_cover · sct_scope.text_tol · ds.brk_corner_tol · package_box.mark_margin ·
  ds.anchor_slack · sct_scope.drop_x_tol · drop_end_tol`.
* **묶음 토글 11회**(전부 되살리고 한 묶음만 지움) 로 남은 것을 갈랐다 — 완주 8 · 멈춤 3 (`qty_note` · `dv` · `valves.actuator_*`).
* **★ 전량 58장 2회**: ㉠/㉡ 61개를 지운 채 **`fb85b039 · 1037 · 1931`** · 아무것도 안 지우고 유도 overlay 만 **`fb85b039 · 1037 · 1931`**.
  낯선 프로필 경로가 얹는 19칸(`regions.*`·`title_block.*` 15 · `formats.revision` · `box_from_both_edges None→True` …)은 지문을
  한 칸도 안 옮긴다.  ⚠ 지문 밖 열(REV 의 `A1` 장 · Description)은 기록하지 않았다.

## E. 차이 분류 — `5_classified.md`

| 판정 | 개수 | 무엇 |
|---|---:|---|
| ㉠ 영향 없음 | 18 | `ds` 기본값 11 은 **config 잎이 덮는 죽은 값**(같은 수가 두 곳) · `qty_note` 2 는 코드 기본값과 같음 · `x_slack`·`note_row_tol`·`side_tol`·`title_line_tol`·`scope_box_both_edges` |
| ㉡ 유도가 대신 · 지문 같음 | 20 | `valves.legend_fallback` 15 + `unit_multiplier_fallback` 3 (범례 p2·p3·p5 가 답해 폴백이 읽히지 않는다) · `formats.revision` · `vendor_marks.glyph_sizes` (둘은 `_fit_layout` 이 얹어 **지운 적이 없다**) |
| ㉢ 유도가 대신 · 지문 다름 | **0** | — (`brk_max_mark` 는 유도가 18.6 을 재고 스스로 버려 ㉣) |
| ㉣ 유도 없음 → 멈춤 | 26 | 되살린 23 + `qty_note.same_words`(다음 차례) + 묶음 멈춤 `dv.seg_span` · `valves.tag_reach`.  등급 A **9** · B 1 · C 15 · ? 1 |
| ㉣-D 도면에 없음 | 91 | description 49 · excel 23 · valves 어휘 8 · scope/revision/client_form/supplier_interface_span/multi_signal_bundle 11 |
| ㉤ None 이 조용히 흘러 틀린 값 | **0** | sentinel 이 흐름 자체를 막는다 — 이 방법에서는 ㉤ 가 ㉣ 로 보인다 (한계) |
| 시험불가 | 25 | `anchors.*` — import 때 `_V3_FIELD_TYPE_MAP`/`RULESET_V3` 로 굳어 유도 뒤 지우기가 닿지 않는다 |
| 미실측 | 19 | `dv` 14 · `valves.actuator_reach/offaxis` · `formats.unit_code_*`/`system_code_chars` 3 — 상한 안에 개별로 못 잼 |
| 열쇠 | 1 | `project.code` |

**"찾고 있는데 안 쓰던 것" — 이 회차 실측 9** (㉣ 중 A 등급): `ds.cap_span`(범례 버블) · `ds.brk_max_gap/brk_bridge/brk_min_marks/
brk_min_span` + `broken_line.brk_max_mark`(범례 p2 선 종류 표 — 유도가 재고 UNAVAILABLE 로 버린다) · `dv.seg_span`(범례 몸체) ·
`tb.zoom`(글자 높이는 잰다) · `formats.drawing_no`(칸은 유도).  25회차가 A 로 적은 19개(폴백 18 + `glyph_sizes`)는 **이미 쓰고 있었다**(㉡)
— 그것들은 "안 쓰던 것" 이 아니라 "범례가 없을 때의 조용한 폴백" 이다.

## F. 남은 일 — `6_remaining.md` · `7_human_vs_rule.md`

| 목록 | 개수 | 다음 회차가 하는 일 |
|---|---:|---|
| 삭제 | 38 | ㉠ 18 + 폴백 18 + `glyph_sizes` + `project.code`(실험 장치).  폴백을 비우면 범례 없는 개정본이 **시끄럽게** 멈춘다 (§9 4 · 15회차 보류 항목의 답) — 적용은 실무 판단 |
| 조사 | 20 | 미실측 19 + `formats.revision`(유도 `^[A-Z]$` 가 `A1` 장 REV 를 바꾸는지 58장 대조) |
| 유도 구현 | 26 | ㉣ 26 — A 9 부터 (코드가 이미 잰 값을 잇는다), C 15 는 본문 실물 분포, B 1(`formats.date`) |
| 사람 지정 | 91 | ㉣-D — 프로필·사람 지정 화면 (15·18·31회차 구조) |
| 결함 | 25 | `anchors` 표가 `_rebind_config` 밖에 있다 — 유도든 overlay 든 닿을 수 없다 (22회차 격리가 못 본 자리) |
| 합 | **200** | |

㉮ 어휘·문형 **84** (description 49 · anchors 25 · valves 어휘 6 · qty_note 1 · multi_signal_bundle 2 · vendor_description) ·
㉯ 범례·구조 **45** (기하 허용치 · 형식 정규식 · zoom) · ㉰ 도면에 없음 **33** (양식 23 · 실무 판단 10).  ㉮ 는 **LLM 이든 규칙이든 다음 회차**다 —
이 회차는 개수만 냈다.

## 스스로 뒤집은 판단 (4)

1. **import 시점 지우기 → 유도 뒤 지우기.**  1단계 20회가 몇 초 만에 다 찼고 전부 내 변환기였다.  기록은 남기고 판정에 안 쓴다.
2. **사전 복원 2단계를 시도했다가 버렸다** — 유도 뒤 지우기가 되면 필요 없다.
3. **런타임 D 표 91개를 덩이로 되살렸다** — 하나씩이면 상한을 정보 없이 쓴다.  개별 실측은 1개(`description.unit_prefix_skip_codes`)뿐이고 그렇게 적었다.
4. **"유도가 옮긴 키만 안 지운다" → "유도가 답한 키는 값이 같아도 안 지운다".**  58장 첫 시도가 `glyph_sizes` 에서 멈췄다 — 10장에서는 유도값이
   설정과 달라 살았고 58장에서는 같아서 지워졌다.  판정은 "유도가 답하는가" 이지 "유도가 설정을 바꾸는가" 가 아니다.
   (그리고 §8 의 `pgrep -f` 자기참조를 `pkill -f` 로 또 겪었다 — 자기 셸을 두 번 죽였다.)

## 정밀도 — ㉢ 가 유도 쪽으로 풀리면

㉢ 가 0 이므로 **변화 없음** — 축2 99.3 / 94.4 그대로다.  유도 overlay 19칸도 지문을 안 옮기므로 유도로 가도 정밀도는 같다.
정밀도를 움직일 수 있는 것은 ㉣ 26 의 유도 구현이고, 그것은 이 회차 밖이다.

## 멈춘 곳

[D] 되살리기 상한 **20** 에서 멈췄다 (시작 07:25 → 09:50 · 2시간 25분 · 되살린 23 = 1차 3 + 2차 20).  그 뒤의 묶음 토글·58장 확인·[E]~[G] 로
**4시간을 넘겼다** (중간에 작업 환경이 재부팅되어 58장 실행을 다시 돌렸다).  넘긴 부분은 되살리기가 아니라 [D] 가 요구한 최종 지문과 분류·보고다.

## 이 방법의 한계 (다음 회차가 알아야 할 것)

* **sentinel 은 ㉤ 를 못 본다** — None 이 흘러 틀린 값을 내는 자리가 있어도 sentinel 은 그 자리에서 멈추므로 ㉣ 로 보인다.  ㉤ 0 은 "없다" 가 아니라 "이 방법이 못 잰다" 다.
* **import 때 굳는 표는 닿지 않는다** — `anchors` 25.  같은 성격의 자리가 더 있을 수 있다(`_rebind_config` 이 다시 만드는 것의 목록이 곧 경계다).
* 탐침 10장의 ㉠ 는 58장에서 확인했지만(61개 지워도 같음), **지문 밖 열**은 확인하지 않았다.

worktree: **`/tmp/r35_strip`** (남김 · 본선 `1b967ee` + `app/engine/missing.py` + 5파일 패치 · `worktree.diff`).
