# 갈음 꾸러미 실제 적용 검증 — 회사 PC 조건 재현

`handover_final.zip` 을 **aac64ba + 팀원 데이터가 든 DB** 위에 실제로 풀고
서버를 띄워 확인한 기록.  화면은 전부 실제 렌더다 (합성·목업 없음).

**격리** — 실 DB(`app/_data/app.db`)는 열지도 않았다.
sha256 `df9875e5…87d2324` 는 작업 전후 같다.

| | |
| --- | --- |
| 옛 코드 | `git worktree` @ aac64ba (검증 뒤 제거) |
| 데이터 | `PID_DATA_DIR=<scratchpad>/pack/site/_data` (그 자리에서 만든 824행 DB) |

## 절차와 결과

| 단계 | 한 일 | 결과 |
| --- | --- | ---: |
| 1–3 | 옛 코드로 분석 + 손편집 3칸 | 824행 · 지문 `91ba3e33` · 편집 3 |
| 4 | DB 백업 | 32MB |
| 5 | zip 을 덮어씀 | 24파일 (수정 17 · 신규 7) · 전부 HEAD 와 바이트 동일 |
| 6 | 수동 삭제 | **0개** (삭제·이름변경이 없음) |
| 7 | 마이그레이션 | `db.connect` 가 자동 · 6열 추가 · 행 824→824 · 무결성 ok |
| 8 | 서버 기동 | 1.0초 · 예외 0 · JS 오류 0 |
| 9 | 첫 화면 | `회사PC재현` 이 목록에 뜸 (`d9_home.png`) |
| 10 | 옛 프로젝트 클릭 | **824행이 0.67초에 열림** · 재분석 없음 (`d10_old_rows.png`) |
| 11 | 같은 PDF 재분석 | 326초 · **1029행 · 지문 `69ad281f`** (`d11_after.png`) |

## 캡처

| 파일 | 무엇 |
| --- | --- |
| `d9_home.png` | 반영 직후 첫 화면 — 옛 프로젝트가 `개정 판별 불가` 배지와 함께 |
| `d10_old_rows.png` | 옛 824행이 새 코드에서 열린 화면.  SCOPE 열은 빈칸 |
| `d11_progress_1.png` · `_2.png` | 재분석 진행 (md5 가 서로 다름 — 12회차 회귀 기준) |
| `d11_after.png` | 재분석 뒤 1029행 · TW 행 보임 · p6 범례 SCT 18 / VENDOR 28 |
| `d11_home_after.png` | 첫 화면이 `도면 Rev.D` 로 바뀜 |

## 실측 (`verify_*.json` 이 원본)

- 옛 결과의 SCOPE: 빈칸 **822** · SCT 2 → 발주처 양식은 **전부 담김**
  (`legacy_no_scope_rows: 822`)
- 재분석 뒤: `legacy_no_scope_rows: 0` · FIELD **667** · MOV **66** · BFV **22**
  (11회차 기준선과 같음) · 양식 밖 249
- 손편집 3칸은 재분석을 건너 그대로 남음.  Q'ty 합계는 **엔진층 1911**
  (기준선) / 화면값 1912 (편집 +1) — 두 층이 갈려 있다는 증거
- 13회차 이전 편집의 작성자는 **빈칸**이고 화면이 "이름 없음" 이라 적는다.
  없는 이름을 지어내지 않는다
