회사 PC 업데이트 꾸러미 — 2026-09-07 · 18회차까지 누적 (최종본)
=========================================================================

이 zip 안에 세 개가 있습니다.

  1_README_FIRST.txt      ← 지금 읽는 이것.  넣는 순서와 되돌리는 법
  2_FILE_LIST.txt         ← 덮어쓰는 파일 40개의 이름·크기·해시·이유
  3_OVERWRITE_FILES.zip   ← ★ 이것만 저장소 루트에 풀면 됩니다

  ※ 3_OVERWRITE_FILES.zip 안에는 **덮어쓸 40개 파일만** 들어 있습니다
    (2_FILE_LIST.txt 과 한 줄도 다르지 않게 — 기계가 확인합니다).
    안쪽 파일 이름은 전부 영문이라 어느 압축 프로그램에서도 안 깨집니다.

  ※ 파일 이름을 영문으로 둔 이유 — 한글 이름은 압축 프로그램에 따라
    `1_?쉽??쎄린_README.txt` 처럼 깨져 보입니다.  내용(한글)은 그대로입니다.


꾸러미  PID_update_2026-09-07_r18final.zip   (10 ~ 18 회차 **누적**)
대상    C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool\
기준    회사 PC 현재 커밋 aac64ba → bb3799d

★ 이것 하나만 풀면 됩니다.
  이전 꾸러미(handover_10 · 11 · 12 · r15 · r17 · r18)를 따로 넣지 마세요.
  그 꾸러미들의 파일이 하나도 빠지지 않았음을 기계가 확인했습니다.


■ ★ 이번에 크게 달라지는 것 — 이 다섯 줄만 읽어도 됩니다
=========================================================================

  1. **발주처 양식이 전량(1037행)을 담습니다.**  벤더 공급분도 들어갑니다 —
     그 계기를 당사가 공급하지 않아도 설치에 필요한 Bulk material 은
     당사(SCT)가 공급하므로 물량을 세야 하기 때문입니다.
     SCT 만 담으려면 config\project_alnouf1.yaml 의
         client_form:  scope_filter: all  →  sct
  2. **첫 화면에서 저장된 프로젝트를 눌러 바로 열 수 있습니다** (재분석 불필요).
  3. **결과 화면에서 첫 화면으로 돌아가는 버튼**이 생겼습니다 (왼쪽 위).
  4. **값을 고치면 저장되고 다른 팀원에게도 보입니다.**
     처음 고칠 때 이름을 묻습니다 (로그인은 없습니다).
  5. **분석 기록을 삭제할 수 있습니다** — 되돌릴 수 없으니 자기가 만든
     것만 지우세요.  프로젝트 통째 삭제는 일부러 만들지 않았습니다.


■ 이번에 확인한 것 (실제로 돌려서 잰 값)
=========================================================================

  회사 PC 와 **같은 상태를 처음부터 만들어** 확인했습니다 —
  옛 코드(aac64ba)로 도면을 분석해 **824행 · SCOPE 빈칸** DB 를 만들고,
  거기에 **3칸을 편집한 뒤** 이 꾸러미를 풀었습니다.
  12단계 모두 통과했습니다.

   1. 꾸러미에 app\_data · data(발주처 자료)가 하나도 없다   40개 중 0개
      (목록을 만들 때와 zip 을 쓸 때 두 번 검사했고 두 번 다 0)
   2. 2_FILE_LIST.txt 과 3_OVERWRITE_FILES.zip 의 내용이 같다   40 = 40
   3. 삭제·이름변경 파일 0건 — 수동으로 지울 것이 없다
   4. 옛 DB 를 백업하고 시작 (app.db · -shm · -wal 세 개)
   5. 꾸러미를 풀었다 — 파일 40개 · 백업 3개 (수동 삭제 없음)
   6. 옛 분석이 재분석 없이 열리고 **편집이 살아 있다**
        824행 · 1.09초 · 편집 3/3 유지 · 첫화면 키 ['loose', 'projects']
   7. ★ 결과 화면에서 첫 화면으로 돌아간다 (18회차)
        버튼 1개 · 결과화면 숨김 True · pageerror 0
   8. ★ 새로 분석하면 새 값이 나온다
        지문 015e34b3 · 1037행 · Q'ty 1931 · 469초
   9. 발주처 양식이 전량을 담는다
        FIELD 885 · MOV 76 · BFV 23   (타사 공급분 271행 포함)
  10. 정확도 두 축이 기준선 그대로
        99.3/70.5 · 99.3/89.1   (축1 전량 / 축2 SCT)
  11. 시험이 통과하고 실 데이터를 안 건드린다
        빠른  185 passed, 3 skipped, 36 deselected in 12.53s
        UI    24 passed, 200 deselected in 615.34s (0:10:15)
        느린  12 passed, 212 deselected in 1131.03s (0:18:51)
  12. 검증 내내 개발 PC 의 실 DB 가 바이트로 불변   sha 643748dc…

  ※ 빠른 시험의 "skipped" 는 개발용 후보 워크북이 없는 기계에서 건너뛰는
    것으로, 회사 PC 도 같습니다.


■ ★ 다른 프로젝트에도 그대로 쓰이는가 — SADARA 교차검증
=========================================================================

  이번 꾸러미는 **검출 규칙을 실제로 두 개 바꿨습니다** (18회차).
  이 발주처 도면에만 맞춘 것이 아닌지 다른 프로젝트 도면으로 쟀습니다.

    지문 0767ba79 · 82행 — **기준선과 정확히 같습니다** (65.8초)

  "안 걸려서 안 움직인 것" 이 아니라 규칙별로 무엇을 했는지 쟀습니다.

  · 범례의 ANGLE 밸브 규칙은 **그 도면에서도 발동합니다** — p7 에서 몸체
    2개가 새로 섰고, 예전에 CHECK 로 잘못 읽히던 삼각형이 접혔습니다.
    그런데 **행에는 닿지 않았습니다**: 그 둘은 액추에이터가 없어 산출물
    대상이 아니고, CHECK 로 읽히던 때도 대상이 아니었기 때문입니다.
    → 규칙은 남의 도면에서도 도형을 잡되 **행을 함부로 늘리지 않습니다.**
  · 별표 크기를 그 장에서 다시 재는 규칙은 **아무 것도 추가하지 않았습니다** —
    분석 9장 전부에서 새로 받아들인 크기가 0 입니다. 그 도면은 범례 크기와
    도면 크기가 같아서 더할 것이 없습니다.

  ⚠ 같이 관찰한 것: SADARA 82행의 Q'ty 가 전부 비어 있습니다.
    **이번 변경과 무관하며 이전부터 그랬습니다** (지문이 같습니다).
    그 문서의 범례에 유닛 승수 표가 없어서로 보이고, 다음 회차 후보입니다.


□ 18회차 — 발주처 양식이 **전량**을 담고, 놓치던 밸브 8개가 나옵니다

  1. ★ 발주처 양식에 **모든 행이 나갑니다** (885 / 76 / 23).
     타사 공급 계기도 들어갑니다 — 그 계기를 당사가 공급하지 않아도
     설치에 필요한 Bulk material 은 당사(SCT) 몫이라 물량을 세야 하기
     때문입니다. 요청하신 그대로입니다.
     SCOPE 열은 그대로 남아서 어느 행이 타사 공급인지 보입니다.
     되돌리려면 config/project_alnouf1.yaml 의
        client_form:  scope_filter: all   →   sct
     한 낱말이면 11회차 상태(651 / 66 / 22)로 돌아갑니다.
  2. ★ 벤더 별표를 놓치던 것을 고쳤습니다 (5행).
     16회차에 "전수 검사 0건" 이라고 보고드렸는데, **그 검사가 잘못이었습니다**
     — 검사가 프로그램 자신에게 물어보고 있어서 언제나 0 이 나왔습니다.
     원인은 별표 크기를 범례에서 빌려 쓴 것이고(도면마다 크기가 다릅니다),
     이제 **그 장에서 직접 재서** 씁니다.
     정밀도 88.4% → 89.1% 로 오릅니다.
  3. ★ 안 나오던 밸브 8개가 나옵니다 (p10 3 · p11 3 · p20 1 · p21 1).
     원인은 액추에이터가 아니라 **몸체 모양**이었습니다 — 범례 2쪽의
     ANGLE 밸브(직각으로 맞댄 두 삼각형)를 프로그램이 몰라서 반쪽만 읽었고,
     그래서 옆에 있는 공압 실린더를 "내 것이 아니다" 라고 버렸습니다.
     p20 의 "무엇인지 모르겠다" 던 심볼도 이것이었습니다.
  4. 범례에 없어 판정하지 못한 심볼을 **모아 보여줍니다** (169건 · 19종류).
     머리줄의 `심볼` 버튼입니다. 무엇인지 적어서 등록하면 다음 프로젝트에도
     쓰입니다. **프로그램이 저절로 등록하지 않습니다** — 무엇인지는 사람이
     적습니다. 등록하면 누가 언제 넣었는지 남습니다.
  5. 결과 화면 왼쪽 위에 `← 첫 화면` 이 생겼습니다.
  6. (고친 것) `일괄 적용` 이 **한 행만 바꾸고 있었습니다.**
     17회차에 중복 문장에 A·B·C 를 붙이면서, 화면은 "3행 있습니다" 라고
     하는데 실제로는 1행만 바뀌었습니다. 이제 화면 말대로 바뀝니다.

  7. (개발자용) 같은 도면을 두 번 열면 근거 패널의 '끊긴 지점' 좌표가
     달라지던 것을 고쳤습니다. 판정값은 바뀌지 않습니다.

  ※ 행 수 1029 → 1037 · Q'ty 1911 → 1931 · 사라진 행 0.
    기존 행에서 움직인 칸은 벤더 별표 5행뿐입니다.

□ 10회차 — 리스트에 들어가는 행이 늘었습니다 (824 → 1029)

  1. TW 15행과 벤더 공급분 204행이 나옵니다.
  2. SCOPE 열이 생겼습니다 — SCT / VENDOR(HRSG) 처럼. 공급자 이름은 그 장의
     NOTES 원문에서 읽습니다 (프로그램에 이름을 넣어 두지 않았습니다).
  3. Description 이 비어 있던 밸브 39행이 그 라인에서 가장 가까운 TO/FROM
     문구로 찼습니다. 등급이 LOW 이므로 확인하고 쓰세요.

□ 11회차 — 발주처 양식에 나가는 행이 걸러집니다

  1. 밸브 144행의 SCOPE 가 찼습니다 (SCT 113 · VENDOR 31 · 공란 0).
  2. 발주처 양식 Excel 이 SCOPE=SCT 만 담게 됐습니다.
     ※ **18회차가 이것을 되돌렸습니다** — 지금은 전량을 담습니다.
       설정 한 줄로 다시 SCT 만 담게 할 수 있습니다.
     CZE 885 → 667 · CZH 76 → 66 · CZI 23 → 22.
  3. 정확도 측정축이 둘이 됐습니다 (개발자용. 화면에 안 나옵니다).

□ 12회차 — 화면만 고쳤습니다. 숫자는 하나도 안 움직였습니다

  1. 근거 패널이 사실대로 말합니다 — 공급 주체 / 추출 결과 / 발주처 양식.
  2. 도면 위 색 범례의 라벨과 숫자가 맞고, SCOPE 열이 안 잘립니다.
  3. 분석 진행 화면이 실제로 움직이고 [취소] 버튼이 생겼습니다.
     도면을 마우스로 끌어 이동할 수 있습니다.

□ 13회차 — 다시 분석하지 않고 열고, 고친 것이 남습니다

  1. ★ 첫 화면이 프로젝트 단위가 됐습니다. 눌러서 여는 데 1~3초입니다.
  2. ★ 사람이 고친 칸이 다음 개정본으로 이어집니다 (10개 열 전부).
     비어 있는 칸만 채우고 이미 값이 있는 칸은 덮지 않습니다.
  3. ★ 누가 고쳤는지가 남습니다. 도면의 개정(Rev)도 읽어 첫 화면에 냅니다.

□ 14회차 — 값을 고칠 때 그 값이 어디로 가는지 화면이 말합니다

  1. ★ 칸을 고치면 그 값이 발주처 양식에 나가는지 화면이 말합니다.
        SCT 행     "이 행은 발주처 양식에 나갑니다"
        벤더 행    "SCOPE=VENDOR(HRSG) 라 발주처 양식에는 나가지 않습니다
                   (화면에는 남습니다)"
        판정 없음   "SCOPE 를 판정한 적이 없습니다 — 다시 분석하면 정해집니다"
     막지 않습니다. 고친 값은 화면과 DB 에 그대로 남습니다.
  2. SCOPE 를 고치면 그 자리에서 행수가 보입니다 —
     "발주처 양식에서 빠집니다 — 지금 779행" / "들어갑니다 — 지금 780행".
  3. (개발자용) UI 자동시험이 21 → 24건. 예전 21건은 SCOPE 가 없던 옛
     자료에서만 돌던 값이라 필터를 실제로 검사하지 못했습니다.

□ 15회차 — 같은 프로젝트의 다음 Rev 는 범례를 다시 읽지 않습니다

  1. ★ 새 프로젝트를 처음 분석하면 Symbol & Legend 에서 읽은 값 8항목이
     그 프로젝트에 저장됩니다. 같은 프로젝트의 다음 개정본은 그 값을 씁니다.
     저장 자리는 app\_data\projects\<프로젝트>\legend_profile.json 이라
     코드를 덮어써도 사라지지 않습니다.
  2. ★ 개정본에 Symbol & Legend 장이 없어도 분석이 끝납니다.
     예전에는 그런 PDF 를 넣으면 오류로 멈췄습니다(사유도 안 나왔습니다).
  3. ★ 개정본의 범례가 저장된 것과 다르면 화면 위에 무엇이 무엇에서
     무엇으로 바뀌었는지 뜨고, **자동으로 갱신하지 않습니다** — 항목별로
     고르는 버튼이 있습니다. 결과 화면 머리 아래 띠가 "범례 유도" 인지
     "범례 재사용" 인지도 늘 적혀 있습니다.

  ※ 빨라지지 않습니다. 범례 읽기는 전체 분석의 1.5% 라서 체감되지 않습니다.
    이 기능의 값어치는 "같은 기준으로 분석된다" 와 "범례 없는 개정본도
    돌아간다" 두 가지입니다.


□ 16회차 — 별표(벤더 표시)를 제대로 읽습니다

  1. ★ 지목해 주신 7건이 세 원인이었습니다 —
     ㉠ 점선 사각형(패키지 상자)이 아예 안 잡히던 것,
     ㉡ 별표 하나가 나란한 두 버블에 둘 다 세어지던 것,
     ㉢ **개정 클라우드의 빨간 선이 별표로 읽히던 것**.
  2. SCOPE 가 바뀐 행은 14행 · 4장입니다 (p9 6 · p47 6 · p25 1 · p35 1).
     **p47 은 지목되지 않은 장인데 6행이 고쳐졌습니다** — 그 장의 빨간
     클라우드가 별표 크기를 잘못 알려 주고 있었습니다.
  3. 발주처 리스트와의 정밀도가 86.8% → **88.4%** 로 오르고, 놓친 행이
     5 → 4 로 줄었습니다.

□ 17회차 — 중복 문장을 구분하고, 공급/환수를 커넥터가 말할 때만 붙입니다

  1. ★ 같은 도면 안에서 **글자 그대로 같은 Description** 이 여럿이던 것에
     끝에 A · B · C … 를 붙입니다 (위→아래 · 좌→우). 91묶음 263행입니다.
     ※ 요청하신 좌표 (F:7) 는 **붙이지 못했습니다** — 도면 테두리의 눈금이
       그림이지 글자가 아니라, 칸 이름을 프로그램이 지어내야 합니다.
       지어낸 값은 넣지 않는 것이 이 도구의 원칙이라 사유만 남겼습니다.
       꼭 필요하면 눈금 이름을 한 번 정해 주시면 설정에 넣습니다.
  2. ★ Description 의 SUPPLY / RETURN 을 **가까운 커넥터가 말할 때만**
     붙입니다 (18행). 두 커넥터가 비슷한 거리면 도면이 구분한 것이 아니므로
     **붙이지 않고** 근거 패널에 "갈리지 않음" 으로 남깁니다 (14행).
  3. AT · AIT 는 화면·Excel 에 Analyzer 로 적습니다 (이 도면에는 0건이고
     다음 도면을 위한 것입니다). 내부 판정값은 그대로입니다.
  4. 분석 기록을 하나씩 지울 수 있습니다 (미리보기 → 확인 → ID 타자).
     **프로젝트 통째 삭제는 만들지 않았습니다** — 그 안의 번호 장부가
     사라지면 개정 대조가 깨집니다.
  5. (개발자용) 시험이 실 데이터베이스를 여는 길을 끊었습니다.
     예전에는 회사 PC 에서 시험을 한 번 돌리면 팀원 데이터가 든 app.db 가
     열렸습니다. 이제 열리지 않고, 열려고 하면 시험이 실패합니다.
     빠른 시험 170 → 173건.

  ※ 검출은 한 칸도 안 바뀝니다 — 1029행 · Q'ty 1911 · SCOPE 그대로입니다.
    바뀐 것은 Description 열 274칸뿐입니다.


■ 넣기 전에 — 백업 (건너뛰지 마세요)
=========================================================================

  cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
  mkdir "$env:USERPROFILE\Desktop\pid_backup_20260907"
  Copy-Item app\_data\app.db* "$env:USERPROFILE\Desktop\pid_backup_20260907\"
  Copy-Item app\_data\uploads "$env:USERPROFILE\Desktop\pid_backup_20260907\" -Recurse

  확인 — 네 가지가 다 있어야 합니다:
      uploads · app.db · app.db-shm · app.db-wal

  ★ 이번에 DB 구조가 바뀝니다 (6열 추가, 여는 순간 자동 적용).
    백업이 유일한 되돌리기 수단입니다.

  ※ 이 꾸러미에는 app\_data\ 로 시작하는 파일이 **하나도 없습니다**
    (목록을 만들 때와 zip 을 쓸 때 두 번 검사했고 두 번 다 0 입니다).
    그래도 백업은 하세요.


■ 넣는 순서
=========================================================================

  1. 쓰는 사람이 없는지 확인합니다 (터미널 로그에 다른 IP 가 조용한지).
  2. 서버 중지 — Ctrl + C
  3. handover_final.zip 을 저장소 루트에 덮어쓰기 ("모두 예").
  4. 수동으로 지울 파일: **삭제할 파일 없음.**
     여섯 회차 동안 지워지거나 이름이 바뀐 파일이 0개입니다.
  5. 서버 시작
        cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
        $env:PYTHONUTF8 = "1"
        .\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
  6. 브라우저에서 Ctrl + Shift + R

  ※ 데이터베이스는 여는 순간 저절로 맞춰집니다. 따로 실행할 것이 없습니다.
  ※ pip install 을 다시 할 필요가 없습니다 (requirements 4파일 무변경).


■ 넣고 나서 확인
=========================================================================

  1. 첫 화면에 기존 프로젝트가 보이는가
  2. 클릭해서 열리는가 — **824행이 나옵니다 (옛 분석본이라 정상)**
  3. 새로 분석하면 **1037행** · 약 8분 소요 (이 검증 기계에서 469초)
  4. 같은 도면 안에서 겹치던 Description 끝에 A · B · C 가 붙었는가

  ※ 지금까지 824행이었으므로 1037 로 느는 것이 정상입니다.
  ※ 지문(개발자용 확인값)은 015e34b3 입니다. Windows 에서 다르게 나오면
     docs\offline_setup_windows.md 의 확인 순서를 따르세요.


■ 팀원에게 알릴 것
=========================================================================

  · 첫 화면에서 기존 프로젝트를 클릭해 바로 열 수 있습니다 (재분석 불필요).
  · 값을 고치면 저장되고 다른 팀원에게도 보입니다.
    처음 고칠 때 이름을 묻습니다 — 적고 Enter 를 누르면 그 브라우저가
    기억해서 다음부터는 채워진 채로 뜹니다. 로그인은 없습니다.
  · ★ 발주처 양식 Excel 은 이제 **전량**을 담습니다 (18회차).
    벤더 공급분과 TW 도 나갑니다 — 그 계기를 당사가 공급하지 않아도
    설치에 필요한 Bulk material 은 당사 몫이라 물량이 필요하기 때문입니다.
    어느 행이 누구 공급인지는 SCOPE 열과 근거 패널 세 줄이 말해 줍니다.
    SCT 만 담으려면 config\project_alnouf1.yaml 의
        client_form:  scope_filter: all  →  sct
  · 반영 직후 옛 분석의 SCOPE 열은 비어 있습니다.
    "판정한 적 없음" 이라는 뜻이며, 재분석하면 채워집니다.
  · 재분석해야 새 값(1037행 · 양식 전량)이 적용됩니다.
    사람이 고친 칸은 재분석해도 그대로 남습니다.
  · 화면이 예전 그대로면 Ctrl + Shift + R 을 한 번 누르세요.
  · 프로젝트를 새로 만들고 첫 분석을 하면 그 프로젝트의 범례가 저장됩니다.
    다음 개정본을 같은 프로젝트로 올리면 그 범례를 그대로 씁니다.
    화면 위 띠에 "범례 재사용" 이라고 적힙니다.
    범례가 달라졌으면 그 띠가 무엇이 달라졌는지 적고 고르라고 합니다 —
    **누르지 않으면 아무것도 갱신되지 않습니다.**


■ 되돌리려면
=========================================================================

  1. 서버 중지 — Ctrl + C
  2. 백업한 app\_data 되돌리기
        cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
        Remove-Item app\_data\app.db* -Force
        Copy-Item "$env:USERPROFILE\Desktop\pid_backup_20260907\app.db*" app\_data\
     (uploads 는 덮어쓰지 않아도 됩니다 — 이 꾸러미가 건드리지 않습니다)
  3. 코드를 aac64ba 로 되돌리기
        git checkout aac64ba
     git 을 쓰지 않는다면 반영 전 폴더를 통째로 복사해 두었다가 그것으로
     되돌리세요.
  4. 서버 시작 (위 "넣는 순서" 5번과 같은 명령)

  ※ 데이터베이스는 되돌리지 않아도 옛 코드가 동작합니다 (열만 늘었고 옛
    코드는 그 열을 안 봅니다). 다만 새로 저장된 개정·작성자 정보는 화면에
    안 나옵니다.

  화면만 되돌리려면 app\static\ 세 파일(app.js · index.html · styles.css)을
  이전 것으로 되돌리면 됩니다.
  발주처 양식 필터를 끄려면 app\excel_out.py 의 `in_client_scope` 가 항상
  True 를 돌려주게 하면 됩니다 (한 줄).
  TW · 벤더 계상은 config\project_alnouf1.yaml 에서:
      TW 를 다시 빼기        anchors 의 `TW: TW` 삭제 → `not_field: [TW]`
      벤더 공급분 다시 빼기   scope.exclusion_rules: glyph+text+box


■ 문제가 생기면
=========================================================================

  터미널 화면을 사진으로 찍어 둡니다. 그것이 가장 빠른 단서입니다.
  화면 안에서 [진단 내보내기] 를 누르면 zip 이 생기는데, 원본 PDF 와
  발주처 Excel 은 들어가지 않습니다 (무엇을 뺐는지 zip 안 MANIFEST 에
  적혀 있습니다).


■ 아직 발주처 · 실무 판단이 필요한 것
=========================================================================

     out\round17\7_미구현.md (좌표 (F:7) · Bypass 언급 · p8 의 MOV 수 ·
       미검출 완화 네 안 — **p8 은 피드백과 발주처 리스트가 서로 다른 말을
       합니다**: 리스트는 그 장에 MOV 5개, 우리는 이미 6개를 냅니다)
     docs\round16_scope.md §10 (겹친 패키지 상자에 별표가 붙는 경우의 규칙 —
       이 도면에는 그런 사례가 0건이라 정하지 않았습니다)
     docs\round15_legend_profile.md §8.4 (범례 변경 중 감지하지 못하는
       종류 — 범례 장이 통째로 빠진 경우 세 항목은 config 기본값이 같은
       숫자라 `source` 로만 잡힙니다)
     docs\round14_edit_scope.md §2 (근거 패널의 `공급 주체` 가 검토 필요
       행에서 사람이 넣은 값을 안 보여줍니다 — 결과는 바로 아래 줄이
       정확히 말하므로 이번에는 문구를 바꾸지 않았습니다)
     docs\round13_persistence.md §3 (문서 대표 Rev 기준 — `max` 로
       확정했으나 도면에서 유도된 값이 아닙니다)
     docs\round12_ui.md §2 (오버레이 주황색은 대비 미달이지만 발주처가
       이미 본 색이라 바꾸지 않았습니다)
     docs\round12_annotation_design.md (도면 주석 — 설계안만. 미구현)
     docs\round11_scope_axis.md §7 (8건)
     docs\round10_feedback.md §10 (9건 — 벤더 공급분 계상 여부 포함)


■ ★ 아직 답이 서지 않은 실무 판단 넷 (쓰다 보면 물어보게 됩니다)
=========================================================================

  1. p12 의 PCV 밸브가 안 잡힙니다.
     몸체가 원뿔+사각형 모양인데, 범례 2쪽에 그 갈래가 있는지 아직
     확인하지 않았습니다.  있으면 규칙으로 잡히고, 없으면 [심볼] 화면에서
     등록하는 대상입니다.

  2. p17 의 PRV 밸브가 안 잡힙니다.
     GLOBE 옆에 90도 돌아간 돔이 붙은 모양인데, 프로그램이 세로 방향 돔을
     **설계상 읽지 않습니다**.  넓히면 58장 전체에 영향이 가므로 별도
     회차로 미뤘습니다.

  3. p8 의 MOV 를 몇 개로 볼 것인가 — **피드백과 발주처 리스트가 서로 다른
     말을 합니다.**  발주처 CZH 리스트는 그 도면에 MOTOR 5행인데 우리는
     이미 6행을 냅니다.  더 넣으면 7행이 되어 리스트와 더 멀어집니다.

  4. Description 세 가지
     · Bypass Valve 이름을 밸브 행 주어로 쓸지 — 라벨과 밸브의 거리가
       89.5pt 인데 유도된 허용치는 45.8pt 입니다 (안 3개를 정리해 뒀습니다:
       현행 유지 / 라벨을 주어로 / 라벨을 덧붙임 — 바뀌는 행은 6행).
     · 공급/환수(SUPPLY·RETURN) 보류 14행 — 두 커넥터가 거의 같은 거리라
       도면이 구분하지 않았다고 보고 안 붙였습니다.  붙이라고 하시면
       요구치 한 줄입니다.
     · 판정하지 못한 심볼 169건 · 19종류 — [심볼] 화면에 모아 뒀습니다.
       무엇인지는 **사람이 적습니다** (프로그램이 짐작하지 않습니다).
     · AT · AIT 를 Analyzer 로 적는 규칙은 준비돼 있으나, 발주처 CZE
       리스트에 A 로 시작하는 항목이 0행이라 검출은 켜지 않았습니다.

  자세한 근거는 꾸러미에 함께 들어가는 문서에 있습니다 —
  docs\round18_description.md · docs\round18_global_symbols.md


