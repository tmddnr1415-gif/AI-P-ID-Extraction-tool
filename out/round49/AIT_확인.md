# AL NOUF1 에서 AIT 는 지금 식별되지 않습니다

근거: **커밋된 실제 분석 결과** `out/round20/run_base.json` (1037행 · 지문 `fb85b039`).
겹쳐 그린 그림: `nouf1_ait.png` · `nouf1_ait_zoom.png`.

## 1. 답

| | 값 |
| --- | --- |
| AIT 행 | **0 행** |
| AT 행 | **0 행** |
| 미판정 기록 | **p16 AIT 4건** · p14 AT 2건 |
| 사유 (기록 원문) | `ISA 태그 모양인데 이 프로젝트 사전에 없음` |
| p16 이 낸 행 | 48개 — PI 5 · PIT 6 · TIT 5 · RO 12 · FIT 2 · FE 2 · TI 1 · LIT 3 · LI 2 · PDIT 3 · 밸브 7 → **AIT 0** |

**검출 실패가 아닙니다.**  버블도 찾았고 글자도 `AIT` 로 읽었는데, 그 낱말이
`anchors.type_map`(24종)에 없어 행이 되지 못하고 `unjudged_symbols` 로 갔습니다.
미판정 좌표 네 개 — `x 657.2 / 728.1 / 799.0 / 869.8 · y 990.6` — 는 지적하신
빨간 상자 안 **AIT 네 개와 정확히 같은 자리**입니다.

## 2. 좌표계 검증

겹쳐 그린 자리가 추정이 아님을 보이려고, **이미 행이 된 다섯 상자**(같은 장의
`PI [622.6,872.0,690.6,894.6]` · `TIT` · `TI` · `LI` · `LIT`)를 같은 변환으로
다시 그렸습니다 — 화면의 기존 상자와 겹칩니다 (그림의 청록).

    x_px = 1.8419 × (x_pt − 656.6) + 1154.5
    y_px = 1.8419 × (y_pt − 872.0) + 643.0

오른쪽 그리드도 같은 사실을 말합니다 — p16 행 목록에 `PI · PDIT · LI · PIT · TI`
는 있고 **AIT 는 한 줄도 없습니다.**

## 3. 고치는 법과 그 대가

`anchors.type_map` 에 `AIT → AIT`(요구대로면 표기는 `Analyzer`)와 `AT` 를 더하면
됩니다.  표기 이름은 이미 `config` `description.type_display_names` 에
`AT: Analyzer` · `AIT: Analyzer` 로 **적혀 있습니다** — 사전에 낱말이 없어 그
표기가 쓰일 기회가 없었을 뿐입니다.

⚠ **18회차 실측**: AIT 앵커를 넣으면 **축2 정밀도 89.1 → 88.3** 으로 내려갑니다.
발주처 CZE 리스트에 A 계열이 **0행**이라 넣은 행이 전부 FP 가 되기 때문입니다.
지금 기준선은 축2 **94.4%** 이므로 다시 재야 합니다.

→ **실행 프로젝트(발주처 리스트가 있는 AL NOUF1)와 입찰 프로젝트에서 사전을
다르게 쓸지**가 확인 대상입니다.  38회차에 입찰/실행 선언 자리는 이미 있습니다.

**게이트를 재려면 이 환경에 `data/pid_total.pdf` 가 있어야 합니다** (지금 없음).
