# [B] 39회차 미규명 2건 — TC2 p10 TIT · p16 PIT 의 검토 사유 소거

## 원인 (결과 json 칸 단위 대조 · `TC2_H3_38` ↔ `TC2_F39`)
`candidates/distance_limit_pt` **276.3 → 290.2**.  이 값은 `pipeline` 이 *행마다 같은 장의 가장 가까운 기기까지의 거리* 를
모아 **90분위**로 정하는 문서 단위 문턱이다(`describe_candidates.distance_limit`).  모집단은 **접힌 뒤의 행**(`description_needed` 행)
이고, #7 로 FIT/FE 15행이 접히지 않게 되자 모집단이 441 → 456 으로 늘어 90분위가 13.9pt 올랐다.
- p10 TIT `[733.8,109.8]`: 가장 가까운 기기 `HRH BYPASS VALVE` **276.3pt** — 옛 문턱과 같은 값이라 걸러졌고 새 문턱 아래로 들어왔다.
- p16 PIT `[735.0,399.6]`: `CONDENSATE EXTRACTION PUMP A` **277.5pt** — 같음.
둘 다 후보가 CONNECTOR 뿐 → EQUIPMENT 주어가 생김 → 등급 PARTIAL → CONFIRMED · `DESCRIPTION_INCOMPLETE` 사유 소거 · 접미 `A` 도 사라짐
(같은 문장 2행이 이제 다른 문장이 됐으므로).

## 사라진 것이 맞는가
**규칙대로다 — 그러나 렌더는 판정이 문턱 위에 걸린 흔적을 보여 준다.**  `캡처/crop_tc2_p16_pit_row.png`: 같은 라인 위에 PIT·PIT·TIT 셋이
나란히 있고 그중 **첫 PIT 하나만** 277.5pt 로 문턱 안에 들어 주어를 얻었다(옆 PIT 는 313pt 로 밖).  p10 TIT 셋(`crop_tc2_p10_tit.png`)도 같다.
셋 중 하나만 주어를 갖는 것은 도면이 말한 것이 아니라 문턱 자리의 결과다.  그렇다고 되살리는 것은 행 하나에 예외를 두는 일이고,
문턱 규칙을 바꾸는 것은 §2.2 "거리 제한 전역 완화" 이며 AL NOUF1 의 같은 띠 24행이 움직인다 → **코드는 손대지 않고 기록한다.**
근거 패널은 두 행에 `SUBJECT: … 거리 277.5pt` 를 그대로 적고 있어 조용한 통과는 아니다.

## 전수 — 문턱 ±5% 띠 안의 행 (행 수가 바뀌면 넘나들 수 있는 행)
| 프로젝트 | 문턱(pt) | 기기 후보 있는 행 | ±5% 띠 |
| --- | ---: | ---: | ---: |
| AL NOUF1 | 584.6 | 686 | **24** |
| SADARA | 2048.4 | 35 | 2 |
| TC2 (전) | 276.3 | 441 | 7 |
| TC2 (후) | 290.2 | 456 | 12 |
| UAD | 227.6 | 103 | 2 |
→ 여러 건 · 방법론(문턱이 모집단에 매달려 있다).  **고치지 않는다** — 문턱 정의 변경은 금지 항목이고 AL NOUF1 지문 대상(`needs_review`)이다.
다음 회차 후보로 적는다: 문턱을 "행" 이 아니라 "검출(접기 전)" 에서 재면 접기 규칙과 독립이 되지만, AL NOUF1 의 접힌 68행이 모집단에 들어가 지문이 움직인다 → 사용자 판단.

## 닫음
원인 규명 완료 · 코드 0줄 · 사용자 판단 대기에 1건 추가 (문턱 모집단 정의).
