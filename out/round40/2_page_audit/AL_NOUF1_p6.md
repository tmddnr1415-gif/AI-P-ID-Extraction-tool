# AL NOUF1 p6 — D00P-10LBA10-M05-0001 (HP STEAM GROUP 10) · 행 46

## ① 눈으로 센 것 (원본 `캡처/AL_NOUF1_p6_원본.png`)
위 반쪽(HRSG#12): 세로 버블 5(TT TT TT PT PT · 벤더 별표) · FT FT FE · PT TT · TT TT · MOV 게이트(M) 1 · MOV 글로브 2(TO CLEAN DRAIN TANK) ·
BD TANK 라인 MOV 1 · ST SUPPLIER 묶음 TT/TW/PT 10.  아래 반쪽(HRSG#11) 거울 · UNIT#10 TT TT · TT TT(HP TURBINE) · MOV 3.
수동 밸브(액추에이터 없음) 다수 · 개정 클라우드 `C` 2 · 별표 `*`·`**` 정의줄 NOTES.

## ② 대조
㉠ 정상 46 · ㉡ 도면에 있는데 행 없음 **0** · ㉢ 도면에 없는데 행 있음 **0** · ㉣ 값 틀림 **1 후보** —
`TO HRSG#12 BD TANK` 밸브는 **GLOBE**, 거울 자리 `TO HRSG#11 BD TANK` 밸브는 **BALL** (같은 심볼).  rect 높이 10 ↔ 7.

## ③ 원인
아래쪽 밸브 자리에 도형이 **74개**(위쪽 38개의 두 배) · 원(`c`)이 둘 — 같은 심볼이 **두 번 겹쳐 그려져** 있다.
`_bowtie_bodies` 가 허리 원반 하나를 `spent` 로 쓰고, 남은 **둘째 원**이 `_round_bodies` 에 들어가 양끝 바 사이의 원 = **BALL** 몸체가
따로 서고, 그 BALL(높이 7.2 = 원 지름)이 행이 됐다.  §4 유형: 도면이 두 번 말하는 자리(20회차 p12 "두 번 그려져" 와 같은 뿌리).

## ④ 전수 (`dup_census.py` — 행 rect 안 같은 도형 두 번)
AL NOUF1 밸브 행 111/144 에 겹친 도형이 있다(대부분 나비 대각선 하나가 두 번 · 판정 무관) · **원이 겹친 행 6** (p6 2 · …) ·
SADARA 2 · UAD 15 · TC2 (아래 `3_전수.md`).  여러 건 → **방법론**.  단, 원이 겹쳐도 GLOBE 로 남는 행이 있어(p6 `[840,1443]`) 뒤집히는
조건은 몸체 단위로 다시 잰다 (`body_census.py`).

## ⑤ 수정 / 보류
**보류** — AL NOUF1 지문이 걸린 밸브 TYPE 판정이라 이번 회차에는 원인·전수만 적는다.  고칠 방향: 겹쳐 그린 원은 하나로 본다
(같은 bbox 의 `c` 경로 중복 제거 — 도면이 두 번 말한 것을 두 개로 세지 않는다).  AL NOUF1 지문이 움직이므로 실무 판단.
