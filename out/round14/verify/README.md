# 14회차 갈음 재검증 — 8단계까지

지난 검증(`19b5479`)은 UI 2건이 깨진 상태에서 나온 것이라 다시 했다.
**이번에는 적용 후 UI 스위트 전량을 돌렸다** — 지난번에 빠졌던 지점이다.

**격리** — 실 DB(`app/_data/app.db`)는 열지 않았다.
sha256 `df9875e5…87d2324` 가 작업 전후 같다.
옛 코드는 `git worktree` @ aac64ba, 데이터는 그 밖의 격리 경로.

| 단계 | 결과 |
| --- | --- |
| 1–4 회사 PC 조건 DB | 824행 · 지문 `91ba3e33` · SCOPE 빈칸 822 / SCT 2 · 편집 3칸 |
| 5 zip 풀기 | 26엔트리 · `app/_data` **0** (목록·zip 두 시점 검사) |
| 6 수동 삭제 | **0개** (삭제·이름변경 없음) |
| 7 마이그레이션 | `db.connect` 자동 · 따로 실행할 것 없음 |
| 8 서버 | 1.5초 · 예외 0 · JS 오류 0 |
| 9 첫 화면 | `회사PC재현` 이 목록에 뜸 |
| 10 옛 프로젝트 | **824행이 0.73초**에 열림 · SCOPE 열 빈칸 · 편집 3칸 살아 있음 |
| 11 재분석 | 337초 · **1029행 · 지문 `69ad281f`** · 도면 Rev.D · 옛 편집 3칸 생존 |
| **★ 8 적용 후 UI 시험** | **24 통과** (FIELD SCT 667 / VENDOR 218 인 데이터에서) |

재분석 전 발주처 양식: `legacy_no_scope_rows: 822` · 옛 행 전부 담김.
Q'ty 는 엔진층 1911 / 화면값 1912 (손편집 +1) — 두 층이 갈려 있다는 증거.

## 캡처

| 파일 | 무엇 |
| --- | --- |
| `f9_home.png` | 반영 직후 첫 화면 — 옛 프로젝트 |
| `f10_old_rows.png` | 옛 824행이 새 코드에서 열린 화면 |
| `f11_progress_1.png` · `_2.png` | 재분석 진행 (md5 서로 다름) |
| `f11_after.png` | 재분석 뒤 1029행 |
