# SADARA p6 — 1A46-10LBA10-M05-0001 (HHP STEAM 1/3) · 행 10 · ★ 처음 눈으로 본 장

## ① 눈으로 센 것 (`캡처/SADARA_p6_원본.png`)
PIT PIT TIT TIT + TIT(**)×3 = 7 · 상세 상자 `D : DETAIL OF DRAIN CONFIGURATION FOR DRAIN : 1, 2` 안 TIT TIT + MOV(M · `INCH`) ·
본문 `DRAIN 1` · `DRAIN 2` 자리에 원 표식 `D` 둘 · 왼쪽 위 **파란색**으로 그린 바이패스 MOV 둘(M · `LATER` · ASME 표기도 파랑) ·
개정 삼각형 `D`(빨강) · `FROM HRSG — LATER` 커넥터.

## ② 대조
㉠ 10 · ㉡ **2** — 파란 MOV 둘 · ㉢ 0 · ㉣ Q'ty 전부 빈칸(유닛 `10` 승수 — 사용자 판단 1 · 알려진 것) ·
★ **Typical 미적용** — 상세 한 벌(TIT TIT MOV)이 `DRAIN 1`·`DRAIN 2` 두 자리에 쓰이므로 배수 ×2 여야 하는데 x1.

## ③ 원인
(가) 파란 MOV: 몸체 하나는 `CHECK · MOTOR` 로 검출되고(대각선 하나만 검정 · 나머지 파랑?) 행이 안 됐고, 하나는 몸체가 없다.  색: 그 자리 도형 21개 중 파랑 10.
    §4 유형 — 16회차 "잉크와 종이 둘뿐" 규칙의 반대편: 파랑은 개정 클라우드가 아니라 **설계 내용(LATER 추가분)** 이다.  세는지는 실무 판단.
(나) Typical: 캡션 표식이 **원이 아니라 네모**(`qu` 26×26 · 안에 `D`) — 38회차 `typical.circle_marks` 가 호 4개 원만 받아 캡션을 못 찾았고
    "SADARA `D` 34개는 상자가 없다" 고 적었다.  **상자는 있었다.**  보정 프롬프트 ② "( D ) · [T1] · ⓐ — 어떤 꼴인지 그 도면에서 잰다" 가 정확히 이 경우.
    본문 표식은 원(`cccc` 14.2) + 리더선 50pt.

## ④ 전수
(나) SADARA `D` 원 34개(p6 2 · p7 15 · p8 5 · p9 12) — 네 장 전부 네모 캡션 상자가 있는지 이 회차에서 본다.  TC2 캡션은 원(7건) · AL NOUF1·UAD 0.
    **여러 건(4장 34개) → 방법론.**
(가) 파란 도형 전수 `color_census.py` (`3_전수.md`).

## ⑤ 수정
(나) `typical.py` 의 표식 후보를 "원" 에서 "닫힌 작은 테두리(원·네모)" 로 넓힌다 — 모양을 하나 더 받는 것이지 글자를 받는 것이 아니다.  AL NOUF1·TC2·UAD 불변이어야 한다.
(가) 보류 — 실무 판단(파란 추가분을 세는가).
