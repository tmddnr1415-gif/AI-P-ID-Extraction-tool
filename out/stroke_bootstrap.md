# 스트로크 서명 부트스트랩 검증 (스파이크 4-B)

교차검증에서 홀드아웃 96pp 하락의 단일 원인이 V8(`valves.stroked_letters`)
이었습니다. 밸브 규칙 중 신규 프로젝트가 물려받을 수 없는 유일한 항목이므로,
**신규 적용 비용 = 이 서명을 얻는 비용**입니다. 그 비용을 추정하지 않고 실측합니다.


## 1. 현재 서명의 출처 (수동 등록 기록)

| 글자 | 어디서 읽었나 | 검증 |
|---|---|---|
| `H` 3획 (평행 2 + 수직 1) | **p26** `D00P-00PAB10-M05-0001`, 상자 `(1185.3,559.5)` 11.3x11.3, 획 `(5.6,0) (5.6,0) (0,3.1)` | 렌더링 육안 확인 + CZI 의 HOV 행수 일치 |
| `M` 4획 (평행 2 + 대각 2) | **p33** `D00P-00EGD00-M05-0002`, 원 `(296.9,864.6)` 14.2x14.2 및 19.3x19.3, 획 `(0,5.6) (1.7,5.6) (1.7,5.6) (0,5.6)` | 렌더링 육안 확인 + CZH 의 MOV 12행과 정확히 일치 |
| `X` 2획 (교차 대각) | **관측 없음** | 범례 p3 `UNCLASSIFIED (TYPE OF ACTUATOR)` 를 보고 미리 적어둔 것. 문서 어디서도 발동하지 않은 **미검증 서명** |

요약: 지금 서명은 **사람이 도면 2장을 열어 벡터를 덤프하고 손으로 적은 것**입니다. 신규 프로젝트마다 이 작업이 반복된다면 그게 적용 비용입니다.

## 2. 문서 안의 액추에이터 울타리 분포

울타리 후보 총 **539건** (줄기 부착 전 기준)

| 글자 출처 | 건수 |
|---|---:|
| TEXT | 76 |
| STROKE | 73 |
| UNREAD | 390 |

텍스트 글자 페이지: `[2, 3, 4, 6, 7, 8, 9, 10, 11, 12, 14, 16, 18, 20, 25, 30]`
스트로크 글자 페이지: `[26, 27, 28, 32, 33, 35, 46, 47, 49, 51]`

## 3. 공존 페이지

텍스트 글자와 스트로크 글자가 같은 페이지에 있는 경우: **0건** []

사실상 두 표기는 페이지가 갈립니다. 다만 페이지 단위 공존은 자동 학습의 필요조건이 아닙니다 — 문서 전체가 한 프로젝트이므로 텍스트 페이지에서 만든 템플릿을 스트로크 페이지에 쓸 수 있는지가 진짜 질문이고, 아래에서 직접 시험했습니다.

## 4. 시험 1 — 텍스트 템플릿으로 스트로크 글자 읽기 (스파이크 1 방식): **실패**

템플릿 {'M': 65} / 시험 73건 중 정답 69건

| 정답 → 판정 | 건수 |
|---|---:|
| `H->M` | 4 |
| `M->M` | 69 |

| 정답 글자 | 최근접 텍스트 템플릿까지 거리 (min / 중앙 / max) |
|---|---|
| `H` (n=4) | 0.4633 / 0.4633 / 0.4633 |
| `M` (n=69) | 0.4331 / 0.4529 / 0.4791 |

**거리가 글자와 무관합니다.** M 이든 H 든 텍스트 템플릿까지 0.43~0.52 로 똑같이 멉니다. 이 표현에서 거리를 지배하는 것은 글자 모양이 아니라 **렌더링 방식**(채워진 글리프 외곽선 vs 가는 선분)입니다. 따라서 임계값을 어디에 두어도 '모르는 글자'를 걸러낼 수 없습니다.

스파이크 1 이 됐던 이유가 여기서 드러납니다. REV 칸은 텍스트 표본만으로 맞춘 게 아니라 **개정이력 행 순서(A, B, C…)라는 위치 규약이 스트로크 글자에 직접 라벨을 붙여줬기** 때문입니다. 액추에이터 글자에는 그런 위치 규약이 없습니다.

## 5. 시험 2 — 스트로크끼리 최근접 (leave-one-out): **성공**

스트로크 표본 73건 전수 leave-one-out 최근접 이웃 정확도 **73/73 = 100.0%**

즉 **글자당 라벨 표본이 하나만 있으면 나머지는 전부 자동으로 읽힙니다.** 남은 문제는 그 하나를 어디서 얻느냐입니다.

## 6. 시험 3 — 군집화 + 태그 버블 자동 라벨

스트로크 글자를 서로 군집(단일연결)한 뒤, 각 군집에 **텍스트 태그 버블이 달린 밸브**가 하나라도 있으면 그 군집 전체를 자동 라벨합니다. 범례 p4 가 `MOV` 버블과 `M` 원을, `HV` 버블과 유압 상자를 짝지어 그리므로 태그는 스트로크 글자의 정답을 말해 줍니다 — 스파이크 1 의 행 순서 규약에 해당하는 장치입니다.

태그가 달린 스트로크 표본: **15/73**

| 반경 | 군집 수 | 순수 군집 | 자동 라벨 | **사람 필요** |
|---:|---:|---:|---:|---:|
| 0.005 | 13 | 13 | 8 | **5** |
| 0.01 | 10 | 10 | 6 | **4** |
| 0.02 | 4 | 4 | 4 | **0** |
| 0.025 | 4 | 4 | 4 | **0** |
| 0.03 | 3 | 2 | 3 | **0** |
| 0.05 | 3 | 2 | 3 | **0** |

반경 0.02 에서 군집 4개가 전부 순수하고 전부 자동 라벨됩니다.

| 크기 | 페이지 | 서명 판독 | 태그 | 자동 라벨 | 일치 |
|---:|---|---|---|---|---|
| 54 | [26, 27, 28, 32, 33, 35, 46, 47, 49, 51] | {'M': 54} | {'MOV': 4} | ['M'] | 예 |
| 13 | [26, 27, 28] | {'M': 13} | {'MOV': 6} | ['M'] | 예 |
| 4 | [26, 27] | {'H': 4} | {'HV': 4} | ['H'] | 예 |
| 2 | [46] | {'M': 2} | {'MOV': 1} | ['M'] | 예 |

반경 0.03 이상에서는 M 군집과 H 군집이 합쳐져 순수도가 깨집니다. 동작점은 0.02~0.025 입니다.

## 7. 결론 — 신규 프로젝트 적용 절차

1. 액추에이터 울타리를 찾는다 (범례 유도, 프로젝트 무관)
2. 텍스트 글자가 든 것은 그대로 읽는다
3. 나머지(스트로크)를 반경 0.02 로 군집한다
4. 각 군집에서 **텍스트 태그 버블이 달린 멤버**를 찾아 군집 전체를 라벨한다
5. 태그 멤버가 없는 군집만 사람에게 보여준다 — **군집당 클릭 1회**

이 문서에서는 5단계에 남는 군집이 **0개**입니다. 즉 `stroked_letters` config 는 원리적으로 필요 없고, 손으로 적은 서명 2개는 이 절차로 재현됩니다(M 69건, H 4건 전부 일치).

**다만 이건 이 문서 1건의 결과입니다.** 태그 버블이 하나도 없는 계통만 있는 프로젝트라면 5단계가 남고, 그때 필요한 UI 는 '군집 대표 이미지 + 글자 입력' 한 화면이며 클릭 수는 **서로 다른 스트로크 글자 수**(이 프로젝트 기준 2)로 상한이 잡힙니다.

