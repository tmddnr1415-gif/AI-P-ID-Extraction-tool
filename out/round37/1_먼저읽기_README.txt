P&ID 추출 도구 — 업데이트 꾸러미 r37
================================================================================
만든 날 2026-09-13 · 10 ~ 37 회차 누적 · 기준 커밋 aac64ba -> HEAD


━━ 담긴 것
--------------------------------------------------------------------------------
    0_report.md             <- 반영 사항 전체 + 다음 개발 사항 (읽을거리)
    1_먼저읽기_README.txt   <- 넣는 순서 (이 파일)
    2_파일목록.txt          <- 파일 목록 ([A] 새로 · [M] 덮어쓰기 · 삭제 0 · 이동 0)
    3_덮어쓸파일.zip        <- 소스 파일 (이것을 저장소 루트에 풉니다)

r24 · r27 을 이미 넣으셨어도 **이것을 그대로 덮어쓰면 됩니다** (누적본입니다).
아무것도 안 넣으셨어도 됩니다 — 기준이 같은 aac64ba 입니다.


━━ ★ r27 뒤에 무엇이 달라졌나 (28 ~ 37 회차)
--------------------------------------------------------------------------------
1. 글자를 획으로 그린 도면(AutoCAD SHX 글꼴)을 읽습니다.  UAD 도면이 그것으로
   149행을 냅니다.  같은 글자가 도면 안 주석에 원문으로 남아 있어 그것을 받습니다.
2. 태그가 인쇄된 도면은 태그로 기기를 붙입니다 (1급).  AL NOUF1 · SADARA · TC2 는
   태그가 없어 한 행도 안 바뀝니다.  태그로 SCOPE 는 판정하지 않습니다 (별표가 답).
3. 버블 안 글자를 별표로 읽던 것을 고쳤습니다 — 사각형이 아니라 그려진 윤곽으로 봅니다.
4. 개정(REV) 표기 모양을 이력 표에서 읽습니다 — 숫자로 개정을 매기는 양식도 됩니다.
5. 승수를 사람이 한 번 지정하는 화면 — 결과 화면의 승수 패널에서 유닛코드별로 답합니다.
   ("다시 분석해야 적용된다" 가 패널에 적혀 있습니다.)
6. 오버레이 색은 SCOPE, 검토 필요는 모서리 ● 표식 — 검토 사유가 전량에 붙은
   문서에서 도면이 한 색이 되던 것을 고쳤습니다.
7. 수량 NOTES 읽기에서 낱말 조건(IDENTICAL / SIMILAR / TYPICAL …)을 없앴습니다.
   번호 붙은 NOTES 문단이 유닛 표기를 둘 이상 열거하면 셉니다.  네 문서 Q'ty 불변.
   ※ r27 README 가 안내한 config 의 `qty_note` 줄은 이제 **없어도 됩니다** (지웠습니다).
      유닛을 부르는 낱말(UNIT · GROUP · TRAIN)만 코드 기본값이고, 새 낱말이 필요하면
      config 에 `qty_note: {unit_words: [...]}` 로 늘릴 수 있습니다.
8. 유닛 표기의 꼴(`#10` · `3-1` · `NO.3-1`)을 그 도면에서 배웁니다 — TC2 p33 · p34 가
   x1 -> x4 로 (Q'ty 3047 -> 3167).
9. 도면번호 형식을 그 도면의 DWG NO. 칸에서 읽습니다 — TC2 에서 못 읽던 4장
   (6자리 코드)이 살아 607 -> 684행 · 도면번호 60/60.
10. 코드에 외워둔 값을 199 -> 175 로 줄였습니다 (전부 지우고 유도만으로 재현해 확인).
11. 메모리 — 끝나면 잉크 인덱스를 놓습니다.  AL NOUF1 4.9GB · 큰 도면 7.2GB.


━━ AL NOUF1 은 아무 것도 달라지지 않습니다
--------------------------------------------------------------------------------
1037행 · Q'ty 1931 · 지문 fb85b039 · 발주처 양식 885 / 76 / 23 ·
정확도 축1 99.3% / 70.5% · 축2 99.3% / 94.4% — **전부 그대로**입니다.
(회사 PC 의 옛 코드는 824행 · 84.1% 입니다 — 10회차에 벤더 공급분과 TW 를 계상하기로
 해서 행이 늘고 축1 정밀도가 내려간 것이고, SCT 만 재는 축2 는 94.4% 입니다.
 되돌리려면 config `scope.exclusion_rules: glyph+text+box` 한 줄입니다.)


━━ 넣기 전에 — 백업 (건너뛰지 마세요)
--------------------------------------------------------------------------------
PowerShell 에서:

    cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
    $d = "$env:USERPROFILE\Desktop\pid_backup_" + (Get-Date -Format yyyyMMdd_HHmm)
    mkdir $d
    Copy-Item app\_data\app.db* $d\
    Copy-Item app\_data\uploads $d\ -Recurse
    Get-ChildItem $d


━━ 넣는 순서
--------------------------------------------------------------------------------
1. 쓰는 사람이 없는지 확인.
2. 서버 중지 — Ctrl + C.
3. 3_덮어쓸파일.zip 을 저장소 루트에 풀어 덮어씁니다 ("모두 덮어쓰기").
4. 삭제할 파일 없음.
5. 의존성 확인 (playwright 는 시험용이라 없어도 됩니다):
        .\.venv\Scripts\python.exe -m pip install -r requirements.txt
6. 서버 시작:
        cd C:\Users\SAMSUNG\.claude\pid\AI-P-ID-Extraction-tool
        $env:PYTHONUTF8 = "1"
        .\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
7. 브라우저에서 Ctrl + Shift + R.

★ app\_data\ 폴더는 이 꾸러미에 들어 있지 않습니다 — 기존 분석 기록은 그대로입니다.
   DB 표는 서버가 켜질 때 가산 마이그레이션으로 늘어납니다 (지우는 것 없음).


━━ 넣고 나서 확인
--------------------------------------------------------------------------------
1. 첫 화면에 기존 프로젝트가 보이는가 (단위가 프로젝트 -> 리비전입니다).
2. AL NOUF1 을 다시 분석하면 1037행 · Q'ty 합계 1931 · 지문 fb85b039 인가.
   ※ 재분석해야 새 값이 적용됩니다 — 옛 분석본은 옛 값 그대로입니다.
3. 큰 도면(TC2)을 다시 분석하면 684행 · 도면번호 60/60 · Q'ty 3783 인가.
4. 시험:  .\.venv\Scripts\python.exe -m pytest -q -m "not slow and not ui"
   -> 283 통과 (발주처 자료가 없는 PC 에서는 3건 안팎이 건너뜀으로 나옵니다).


━━ 넣은 뒤 사람이 할 일
--------------------------------------------------------------------------------
· 승수 값 — SADARA(유닛 10) · TC2(00) · UAD(00) 는 도면이 승수를 말하지 않습니다.
  결과 화면의 승수 패널에서 유닛코드별로 한 번 답하고 다시 분석합니다.
· TC2 p25 ~ p28 의 SCOPE 가 맞는지 한 번 봐 주세요 (이번에 처음 행이 된 장입니다).


━━ 되돌리려면
--------------------------------------------------------------------------------
1. 서버 중지.
2. git checkout aac64ba -- .
3. 2_파일목록.txt 의 [A] 줄에 적힌 새 파일들을 지웁니다 (안 지워도 동작에 지장 없음).
4. 백업한 app\_data 를 되돌립니다.
5. 서버 시작.


━━ 문제가 생기면
--------------------------------------------------------------------------------
터미널 화면을 사진으로 찍어 두고, 결과 화면까지 갔으면 "진단 내보내기" zip 을
함께 보내 주세요 (원본 PDF · 발주처 Excel 은 그 zip 에 들어가지 않습니다).
