# 회사 PC 갱신 꾸러미 — 진행 기록

압축·중단 대비.  **재개하면 이 파일을 먼저 읽는다.**
**개발 회차가 아니다 — 마무리 · 포장 · 검증이다.  코드 수정은 [A] 외에 없다.**

## 기준선 (18회차 후)

```
지문 015e34b3 · 행 1037 · Q'ty 1931
축1 99.3% / 70.5%   ·   축2 (SCT) 99.3% / 89.1%  (FP 73 · FN 4)
발주처 양식 885 / 76 / 23  (전량 모드 · CZF 45 는 양식 미수령)
Description 식별 41.6% · F1 68.7 · 완전일치 19
테스트 빠른 188 · UI 24 · slow 12
회사 PC 현재 커밋 aac64ba (10~18회차 전부 미반영 · 824행 DB)
```

## 단계별 상태

| 단계 | 상태 | 비고 |
|---|---|---|
| A 미커밋 마무리 | **완료** | `d700eed` 커밋·푸시.  지문 `015e34b3` · 1037행 · Q'ty 1931 **불변** 확인 후 넣었다 |
| B SADARA 교차검증 | **완료 · 통과** | `app/_data/uploads/2391ee0f1ea6_sadara_1A46.pdf` 를 **사본으로** 분석 (원본 sha 불변).  **`0767ba79` · 82행 — 기준선과 정확히 같다** |
| C 목록 | | |
| D 적용 검증 (12단계) | | |
| E 산출물 | | |

## 기록

## [A] 미커밋 마무리 — 넣었다

`app/engine/pipe_graph.py` 의 `dead_ends` 순서 비결정성.

- 원인: 방문 노드 **집합(`seen`)을 그대로 순회** → 파이썬 문자열 해시가
  프로세스마다 섞여 뒤따르는 `[:40]` 이 **어느 40개를 남길지**가 갈렸다.
- 검증: 해시 씨앗을 달리한 두 프로세스(`PYTHONHASHSEED=1` / `999`)로 전량 2회
  - 지문 **015e34b3** 동일 · **1037행** · **Q'ty 1931** 동일
  - `trace` 가 있는 1037행 중 **순서까지 다른 행 0** (17회차 실측 282)
- 빠른 시험 188 통과 · 실 DB sha256 `643748dc…` 불변 → **커밋 `d700eed`**

**즉 비결정성 제거가 결과를 바꾸지 않았으므로 꾸러미에 넣는다.**

## [B] SADARA 교차검증 — **기준선과 같다 (0767ba79 · 82행)**

**실 uploads 를 건드리지 않았다** — 사본을 scratchpad 로 떠서 분석했고,
원본 sha256 `7e10923a…` 는 전후 동일하다.  `PID_PROJECT_CONFIG` **미지정**
(15·17회차가 정한 조건 — AL NOUF1 규칙을 SADARA 도면에 적용한 값).

```
지문 0767ba79 · 행 82 (FIELD 62 · MOV 14 · REVIEW 6) · 65.8초
TYPE  TIT 27 · PIT 22 · GATE 14 · LS 4 · GLOBE 4 · PDIT 3 · FIT 3 · FE 3 · CHECK 2
scope SCT 75 · VENDOR(ST SUPPLIER) 4 · VENDOR(ST OEM) 3
```

### ★ 안 움직인 이유를 규칙별로 쟀다 — "안 걸렸다" 가 아니다

| 새 규칙 | SADARA 에서 무엇을 했나 | 행에 닿았나 |
| --- | --- | --- |
| [E] 범례 p2 **ANGLE 몸체** | **실제로 발동한다** — p7 에서 도형 3개가 정의에 걸리고 몸체 **2개**가 섰다.  켬/끔 대조: `CHECK 37 → 35` · `ANGLE 0 → 2` (CHECK 로 잘못 읽히던 삼각형이 접혔다 — AL NOUF1 과 같은 모양) | **닿지 않았다** — 둘 다 `actuator=NONE` → `EXCLUDED`.  **CHECK 였을 때도 EXCLUDED** 였으므로 행 수도 지문도 그대로다 |
| [C] **그 장에서 별표 크기 재측정** | **아무 것도 추가하지 않았다** — 분석 9장 **전부**에서 새로 받아들인 크기 **0**.  이 도면은 범례 크기와 도면 크기가 일치한다 (AL NOUF1 p35 같은 사례가 없다) | 닿지 않았다 |

**결론**: ANGLE 규칙은 **다른 프로젝트에서도 도형을 잡고**(범용성 확인),
**액추에이터가 있어야 산출물에 들어가므로 행을 함부로 늘리지 않는다**(안전).
별표 재측정은 **더하기만 하는 규칙**이라 그런 도면이 없으면 아무 일도 없다.

### ⚠ 그 김에 발견한 것 (이번 꾸러미와 무관 · 고치지 않음)

**SADARA 82행의 `qty` 가 전부 `None` 이다.**  지문이 기준선과 같으므로
**이전부터 그랬고 이번 변경과 무관**하다.  AL NOUF1 은 범례에서 유닛 승수를
읽는데 SADARA 범례에는 그 표가 없어 승수가 서지 않는 것으로 보인다.
다음 회차 후보로만 적어 둔다.
