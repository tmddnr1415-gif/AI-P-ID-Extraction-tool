# [G] Nouf1 잔여 일곱 — 원인 · 이번에 한 것 · 넘긴 것

| # | 피드백 | 원인 (근거 회차) | 이번 회차 | 넘긴 이유 |
| --- | --- | --- | --- | --- |
| 2 | p8 `D00P-10LBC50-M05-0001` MOV 미출력 | 발주처 CZH 는 그 장에 MOTOR **5행**, 우리는 **6행**을 이미 낸다.  잉크 걸림을 밸브에 넣으면 7행(순수 FP) (17회차 [B]3) | **사용자 질문 3** 으로 다시 올린다 | 실무 판단 — 그 장 MOV 를 몇 개로 볼 것인가 |
| 3 | PCV 미출력 + NOTE 언급 밸브 Description | p12 `AUX. STEAM LETDOWN VALVE #10` = 범례 ANGLE + DESUPERHEATER 가 **여섯 객체**로 흩어지고 아래 삼각형이 칠해져 두 번 그려짐 (20회차 [8][9]).  흩어진 선분을 모으는 규칙은 58장에서 2,208건이 걸려 잡음 | 등급 판정: **D(등록 화면)** — `unclassified_bodies` 가 "배관 끝막대 둘" 을 요구해 미판정 목록에도 안 뜬다(20회차 [10]) → 그 조건을 넓혀 **등록 화면에 뜨게** 하는 것이 다음 단계 | 형상 매칭 금지(§2.2) |
| 4 | AIT → Analyzer | 표기층(`type_display_names`)은 준비됨.  앵커에 넣으면 축2 89.1 → 88.3 (18회차 [G]) — CZE 에 A 계열 0행 | **사용자 질문 4** — 행을 만들되 발주처 양식에서 뺄 것인가 | 게이트가 잴 수 없다 |
| 5 | PRV 미출력 | `_is_dome` 이 **세로 런의 돔**을 설계상 읽지 않는다 (18회차 미구현 p17).  넓히면 전 도면 영향 | 규명만 | 별도 회차 — 게이트 AL NOUF1 불변 · 축2 94.4 |
| 6 | XV 미출력 | 18회차 [E] 뒤 XV 12행이 이미 나간다(`dv.derive_layout` 기준).  피드백의 XV 가 어느 장인지 없이는 갈 수 없다 | 규명만 — 장 번호 요청 | 원문에 장 번호가 없다 |
| 7 | 미출력 (Symbol & Legend 참조) | 범례에 있는데 검출 어휘에 없는 심볼 — 18회차 [D] 미판정 169건 · 19종류가 등록 화면에 있다 | 규명만 | 사람이 등록 화면에서 지정 |
| 8장 | `*` 표기가 있음에도 SCT | [F] 로 갔다 — 지목 장 번호가 없어 AL NOUF1 전수 독립 감사(19회차 도구)로 대신한다.  AL NOUF1 지문 `fb85b039` 가 이번 회차 끝 회귀에서 그대로면 SCOPE 열은 한 칸도 안 움직인 것이다 | [F] 참조 | — |

**한 회차에 다 하지 않는다** — 일곱 중 이번에 코드를 고친 것은 없다.  [E] 와 겹치는 원인(액추에이터 절대 pt
창)은 없었다 — Nouf1 의 미출력은 몸체 기하(PCV) · 돔 방향(PRV) · 어휘(XV/AIT/범례) 쪽이다.
