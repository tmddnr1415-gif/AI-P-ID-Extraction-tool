# 38회차 보고 — 입찰/실행 모드 · 다중 양식 · Typical 참조

시작 04:18 · 끝 ⟦END⟧ (UTC) · HEAD 시작 `1043edd` → 끝 ⟦HEAD⟧.  전문은 이 zip 의 3~8 번 문서 · 진행 기록은 `2_progress.md`.

## 0. 게이트 — 네 프로젝트 (프로젝트마다 따로 · `9_regression.json`)

| 프로젝트 | [A] 시작 | [H] 끝 | 판정 |
| --- | --- | --- | --- |
| **AL NOUF1** | 1037 · `fb85b039` · 1931 · 축3 95.1 | **1037 · `fb85b039` · 1931 · 95.1** (714초 · 4.9G) | **불변** — 첫 회귀는 `da28465e`·1939 로 움직였고(§2-3) 되돌린 뒤 같다 |
| SADARA | 82 · `0767ba79` · 0 · 75.4 | ⟦H_SAD⟧ | ⟦H_SAD_V⟧ |
| TC2 | 684 · `8a478417` · 3783 · 90.4 | ⟦H_TC2⟧ | ⟦H_TC2_V⟧ |
| UAD | 149 · `db1a77d1` · 149 · 83.1 | ⟦H_UAD⟧ | ⟦H_UAD_V⟧ |

시험 빠른 283 → **297** (모드 7 · Typical 6 · 별표 1) · UI 24 · slow 16 (안 돌림 — 46분 · 이번 회차 20분 상한).

## 1. 한 것 — 단계별

| 단계 | 결과 | 문서 |
| --- | --- | --- |
| [A] 기준선 | 넷 다 "기준선과 같습니다" — 프로젝트마다 따로(nohup · 완료 표식) | `A_*.json` (zip 밖 · 저장소) |
| [B] 입찰/실행 | **있던 것**: `tags.assign`(문서 단위) · 없던 것: 선언 자리.  첫 화면 라디오(자동/입찰/실행) → 프로젝트 장부 `mode{value,author,set_at}` · 결과 화면 띠.  방법은 하나 — `_attach_tags(declared_mode=)` 가 1급 경로를 켜고 끈다.  선언≠실측이면 `conflict` 를 기록하고 붉은 띠 + "실측대로 바꾸기" 버튼.  **게이트: UAD 를 `bid` 로 선언해도 `db1a77d1` · 149 · 태그 0 · 후보 128** | `3_mode.md` · 캡처 B1·B2 |
| [C] UAD p11~17 | **다중 양식이 아니다** — 32장이 한 양식(FICHTNER).  p11~15·p22 는 글자가 획뿐(텍스트 0 · SHX 주석 0), p16~18 은 **타이틀블록만** 획(본문은 TrueType — 30회차 "계기 목록 시트" 는 오기), p32 는 도면번호 꼬리 3자리.  [C-3] 장 단위 양식 판정은 **원인이 아니라서 만들지 않았다** → 범위 재설정은 사용자 판단 (§4 ㉠㉡㉢) | `4_uad_forms.md` · 캡처 C1~C3 |
| [D] Typical | 실측(TC2 54장): 라인 표식 **21 · 5장** · 상세 **7** · 짝 7/7 · 상세 없는 표식 `G` 8(p24) · 모터 `M` 원 18.  구현 `app/engine/typical.py` + `pipeline._apply_typical`(곱하는 곳 하나) · `TYPICAL_AMBIGUOUS`.  ⟦D_RESULT⟧ | `5_typical.md` · 캡처 D1 |
| [E] TC2 MOV/NRV/FIT/FE | 원인 하나(밸브): `ValveLayout.act_box=(9.0,34.0)` 절대 pt — TC2 `M` 원 Ø7.08 이 하한 9 아래 (AL NOUF1 이 **빼려던** 글로브 허리 7.1 과 같은 값).  범례 `actuator_stem` 이 `CONFIG_FALLBACK` 이라 원 지름까지 같이 잃는다.  NRV 는 10회차 [D] 와 같다.  FIT/FE 는 짝 버블·미판정·경보 접미가 섞여 장별 대조 몫.  **코드 0줄** | `6_tc2_missing.md` |
| [F] SCOPE 별표 | 옛 결과인지 먼저 갈랐다 — p23 PDIT 2행은 **현재도 틀리고**(별표가 아니라 위 `PDIA++,+` 의 `+` 획), p29 PI 는 활자 `*` 가 실제로 있어 **우리 값이 맞다**.  원인: 판정기(방향 둘 이상)와 26회차 검사 채널(셋 이상)이 다른 기준 — 본문 별표만 `min_dirs=3`.  ⟦F_RESULT⟧ | `7_scope.md` · 캡처 F1~F3 |
| [G] Nouf1 잔여 7 | **코드 0줄** — 원인 표.  p8 MOV(질문 3) · PCV(등록 화면 조건 확장이 다음) · AIT(질문 4) · PRV(`_is_dome` 세로 런) · XV(장 번호 필요) · 범례 심볼(등록 화면) · 8장 `*`([F] 로) | `8_nouf1.md` |
| [H] 회귀 | 위 표.  ⟦H_NOTE⟧ | `9_regression.json` |

## 2. 스스로 뒤집은 판단

1. **[D] 캡션을 `:` 로 찾기** → p8 이 `D HRH TYPICAL …` 이라 놓친다 → "오른쪽 인접 낱말" → 라인 표식까지 캡션이 된다 → **아무 선도 닿지 않는 원 + 같은 줄 두 낱말 이상**.
2. **[D] 상자를 긴 가로선 둘로** → TC2 사변형 상자는 선분으로 안 나와 먼 선을 집는다 → 캡션을 담는 후보(도형 · 선 기반) 중 **가장 작은 것**.
3. **[D] 첫 [H] 가 AL NOUF1 을 옮겼다** (`da28465e` · Q'ty +8) — 모터 `M` 원(Ø14.2 < 버블 22.6 · 오른쪽 `LO VS`)이 캡션이 됐다 → "양쪽에서 들어오는 선이면 라인" 으로 고쳤더니 **TC2 참조가 전부 0** (TC2 라인 표식은 리더 하나 · 모터 M 과 모양이 같다) → 가르는 것은 모양이 아니라 **캡션 짝**, 캡션은 선이 안 닿는 원뿐.
4. **[C] 장 단위 양식 판정을 만들려 했다** → 실측이 "양식은 하나" 라고 답해 만들지 않았다.  30회차의 "p16~18 은 계기 목록 시트" 도 오기였다 — 본문 텍스트가 있는 진짜 P&ID 다.
5. **[F] p29 를 고치려 했다** → 렌더가 `*` 를 보여줘 우리 값이 맞다.  옛 결과인지 먼저 가른 것이 맞았다.
6. `CFG.rect` 가 튜플 — `.contains` 로 AL NOUF1 회귀가 1장에서 죽었다 (7분).  실 쪽 검사를 시험에 넣었다.

## 3. "찾고 있는데 안 쓰던 것" — 열다섯 번째

* `tags.assign` 이 이미 "이 도면이 1급인가" 를 재고 있었다 — [B] 는 그 위에 **선언 자리와 스위치 하나**만 얹었다.
* `store_result` 의 engine_json 화이트리스트에 `evidence_tier` 가 **29회차부터 빠져 있었다** — 계산은 되는데 저장이 안 돼 화면이 읽을 수 없었다.  이번에 `typical`·`typical_stats` 와 함께 넣었다.

## 4. 사용자 판단 (§14.3 — 스스로 정하지 않았다)

| # | 물음 | 걸린 것 |
| --- | --- | --- |
| 1 | SADARA `10` · TC2 `00` · UAD `00` 의 유닛 승수 | 31회차 자리 — 값은 아직 없음 |
| 2 | **Typical 배수 × 유닛 승수** 를 곱할 것인가 | 지금은 둘 다 곱한다(근거 패널에 두 인수 따로).  하나만이면 인수 하나를 뺀다 — ⟦D_DELTA⟧ |
| 3 | p8 MOV — 발주처 5 ↔ 우리 6 | 어느 쪽이 맞는가 |
| 4 | AIT → 행을 만들되 양식에서 뺄 것인가 | 축2 89.1→88.3 |
| 5 | UAD Description 을 태그로 | 102행 순증 · 채점 축 없음 |
| 6 | UAD `VENDOR`(이름 없음) 30행 | 그 장 NOTES 가 정의하지 않는다 |
| **7** | **[C-3] 범위** — ㉠ 획 사전 전체(p11~15·22 본문까지) / ㉡ 타이틀블록만(글리프 라이브러리 확장 · 도면번호·REV 회수) / ㉢ 보류 | 권장 ㉡ — 30회차 [D] 와 같은 자리(글리프 사전)이고 도면번호가 지문 밖 |
| 8 | [E] `act_box` 를 범례 원 지름의 비율로 — 다음 회차에 할 것인가 | TC2 MOV 0 → N · 게이트 AL NOUF1 불변 |

## 5. 시간

4시간 상한 안 (04:18 → ⟦END⟧).  회사 PC 꾸러미는 이번 회차에 만들지 않았다 (프롬프트).
