# 33회차 요약 — 죽는 버그 · 색이 사실을 말하게 · 메모리

**셋 다 "새 프로젝트가 오면 바로 겪는 것" 이었고, 셋 다 닫았다.**  다른 것은 손대지
않았다.  네 프로젝트 회귀가 시작과 끝에서 같다 (AL NOUF1 `fb85b039`·1037·1931·95.1 ·
SADARA `0767ba79`·82·75.4 · TC2 `c0a2d29d`·607·3047·89.1 · UAD `db1a77d1`·149·149·83.0)
· 합성 7 도 32회차와 같다(⑤⑦은 지문까지) · 빠른 시험 266 → **270** · 실 DB sha256 불변.

| # | 무엇 | 답 | 게이트 |
| --- | --- | --- | --- |
| ① | **1급 문서에 태그 붙은 밸브가 있으면 죽는다** | 원래 설계는 **밸브 쪽**(`evidence["tag"]` = 버블 글자, 2026-08-17, `type_display` 가 읽음).  29회차가 태그 번호 근거를 같은 열쇠에 dict 로 넣었다.  → `_attach_tags` **하나** · `evidence["tag_no"]`.  `isinstance` 우회 없음 | 합성 ⑧b **죽음 → 완주** 78행 · 1급 · 태그 38(밸브 8) · **UAD 불변**(밸브 3행에 태그가 없는 것이 사실이다) |
| ② | **검토 사유가 SCOPE 색을 덮는다** | **의도였고 전제가 죽었다** — 오버레이 색을 만든 첫 커밋 `920a5558` 이 "보류한 행" 을 네 번째 색으로 두었다.  31회차 승수 사유가 전량에 붙자 한 색이 됐다.  → 색 = SCOPE · 검토 = 모서리 붉은 ● `!`(사유 말풍선) · 범례 `검토 필요 (그중) N`.  색+형태+라벨 | 지문 넷 불변(층은 지문 밖) · **세 색 칸 합 = 상자 수 = 그리드 행 수** · 28장 전/후 캡처 · 옛 저장의 `REVIEW` 는 그리드 SCOPE 열에서 되찾음 |
| ③ | **최대 RSS 가 안 돌아온다** | **★ 32회차 결론이 틀렸다** — `ru_maxrss` 는 최고 수위.  VmRSS 는 7.38G 뒤 **0.75G** 로 돌아온다.  다만 잔류가 문서마다 조금씩 늘었고 tracemalloc 이 정체를 잡았다: **마지막 장의 잉크 인덱스** 183MB (`_INK_LAST` 가 맨 마지막 장을 놓아 줄 다음 장이 없다).  → `release_ink()` 를 `analyse` 의 `finally` 에 | SADARA→UAD 잔류 0.50 → **0.22G** · 지문 불변.  ⚠ tracemalloc UAD 는 OOM(10.5GB)으로 못 쟀다 — 그 부분은 추론 |

**★ "찾고 있는데 안 쓰던 것" 열세 번째** — 층 항목은 첫 설계부터 `needs_review`·`reason`
을 `scope` 와 따로 싣고 있었다.  새 값을 만든 것이 아니라 실려 있던 값을 그리게 했다.

**스스로 뒤집은 판단** — 32회차 메모리 결론(측정 정의 오독) · "고칠 것이 없다" 도
절반이었다(잔류 증가는 사실) · 캡처 도구가 완료 화면을 못 알아봤다.

## 파일

`1_보고서.md`(전문) · `3_죽는버그.md` · `4_색.md` · `5_메모리.md` · `6_regression_4p.json` ·
`7_캡처/` (`synthetic_8b_전_사망.png` · `후_완주.png` · `synthetic_8b_후/` · `색_전후/<프로젝트>_전|후/` · `범례패널/`)
