# 33회차 보고서 — 죽는 버그 · 색이 사실을 말하게 · 메모리

출처: 32회차 "안 되는 것 여덟" 중 §5 1순위 둘 + 메모리 경고.  **다른 것은 손대지
않았다** (경보 접미 · 승수 값 · 버킷 · 나머지 여섯은 대기 목록 그대로).

## A 기준선 · ⑧b 사망 재현

`python3 spike/regression_3p.py` — 넷 다 기준선과 같음 (AL NOUF1 `fb85b039`·1037·
1931·95.1 · SADARA `0767ba79`·82·75.4 · TC2 `c0a2d29d`·607·3047·89.1 · UAD
`db1a77d1`·149·149·83.0).  기록 `6_regression_4p_before.json`.

합성 ⑧b (`tests/data/synthetic/08b_tagged_same_size.pdf`, 태그 한 축만 바꾼 AL NOUF1
발췌 6장):

```
AttributeError: 'str' object has no attribute 'update'
  app/pipeline.py:1042   r.evidence.setdefault("tag", {}).update(
138.9초 · 죽는 행 p5 MOV GLOBE/MOTOR · evidence["tag"]='MOV' · 붙이려던 태그 14LBA14CP039
같은 경로: 태그 붙은 밸브 8행 ('MOV' 2 · '' 6) — '' 도 .update 가 없다
```

화면은 `이 PDF 를 분석하지 못했습니다. 사유를 특정하지 못했습니다.` (일반 문구) —
`7_캡처/synthetic_8b_전_사망.png` (고치기 전 코드 `2410e73` worktree 에 같은 절차로 올림).

## B 죽는 버그 — 원래 설계는 **밸브 쪽(문자열)** 이다  (`3_죽는버그.md`)

| 자리 | 커밋 | 뜻 | 모양 | 읽는 곳 |
| --- | --- | --- | --- | --- |
| `pipeline.py` 밸브 행 `"tag": b.tag` | `786a1fbe` 2026-08-17 (Phase 1 MVP) | 밸브의 **버블 글자** MOV/HOV | `str` | `type_display` → `MOV(GLOBE)` · 근거 패널 "태그 버블" |
| `pipeline.py:1042` 1급 경로 | `08d2e5e8` 2026-09-11 (29회차) | 도면이 인쇄한 **태그 번호** 근거 | `dict` | 아무도 — 화면·산출물은 `r.tag_no` 만 본다 |

이름만 같고 뜻이 다르다.  **고친 쪽은 1042** — 태그 붙이기를 `_attach_tags(rows,
pages)` **하나**로 떼고 열쇠를 행 필드와 같은 `tag_no` 로.  `isinstance` 분기 없음.
근거 패널도 `Tag No. 근거 / 규칙` 을 `e.tag_no` 에서 읽는다 (같은 충돌로 UAD 계기
128행이 "태그 버블 `[object Object]`" 를 찍고 있었다 — 옛 저장분의 dict 는 그
자리로 받는다).

**★ UAD 가 안 죽은 것은 우연이 맞다** — 밸브 3행(`''`·`''`·`'BPRV'`)에 1급 태그가
0/3 붙어 `.update` 가 문자열을 만난 적이 없다.  다른 셋은 2급이라 이 줄에 안 온다.

게이트: ⑧b **완주** 132.4초 · **78행**(태그 없는 ⑤ 와 같다) · 1급 · 태그 붙은 38행
(계기 30 · **밸브 8**) · `7_캡처/synthetic_8b_후/p5_rows_right.png` 에 죽던 행이
`14LBA14CP039 · TO HRSG#12 BD TANK GLOBE VALVE` 로 서 있다 · 완료 화면
`synthetic_8b_후_완주.png`.  UAD 149 · `db1a77d1` 불변 (→ E).

## C 색 — **의도였고 전제가 죽었다**  (`4_색.md`)

`git blame`: `SCOPE_REVIEW if r.needs_review` 는 오버레이 색을 처음 만든 커밋
`920a5558` (2026-08-17, *"Colour the drawing by scope…"*) 의 것이고 상수 주석이
*"a row whose judgement is held open"* 이다.  검토 행이 드물 때(AL NOUF1 14.7%)
"보류한 행을 눈에 띄게" 였다.  12회차가 "색은 SCOPE 열 하나에서" 라고 적으면서
이 첫 줄을 남겼고, 31회차 승수 사유가 SADARA 82/82 · UAD 149/149 에 붙자 전제가
죽었다.  §6 의 "`_layers` 가 SCOPE 열과 같은 값을 읽는다" 는 **검토 사유가 없는
행에서만** 사실이었다.

고른 설계 **(a)+(c)** — 색 = SCOPE(파랑/주황/보라) · 형태 = 상자 모서리 붉은 ● `!`
(마우스에 사유) · 라벨 = 범례 `검토 필요 (그중) N` + 주석 `모서리 ● 검토 필요`.
(b) 반투명은 버렸다 — 검토가 전량인 문서에서 도면 전체가 흐려져 같은 "한 색" 이
된다.  테두리에 넣지 않은 이유: 색·파선(밸브)·바깥 링(개정) 셋이 이미 테두리를
쓰고 그 다툼이 실제로 깨졌던 기록이 `drawOverlay` 에 있다.

**범례 등식** — `SCT + VENDOR + 판정없음 = 상자 수 = 그리드 행 수` 를 유지하고 검토
칸은 **그중**이다.  옛 "네 칸 합" 은 검토가 공급 주체를 지운 결과였으므로 그 산술을
유지하는 것이 곧 결함을 유지하는 것이었다.  옛 분석의 `REVIEW` 항목은
`itemScope()` 가 그리드 SCOPE 열(`cellValue`)에서 되찾는다.

**★ 열세 번째 "찾고 있는데 안 쓰던 것"** — 층 항목은 처음부터 `needs_review`·
`reason` 을 `scope` 와 **따로** 싣고 있었다.  한 칸이 두 축을 접었을 뿐이다.

캡처 전/후 28장 → E 뒤에 (`7_캡처/색_전후/`).

## D 메모리  (`5_메모리.md`)

(E 뒤에 채운다)

## E 회귀

(채운다)

## F 출력 · state_and_roadmap

(채운다)

## + 스스로 뒤집은 판단

1. **32회차 결론 ④ "메모리가 안 돌아온다"** — `ru_maxrss` 는 최고 수위라 내려올 수
   없는 숫자였다.  VmRSS 로 다시 재니 AL NOUF1 직후 **0.58G**(최고 4.9G), TC2 직후
   **0.75G**(최고 7.38G).  **돌아온다.**
2. (E 뒤에 더 있으면 적는다)
