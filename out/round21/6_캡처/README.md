# 21회차 캡처 — 실물 렌더

전부 **실제 서버 + 실제 Chromium** 입니다.  격리된 `PID_DATA_DIR` 두 벌을 썼고
실 DB(`app/_data/app.db`)는 열지 않았습니다 (sha256 시작·끝 동일).

| 쪽 | 서버 | PDF |
|---|---|---|
| `before_*` | **20회차 배포본** (`ec43fbd` worktree) · 포트 8412 | 아래 둘 |
| `after_*` | 이번 회차 HEAD · 포트 8411 | 같은 둘 |

시험용 PDF 두 개는 합성입니다 (`data/` 를 쓰지 않습니다):

| 파일 | 무엇을 재현하나 |
|---|---|
| `other_form.pdf` — A3 1191x842, 20장 | 다른 회사 양식 → 타이틀블록 칸이 **종이 밖** |
| `thin_history.pdf` — A1 2384x1684, 12장, 이력 괘선 간격 **2.0pt** | **TC2 의 원본 예외와 같은 갈래** (`extract_titleblocks.py:321`) |

## 무엇이 달라졌나

| | before (r20) | after |
|---|---|---|
| 사유 | `이 PDF 를 분석하지 못했습니다. 사유를 특정하지 못했습니다.` | 어느 칸을 어디로 보고 있었는지 · 이 문서의 쪽 크기 · **무엇을 하면 되는지** |
| 어디서 멈췄나 | `도면을 한 장도 읽기 전에 멈췄습니다` (여섯 단계를 한 문장이 덮음) | `도면을 읽기 전, 문서를 여는 중 / 타이틀블록을 읽는 중 단계에서 멈췄습니다` |
| 예외 원문 | 화면에 없음 (진단 zip 을 풀어야 함) | **접힌 채로 있고 펼치면 받아 옵니다** |
| A3 20장 도달 시간 | 선분 캐시 · 치수 계산을 다 지난 뒤 | **0초** (`opening the document` 에서 끊음) |

## 파일

| 파일 | md5(앞 12) |
|---|---|
| `before_A3_1_실패화면.png` | `8e8b8d3ce9b2` |
| `before_thin_1_실패화면.png` | `531e4279bf21` |
| `after_1_실패화면.png` | `819dca1c2720` |
| `after_2_예외원문펼침.png` | `74e79bac919f` |
| `after_thin_1_실패화면.png` | `c54933fb6cbe` |
| `after_thin_2_예외원문펼침.png` | `f30fa95c214a` |

여섯 장의 md5 가 전부 다릅니다 — 같은 화면을 여섯 번 찍은 것이 아닙니다
(§8 "서로 다른 시점의 캡처가 바이트로 달라야 한다").

`*_facts.json` 은 그 캡처에서 화면이 실제로 말한 문장을 그대로 담은 것입니다.
