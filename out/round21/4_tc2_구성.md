# 21회차 [C] — TC2 60장 구성

## 못 셌습니다 — 파일이 이 작업 환경에 없습니다

`TC2_260821.pdf` (13.41MB · 60장) 를 아래를 **전부** 뒤졌고 없습니다.
20회차에 SADARA 를 한 자리만 보고 "없다" 고 오보한 전례가 있어 넓게 봤습니다
(`out/round21_progress.md` §0 에 방법과 결과가 그대로 있습니다).

| 찾은 방법 | 결과 |
|---|---|
| 이름 패턴 `*TC2*` `*tc2*` `*260821*` — 파일시스템 전체 | 0건 |
| 시스템 전체 `*.pdf` 를 크기순으로 나열 | 최대 `pid_total.pdf` 20.4MB · `sadara_1A46.pdf` 10.5MB |
| 13~15MB PDF | **0건** |
| `app/_data/uploads/` | 4개 — pid_total x3 · sadara x1 |
| `/mnt/user-data/uploads` · `/mnt/user-data/working` · `/mnt/attach` | 비어 있음 |
| 실 DB `job` 표 (읽기 전용) | 4건 · **TC2 없음** |

## 파일이 오면 이 표를 채웁니다

| 물음 | 왜 필요한가 |
|---|---|
| 쪽마다 `page.rect` (너비 x 높이) | 타이틀블록 칸이 종이 안인지 밖인지 |
| 이력 표 괘선의 y 목록과 **간격 분포** | §3 문서 §2-1 이 예측한 "2.4pt 이하 간격" 이 실제로 있는지 |
| 도면번호 · 제목 칸의 실제 좌표 | `config/project_tc2.yaml` 의 `title_block` |
| 60장의 종류 (범례 / 도면 목록 / PID) | `targets` 가 몇 장이 되는지 |
| 범례 장이 있는지 · 어떤 표를 인쇄하는지 | `legend_rules` 가 설 수 있는지 |
| 도면번호 형식 | `formats.drawing_no` 정규식 |

## 예측 (파일이 오면 맞는지 확인할 것)

§3 문서 §2-1 의 추론:

> TC2 의 종이는 **최소 2320 x 1390pt** 이고, 이력 표에 **세로 간격 2.4pt 이하**
> 인 괘선 두 줄이 있다.

이 둘이 맞는지가 이번 규명이 옳았는지의 시험입니다.  틀리면 그 사실을 그대로
적고 다시 봅니다.
