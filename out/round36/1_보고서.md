# 36회차 보고서 — `type_map` 재바인딩 · 승수 문지기 제거 · 유닛 표기 규칙 · 삭제 목록 적용

시작 HEAD `84d192d` · 21:07.  본선 수정은 커밋 여섯(4b2f346 · f7fec47 · e615003 · 87e2705 · f4cbca1 · ec5fa21).
**AL NOUF1 `fb85b039` · 1037 · 1931 · 축2 94.4 · 축3 95.1 은 처음부터 끝까지 그대로다.  움직인 것은 TC2 하나이고 [D] 가 움직였다.**

## 한 줄

결함 하나(`anchors` 표가 import 때 굳음)를 고쳐 35회차의 "시험불가 25" 가 전부 ㉣ 로 섰고, 승수 낱말 문지기 넷을 지웠는데
네 프로젝트 Q'ty 가 한 칸도 안 움직였고, 유닛 표기의 **꼴을 도면에서 배우게** 하니 TC2 p33·p34 40행이 x1 → x4 로 섰다
(Q'ty 3047 → 3167 · 설정 폴백 203 → 163).  35회차 삭제 목록 38 중 **24 를 적용**했고 14 는 적용할 수 없었다 — 그 13 을
SADARA·TC2·UAD 가 3급 폴백으로 읽고 있었다.  외워둔 값 199 → **175** (+ 코드 낱말 4).

## A. 기준선 + 합성 8

| | AL NOUF1 | SADARA | TC2 | UAD |
|---|---|---|---|---|
| 기준선 | `fb85b039`·1037·1931 | `0767ba79`·82 | `c0a2d29d`·607·3047 | `db1a77d1`·149 |
| 시작 (`8_regression_4p_start.json`) | 같음 (84d192d 코드 · 새 실행 21:17) | 같음 (35회차 끝 회귀 · 엔진 동일 커밋) | 같음 (〃) | 같음 (〃) |

⚠ 첫 회귀가 AL NOUF1 뒤에 **멈췄다** — 하네스 `spike/regression_3p.py` 가 [B] 가 지운 사설 이름 `ds._V3_FIELD_TYPE_MAP` 을
읽고 있었다 (`spike/multiplier_gate.py` 도).  공개 이름 `RULESET_V3.field_type_map` 으로 고쳤다.  하네스는 프로젝트마다
자식 프로세스를 새로 띄우므로 그 실행의 뒤 세 프로젝트는 [B][C] 코드로 돌았다 → [B][C] 게이트 실행(`8_regression_4p_BC.json`)이 그 몫을 잇는다.
합성 8: __SYN__

## B. `anchors.type_map` 재바인딩 — `3_type_map.md`

* `ds.ruleset_v3(cfg)` 한 곳 · `_rebind_config` 이 `RULESET_V3`+별칭 3 을 다시 만든다.  같이 넣은 셋: `formats.unit_code_*` · `formats.system_code_chars` · `text.dedup_exact_duplicates`(유도 항목).
* **★ import 때 굳는 값 전수 37** (AST) — `_rebind_config` 이 다시 만드는 것 20 · **안 만드는 것 17**(D·E 등급 — 유도가 안 건드려 지금은 갈릴 길이 없다 · 목록은 3_type_map.md).  `tests/test_config_isolation.py` 는 `global` 문만 세어 이 표를 못 본다.
* **25개 재판정: 전부 ㉣.**  worktree 탐침이 `cfg.anchors.type_map.TT` 에서 멈춘다 (35회차엔 199행으로 완주했다 — 굳은 표를 읽어서).  ㉢ 는 없다.
* 게이트: [B]+[C] 회귀 넷 일치.

## C. 승수 낱말 문지기 제거 — `4_gatekeeper.md`

* `note_unit_span` 은 **번호 붙은 NOTES 문단 + 유닛 표기 둘 이상** 만 본다.  `_DEFAULT_SAME` 4 · `qty_note.same_words` 삭제.
* **네 프로젝트 Q'ty 불변** 1931 · 0 · 3047 · 149 · 지문 넷 불변 · TC2 `unit_notes` 35 · 폴백 203 그대로.  **새로 잡힌 문단 0.**

## D. 유닛 표기 — `5_unit_token.md`

* **전수 원문**: 유닛은 세 꼴로만 인쇄된다 — TC2 `D-D`(`UNIT 3-1`) · AL NOUF1 `#DD`(`GROUP#10`) · UAD `UNIT DD`(`GT UNIT 11`).  SADARA 는 없다.
* **오검 위험 목록**: 맨 숫자는 문단 번호(`10.`) · 날짜(`26.08.21`) · `LEGEND (1/2)` · `20 DIAMETERS` · 도면번호 참조 `00GEN00-M05-0002~0005` 에 전부 걸린다 → **맨 숫자는 배우지 않는다** (거절 규칙).  표식 있는 꼴(`D-D`·`#DD`)이 유닛 아닌 곳에 쓰인 예는 번호 문단에서 **0건**.
* **그 도면에서 읽는 길 — 있다.**  장마다 도면번호의 유닛 자리(`tb.parse_unit_code`, 이미 읽는다)가 있고 그 장의 NOTES 가 **유닛 낱말 뒤에 자기 코드**를 어떤 꼴로 적는지 세면 문서마다 꼴이 나온다: AL NOUF1 `#DD` 25장 · TC2 `D-D` 18장 · UAD·SADARA 없음.  `learn_unit_forms` 가 그것을 두 장 이상에서 배우고(18회차 규칙), 배운 꼴은 낱말 없이 나와도 유닛이다.  `3-1`·`#10` 을 코드에 나열하지 않았다 — 나열한 것은 "코드 c 를 이 표식으로 찾는 법" 셋이고 어느 것을 쓸지는 문서가 정한다.
* **p33·p34 는 4** — 옆 장 p35(`… FOR UNIT 3-1 & 3-2, SIMILAR … FOR 4-1 & 4-2, …`)를 엔진이 이미 8 로 세던 것과 같은 규칙(열거를 센다 · 해석하지 않는다).  "SIMILAR P&ID 가 같은 계기를 갖는가" 는 SIMILAR 23장 전체에 걸린 의심이라 **실무 판단 목록**에 한 묶음으로 올린다 (사용자에게 묻는 항목).
* **TC2 Q'ty 3047 → 3167** · p33 20행 · p34 20행 (x1 → x4) · `MULTIPLIER_FROM_CONFIG` 203 → **163** · `unit_notes` 35 → 37 · 배운 꼴 `{'D-D': 23}`.  지문 `c0a2d29d` → **`018315ef`** (qty 열만).  AL NOUF1(범례가 이긴다)·SADARA·UAD(배운 꼴 없음) 불변.

## E. 삭제 목록 — `6_delete.md`

* **적용 24** (두 커밋): dataclass 14 (죽은 필드 `side_tol`·`note_mark_row_tol` 삭제 · config 가 언제나 덮던 기본값 12 는 `None` + `cfg.get` 필수) · config 잎 10 (`x_slack`·`note_row_tol` 코드 참조 0 · `qty_note` 3 = 코드 기본값 · `legend_fallback.butterfly` 5 = 네 프로젝트 전부 LEGEND).  죽은 참조 0 (grep).
* **적용 불가 14**: `legend_fallback.actuator_stem` 4 (SADARA·TC2·UAD 가 `CONFIG_FALLBACK`) · `pneumatic` 6 (SADARA) · `unit_multiplier_fallback` 3 (TC2 203행 · UAD 149행) — 지우면 게이트가 깨진다 → **사람 지정** 목록으로.  `glyph_sizes` 1 — 35회차 ㉡ 가 낯선 프로필 경로에서만 참(스스로 뒤집음) → 조사.  `project.code` — 열쇠.
* 회귀 E-1·E-2 각각 넷 일치.

## F. 회귀 — `8_regression_4p.json`

| | 행 | 지문 | Q'ty | 축3 | 원인 |
|---|---:|---|---:|---:|---|
| AL NOUF1 | 1037 | `fb85b039` | 1931 | 95.1 | 불변 |
| SADARA | 82 | `0767ba79` | 0 | 75.4 | 불변 |
| TC2 | 607 | **`018315ef`** | **3167** | 89.1 | **[D]** — p33·p34 40행 qty 1→4.  다른 열 0 (행 607 · 축3 여섯 지표 그대로) |
| UAD | 149 | `db1a77d1` | 149 | 83.0 | 불변 |

시험 270 → **276** (`tests/test_unit_forms.py` 6).  합성 8 · 순서 바꿔 돌리기: __F2__

## G. 출력 — `out/round36_result.zip` · `docs/state_and_roadmap.md` §5

## 스스로 뒤집은 판단

1. **35회차 삭제 38 중 13 은 삭제가 아니었다** — ㉡ 판정이 AL NOUF1 한 문서에서 나온 것이었고, 같은 값을 세 프로젝트가 3급 폴백으로 읽는다.
2. **`glyph_sizes` ㉡ 도 조건부였다** — 35회차 묶음 토글은 `project.code` 를 표식으로 바꿔 낯선 프로필 경로를 강제했고, 자기 프로필 문서에서는 `_fit_layout` 이 아무것도 얹지 않는다.
3. **첫 회귀를 [A] 로 쓸 수 없었다** — 자식 프로세스가 코드를 새로 import 한다는 것을 잊고 [B][C] 를 커밋했다.  [A] 는 AL NOUF1 새 실행 + 35회차 끝 회귀로, [B][C] 게이트는 별도 실행으로 갈랐다.
4. **하네스가 사설 이름을 읽고 있었다** — 내가 지운 이름이라 내가 만든 죽은 참조다.

## ★ "찾고 있는데 안 쓰던 것" — 14번째

**유닛 표기의 꼴.**  도면번호의 유닛 자리는 `tb.parse_unit_code` 가 처음부터 읽고, NOTES 문단은 `_note_paragraphs` 가 읽고 있었다.
둘을 맞대면 "이 문서는 코드 31 을 `3-1` 로 적는다" 가 나오는데, `_unit_tokens` 는 유닛 낱말 바로 뒤만 보고 있었다.

## ★ 사람 몫 — 검토 장/행 전후

| | 전 | 후 |
|---|---|---|
| TC2 `MULTIPLIER_FROM_CONFIG` | 203행 (15장) | **163행 (13장)** — p33·p34 가 도면 값으로 |
| AL NOUF1 · SADARA · UAD | 변화 없음 | 변화 없음 (SADARA 82 UNDEFINED · UAD 149 FROM_CONFIG 그대로) |

## ★ 외워둔 값 — 199 → 175

삭제 24 (dataclass 14 · config 잎 10).  목록 밖 코드 낱말 4.  [B] 는 더한 것이 없다 (25 는 재분류).  남은 175 의 다섯 목록: `7_remaining.md`.

## 실무 판단 목록 (§14.3 — 스스로 정하지 않았다)

1. SIMILAR 장(TC2 23장 + p33·p34)의 배수 — "SIMILAR P&ID" 가 같은 계기를 갖는가.  지금은 열거를 센다.
2. 3급 폴백 13 (`actuator_stem`·`pneumatic`·`unit_multiplier_fallback`) — 지우려면 사람 지정 자리가 먼저다.
3. `glyph_sizes` — 자기 프로필 문서에서도 설정에 없는 키를 유도값으로 얹을지.
