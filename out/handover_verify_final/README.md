# 회사 PC 반영본 — 최종 검증 (HEAD `30a5908`)

꾸러미를 **현재 HEAD 로 다시 만들고**, 회사 PC 조건에서 적용한 뒤
**시험 전량**을 돌린 기록.

**격리** — 실 DB(`app/_data/app.db`)는 열지 않았다.
sha256 `df9875e5…87d2324` 가 작업 전후 같다.

| | |
| --- | --- |
| 옛 코드 | `git worktree` @ aac64ba (검증 뒤 제거) |
| 데이터 | `PID_DATA_DIR=<scratchpad>/fin/site` (그 자리에서 만든 824행 DB) |

## 꾸러미가 최신인가

`f4b055f`(직전 꾸러미 시점) → `30a5908`(현재 HEAD) 사이에 바뀐 것은
`out/round14/` 의 캡처 3개뿐이고 **배포 대상은 하나도 안 바뀌었다.**
그래도 HEAD 로 다시 만들었고, 결과가 **바이트까지 같았다**
(엔트리 26개 전부 동일 · 추가·빠짐 0).

## 절차와 결과

| 단계 | 한 일 | 결과 |
| --- | --- | ---: |
| 1–3 | 옛 코드로 분석 + 손편집 3칸 | 824행 · 지문 `91ba3e33` · SCOPE 빈칸 822 / SCT 2 |
| 4 | DB 백업 | 32MB |
| 5 | zip 을 덮어씀 | 26엔트리 · `app/_data` **0** · 푼 25파일이 HEAD 와 바이트 동일 |
| 6 | 수동 삭제 | **0개** (D·R 없음) |
| 7 | 서버 기동 | 1.5초 · 예외 0 · JS 오류 0 |
| 8 | 첫 화면 | `회사PC재현` 이 목록에 뜸 |
| 9 | 옛 프로젝트 클릭 | **824행이 0.8초**에 열림 · **편집 3칸 살아 있음** |
| 10 | 재분석 | 353초 · **1029행 · 지문 `69ad281f`** · 도면 Rev.D · 옛 편집 3칸 생존 |
| 11 | ★ 적용 후 시험 전량 | 아래 표 |

## 합격 기준 대조

| 기준 | 실측 |
| --- | --- |
| 서버 기동 · 예외 0 · JS 오류 0 | ✓ |
| 옛 프로젝트가 열리고 편집 생존 | ✓ 824행 0.8초 · 편집 3칸 |
| 새 분석 1029행 · Q'ty 1911 · 지문 69ad281f | ✓ (Q'ty 화면값 1912 = 엔진 1911 + 손편집 +1) |
| 발주처 양식 CZE 667 · CZH 66 · CZI 22 · CZF 25 | ✓ (CZF 는 양식 미수령이라 skipped 25) |
| 축1 99.3%/70.5% · 축2 99.2%/86.8% | ✓ 둘 다 실측 일치 (아래) |
| ★ 시험 전량 통과 | ✓ 빠른 150 · UI 24 · slow 10 (아래) |

재분석 **전** 발주처 양식은 `legacy_no_scope_rows: 822` 로 옛 행이 전부
담긴다. 재분석 **후** 0 이 된다.

## 축1 · 축2 (적용된 나무에서 다시 잼)

```
== 축 [전량]  (우리 쪽 모수 1029행)
전체              599     844   595    99.3%    70.5%
== 축 [SCT]  (우리 쪽 모수 780행)
전체              599     684   594    99.2%    86.8%
```

기준선과 **네 값 전부 같다.**  정답지 쪽 모수 599 도 두 축에서 같다.

## ★ 11단계 — 적용된 나무에서 시험 전량

| 묶음 | 결과 | 시간 |
| --- | --- | ---: |
| 빠른 (`-m "not slow and not ui"`) | **147 통과 + 3 건너뜀** (= 150) | 12초 |
| UI (`-m ui`) | **24 통과** | 1876초 |
| slow (`-m slow`) | **10 통과** | 2091초 |

**실패 0.**  건너뛴 3건은 `tests/test_candidate_intake.py` 이고, 사유는
꾸러미에 넣지 않는 산출물 `out/candidates_nouf1.xlsx` 가 없기 때문이다
(발주처 문장이 들어가는 파일 — `.gitignore` 대상).  회사 PC 에서도 같은
이유로 건너뛴다.

UI 24 는 **SCOPE 가 있는 데이터**(재분석 뒤 SCT 667 / VENDOR 218)에서
돈 값이다.  14회차 `test_step0_the_suite_runs_against_scope_aware_data`
가 그 전제를 시험으로 못박고 있으므로, 옛 DB 였다면 통과가 아니라
**실패**했을 것이다.
