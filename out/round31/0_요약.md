# 31회차 요약 — 승수를 사람이 한 번 지정하는 길 (3급 경로)

**이 회차가 한 일은 묻는 자리를 만든 것이다.  답을 넣는 것은 사용자다.**

## 결과 한 장

| | 전 | 후 |
| --- | --- | --- |
| 승수 때문에 검토에 오르는 행 | **434행 · 33장** (SADARA 82 · TC2 203 · UAD 149) | 같음 — 다만 **3번의 답**으로 덮인다 |
| 사람이 누르는 횟수 | 없음 (넣을 자리가 없었다) | **3** (유닛코드 단위) |
| AL NOUF1 | 대상 **0행** | **0행** (범례가 답한다 — 묻지 않는다) |
| SADARA 축3 | 75.4 | **92.0** (값을 넣었을 때) |
| 빠른 시험 | 247 | **266** |

## 네 프로젝트 — 사람 값이 없으면 전부 불변 (실측 · 약 22분)

| 프로젝트 | 행 | 지문 | Q'ty | 축3 |
| --- | ---: | --- | ---: | ---: |
| **AL NOUF1** | 1037 | **`fb85b039`** | 1931 | 95.1 |
| SADARA | 82 | `0767ba79` | 0 | 75.4 |
| TC2 | 607 | `c0a2d29d` | 3047 | 89.1 |
| UAD | 149 | `db1a77d1` | 149 | 83.0 |

축1 99.3/70.5 · 축2 99.3/94.4 · 양식 885/76/23 · Tag `.....` · REV 58/58 ·
글리프 H4/M32 — 전부 불변.

## 게이트 4단계

1. 사람 값 없음 → 넷 다 **기준선과 같음**
2. AL NOUF1 에 전 유닛 `x9` → **움직인 칸 0** (지문·행·Q'ty 그대로)
3. SADARA `10 → x4` → Q'ty 0 → **328** · 축3 **92.0**
4. UAD `00 → x4` → 149 → 596 / TC2 `00 → x4` → 3047 → **3656**

## UI 는 띄워서 눌러 확인했고 결함 넷을 고쳤다

`8_UI검증/` — ① 마크다운 별표가 글자 그대로 보였다(안내 · **사유 문장**) ·
② **★ 화면이 "이미 반영된 것처럼" 읽혔다**(다시 분석해야 적용된다는 사실이
4.9초 뒤 사라지는 안내에만 있었다) · ③ 프로젝트에 안 묶인 분석의 **눌러도 안 되는
버튼** · ④ 유닛코드를 못 읽은 묶음.  시험 4건으로 못박았다.

## 만든 것

* `app/unit_multipliers.py` — 프로젝트 폴더 · **작성자 필수** · 읽는 곳 하나 ·
  `enabled:false` 면 없던 때와 정확히 같아진다
* `pipeline._note_factor(user=)` — `①범례 → ②NOTES → ③사람 → ④설정 → ⑤빈칸`
* API 셋 + 검토 화면 패널 (적용 범위 · Q'ty 전/후 · 되돌리기)
* `MULTIPLIER_BY_USER` 사유 — **지우지 않는다** (감사에 남는다)

## 알아낸 것

* **★ 열두 번째 "찾고 있는데 안 쓰던 것"** — `_note_factor` 가 27회차부터
  ③ 자리를 갖고 있었다.  **넣을 값이 없었을 뿐이다.**
* **★ [C] AL NOUF1 의 경보 접미 위험 2건은 계기가 아니라 사람이 쓴 검토 메모**
  (p17 `XV-> CV`).  32회차 항목은 **두 규칙이 한 벌**이 된다.
* **(b) 갈래(뒤 `A` 까지 떼기)는 §5.1 위반이라 버린다** — UAD p30 실측으로
  태그 하나에 버블 셋.

## 사용자가 답할 것

| | 물음 |
| --- | --- |
| SADARA 유닛 `10` | 몇 벌인가 (지금 Q'ty 빈칸 82행) |
| TC2 유닛 `00` | 지금 **AL NOUF1 설정값 x1** 로 203행이 세어진다 |
| UAD 유닛 `00` | 지금 **AL NOUF1 설정값 x1** 로 149행이 세어진다 |
| 경보 접미 | (a)+리뷰 메모 제외를 32회차에 구현할지 |

## 첨부

`1_보고서.md` · `2_progress.md` · `3_기존구조_조사.md` · `4_승수_대상행.md` ·
`5_alarm_suffix.md` · `6_regression_4p.json` · `7_캡처/` · `8_UI검증/` · `9_전수대조.txt`
그리고 `docs/program_overview.md`(구조·성능·범용성) · `docs/state_and_roadmap.md`(갱신)
