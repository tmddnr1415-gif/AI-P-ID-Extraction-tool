# 44회차 요약 — 사용자 마크업 · 피드백 내보내기 · 회사 PC 꾸러미 r44

* **화면 회차** — 검출 코드 0줄.  네 프로젝트 지문·행·Q'ty·축3 **전부 불변** ([A] 기준선 4종 ·
  [G] 최종 코드 회귀 4종 — `8_regression.json`).
* **열일곱 번째 "찾고 있는데 안 쓰던 것"** — 요구 여섯 중 넷이 이미 서버에 있었다: `＋행`+지점
  기록 · 삭제 = `removed` 표시(지우지 않음) · `probe_point` · `feedback` 표.  새로 만든 것은 잇는
  코드 여섯 (`3_survey.md`).
* **[C] p48** — 다른 프로젝트(PORT DICKSON) 장은 PROJECT NAME 다수결(57/1)로 이미 범위 밖 · 행 0 ·
  축3 불변.  양식은 같고 프로젝트가 다르다.  AL NOUF1 의 진짜 (2/2) 장은 이 PDF 에 없다.
* **[D] 마크업** — 드래그 = 누락 행(제안값은 별표·NOTES·같은 장 수량·앵커 낱말을 **먼저 읽음**,
  `scope_source`/`qty_source` 기록, 안정 ID 는 같은 장부) · 상자 클릭 = 오검출 표시(행 안 지움 ·
  Excel 제외 선택 · 되돌리기) · ㉣ 값 틀림 은 칸 편집으로 · 같이 적용은 장마다 제안만.
* **[E]** 색 (b): 색은 SCOPE · 표시는 모서리 ✚(왼쪽 위)/✕(오른쪽 아래) — 검토 ● 와 다른 모서리.
  범례 "(그중) 사용자 추가 / 오검출 표시" — 세 색 칸 합 = 상자 수 등식 불변(캡처 18+28+1=47).
  Excel: REMARK 앞머리 `사용자 추가 · 이름` · 새 열 0 · 시각 없음.  **축5 는 더하지 않는다(판단만)**.
* **[F]** `feedback_<프로젝트>_<날짜>.zip` = feedback.json · feedback.md · 조각/ · 장/.  마크업은 남는다.
* 시험 315 → **324** (빠른) · UI 24 → **26**.  실 DB sha256 불변.
* **[H]** 꾸러미 r44 — 17단계 검증 (`verify/state.json`) · 첨부.
