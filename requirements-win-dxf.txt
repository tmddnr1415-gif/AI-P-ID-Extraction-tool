# Windows x86_64 · Python 3.11 — 55회차 DXF 입력 경로가 새로 요구하는 것
#
# 9/3 이관 꾸러미(`requirements-win.lock.txt` · 39개)에 **없던 것만** 적었습니다.
# 그 목록에 이미 있고 버전도 같은 셋은 여기 없습니다:
#     numpy==2.4.6 · packaging==26.3 · typing_extensions==4.16.0
#
# 설치 (회사 PC · 오프라인):
#   pip install --no-index --find-links wheels_r55 ezdxf matplotlib
#   확인: python -c "import ezdxf, matplotlib; print('ok')"
#
# 무엇에 쓰이나 — 둘로 갈립니다.
#   · ezdxf 계열  : DXF 를 **읽습니다** (`app/engine/dxf_reader.py`).  없으면
#                   DXF 업로드가 실패합니다.  PDF 경로는 영향이 없습니다.
#   · matplotlib 계열 : 읽은 장을 화면 배경 PNG 로 **그립니다**
#                   (`app/engine/dxf_render.py`).  그 모듈은 함수 안에서
#                   import 하므로, 없어도 서버는 뜨고 DXF 분석·Excel 출력은
#                   됩니다 — 도면 그림만 안 나옵니다.
#
# 버전은 이 저장소에서 실제로 돌린 것과 같게 고정했습니다.

# --- ezdxf 계열 (읽기) --------------------------------------------------
ezdxf==1.4.4
pyparsing==3.3.3          # ezdxf 요구
fonttools==4.65.0         # ezdxf · matplotlib 둘 다 요구

# --- matplotlib 계열 (그리기) -------------------------------------------
matplotlib==3.11.2
pillow==12.3.0
contourpy==1.3.3
cycler==0.12.1
kiwisolver==1.5.1
python-dateutil==2.9.0.post0
six==1.17.0               # python-dateutil 요구
