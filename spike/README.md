# Phase 0 검증 스파이크

`docs/design.md` §15 Phase 0 / 부록 A 의 검증 스크립트.

| # | 스크립트 | 목표 | 상태 |
|---|---|---|---|
| 1 | `extract_titleblocks.py` | 58페이지 타이틀블록 파싱 성공률 100% | ✅ **통과 (58/58)** |
| 2 | `detect_symbols.py` | 단일 페이지(p6) 심볼 검출 재현율 ≥ 90% | ✅ **통과 (재현율 100%, 정밀도 100%)** |
| 2b | `detect_all.py` | 전수 확장 — p6 규칙이 어디서 깨지는지 측정 | ⚠️ 재현율 81.9%, 정밀도 73.5% (조인 오염 포함) |
| 2c | `detect_all.py` | 측정 재정의 — 페이지 단위 조인 + 앵커 사전 수정 | ✅ 재현율 91.6%, 정밀도 80.6% (MATCHED 기준선) |
| 2d | `detect_all.py` | 앵커 감사 완료 + 벤더마크 표기 3종 통합 | ✅ **재현율 97.0%, 정밀도 87.0%** |
| 2e | `crossval.py` | 교차검증 — Steam 만으로 규칙 세워 미지 계통 적용 | ⚠️ **미지 계통 95.3% / 81.4%** (전체 튜닝 대비 −1.7 / −5.6pp) |
| 2f | `projectconfig.py` + `config/` | PROJECT 18건 config 분리 | ✅ **97.0 / 87.0 그대로 재현** |
| 3 | `parse_notes.py` | Q'ty 승수 검증 (범례 유도) | ✅ **재현율 99.5%, 정밀도 91.2%** |

공통 모듈 `pidcache.py` 가 회전 정규화와 `analysis_scope` 판정을 담당합니다
(docs/design.md §4 설계결정 5·6, §5 단계 [0]). 하위 모듈은 회전을 인지하지 않습니다.

## 1. 타이틀블록 파싱

```bash
pip install pymupdf numpy
python spike/extract_titleblocks.py --pdf data/pid_total.pdf \
                                    --out out/titleblocks.csv \
                                    --rev-sheet out/rev_cells.png
```

`--rev-sheet` 는 58개 REV 셀을 판독 결과와 함께 한 장으로 붙인 PNG를 만듭니다.
REV 는 대부분의 페이지에서 텍스트 레이어와 대조할 수 없는 유일한 필드라
육안 검증용으로 붙였습니다.

### 결과

| 필드 | 성공 | 비율 |
|---|---|---|
| `drawing_no` | 58/58 | 100% |
| `drawing_title` | 58/58 | 100% |
| `unit_code` | 58/58 | 100% |
| `rev` | 58/58 | 100% (전부 HIGH) |
| `rev_date` | 9/58 | 15.5% (아래 §설계 영향 참조) |

파싱 실패 페이지 **없음**.

### 설계 영향 — 설계안 수정이 필요한 실측 발견

**① 타이틀블록이 두 가지 변종으로 섞여 있다.**
58페이지 중 10페이지만 타이틀블록 소문자 텍스트(라벨·REV·리비전 이력)가
실제 텍스트 레이어입니다. 나머지 48페이지는 **모노라인 CAD 스트로크 폰트로
플롯되어 텍스트가 전혀 없는 벡터 폴리라인**입니다.
도면번호·제목·프로젝트명은 전 페이지 Arial 텍스트라 영향 없습니다.

`docs/design.md` §17 은 "대상 PDF가 100% 벡터이므로 OCR 미구현"이라고
적었는데, 그 결론 자체는 맞지만 근거가 한 단계 어긋납니다. 벡터라서 OCR이
불필요한 게 아니라, **벡터 도형을 문서 자체에서 부트스트랩한 글리프 템플릿과
매칭**해서 읽어야 합니다. 이 스크립트가 쓰는 방식입니다:

- 글자 템플릿 ← 리비전 이력 REV 칸 중 **1글자짜리 칸**. 이력 행은 아래에서
  위로 A, B, C… 순으로 채워지므로 행 순서가 곧 라벨입니다.
- 숫자 `1` 템플릿 ← SHEET 칸. 전 도면이 `1/1` 이고, 텍스트 변종 페이지에서
  그 값이 확인됩니다.
- 글리프는 잉크 바운딩박스로 크롭 후 고정 격자로 면적평균 → 위치·크기·선
  굵기에 무관. 문자당 템플릿을 **평균 내지 않고 리스트로 보관**해 서로 다른
  서체(스트로크 / Arial / 페이지 48의 굵은 서체)를 함께 인식합니다.

**② 리비전 이력 행 수로 Rev 를 추론하면 안 된다.**
페이지 7·9·20 은 이력 행이 A·B 두 줄인데 정작 REV 칸은 **A** 입니다.
원본 도면 자체가 불일치합니다. 행 수 기반 추론은 이 3페이지에서 틀립니다
(10개 정답 페이지 중 3개 오답 = 30%). 따라서 REV 칸은 **추론하지 않고 판독**
합니다.

**③ Rev 가 항상 한 글자가 아니다.**
페이지 48 은 Rev **`A1`** 입니다. REV 칸을 단일 글리프로 보지 않고 문자
단위로 분할해야 합니다.

**④ 페이지 48 은 다른 프로젝트 도면이다.**
EMPLOYER `PDP GEN TWO SDN BHD`, PROJECT NAME `PORT DICKSON 1400MW CCGT`
(나머지 57페이지는 `AL NOUF1 PROJECT`), Description 도 `FOR PROPOSAL`
(나머지는 `FOR INTERNAL USE`). 유입 경위 확인이 필요합니다.

**⑤ 도면번호가 중복된다.** `pid_page` 를 `drawing_no` 로 키잉하면 깨집니다.

| drawing_no | 페이지 | 제목 |
|---|---|---|
| `D00P-00GHC10-M05-0001` | 46 / 47 | DESAL. WATER SUPPLY / DEMINERALIZED WATER DISTRIBUTION (1 OF 2) |
| `D00P-00GMA10-M05-0001` | 52 / 55 | CHEMICAL WASTE WATER TRANSFER / WASTE WATER TRANSFER (SEA WATER / STORM WATER) |
| `D00P-10LBG10-M05-0001` | 12 / 15 | AUXILIARY STEAM SYSTEM GROUP 10 / GT FLASH PIPE SYSTEM UNIT 11 |

서로 다른 도면인데 번호가 같으므로 페이지 번호를 포함한 키가 필요합니다.
§11 중복 판정 로직과는 **별개 사안**입니다(같은 기기의 중복이 아니라 채번 오류).

**⑥ 2페이지가 270° 회전 저장되어 있다** (7, 48). `page.rotation_matrix` 로
동일 좌표계에 매핑하면 나머지 56페이지와 같은 규칙이 그대로 적용됩니다.
전 파이프라인이 이 변환을 거쳐야 합니다.

**⑦ `rev_date` 는 9/58 만 추출됩니다.** 날짜 역시 48페이지에서 스트로크
폰트라 글자 단위 판독이 필요한데, 숫자 0·2·5·9 와 월 약어 템플릿을
부트스트랩할 근거가 문서 안에 없습니다(SHEET 칸이 `1` 만 제공).
`rev_date` 는 필수 필드가 아니라 CSV에 공란으로 두고 실패로 잡지 않았습니다.
필요해지면 텍스트 변종 페이지에서 날짜 템플릿을 라벨링하는 작업이 별도로
필요합니다.

### 검증 방법

- 텍스트 레이어가 있는 10페이지에 대해 판독 결과와 텍스트를 대조 → **10/10 일치**.
- 나머지 48페이지는 `--rev-sheet` 산출물로 육안 확인 → **58/58 일치**.
- `unit_code` 는 `drawing_no` 에서 재유도해 교차 검증 → 불일치 0.

### 산출물

- `out/titleblocks.csv` — 58행. 필드별 값과 함께 `rev_method`(TEXT/GLYPH),
  `rev_confidence`, `rev_detail`(매칭 거리·마진), `status`, `issues` 를 같이
  기록합니다. 설계안 §4 의 "근거 없는 값은 저장하지 않는다" 원칙에 맞춘 것으로,
  그대로 `pid_page` + `item_evidence` 에 적재할 수 있습니다.
- `out/rev_cells.png` — REV 셀 58개 육안 검증 시트.

---

## 2. 단일 페이지 심볼 검출

```bash
pip install pymupdf numpy openpyxl
python spike/detect_symbols.py --pdf data/pid_total.pdf --page 6 \
       --compare data/CZE_Field_Instrument.xlsx \
       --json out/detect_page006.json --png out/detect_page006.png
```

대상은 **page 6 (`D00P-10LBA10-M05-0001`, HP Steam System Group 10) 한 장**입니다.
LLM·OCR 미사용, 텍스트 앵커 + 지오메트리 검증 (design.md §6.1).

### 결과 — Excel 12행과 정확히 일치

| TYPE | 검출 | Excel | 차이 |
|---|---|---|---|
| PIT | 2 | 2 | 0 |
| TIT | 10 | 10 | 0 |
| **합계** | **12** | **12** | **0** |

**재현율 12/12 = 100%** (목표 90%), **정밀도 12/12 = 100%**.

> **2c 에서 일시 하락 → 2d 에서 복구**: `FE` 를 Field 대상으로 되돌리자 p6 이
> 14/12 (정밀도 85.7%) 로 내려갔습니다. p6 의 FE 2건은 실제로 벤더 공급인데
> `*` 마크가 개별 버블이 아니라 **패키지 점선 박스 모서리**(527.2, 412.0)에
> 붙어 있었기 때문입니다. 2d 의 패키지 박스 규칙이 이를 처리해 **다시 12/12
> (100% / 100%)** 입니다.

앵커 42개 전부 지오메트리 검증 통과 (`geometry-unverified: 0`).
버블은 이 도면에서 원이 아니라 **68.0 × 22.6 pt 스타디움**이고, 캡(원호) 2개가
56.6 pt 간격으로 짝을 이룹니다. 인접 버블 간격이 25.5 pt뿐이라 "근처 원호를
전부 합집합"하면 이웃끼리 뭉치므로 **캡을 정렬·간격으로 짝지어야** 합니다.

### 제외 규칙별 기여도

`--without` 로 규칙을 끄고 측정한 값입니다.

| 활성 규칙 | 재현율 | 정밀도 | 오검출 |
|---|---|---|---|
| SCT 규칙만 (`--without VENDOR_MARK`) | 100% | **66.7%** | PIT +3, TIT +3 |
| 벤더마크 규칙만 (`--without SCT_SUPPLIER_SCOPE`) | 100% | 100% | 0 |
| 둘 다 (기본) | 100% | 100% | 0 |

| 규칙 | 잡아내는 심볼 | 단독으로만 잡는 것 |
|---|---|---|
| `SCT_SUPPLIER_SCOPE` | 10 | 0 |
| `VENDOR_MARK` | 20 | 6 |
| `NOT_FIELD_INSTRUMENT` (2d 기준 `TW` 만) | 10 | 6 |
| `VALVE` (MOV) | 4 | 4 |

### 설계 영향 — 요청받은 SCT 규칙에 대한 실측 정정

**① 이 도면의 SCT 스코프는 "박스"가 하나뿐이고, 나머지 둘은 박스가 아니다.**
지시받은 대로 SCT 텍스트 주변 벡터 도형을 찾은 결과 두 가지 형태가 나왔습니다.

- `SCT SUPPLIER` (1곳) — **닫힌 사슬점선 사각형** `(1183,162)-(1910,484)`.
  터빈(HP/IP/LP)을 감싸며, 지시대로 박스 내부 제외를 적용합니다.
  다만 **이 박스 안에는 TT·PT 앵커가 하나도 없어 실제 제외 기여는 0건**입니다.
- `SCT HRSG` (2곳) — **사각형이 아니라 배관 위의 스코프 브레이크**입니다.
  배관 자체가 `x=633.3` 까지 점선(= not in contract), 그 이후 실선으로 그려져
  있고, `SCT` 텍스트의 x 시작점이 정확히 633.3 으로 브레이크 지점과 일치합니다.
  그래서 "박스 내부"가 아니라 **계기의 드롭라인이 점선 구간에 닿는지**로
  판정했습니다. 이 판정이 브레이크 바로 왼쪽 5개(제외)와 바로 오른쪽 2개(포함)를
  정확히 갈라줍니다 — 단순 좌표 밴드로는 이 구분이 안 됩니다.

**② SCT 규칙만으로는 목표를 못 맞춘다. 벤더 마크 규칙이 필수다.**
지시하신 "y≈469 / y≈1006 라인의 TT·PT"는 SCT 규칙으로 정확히 10건 걸립니다.
그런데 그것만으로는 **정밀도 66.7%** 에 그칩니다. 터빈 주변 우측 계기 6건
(TT 3 + PT 3, 좌표 `(1191,768)` `(1339,768)` `(1339,811)` `(1191,896)`
`(1191,938)` `(1339,938)`)이 남기 때문입니다. 이들은 SCT 브레이크의 **오른쪽**,
즉 SCT 배관 스코프 안에 있지만 **기기 자체는 ST 공급자 납품**입니다.

근거는 이 페이지 NOTES 에 있습니다(design.md §10.1 페이지 스코프 마크 사전):

```
*    DENOTES EQUIPMENT WILL BE SUPPLIED BY HRSG.
**   DENOTES EQUIPMENT WILL BE SUPPLIED BY ST SUPPLIER.
```

마크는 텍스트가 아니라 **4×4 pt 벡터 도형**이라 `get_text` 로는 안 잡힙니다.
버블 위 마크 **개수**를 세고 그 개수를 NOTES 의 정의에 대응시켜 해석합니다.
좌측 5개 묶음은 `*`(HRSG), 우측 터빈 계기는 `**`(ST SUPPLIER)입니다.

즉 **배관 스코프(SCT 브레이크)와 기기 공급자(마크)는 별개 축**이고, Field
Instrument 리스트에서 빠지는 기준은 후자입니다. 이 도면에서는 벤더 마크 규칙이
SCT 규칙을 완전히 포함합니다(`SCT_SUPPLIER_SCOPE` 단독 검출 0건).
SCT 규칙은 지시대로 유지했고 교차 검증용으로 유효하지만, **MVP 필수 규칙은
벤더 마크 쪽**입니다.

**③ 불일치 0건.** 대조 결과 TYPE별 개수가 정확히 일치해 원인 추정이 필요한
건이 없습니다. 참고로 `Q'ty` 는 Excel 12행 모두 2인데, 이는 심볼 개수가 아니라
NOTES 1 `THIS P&ID IS FOR GROUP#10, CONFIGURATION IS IDENTICAL FOR GROUP#20`
에서 오는 승수입니다(design.md §8). 스파이크 3의 검증 대상이라 여기서는
심볼 개수만 대조했습니다.

### 산출물

- `out/detect_page006.json` — 앵커 42건 전량. 각 건의 bbox·중심좌표·분류·
  포함여부·적용 규칙(`rules_hit` 은 중복 적용까지 전부)·근거(`evidence`),
  그리고 마크 사전·SCT 스코프 영역·Excel 12행·지표를 함께 저장합니다.
- `out/detect_page006.png` — bbox 오버레이. 초록=리스트 포함, 주황=벤더 마크,
  빨강=SCT 스코프 영역, 회색=Field 비대상, 파랑=밸브.

---

## 2b. 전수 확장 — p6 규칙은 어디서 깨지는가

```bash
python spike/detect_all.py --pdf data/pid_total.pdf \
       --compare data/CZE_Field_Instrument.xlsx \
       --titleblocks out/titleblocks.csv \
       --json out/detect_all.json --failures out/failure_report.md
```

**새 규칙을 추가하지 않고** p6 에서 검증한 규칙을 `analysis_scope=true` 인
P&ID 52페이지(49개 도면번호)에 그대로 적용했습니다. Field Instrument 만 대상,
Q'ty 는 제외하고 행 수만 비교합니다. Excel 563행 중 `P&ID No.` 가 있는 557행이
조인 대상입니다(나머지 6행은 SPARE).

결과: **재현율 81.9% (456/557), 정밀도 73.5% (456/620)**.

### 핵심 질문의 답 — 벤더마크가 SCT 를 포함하는가

| 활성 규칙 | 검출 | 정답 | 일치 | 재현율 | 정밀도 |
|---|---|---|---|---|---|
| 둘 다 끔 | 759 | 557 | 461 | 82.8% | 60.7% |
| SCT 만 | 727 | 557 | 459 | 82.4% | 63.1% |
| 벤더마크 만 | 622 | 557 | 458 | 82.2% | **73.6%** |
| 둘 다 | 620 | 557 | 456 | 81.9% | 73.5% |

**p6 의 관찰이 전 계통에서 성립합니다.** 벤더마크를 켠 상태에서 SCT 를 추가로
켜면 검출이 622 → 620 으로 **단 2건** 줄고, 그 2건은 Excel 이 유지하는 행이라
일치 수가 458 → 456 으로 **줄어듭니다**. 즉 SCT 규칙이 단독으로 잡아내는 것은
전 도면에서 2건뿐이고 그 2건은 모두 오제외입니다
(`D00P-11LAB00-M05-0001` p20 의 `RO` 2건, 좌표 `(1118.5, 809.4)` `(1118.5, 1300.0)`).

SCT 단독으로만 올바르게 걸리는 계통은 **없습니다**. SCT 규칙은 유지하되
MVP 필수 규칙에서는 빼고, 교차검증용으로만 두는 것이 맞습니다.

### 경고했던 두 가지 — 둘 다 실제로 문제였음

**① 버블 치수는 p6 기준이 아니다.** 하드코딩했으면 절반을 잃었습니다.

| 스타디움 | 개수 | 페이지 수 |
|---|---|---|
| 68.0 × 22.6 (p6) | 558 | 29 |
| **85.1 × 28.4** | 361 | 20 |
| 50.0 × 16.6 / 54.5 × 18.2 등 | 19 | 4 |

p6 치수는 전체 1,179개 버블 중 558개만 덮습니다. 그래서 치수 상수를 버리고
**"원호 캡 2개 + 그 사이를 잇는 직선 변 2개"** 라는 순수 기하 조건으로 바꿨습니다.
이웃 버블에서 온 캡 쌍은 사이에 변이 없어 자동으로 탈락하므로 크기를 몰라도 됩니다.
p6 결과는 12/12 그대로입니다(회귀 없음).

**② 벤더마크 표기가 페이지마다 다르다.** 이게 최대 오검출 원인입니다.

| 표기 방식 | 페이지 | 규칙 적용 |
|---|---|---|
| 벡터 글리프 (p6 방식) | 19 | 가능 |
| **텍스트 `(*)` / `(**)`** | 7 (26,27,28,32,46,49,51) | **불가 — 규칙이 못 봄** |
| 마크 없음 | 26 | 해당 없음 |

p6 은 NOTES 범례의 마크를 **벡터 글리프**로 그립니다. 그런데 7개 페이지는
`4. (*) SYMBOL INDICATES COMPONENTS FURNISHED BY SEAWATER INTAKE FACILITY SUPPLIER`
처럼 **문자 `(*)`** 로 씁니다. 마크 사전 파서가 글리프만 찾으므로 이 페이지들은
사전이 비고, 도면 안의 `(**)` 텍스트 마크도 못 봐서 **제외가 하나도 일어나지
않습니다**.

부작용으로 `VENDOR_MARK_UNDEFINED` 가 0건입니다. 마크를 아예 못 보니 "정의 없는
마크" 상태에 도달하지 못합니다. 분류 체계는 있으나 현재 계측기로는 트리거되지
않는다는 점을 명시해 둡니다.

### 실패 유형별 집계

| 유형 | 케이스 | 행 수 |
|---|---|---|
| `OVER_DETECT` | 38 | **164** |
| `TYPE_MAPPING_MISS` | 19 | 58 |
| `NO_ANCHOR` | 15 | 32 |
| `AMBIGUOUS_GEOMETRY` | 5 | 6 |
| `SCOPE_CONFLICT` | 3 | 5 |
| `VENDOR_MARK_UNDEFINED` | 0 | 0 |

`TYPE_MAPPING_MISS` 58행의 내역이 특히 중요합니다.

- **FE 18행** — p6 에서 "FE 는 Field Instrument 비대상"으로 확정했는데, 전체
  Excel 에는 FE 가 18행 있습니다. **p6 에서 검증한 규칙이 틀렸습니다.**
  p6 의 FE 2건이 마침 대상 외였을 뿐입니다.
- **LSH / LSHH / LSL → LS 16행** — 앵커 사전에 없는 태그.
- ZS/ZSC 8행, LG 11행, DPIT 등 — 앵커 사전 미포함.

### 계통별 정확도 (재현율 오름차순, 상위 발췌)

| 계통 | 도면 | 검출 | Excel | 재현율 | 정밀도 | 비고 |
|---|---|---|---|---|---|---|
| `GBL` | 1 | 0 | 11 | 0.0% | n/a | PDF 에 도면 없음 (조인 실패) |
| `LCQ` | 2 | 5 | 5 | 0.0% | 0.0% | Excel `10LCQ10` vs 도면 `11LCQ10` — 번호 불일치 |
| `SCB` | 1 | 0 | 1 | 0.0% | n/a | |
| `GHB` | 2 | 18 | 17 | 52.9% | 50.0% | |
| `LAB` | 1 | 19 | 33 | 57.6% | 100.0% | 미검출 14행 |
| `LBC` | 2 | 13 | 21 | 61.9% | 100.0% | |
| `LBA` | 2 | 17 | 24 | 66.7% | 94.1% | p6 은 12/12, `10LBA30` 이 실패 |
| `MAN` | 2 | 48 | 64 | 75.0% | 100.0% | 미검출 16행 (최대) |
| **`PAB`** | 4 | 50 | 14 | 85.7% | **24.0%** | **오검출 최다** |
| `PGB` | 7 | 177 | 170 | 100.0% | 96.0% | |

### 가장 많이 틀린 계통: `PAB` (Seawater Intake System)

오차 총량 기준 최악입니다 — 4개 도면에서 **검출 50 vs Excel 14, 정밀도 24.0%**.

**원인 가설 (근거 확보됨):** 이 계통의 벤더 스코프가 **p6 규칙이 볼 수 없는
형태로만 표현**되어 있습니다.

1. p26 은 검출 29건 중 **벡터 마크를 가진 것이 0건**입니다. 도면 안의 마크는
   전부 텍스트 `(**)` (7개)이고, 범례도 `4. (*) SYMBOL INDICATES ...` 형태의
   문장입니다. → 마크 사전 비어 있음 → 제외 0건.
2. NOTES 1 이 `AUTOMATIC AIR RELEASE VALVE AND PRESSURE GAUGE WILL BE SUPPLIED
   BY CWP SUPPLIER` 라고 **산문으로만** 선언합니다. p26 의 `PI` 7건이 여기
   해당하는데, 심볼에는 아무 마크도 붙어 있지 않습니다.
3. Excel 의 `00PAB10-M05-0001` 5행은 전부 `PIT` — "CIRCULATING WATER PUMP
   DISCHARGE PRESSURE" 뿐입니다. 즉 취수 설비의 `LIT` 10건·`PI` 7건은 통째로
   SCT 스코프 밖입니다.

→ **마크 기반 제외만으로는 이 계통을 못 잡습니다.** 다음 단계에서 필요한 것은
(a) 텍스트형 마크 `(*)`/`(**)` 파싱, (b) NOTES 산문의 공급자 선언을 심볼 종류
(PRESSURE GAUGE → PI)에 연결하는 규칙입니다. (b) 는 §10.1 이 예고한 "페이지
스코프 마크 사전"의 확장이며, 자연어가 개입하므로 설계안 §2 기준으로 LLM 을
쓸 수 있는 몇 안 되는 지점입니다.

### 지표를 왜곡하는 조인 문제 (별도 계상 필요)

- Excel 에만 있고 PDF 에 없는 도면 **5건 / 21행** — 검출 불가인데 재현율 손실로 계상.
- PDF 에만 있고 Excel 에 없는 도면 **14건 / 검출 31행** — 전부 정밀도 손실로 계상.
- 도면번호 공유 **3건** — 1:N 조인이라 서로 다른 도면의 검출이 합산됩니다.
  `00GHC10-M05-0001` 은 p46(Desal.)+p47(Demin.) 이 합쳐져 검출 54 vs Excel 16 인데,
  Excel 의 SYSTEM 값이 `Demineralized Water Distribution System` 이므로 그 16행은
  **p47 것**이고 p46 의 20건은 순수 조인 오염입니다. `10LBG10-M05-0001` 도 동일
  (Excel SYSTEM = `Aux. Steam System` = p12, p15 는 오염).

이 세 요인을 걷어내면 실제 규칙 성능은 위 수치보다 높습니다. 다음 단계 측정
전에 조인 기준을 페이지 단위로 확정할 필요가 있습니다.

### 산출물

- `out/detect_all.json` — 도면별 검출/정답/TYPE별 카운트, 규칙 조합별 지표,
  버블 치수 분포, 실패 분류 전량.
- `out/failure_report.md` — 실패 유형별 집계 + 유형별 대표 사례 3건
  (도면번호·좌표·추정 원인), 조인 무결성, 마크 표기 방식, 계통별 정확도.

---

## 2c. 측정 재정의 — 새 검출 규칙 없이 기준선 확보

이번 단계는 **검출기를 고치지 않고 측정을 고쳤습니다.** 변경은 두 가지뿐입니다.

### [A] 조인 기준을 페이지 단위로 확정

도면번호 공유 3건을 Excel `SYSTEM` 열 ↔ 페이지 `drawing_title` 토큰 대조
(접두 일치 허용)로 **행 단위 귀속**했습니다.

| 도면번호 | Excel SYSTEM | 귀속 결과 |
|---|---|---|
| `00GHC10-M05-0001` | Demineralized Water Distribution System | p47 **16행**, p46 **0행** |
| `10LBG10-M05-0001` | Aux. Steam System | p12 **13행**, p15 **0행** |
| `00GMA10-M05-0001` | — | **양쪽 0행** |

`00GMA10-M05-0001` 은 Excel 전체에서 `GMA` 를 포함한 `P&ID No.` 가 **한 건도
없어** 귀속시킬 행 자체가 없습니다. p52·p55 둘 다 `PDF_ONLY` 입니다.

대조 대상을 3개 집합으로 분리했습니다.

| 집합 | 규모 | 처리 |
|---|---|---|
| `MATCHED` | 35페이지 / Excel 536행 | **규칙 성능은 여기서만 측정** |
| `EXCEL_ONLY` | 5도면 / 21행 | 검출 불가 — 점수 제외 |
| `PDF_ONLY` | 17페이지 | 정답 없음 — 점수 제외 |

**EXCEL_ONLY 5건은 전부 "도면 누락"이 아니라 "채번 불일치"였습니다.**
SYSTEM 명으로 대조하니 5건 모두 PDF 안에 다른 번호로 존재합니다 (일치도 1.0).

| Excel 도면번호 | 행 | PDF 상의 실제 페이지 | 그 페이지 타이틀블록 번호 |
|---|---|---|---|
| `00GBL10-M05-0001` | 11 | p46 DESAL. WATER SUPPLY | `00GHC10-M05-0001` ← p47 번호와 중복 |
| `10LCQ10-M05-0001` | 5 | p13 HRSG BLOWDOWN UNIT 11 | `11LCQ10-M05-0001` ← 유닛 자리 불일치 |
| `11LCM10-M05-0002` | 1 | p15 GT FLASH PIPE UNIT 11 | `10LBG10-M05-0001` ← p12 번호와 중복 |
| `00EGD00-M05-0003` | 2 | p34 GT FUEL OIL SUPPLY | `11EGD00-M05-0001` |
| `00EKG10-M05-0004` | 2 | p25 GT FUEL GAS SUPPLY | `11EKG10-M05-0001` |

즉 **도면번호 중복 3건의 진짜 원인은 p46·p15 의 타이틀블록 채번 오류**입니다.
p46 은 `00GBL10-M05-0001`, p15 는 `11LCM10-M05-0002` 가 맞습니다.
이걸 정정하면 중복 3건 중 2건이 소멸하고 EXCEL_ONLY 21행 중 17행이 회수됩니다.
원본 도면 수정 사안이라 이번 단계에서는 **진단만 하고 조인에 반영하지 않았습니다.**

**PDF_ONLY 17페이지는 대부분 리스트 대상이 아닌 계통입니다.** Excel `SYSTEM`
분포에 Waste Water Transfer / Sanitary Sewer / Sampling / Chemical Dosing /
Service Gas / Seal Pit / Condenser Waterbox Air Removal 계통이 **아예 없습니다**.
p19·p29·p45·p50·p57·p58 은 검출도 0건이라 서로 일관됩니다. 다만
p52(30) p53(24) p54(21) p55(15) p56(18) 는 검출이 많은데 정답이 없어,
Excel 누락인지 다른 산출물 소관인지 **발주처 확인이 필요합니다.**

### [B] 앵커 사전 수정 — 전체 Excel 로 확인된 것만

| 코드 | 조치 | 근거 |
|---|---|---|
| `FE` | 비대상 → **Field 대상** | Excel `FE` **18행**. `10MAN10-M05-0001` 행 61/71/82 (`… SPRAY WATER FLOW ELEMENT`) ↔ p10 도면의 FE 버블 3개 |
| `LSH`/`LSHH`/`LSL` | **→ `LS`** | Excel `LS` **22행**, 설명이 `… LEVEL HIGH HIGH`/`… LEVEL HIGH`. `10LBC40-M05-0001` 행 20~23 ↔ p7 도면의 LSHH 4 + LSH 4 |
| `ZS` | **추가하지 않음** | Excel `TYPE` 열에 `ZS` **0행** |
| `LG` | **추가하지 않음** | Excel `TYPE` 열에 `LG` **0행** |

> **정정**: 지시하신 "ZS 8행 / LG 11행"은 제 이전 리포트가 오해를 부른
> 결과입니다. 그 숫자는 `ZS`·`LG` 의 행 수가 아니라 **같은 페이지에서 결손된
> 다른 TYPE(FIT·PDIT·LI·PIT)의 행 수**였고, 토큰 이름은 "그 페이지에 있던
> 미매핑 토큰" 목록이었습니다. Excel `TYPE` 열에는 `ZS`·`LG` 가 없으므로
> 추가하면 없는 항목을 만들어내게 됩니다. 리포트 문구를 고쳤습니다.

**한 페이지 표본으로 굳어진 규칙 재점검** (전체 Excel 대조):

| 규칙 | 근거 | 판정 |
|---|---|---|
| `FE` = 비대상 | Excel FE 18행 | ❌ 오류 — 철회함 |
| `FT` = 비대상 | Excel `FIT` **31행**. `10MAN10-M05-0001` 이 FIT 5행을 요구하고 p10 도면에 `FT` 버블이 **정확히 5개** | ⚠️ **오류로 보임 — 지시 목록 밖이라 미적용** |
| `TW` = 비대상 | Excel `TYPE` 에 `TW` 0행 | ✅ 유지 |
| `TT`→`TIT`, `PT`→`PIT` | Excel 에 `TT`/`PT` 0행, `TIT` 99 / `PIT` 108행 | ✅ 유지 |
| `MOV` = 밸브 | Field 리스트에 밸브 TYPE 없음 | ✅ 유지 |

`FT`→`FIT` 는 `FE` 와 완전히 같은 실패 양상(한 페이지에서 벤더 제외된 표본을
타입 제외로 오인)이고 근거도 확보됐지만, 지시하신 4개 목록에 없어 **적용하지
않았습니다.** 승인해 주시면 한 줄 변경입니다.

### [C] SCT 규칙 — 기본 비활성

`DEFAULT_DISABLED` 로 기본 비활성화하고 사유·수치를 주석에 기록했습니다.
코드는 남아 있고 `--enable-rule SCT_SUPPLIER_SCOPE` 로 켤 수 있습니다.

### 1. 측정 재정의 전후

| 변형 | 검출 | Excel | 일치 | 재현율 | 정밀도 |
|---|---|---|---|---|---|
| v1 규칙 + 도면 조인 (이전 발표값) | 620 | 557 | 456 | 81.9% | 73.5% |
| v1 규칙 + 페이지 조인 (**조인만** 수정) | 568 | 536 | 456 | 85.1% | 80.3% |
| **v2 규칙 + 페이지 조인, SCT off (새 기준선)** | 609 | 536 | 491 | **91.6%** | **80.6%** |
| v2 규칙 + 페이지 조인, SCT on | 607 | 536 | 489 | 91.2% | 80.6% |

조인 수정만으로 재현율 +3.2pp / 정밀도 +6.8pp, 앵커 사전 수정으로 재현율
+6.5pp 가 추가됩니다. **재현율 91.6% 는 설계안 §15 MVP 목표(90%)를 넘겼습니다.**
SCT 는 켜면 여전히 손해라 비활성 판단이 유지됩니다.

### 2. 계통별 정확도 (MATCHED, 재현율 오름차순)

| 계통 | 페이지 | 검출 | Excel | 재현율 | 정밀도 |
|---|---|---|---|---|---|
| `SCB` | 1 | 0 | 1 | 0.0% | 0.0% |
| `QFB` | 1 | 1 | 2 | 50.0% | 100.0% |
| `GHB` | 1 | 18 | 17 | 52.9% | 50.0% |
| `LAB` | 1 | 25 | 33 | 75.8% | 100.0% |
| `LAC` | 1 | 18 | 23 | 78.3% | 100.0% |
| `MAN` | 2 | 54 | 64 | 84.4% | 100.0% |
| `LCA` | 2 | 36 | 41 | 85.4% | 97.2% |
| **`PAB`** | 3 | 50 | 14 | 85.7% | **24.0%** |
| `GKB` | 1 | 12 | 7 | 85.7% | 50.0% |
| `LBG` | 1 | 12 | 13 | 92.3% | 100.0% |
| `EGD` | 2 | 57 | 33 | 93.9% | 54.4% |
| `PGB` `LBA` `LBC` `PAC` `GHC` `PCB` `LCM` `EKG` `MAJ` | 18 | — | — | 100.0% | 47~100% |

### 4. TYPE_MAPPING_MISS 변화 (v1 → v2, MATCHED 기준)

| 유형 | v1 케이스/행 | v2 케이스/행 | 변화 |
|---|---|---|---|
| `TYPE_MAPPING_MISS` | 18 / 50 | 10 / 25 | **-25행** |
| `NO_ANCHOR` | 5 / 20 | 4 / 12 | -8행 |
| `SCOPE_CONFLICT` | 3 / 5 | 2 / 3 | -2행 |
| `AMBIGUOUS_GEOMETRY` | 4 / 5 | 4 / 5 | 0 |
| `VENDOR_MARK_UNDEFINED` | 0 / 0 | 0 / 0 | 0 |
| `OVER_DETECT` | 24 / 112 | 27 / 118 | +6행 |

남은 `TYPE_MAPPING_MISS` 25행의 대부분이 `FT`→`FIT` 미적용분입니다.

### 5. OVER_DETECT 164행의 행방

| 단계 | OVER_DETECT 행 |
|---|---|
| v1 규칙 + 도면 조인 (이전) | **164** |
| v1 규칙 + 페이지 조인 | **112** (−52) |
| v2 규칙 + 페이지 조인 | **118** (+6) |

**52행은 조인 오염이었습니다** — 도면번호를 공유하는 p46·p15 의 검출이 다른
도면의 정답과 대조되던 분량, 그리고 `PDF_ONLY` 페이지의 검출이 정밀도에
계상되던 분량입니다. 실제 오검출이 아니었습니다.

`v2` 에서 +6행은 `FE` 를 대상으로 되돌리면서 생긴 신규 오검출입니다. p6 이
대표 사례입니다 — 단일 페이지 점수가 12/12 → **14/12 (정밀도 85.7%)** 로
내려갔습니다. p6 의 FE 2건은 실제로 벤더 공급인데, `*` 마크가 **개별 버블이
아니라 패키지 점선 박스 모서리**(527.2, 412.0)에 붙어 있어 현재 규칙이 못
봅니다. 즉 벤더 마크는 버블뿐 아니라 **패키지 경계 박스에도 붙는다**는 것이
새로 확인된 사실입니다. (다음 단계 과제)

남은 최대 오검출은 여전히 `PAB` (정밀도 24.0%) 이고, 원인은 2b 에서 규명한
텍스트형 마크 `(*)`/`(**)` 미파싱입니다 — 지시대로 이번엔 손대지 않았습니다.

---

## 2d. 앵커 감사 완료 + 벤더마크 표기 3종 통합

### [A] FT → FIT 및 앵커 사전 전수 감사

지시대로 `FT`→`FIT` 를 적용했습니다. 이어서 Excel `TYPE` 열 12종 전부를,
해당 행을 가진 페이지의 **버블 내부 토큰과 페이지 단위로** 교차 대조했습니다.
그 결과 같은 수준의 근거를 가진 매핑이 **2개 더** 나왔습니다.

| 매핑 | 페이지별 근거 | 판정 |
|---|---|---|
| `FT` → `FIT` | p10 5/5, p11 5/5, p12 1/1, p17 2/2, p20 8/8 | ✅ 적용 (지시) |
| `LG` → `LI` | p33 2/2, p49 2/2, p51 1/1 (p47은 `LI` 직접 표기 2/2) | ✅ 적용 — **신규 발견** |
| `DPIT` → `PDIT` | p21 `DPIT` 4 + `PDIT` 1 = Excel `PDIT` 5 | ✅ 적용 — **신규 발견** |
| `TW` 제외 | Excel `TYPE` 에 `TW` 0행 | 유지 |
| `ZS`/`ZSO`/`ZSC`/`ZT`/`PP` | Excel `TYPE` 에 0행 | 매핑 없음 유지 |

`LG`→`LI` 는 2c 에서 "LG 는 Excel TYPE 이 아니므로 추가 불가"라고 했던 것과
모순되지 않습니다. `LG` 를 **타입으로 추가**하는 게 아니라 도면의 `LG`(level
gauge) 표기를 Excel 의 `LI` 로 **매핑**하는 것이고, 페이지별 개수가 정확히
맞습니다. 지시 목록 밖이라 되돌리려면 `_V3_FIELD_TYPE_MAP` 한 줄입니다.

`FT`·`FE` 가 모두 같은 함정이었습니다 — p6·p9 의 `FT`/`FE` 는 HRSG 유량
패키지라 **벤더 제외** 대상인데, 타입 제외로 잘못 일반화됐던 것입니다.

### [B] 벤더마크 표기 3종 통합

마크는 **3가지 표기**로 존재하고, 범례와 도면이 서로 다른 표기를 쓰기도 합니다.

| 요소 | 실측 |
|---|---|
| 범례 표기 | 글리프 / 텍스트 `(*)`·`*`·`MARKED WITH *` / 없음 |
| 도면 마크 | 글리프(1개 path 4.0pt 또는 2.6pt 4개 묶음) / 텍스트 `(*)` `(**)` |
| 부착 위치 | 버블 바로 위 / **패키지 점선 박스 모서리** |

구현 요점:

- **글리프 크기를 상수로 박지 않습니다.** 페이지 6은 4.0pt 단일 path, 페이지 49는
  2.6pt 4개 묶음으로 별표를 그립니다. 인접 조각을 클러스터링해 하나의 글리프로
  묶고, **그 페이지 범례가 쓰는 크기**를 기준으로 도면 마크를 인정합니다.
  (범례가 없는 페이지에서만 다른 페이지 범례에서 관찰된 크기를 **탐지용으로만**
  씁니다. 의미 해석은 끝까지 페이지 스코프입니다 — §10.1.)
- **범례 파서가 6가지 문형을 흡수합니다** — `(*) SYMBOL INDICATES …`,
  `(*)MARKED ITEMS …`, `* TO BE SUPPLIED BY …`, `MARKED WITH * …`,
  글리프 + `DENOTES …`, 글리프 + `TO BE SUPPLIED BY …`. 줄바꿈된 정의는
  다음 줄까지 이어붙이되 y 간격으로 제한해 무관한 문구가 섞이지 않게 했습니다.
- **패키지 경계 박스**: 점선 사각형을 찾아 모서리 마크를 박스 내부 전체에
  적용합니다. p6 의 FT/FE 패키지가 그 사례입니다.

### 1. 규칙별 기여도 (MATCHED 기준)

| 활성 규칙 | 검출 | Excel | 일치 | 재현율 | 정밀도 |
|---|---|---|---|---|---|
| 벤더 규칙 없음 | 717 | 536 | 524 | 97.8% | 73.1% |
| 글리프만 | 610 | 536 | 520 | 97.0% | 85.2% |
| + 텍스트형 | 604 | 536 | 520 | 97.0% | 86.1% |
| **+ 패키지박스 (새 기준선)** | 598 | 536 | 520 | **97.0%** | **87.0%** |
| + SCT on | 596 | 536 | 518 | 96.6% | 86.9% |

정밀도 기여: 글리프 **+12.1pp**, 텍스트형 +0.9pp, 패키지박스 +0.9pp.
SCT 는 여전히 손해라 비활성 유지.

전체 진행: 2b 81.7/79.4 → 2c 91.4/85.7 → **2d 97.0/87.0**.

### 2. PAB 정밀도 24.0% → 54.5%

검출 50 → 22, 일치 12 유지. 오검출 28건이 정당하게 제거됐습니다.

**단, 2b 에서 제가 세운 가설은 부분적으로 틀렸습니다.** p26 의 `(**)` 텍스트
마크 7개는 확실히 기기(펌프·스크린)에 붙어 있어 계기와 무관합니다. 그런데
p26 의 계기 버블 위에는 **별도로 글리프 별표가 그려져 있었습니다** — 범례는
텍스트로 쓰고 도면 마크는 그리는 혼합 표기입니다. LIT·PI 버블 12pt 위에
정확히 얹혀 있는 것을 렌더링으로 확인했습니다. 즉 PAB 개선은 "텍스트 마크
파싱"이 아니라 **글리프 크기 폴백 + 페이지 텍스트 범례 해석**의 조합에서
나왔습니다.

남은 PAB 오검출 10건은 여전히 NOTES 산문(`PRESSURE GAUGE WILL BE SUPPLIED BY
CWP SUPPLIER`)으로만 표현된 건이라 이번 범위 밖입니다.

### 3. VENDOR_MARK_UNDEFINED = 14건

페이지 25, 33, 40, 41. 마크는 있는데 그 페이지 NOTES 에 정의가 없어 **제외를
보류**한 건입니다. 0건이던 비정상 상태가 해소됐습니다.

### 4. 실패 유형 재집계 (v1 → v3, MATCHED)

| 유형 | v1 행 | v3 행 | 변화 |
|---|---|---|---|
| `OVER_DETECT` | 70 | 78 | +8 |
| `TYPE_MAPPING_MISS` | 39 | **2** | −37 |
| `NO_ANCHOR` | 23 | **1** | −22 |
| `AMBIGUOUS_GEOMETRY` | 13 | 9 | −4 |
| `SCOPE_CONFLICT` | 6 | 4 | −2 |

2c 기준 `OVER_DETECT` 118행 → **78행**. `TYPE_MAPPING_MISS` 는 25 → 2행으로
사실상 소멸했습니다.

### 5. 계통별 정확도 (MATCHED, 재현율 오름차순)

| 계통 | 페이지 | 검출 | Excel | 재현율 | 정밀도 |
|---|---|---|---|---|---|
| `SCB` | 1 | 0 | 1 | 0.0% | 0.0% |
| `QFB` | 1 | 1 | 2 | 50.0% | 100.0% |
| `GHB` | 1 | 17 | 17 | 64.7% | 64.7% |
| `PAB` | 3 | 22 | 14 | 85.7% | 54.5% |
| `LCA` | 2 | 38 | 41 | 90.2% | 97.4% |
| `LAC` | 1 | 22 | 23 | 95.7% | 100.0% |
| `PGB` | 7 | 176 | 170 | 99.4% | 96.0% |
| `GHC` | 1 | 34 | 16 | 100.0% | 47.1% |
| `EGD` | 2 | 51 | 33 | 100.0% | 64.7% |
| `LBA` | 2 | 31 | 24 | 100.0% | 77.4% |
| 나머지 9계통 | 11 | — | — | 100.0% | 67~100% |

### 6. SCB 0% / QFB 50% / GHB 64.7% 원인 — 셋 다 검출기 문제가 아닙니다

**PDF 에 국문 검토 주석 레이어가 있고, Excel 이 그 개정을 반영하지 않았습니다.**
14개 도면 본문에서 `삭제`·`추가`·`위치이동`·`복원` 주석을 찾았습니다.

- **GHB (p49)** — 주석 `PDIT 3EA, PIT 2EA 삭제`. Excel 은 PDIT 6 / PIT 4,
  개정된 도면은 3 / 2, 검출도 **3 / 2**. 정확히 일치합니다. 추가로
  `RO FIT 1EA 삭제` 주석이 Excel 의 `FIT` 1행 결손을 설명합니다.
  주석을 반영하면 **재현율 11/11 = 100%** 가 됩니다(정밀도는 PI 오검출 때문에
  64.7% 유지).
- **SCB (p44)** — 주석 `PIT 삭제`. Excel 의 유일한 1행은
  `Instument air ring header PRESSURE`(SYSTEM `Compressed Air System`)로
  **Service Air Distribution 도면에 속하지 않습니다.** 검출 0건이 맞습니다.
- **QFB (p43)** — Excel 에 `Instument air ring header PRESSURE` 가 **3행 중복**
  되어 `10QFB10`(2행)과 `00SCB10`(1행)에 나뉘어 있습니다. 실제 계기는 1개고
  검출도 1건입니다. Excel 중복 입력 문제입니다.

부수 효과로, 이 주석 텍스트(`PIT 삭제` 등)가 앵커 스캐너에 걸립니다.
지오메트리 검증(버블 없음)이 전부 걸러내 오검출은 없지만, 현재
`AMBIGUOUS_GEOMETRY` 로 분류되는 건 중 상당수가 실은 **주석 텍스트**입니다.
분류 체계에 별도 유형이 필요해 보입니다.

### 남은 최대 오검출

`GHC` 47.1%, `PAB` 54.5%, `EGD` 64.7%, `GHB` 64.7% — 모두 `PI`(로컬 압력계)
과검출이 핵심이고, 대부분 NOTES 산문형 공급자 선언으로만 배제되어 있습니다.
다음 단계 대상입니다.

---

## 2e. 범용 구조 정리 — 종속성 인벤토리 · 교차검증 · 주석 분리

정밀도 개선은 여기서 멈추고, 남은 `PI` 과검출(NOTES 산문 해석 필요)에 들어가기 전에
**무엇이 프로젝트에 묶여 있는지**를 먼저 확정했습니다.

### [A] 프로젝트 종속 값 인벤토리 → `out/project_deps.md`

코드 전체 33개 항목. **`LEGEND` 10 / `PROJECT` 18 / `UNKNOWN` 5.**
config 파일로 빼지는 않았습니다(목록·위치·판정만).

판정에 쓴 실측 근거 중 중요한 것:

- **버블 형상은 범례에 있습니다.** p3·p4 가 같은 스타디움을 43개·45개 그립니다
  → `LEGEND`. 다만 범례에는 68.0×22.6 한 계열만 있고 도면의 85.1×28.4 는
  출처 불명이라 캡 범위 상수는 `UNKNOWN`.
- **앵커 어휘는 범례, 대응표는 Excel.** p3 의 ISA 행렬이 `TT`·`FE`·`LG` 를
  정의하지만 `TIT`·`FIT`·`LI` 는 **범례에 없습니다** — Excel 쪽 관행이므로
  `TT→TIT` 류 매핑은 전부 `PROJECT`.
- **unit_code 승수는 범례에서 유도됩니다.** p5 `UNIT IDENTIFICATION NUMBERS`
  표에 그룹 2개(10·20)·그룹당 유닛 2개(11·12/21·22)가 명시돼 있어
  `00→×1, 10→×2, 11→×4` 가 그대로 나옵니다. **상수로 박지 말고 표를 파싱할 것** —
  그러면 구성이 다른 프로젝트에도 적용됩니다.
- **벤더 마크는 범례에 없습니다.** p2~p5 네 페이지 모두 마크 범례 0건(실측).
  의미는 페이지 NOTES 에만 있으므로 §10.1 페이지 스코프가 맞습니다.
- **SCT 공급자 인터페이스 심볼은 범례 p2 에 정의**되어 있습니다 → `LEGEND`.
  단 판정 수치(`drop_x_tol` 등)는 p6 실측이라 `PROJECT`.

### [B] 교차검증 — Steam 계통만으로 규칙을 세워 미지 계통에 적용

현재 97.0% 는 53개 도면을 다 보고 맞춘 in-sample 값입니다. Steam(LBA/LBB/LBC/
LBG/MAN)만 보고 규칙을 재구성하고 — 다른 계통이 근거인 규칙은 전부 끄고 —
본 적 없는 계통에 적용했습니다.

| 대상 | 페이지 | Steam 규칙 | 전체 규칙 | Δ재현율 | Δ정밀도 |
|---|---|---|---|---|---|
| STEAM (in-sample) | 7 | 100.0% / 94.6% | 100.0% / 94.6% | 0 | 0 |
| **PGB (미지)** | 7 | **99.4% / 96.0%** | 99.4% / 96.0% | **0** | **0** |
| **PAB (미지)** | 3 | 85.7% / **24.0%** | 85.7% / 54.5% | 0 | **−30.5pp** |
| **EGD (미지)** | 2 | **93.9% / 54.4%** | 100.0% / 64.7% | **−6.1pp** | **−10.3pp** |
| ALL MATCHED | 35 | 95.3% / 81.4% | 97.0% / 87.0% | −1.7pp | −5.6pp |

**핵심 결과: 최대 계통인 PGB(7장·170행)가 하락 0 으로 완전히 전이됩니다.**
버블 지오메트리·앵커 스캔·글리프 마크·패키지 박스·`FE`/`FT`/`LS` 매핑으로 이루어진
코어 규칙은 과적합이 아닙니다. 하락은 전부 특정 계통 전용 규칙에서 나옵니다.

**신규 프로젝트 예상 성능의 대리 지표: 재현율 ~95%, 정밀도 ~81%.**
단 계통별 분산이 매우 큽니다 (PGB 99.4/96.0 ↔ PAB 85.7/24.0).

### 과적합으로 판정된 규칙

| 규칙 | 근거가 나온 곳 | 빼면 잃는 것 |
|---|---|---|
| **`KNOWN_GLYPH_SIZES` 의 5.2pt** | p49 (GHB) | **PAB 정밀도 −30.5pp** — 최대 과적합 항목 |
| **`VENDOR_MARK_TEXT` 범례 문형** | p26/32/46/51 | EGD 정밀도 −10.3pp 의 대부분 |
| **`LG` → `LI`** | p33/p49/p51 | EGD 재현율 −6.1pp |
| `DPIT` → `PDIT` | p21 (LAC) | 이번 홀드아웃엔 영향 없음 |

이 넷은 전부 "전수 대조로 찾아낸 매핑"이라 신규 도면에는 없을 값입니다.
config 화 1순위이며, 신규 프로젝트 투입 시 **동일한 전수 대조 절차를 다시
돌려야** 같은 성능이 나옵니다. 그 절차 자체(앵커 감사·마크 표기 조사)를
자동화하는 것이 범용화의 핵심입니다.

### [C] 주석 레이어 분리

`ANNOTATION_TEXT` 유형을 신설해 `AMBIGUOUS_GEOMETRY` 에서 분리했습니다.

- 주석 텍스트로 분류된 앵커: **4건 (p16, p44, p49)**
- `AMBIGUOUS_GEOMETRY` 11행 → **7행**, `ANNOTATION_TEXT` **2행** 신설

실패 유형 재집계 (v1 → v3, MATCHED):

| 유형 | v1 행 | v3 행 |
|---|---|---|
| `OVER_DETECT` | 70 | 78 |
| `NO_ANCHOR` | 23 | 1 |
| `TYPE_MAPPING_MISS` | 39 | 2 |
| `AMBIGUOUS_GEOMETRY` | 11 | **7** |
| `SCOPE_CONFLICT` | 6 | 4 |
| `ANNOTATION_TEXT` | 2 | **2** |
| `VENDOR_MARK_UNDEFINED` | 0 | 0 |

`out/revision_gap.md` — 도면 본문에 국문 주석이 있는 **14개 도면** 목록.
규칙으로 보정하지 않았습니다. 타이틀블록 `Rev.C P&ID 미접수` 표기가 붙은
**22개 페이지**도 함께 정리했습니다 — Excel 이 최신 Rev 기준이라면 이 페이지들의
결손은 검출기 문제가 아니라 도면 버전 차이입니다.

### 산출물

- `out/project_deps.md` — 33개 항목 인벤토리 (LEGEND/PROJECT/UNKNOWN + 근거)
- `out/crossval.md`, `out/crossval.json` — 교차검증
- `out/revision_gap.md` — 도면 개정 ↔ Excel 불일치 (발주처 확인용)

---

## 2f. config 분리 — PROJECT 18건

```
config/project_alnouf1.yaml   프로젝트 종속 값
spike/projectconfig.py        로더 + 범례 승수 유도
```

`out/project_deps.md` 에서 `PROJECT` 로 판정한 18건만 옮겼습니다. `LEGEND` 10건은
코드에 남기고, **어느 범례 페이지의 어느 항목에서 유도했는지 주석으로** 남겼습니다.
예: `VALVE_ANCHORS` 위에는 "범례 p3 SELF-ACTUATED DEVICES 와 p4 VALVE BODY WITH
ACTUATOR" 근거가, `ISA_LIKE_RE` 위에는 "p3 행렬의 최대 길이 5자(PDAHL)" 근거가
붙어 있습니다.

### 동작 요건

- **기본값 없음.** `ProjectConfig.lookup()` 은 키가 없으면 `UNDEFINED` 를 돌려주고,
  호출부는 값을 채우지 않고 `NEEDS_REVIEW` 를 사유와 함께 기록합니다.
  예: 범례 표에 없는 `unit_code '30'` → 수량 공란 + `"unit code '30' is not in
  the legend's UNIT IDENTIFICATION NUMBERS table; multiplier undefined"`.
- **멈추지 않습니다.** 항목 단위 결손은 해당 항목만 검토로 넘기고 나머지는 계속
  처리합니다. 예외는 config 파일 자체가 없거나 구조가 깨진 경우로, 이건 실행 전
  설정 오류이므로 즉시 실패시킵니다.
- **재현 확인**: config 분리 후 재실행 결과 **재현율 97.0% / 정밀도 87.0%**,
  타이틀블록 58/58 — 분리 전과 완전히 동일합니다.

> YAML 함정 하나: `excel.columns` 의 `no` 키는 따옴표가 필요합니다. YAML 1.1 이
> 맨 `no` 를 boolean false 로 읽습니다.

---

## 3. Q'ty 승수 검증

```bash
python spike/parse_notes.py --pdf data/pid_total.pdf \
       --compare data/CZE_Field_Instrument.xlsx \
       --titleblocks out/titleblocks.csv --out out/qty_report.md
```

### 승수는 config 상수가 아니라 범례에서 유도합니다

범례 p5 `UNIT IDENTIFICATION NUMBERS` 표를 파싱해 스코프별 행 수를 셉니다.

| unit_code | 승수 | 스코프 | 근거 |
|---|---|---|---|
| `00` | ×1 | PLANT | POWER PLANT COMMON — 표에 1건 |
| `10` `20` | ×2 | GROUP | FIRST/SECOND GROUP COMMON — 표에 2건 |
| `11` `12` `21` `22` | ×4 | UNIT | 각 그룹 #1/#2 GTG/HRSG — 표에 4건 |

파싱 성공(`source: LEGEND`). **상수 `{00:1, 10:2, 11:4}` 를 config 에 박지
않았습니다** — 그룹이 3개인 플랜트라면 같은 코드가 ×3/×6 을 내놓습니다.
config 의 `unit_multiplier_fallback` 은 파싱 실패 시에만 쓰이고, 사용 여부가
`source` 로 로그에 남습니다.

### 정확도 (MATCHED, 개정 주석 페이지 제외)

| 집합 | 페이지 | 산정 Q'ty | Excel Q'ty | 일치 | 재현율 | 정밀도 |
|---|---|---|---|---|---|---|
| **주석 없는 페이지 (기준)** | 24 | 834 | 765 | 761 | **99.5%** | **91.2%** |
| 개정 주석 페이지 (별도 계상) | 11 | 329 | 311 | — | — | — |
| MATCHED 전체 | 35 | 1163 | 1076 | 1057 | 98.2% | 90.9% |

Excel 563행 Q'ty 합계는 1,120 이고, 그중 MATCHED 집합에 귀속되는 것이 1,076 입니다.

**승수별** — 승수 자체는 정확합니다. 오차는 전부 심볼 과검출에서 옵니다.

| 승수 | 페이지 | 심볼 | 산정 | Excel | 차이 |
|---|---|---|---|---|---|
| ×1 | 7 | 94 | 94 | 59 | +35 |
| ×2 | 15 | 260 | 520 | 482 | +38 |
| ×4 | 2 | 55 | 220 | 224 | **−4** |

**계통별 (오차 큰 순)**: `GHC` +18, `PAC` +16, `LBA` +14, `EGD` +12, `PGB` +7,
`LAC` −4, `GKB` +3, `PAB` +2, `QFB` +1, 그리고 `MAJ`·`LCM`·`LBG`·`LBC`·`LAB`·`EKG`
**6개 계통은 오차 0**. 오차 상위 계통은 심볼 정밀도가 낮았던 계통과 정확히
같습니다(GHC 47.1%, PAC 66.7%, EGD 64.7%) — 즉 **Q'ty 오차는 승수 문제가 아니라
심볼 과검출이 승수만큼 증폭된 것**입니다.

### Note 문구 교차검증 — 16/16 일치

NOTES 의 `GROUP#`/`UNIT#` 열거 **개수만** 세어(문장 해석 없음) 범례 승수와
대조했습니다. 열거가 있는 16페이지 전부 일치합니다.

주의점 하나: 이 도면들의 Note 는 `IDENTICAL FOR UNIT#12,21,22` 처럼 **쉼표로
이어 씁니다.** `UNIT#\d{2}` 만 잡는 정규식은 4를 2로 잘못 세어 p20·p21 에서
불일치가 나왔고, 쉼표 연속을 포함하도록 고쳐 16/16 이 됐습니다.

또한 설계안 §8.1 은 "Note 가 1순위, unit_code 는 검증용"이라 했지만, 실측으로는
**둘이 완전히 일치**하고 범례 표 쪽이 파싱이 훨씬 단순합니다. 승수는 범례에서
유도하고 Note 는 교차검증에 두는 현재 구성을 권합니다.

### 같은 도면 내 승수 예외 — 1건, 가설 검증됨 / 적용 보류

`D00P-10PGB10-M05-0004`(p38) 한 건입니다.

- 도면에 `AUXILIARY BOILER COOLER` 라벨이 있고 **바로 아래 `(PLANT COMMON)`**
  (좌표 `327.6, 814.9`).
- Excel 에서 이 도면의 Q'ty=1 인 3행은 전부 이 설비 것 — `CCW SUPPLY PRESSURE`(PI),
  `CCW RETURN TEMPERATURE`(TI), `CCW RETURN PRESSURE`(PI). 나머지 21행은 Q'ty=2.
- 도면 렌더링 결과 이 설비의 계기는 정확히 **PI 2 + TI 1**, 좌표
  `(243.5,473.4)` `(410.7,1135.4)` `(410.5,1184.4)`.

**가설은 맞습니다. 그러나 승수 강제를 적용하지 않았습니다.** "어느 계기가 이 설비
소속인가"를 판정하려면 설비를 지나는 수직 배관을 따라가야 하는데,

- 설비 점선 박스는 대시가 길어 현재 broken-line 검출기에 안 잡힙니다
  (`brk_max_mark` 는 UNKNOWN 항목이라 이번에 튜닝하지 않았습니다),
- 라벨 x 범위로 묶으면 옆 설비의 `(LUBE OIL` 텍스트가 끼어들어 엉뚱한 계기가
  포함되고 정작 `(243.5, …)` 는 빠집니다.

설계안 §8.3(불확실하면 비우고 검토로) 에 따라 **키워드 발견 사실만 기록**했습니다.
지표 영향은 3행 × (2−1) = Q'ty 3 과다로 전체의 0.3% 입니다. 올바른 귀속은
§7 라인 추적(Phase 2) 이 들어와야 가능합니다.

### NEEDS_REVIEW

이번 문서에서는 0건 — 등장하는 unit_code 가 전부 범례 표에 있습니다.
정의에 없는 코드가 나오면 수량을 비우고 사유와 함께 검토로 보냅니다.

### 산출물

- `out/qty_report.md`, `out/qty_report.json`
