# 반영 보고 — 2026-09-15 (r48 · 10~48회차 누적)

기준 커밋 `aac64ba`(회사 PC 가 마지막으로 맞춘 코드) → HEAD.  이 문서는 **회사 PC 에
아직 없는 것**을 먼저 적고, 그 다음에 **다음 개발 사항**을 적습니다.  회차별 근거는
`CLAUDE.md` 와 `docs/state_and_roadmap.md`, 산출물은 `out/roundNN/` 입니다.
같이 만든 꾸러미: `out/PID_update_2026-09-15_r48.zip` (넣는 순서는 그 안의
`1_먼저읽기_README.txt`).

## 0. 한 줄

**AL NOUF1 은 한 칸도 달라지지 않았고**(1037행 · Q'ty 1931 · 지문 `fb85b039` ·
정확도 축1 99.3/70.5 · 축2 99.3/94.4 · 발주처 양식 885/76/23), 그 위에 **다른 회사
양식 셋(SADARA · TC2 · UAD)이 같은 코드로 완주**하게 됐습니다.  코드에 외워둔 값을
199개 → 175개로 줄였고, 수량·별표·도면번호·개정 표기를 **그 도면에서 읽습니다**.

**45~48회차 (이 꾸러미에 새로 들어간 것)** — **6차 피드백 반영**이 중심입니다.
지적 35건 중 **10건이 한 원인**이었습니다: 액추에이터 울타리의 크기 창이 AL NOUF1
치수(pt)로 박혀 있어 A3 로 그린 도면의 모터 원(7.0pt)을 전부 거부하고 있었습니다.
이제 **그 도면 범례가 그린 액추에이터 원**에 배율로 맵니다 — TC2 731 → **811행**
(MOV 탭 67행) · UAD 192 → 198행 · **AL NOUF1 은 한 칸도 안 움직입니다**.
별표(공급 구분)도 두 군데를 고쳤습니다 — 같은 크기를 재는 자가 배울 때와 맞출 때
12배 달라 별표 하나가 조용히 빠지던 것(47회차), 그리고 나란한 계기 버블이 옆 밸브의
별표까지 세어 공급자 이름이 비던 것(48회차).  45회차에는 **획으로 그린 타이틀블록의
P&ID No. 를 사람이 한 번 적는 경로**가 들어갔습니다(화면 패널은 다음 회차).
46회차는 위생 경고를 한 행씩 펴 보이고, 프로젝트 삭제를 열고(안정 ID 장부는 보존),
마크업 제안을 **6.9~13.8초 → 3.9~6.1초**(같은 장 2회째 0.0초)로 줄였습니다.

## 1. 지금 상태 — 네 프로젝트 (한 명령 `python3 spike/regression_3p.py` 로 잽니다)

| 프로젝트 | 도면 | 행 | 지문 | Q'ty | 축3(식별률) | 최대 RSS | 역할 |
| --- | --- | ---: | --- | ---: | ---: | ---: | --- |
| **AL NOUF1** | 58장 A1 | **1037** | **`fb85b039`** | 1931 | 94.2 | 4.9G | 목표선 · 발주처 리스트가 있는 유일한 프로젝트 |
| SADARA | 9장 A0 | 86 | `53531ef6` | 0 | 76.3 | 0.9G | 교차검증 · 승수는 사람이 답할 자리 |
| TC2 | 60장 A3 | **811** | `f3712abb` | 4875 | 92.4 | 7.5G | 다른 회사 양식 · `/Rotate 270` · ★ 47회차 MOV 탭 67행이 새로 나온다 |
| UAD | 32장 A3 | **198** | `9783173e` | 198 | 83.3 | 3.2G | 태그 인쇄(1급) · SHX 획 글꼴 |

빠른 시험 **376 통과**(24초) · UI 24 · slow 16.  회귀 하네스는 넷을 순차로 돌리고
기준선(`spike/baselines_3p.json`)을 벗어나면 실패합니다.

⚠ 축3 은 44회차 표의 95.1 / 76.0 / 91.2 / 87.5 에서 94.2 / 76.3 / 92.4 / 83.3 으로
바뀌었습니다.  **분모가 움직인 것이 대부분입니다** — 심볼판정 지표의 분모는 그 도면이
그린 심볼 수이고, 47회차가 액추에이터를 더 찾으면서 분모도 같이 늘었습니다.
"맞게 읽었다" 가 아니라 "칸을 채웠다" 를 재는 축이라는 31회차 [6] 의 한계가
그대로입니다.

## 2. 회사 PC 에 아직 없는 것 — 반영 내역 (주제별)

회사 PC 의 코드는 **824행 · 정밀도 84.1%** 도구입니다.  아래가 그 뒤에 들어간 것입니다.
r27 꾸러미(2026-09-11)를 이미 넣으셨다면 **§2.7 이후**가 새 것입니다.

### 2.1 수량 (Q'ty)

* TW 를 계상하고(10회차 · 명시적 제외였음), 벤더 공급분도 행으로 냅니다 — 되돌리기는
  config `scope.exclusion_rules` 한 줄.
* **그 장의 NOTES 가 "이 P&ID 는 유닛 몇 개에 같이 쓰인다" 고 말하면 그 수를 씁니다**
  (27회차).  읽는 순서: ① 범례 UNIT IDENTIFICATION 표 → ② 그 장 NOTES → ③ 사람 지정
  → ④ 설정 폴백 → ⑤ 빈칸+사유.  범례가 답하는 AL NOUF1 은 한 칸도 안 움직입니다.
* **낱말 문지기를 없앴습니다** (36회차).  `IDENTICAL / SIMILAR / TYPICAL …` 같은 낱말이
  있어야만 세던 것을, **번호 붙은 NOTES 문단이 유닛 표기를 둘 이상 열거하면** 셉니다.
  네 문서 Q'ty 불변으로 확인.  config 의 `qty_note` 블록은 지웠습니다 (코드 기본값과 같았음).
* **유닛 표기의 꼴을 그 도면에서 배웁니다** (36회차) — 시트 유닛코드가 NOTES 에 어떤
  꼴(`#10` · `3-1` · `NO.3-1`)로 인쇄되는지를 두 장 이상에서 보고 배운 꼴만 셉니다.
  TC2 p33·p34 40행이 x1 → x4 (Q'ty 3047 → 3167).  맨 숫자는 배우지 않습니다
  (`DN200` · `NOTE 3` · `SET: 00 barg` 가 유닛이 되면 안 됩니다).
* **도면이 승수를 말하지 않을 때 사람이 한 번 답하는 자리** (31회차) — 화면이
  유닛코드로 묶어 묻고 `projects/<프로젝트>/unit_multipliers.json` 에 작성자와 함께
  저장합니다.  사람은 도면을 이기지 못합니다 (AL NOUF1 에 x9 를 넣어도 불변).
  **값은 아직 비어 있습니다 — §4 참조.**
* 승수가 설정 폴백에서 온 행은 `MULTIPLIER_FROM_CONFIG` 로 그렇게 말합니다 (26회차).

### 2.2 SCOPE (SCT / 타사 공급)

* 별표(*)와 패키지 상자(체인 파선)로 공급 주체를 가르고, 공급자 이름은 **그 장 NOTES
  원문**(`* BY HRSG SUPPLIER`)에서 읽습니다 (10·16회차).  코드·config 에 이름이 없습니다.
* 별표를 **그 장에서 다시 재고**(18) · **옆 별표도 받고**(19) · 밸브는 몸체와
  액추에이터를 둘 다 보고(19) · **획으로 그린 별표를 획의 관계로 읽습니다**(25·26 —
  TC2 는 별표를 획 넷으로 따로 그립니다).  `BY SCT` 는 우리 공급입니다 (26).
* 버블 안 글자를 별표로 읽던 것을 **그려진 윤곽**으로 가릅니다 (30회차 · 새 상수 0).
* AL NOUF1 축2 정밀도 86.8 → **94.4%** (FP 78 → 35 · FN 4).  남은 FP 35 중 28은
  "같은 도면번호를 두 장이 쓴다"(16) 와 발주처 계상 관행(12)이라 도면을 옳게 읽은 것입니다.

### 2.3 발주처 양식 (Excel)

* SCT 전용 측정축(축2)을 더했고 양식은 **전량**을 담습니다 (18회차 · `client_form.scope_filter: all|sct`).
* SCOPE 열 · TYPE 표기(`MOV(GLOBE)`) · REMARK(접힌 LS 신호 · 판정축 사유).

### 2.4 화면

* 첫 화면이 **프로젝트 → 리비전** 단위(13) · 재분석 없이 열기 0.69초 · 편집 영속 ·
  개정본으로 편집 승계(안정 ID) · 작성자 자기신고.
* 진행률·취소·완료 화면(12) · 실패 화면이 어디서 멈췄는지 말함(21) · 근거 패널이
  공급 주체 / 추출 결과 / 발주처 양식을 나눠 말함(12·14).
* **오버레이 색은 SCOPE 열, 검토 필요는 모서리 ● 표식**(33) — 검토 사유가 전량에
  붙은 문서에서 도면이 한 색이 되던 것을 고쳤습니다.
* 승수 지정 패널(31) — "다시 분석해야 적용된다"가 패널에 상주합니다.
* 전역 심볼 사전 등록 화면(18) — 미판정 심볼을 세어 두고 사람이 등록합니다 (검출에 안 닿음).

### 2.5 프로젝트 영속 · 개정

* 범례 프로필 저장·재사용·대조(15) — 범례 없는 개정본이 완주하고, 범례가 달라진 항목은
  사람이 고릅니다 (자동 갱신 없음).
* 장별 Rev 를 읽어 저장하고 문서 대표 Rev 는 `max`(13).  같은 도면번호를 쓰는 장이
  여럿이면 판정하지 않습니다.

### 2.6 다른 회사 양식이 와도 같은 코드로

* 타이틀블록 네 칸 유도(22) · 두 양식을 한 서버에서 섞어도 서로 안 망가짐(22 ·
  `_own_config`) · 새 양식 좌표 뽑기 `GET /jobs/{id}/measured_config`.
* **이력 표 다섯 칸을 그 도면에서** (24 · TC2 REV 0/60 → 60/60).
* **개정 표기 모양을 이력 표 REV 열에서** (30 · UAD 개정 0 → 23/32).
* **도면번호 형식을 그 도면의 DWG NO. 칸에서** (37 · TC2 56/60 → 60/60 · p25~28 4장
  회수 · 607 → 684행).

### 2.7 (r27 이후) 글자가 글자가 아닌 도면 · 1급 태그

* **SHX 획 글꼴 도면을 읽습니다** (28) — AutoCAD 가 `AutoCAD SHX Text` 주석에 남긴
  원문을 어구 단위로 받습니다.  UAD 가 이것으로 149행을 냅니다.
* **태그가 인쇄된 도면은 태그로 귀속합니다** (29 · 1급).  태그 형태를 코드에 박지 않고
  "한 검출에만 나오고 그 모양이 두 검출·두 장 이상에서 되풀이"될 때만 태그로 봅니다.
  AL NOUF1·SADARA·TC2 는 0행 (회귀 불변).  태그로 SCOPE 는 판정하지 않습니다.
* 밸브에 태그가 붙는 순간 죽던 결함(33)을 고쳤습니다.

### 2.8 (r27 이후) 외워둔 값 정리 — 범용성

* 상수 전수(23·25) → **199개를 전부 지우고 유도만으로 재현**(35 · 61개는 지워도 불변)
  → **삭제 24 적용 · `anchors` 룰셋과 타이틀블록 정규식을 런타임에 다시 만듦**(36).
  외워둔 값 199 → **175**.
* 합성 도면 8종으로 스스로 깨뜨리기(32) · 프로젝트 순서를 바꿔 돌려도 같음(32·36).
* 메모리: 잉크 인덱스를 한 장만 들고 끝나면 놓습니다 (26·33) — AL NOUF1 4.9G · TC2 7.2G.

### 2.9 시험 격리

시험은 실 DB 에 닿지 않습니다 (17 · 뿌리 `conftest.py`).  UI 스위트는 SCOPE 있는
데이터에서만 뜻이 있고 전제가 깨지면 건너뛰지 않고 실패합니다 (14).

### 2.10 (r41 이후) 사용자 마크업 · 피드백 내보내기 (44회차)

* **마크업 모드** (`[마크업]` 버튼).  빈 자리를 드래그하면 그 사각형이 **누락 행**이 됩니다.
  저장 전에 서버가 그 자리에서 **별표·NOTES(SCOPE) · 사각형 안 낱말(TYPE) · 같은 장의 행이 받은
  수량(Q'ty)** 을 먼저 읽어 채우고, 못 읽은 칸은 빈칸으로 두어 사람이 적습니다 (§9 ①②).
  어느 칸이 도면 값이고 어느 칸이 사람 값인지 `scope_source` · `qty_source` 로 남깁니다.
* **오검출 표시** — 마크업 모드에서 상자를 클릭.  사유(㉢ 오검출 · ㉣ 값 틀림 · 미지정 심볼 ·
  기타) · 작성자 · "Excel 제외" 선택.  **행은 지워지지 않고** 되돌릴 수 있습니다.
  ㉣ 값 틀림은 그 칸을 바로 고치는 길로 이어집니다 (편집 이력).
* **화면** — 색은 SCOPE 그대로이고 표시는 모서리 표식입니다 (추가 ✚ 왼쪽 위 · 오검출 ✕ 오른쪽
  아래 · 검토 ● 오른쪽 위).  범례에 "(그중) 사용자 추가 N" · "(그중) 오검출 표시 N" — 세 색
  칸의 합 = 상자 수 = 그리드 행 수 등식은 그대로입니다.
* **Excel** — 사용자 추가 행은 SCOPE 값으로 같은 필터를 지나 양식에 나가고, REMARK 열 앞머리에
  `사용자 추가 · 이름` 이 붙습니다.  **새 열은 없고 시각도 적지 않습니다** (같은 마크업 = 같은
  파일).  마크업이 없으면 885 / 76 / 23 그대로입니다.
* **안정 ID** — 프로젝트에 묶인 분석이면 추가 행도 같은 장부(`id_registry.json`)에서 다음
  번호를 받습니다 (회수 없음 · 재정렬 없음).  다음 리비전에서 엔진이 그 자리를 찾으면 같은 ID 를
  잇습니다.
* **피드백 내보내기** (`[피드백 내보내기]` 버튼) — `feedback_<프로젝트>_<날짜>.zip`:
  `feedback.json`(지문 · 행 수 · 항목: missing / false_positive / wrong_value / report) ·
  `feedback.md`(표) · `조각/`(항목마다 작은 그림) · `장/`(장 전체 + 표시).  마크업은 내보낸
  뒤에도 남습니다.  **도면 내용이 들어가므로 저장소에 넣지 않습니다.**
* **측정은 안 움직입니다** — 마크업은 편집값 층에만 살고 추출값 · 지문 · 축1·2·3 은 결과 json 만
  읽습니다 (`tests/test_markup.py` 가 소스 검사 + DB 실측으로 못박습니다).

## 3. 검증

* 네 프로젝트 회귀 — 48회차 최종 코드로 넷을 따로 돌려 **넷 다 기준선과 같음**
  (AL NOUF1 1037 · `fb85b039` · 1931 · 776초 · 4.9G / SADARA 86 · `53531ef6` · 81초 /
  TC2 811 · `f3712abb` · 4875 · 615초 · 7.5G / UAD 198 · `9783173e` · 445초 · 3.2G).
* **AL NOUF1 은 47·48회차 두 변경을 거치고도 한 칸도 안 움직였습니다** — 47회차는
  배율이 옛 절대값을 소수점까지 재현하고(9.0/14.22 · 34.0/14.22), 48회차는 첫 구현이
  옮긴 한 칸(p7 NRV)의 원인을 규명해 규칙을 고친 뒤 되돌아왔습니다.
* 빠른 시험 **376 통과** · 실 DB sha256 은 회차마다 시작·끝 동일.
* ⚠ **이 꾸러미의 18단계 적용 검증은 이번 회차에 다 돌리지 못했습니다** — 어디까지
  돌렸는지는 `out/round48/verify/README.md` 에 단계별로 적습니다.  돌리지 못한 단계는
  "통과" 가 아니라 **"안 돌림"** 으로 적혀 있습니다.

## 4. 꾸러미를 넣은 뒤 사람이 할 일

1. **재분석** — 옛 분석본은 옛 값 그대로입니다.  AL NOUF1 을 다시 분석하면 1037행 ·
   Q'ty 1931 이어야 합니다.
2. **승수 값** — SADARA(유닛 `10`) · TC2(`00` 203행) · UAD(`00` 149행)는 도면이 승수를
   말하지 않습니다.  결과 화면의 승수 패널에서 유닛코드별로 한 번 답하면 됩니다
   (세 번의 답으로 434행이 덮입니다).
3. **TC2 p25~28 의 SCOPE** — 37회차에 4장이 새로 행이 되면서 그 장의 별표가 처음으로
   행에 닿았습니다.  값이 맞는지 한 번 보아 주세요.
4. **SIMILAR 장의 배수** — TC2 23장 + p33·p34 가 "SIMILAR P&ID" 문형입니다.  같은
   계기를 갖는다고 볼지는 실무 판단입니다 (`out/round36_multiplier_vocab.md`).

## 5. 이번 회차 — 45~48회차 (6차 피드백 · 별표 소유권 · 꾸러미 r48)

**45회차** — UAD p11~p18 여덟 장이 통째로 빠지던 원인은 검출이 아니라 **타이틀블록이
글자가 아니라 획으로 플롯돼 P&ID No. 칸의 낱말이 0개**라는 것이었습니다.  획 글자를
읽어 보려던 세 시도를 실측으로 전부 기각하고(§9 ⑤), **사람이 한 번 적는 경로**를
만들었습니다 — 도면이 이기고(읽힌 칸은 덮지 않음), 21회차 `TitleBlockUnreadable`
**뒤**에 섭니다.  ⚠ 화면 패널은 아직 없습니다.

**46회차** — 위생 경고를 한 행씩(작성자·시각·분류) 펴 보이고, 프로젝트 삭제를 열되
**안정 ID 장부는 `_deleted/` 로 옮겨 보존**합니다(같은 이름으로 다시 만들면 이어받음).
마크업 제안이 사각형마다 PDF 전체를 다시 열던 것을 그 장만 열고 재료를 한 장치 들게
해 **6.9~13.8초 → 3.9~6.1초**, 같은 장 2회째 **0.0초**.  그리고 TC2 가 처음으로
**정답지**를 갖습니다 (범례부터 읽고, HIGH 만 정답지에 넣습니다).

**47회차 — 6차 피드백.** 지적 35건 중 10건이 `ValveLayout.act_box=(9.0, 34.0)`
**절대 pt** 하나였습니다.  창을 그 문서 범례의 액추에이터 원에 맵니다
(`ACT_BOX_BAND = (0.633, 2.391)` = 9.0/14.22 · 34.0/14.22 — **코드에 새 절대 pt 가
없습니다**).  [B] 로 별표 하나가 조용히 빠지던 것도 고쳤습니다 — 크기를 **배울 때는
반올림 칸(0.05pt) · 맞출 때는 0.6pt** 로 같은 자에 두 눈금이 12배 달랐습니다.

**48회차 — 별표 소유권.** 16회차가 세운 "한 마크는 심볼 하나에만 붙는다" 의
**경쟁자 목록이 버블끼리로 좁혀져** 있었습니다.  이제 밸브와 버블이 같이 겨루고,
판정은 여전히 `read_vendor_mark` 하나 · 거리는 19회차 유클리드입니다 (새 상수 0).
★ **태그 버블은 자기가 이름 붙인 밸브의 남이 아닙니다** — AL NOUF1 p7 의 NRV 가
가르쳤습니다(남으로 보면 `**` 한 쌍이 쪼개져 ST SUPPLIER 가 HRSG 가 됩니다).
TC2 3행 · UAD 24행이 공급자 이름을 되찾고 `VENDOR_MARK_UNDEFINED` 가 줄어듭니다.

## 5-이전. 44회차 (사용자 마크업 · 피드백 내보내기 · 꾸러미 r44)

**[B] 있는 것 조사** — 요구 여섯 중 넷이 이미 서버에 있었습니다 (`＋행` + 지점 기록 · 삭제 = 검출 행을
지우지 않고 `removed` 표시 · `probe_point` · `feedback` 표).  새로 만든 것은 잇는 코드 여섯뿐입니다
(`out/round44_survey.md`).  **[C] p48** — 다른 프로젝트 이름의 장(PORT DICKSON)은 `PROJECT NAME` 칸의
다수결(57/1)로 이미 범위 밖이고 행 0 · 축3 불변입니다.  양식은 같고 프로젝트가 다른 장이며, AL NOUF1
의 진짜 (2/2) 장은 이 PDF 에 없습니다 (실무 확인 항목).
**[D]~[F]** 는 §2.10.  **[G]** 네 프로젝트 회귀 — 넷 다 기준선과 같음 (마크업 0 · 마크업을 넣어도
지문·축 불변은 시험으로).  **[H]** 17단계 적용 검증 (15단계 + 마크업 → 목록·카운팅·Excel + 피드백 zip).

## 5-이전. 41회차 (TC2 비용 · 유령 원형 몸체 · 꾸러미)

**[B] TC2 파선 탐색 비용** — 40회차가 파선 버블(UAD 벤더 스키드 계기 35행)을 세우면서 TC2 가 563 → 1674초 · 7.2 → 8.9G 가
됐습니다.  원인은 SHX 획이 **꼭짓점을 공유**해(끝점 41,852 중 유일점 8,283) 허용치 안 쌍이 22.8M 으로 불어난 것이고, 유일점으로
먼저 접고(거리 0 은 어떤 허용치에서도 이웃) numpy 로 이으니 **같은 답(15장 윤곽 동일)** 에 TC2 장당 19~24초 → 1.4~1.6초 입니다.
회귀 TC2 **623초 · 7.5G** · UAD `877de403` · 184행 그대로.

**[C] 유령 원형 몸체 (TC2 534 · UAD 228 · SADARA 8)** — 원인은 거리 조건의 부재가 아니라 **범례 유도의 좌표계**였습니다.
회전된 문서에서 범례 획을 회전 전 좌표로 읽어 끝막대 대신 표 괘선을 잡았고(`bar_reach_radii` 87·84·116 ↔ AL NOUF1 2.8),
그 값이 거리 조건을 사실상 꺼 놓았습니다.  획을 표시 좌표로 읽고 끝막대·틱을 심볼 자신의 것(양쪽 최근접)으로 잡자 네 범례가
2.8~3.3 반지름으로 모입니다.  판정 코드는 0줄 · AL NOUF1 은 회전 0 이라 한 점도 안 움직입니다.

**[E] 이 꾸러미** — 15단계 적용 검증: 824행(옛 코드) → 편집 5칸 → 꾸러미 → 824행 열림 ·
편집 5/5 생존 → 새 분석 **1037행 · Q'ty 1931 · 지문 `fb85b039`** ·
최대 RSS 5.22G → 양식 **885 / 76 / 23** → 축2 99.3 / 94.4 → 빠른 시험 · UI 24 → 실 DB sha256 불변 → 합성 둘(타이틀블록 못 읽는 PDF 는 멈추고 · 태그 붙은 밸브 PDF 는 완주 78행).
★ 렌더가 잡은 것 하나 — TC2 에서 빠진 유령 행 1개는 진짜 공압 제어 밸브를 가리고 있었습니다(나비가 대각선 토막이라 몸체가 아니고 유령 위에 돔이 붙어 우연히 행이 됐던 것).  그 밸브는 이제 행이 없으며 "제어 밸브를 따로 그린 도면" 항목으로 다음 회차에 봅니다.

## 6. 다음 개발 사항 (로드맵 · `docs/state_and_roadmap.md` §5)

| 순서 | 항목 | 근거 · 게이트 |
| --- | --- | --- |
| 1 | **몸체 검출기 결함** — 높이 2pt 선을 BUTTERFLY 몸체로 받음 (TC2 p10 기준선 1행 · 창을 열면 p18·UAD p30) | §5.  고친 뒤 `seg_span` 을 다시 잰다.  게이트: AL NOUF1 불변 |
| 1' | **유도 구현 목록 A** 남은 것 — `brk_*` 5(체인 파선 허용치) · C 15 | 35회차 목록.  게이트: AL NOUF1 불변 + TC2·UAD 축3 상승 |
| 2 | **경보 접미가 붙은 앵커 낱말** (`LSA++` · `PIA+,-`) — UAD 미검출 36 중 21 | 어휘는 UAD p4 ISA 표가 정의.  두 규칙이 한 벌(접미 제거 + 리뷰 메모 제외).  `anchors` 표를 런타임에 조립한 뒤 |
| 3 | **버킷 둘** — `_connector_labels` 5pt · `_notes_lines` 7pt | 줄 간격이 촘촘한 문서에서 줄을 섞음.  27회차 답(같은 y 끼리만) 그대로.  게이트: 커넥터 218 / 70.2 불변 |
| 4 | 유도 구현 목록 C 15 (본문 실물 분포) · 조사 21 | worktree `/tmp/r35_strip` |
| 5 | 3급 폴백 13 (`actuator_stem` · `pneumatic` · `unit_multiplier_fallback`) 을 사람 지정 자리로 | 자리가 먼저, 삭제는 그 뒤 |
| 6 | 작은 것들 — 표지·목록 시트 REV `NONE` · 네모 상자 심볼(FLOW METER) 등록 · 부개정 `A1` 꼴 · `must_measure_by_hand` 를 하나로 · 건너뛴 장 사유 · 폴백에 누구의 값인지 | 30·32회차 |

**하지 않기로 한 것**은 `CLAUDE.md` §3 과 로드맵 "하지 않기로 한 것" 그대로입니다
(배관 그래프 순회 · 형상 매칭 · 태그로 SCOPE · 별표를 깊이/낱말/bbox 로 거르기).

## 7. 실무 판단 대기 (사용자가 답해야 진행)

1. SADARA·TC2·UAD 의 유닛 승수 값 (§4-2).
2. SIMILAR 장의 배수 뜻 (§4-4).
3. UAD Description 을 태그로 지을지 — 태그만 답한 102행 · 갈린 19행은 렌더로 태그가 맞았음.
4. UAD `VENDOR`(이름 없음) 30행 — 그 장 NOTES 가 정의하지 않음.
5. 3급 폴백 13 을 언제 어떻게 사람 지정으로 옮길지 · `glyph_sizes` 를 자기 프로필 문서에도 얹을지.
6. 축3 ⑥ 분모(기록된 유도 / 적용된 유도).
7. `CLAUDE.md` §5 의 기존 6건 (PI 계상 관행 · 밸브 문형 · LS 문형 …).
8. (44회차) 마크업 색 규칙 (b) — 색은 SCOPE · 표시는 모서리 ✚/✕ 로 두었습니다.  다른 안이 낫다면 말씀해 주세요.
9. (44회차) Excel 표시 자리 — REMARK 앞머리로 두었습니다 (행 칠하기는 Rev.A 무음영 규칙과 충돌).
10. (44회차) 축5(마크업 정답지) 추가 여부 — 판단만 냈고 더하지 않았습니다 (`out/round44/1_보고서.md`).
11. (44회차) AL NOUF1 p48 — PORT DICKSON 장이 (2/2) 자리에 들어 있고 AL NOUF1 의 (2/2) 는 없습니다.
