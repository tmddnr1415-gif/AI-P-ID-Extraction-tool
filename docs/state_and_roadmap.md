# 현황과 방향 — 새 세션이 먼저 읽는 문서

작성 2026-09-11 (30회차 직후) · **갱신 2026-09-13 (38회차)** · 갱신 주체: 회차를 닫는 사람

**읽는 순서**: 이 문서 → `CLAUDE.md` §2(원칙) · §9(범용성) · §10(증거 등급)
→ 필요한 회차의 전문.  `CLAUDE.md` 는 **회차별 기록**이라 길다.  이 문서는
**지금 상태와 다음에 무엇을 할지**만 담는다.
**구조 · 성능 · 범용성**은 `docs/program_overview.md` 가 따로 담는다.

---

## 1. 이 도구가 하는 일 (한 문단)

P&ID PDF 를 읽어 **계기·밸브 리스트**를 만든다.  입력은 도면 PDF 와 발주처의
**빈 양식**뿐이다.  심볼 검출 · 수량 · 스코프(공급 주체)는 **규칙만** 쓰고
LLM 을 쓰지 않는다.  판정에 쓰는 값은 그 도면의 **범례 · NOTES · 타이틀블록**
에서 런타임에 읽는다.  읽히지 않으면 **비워 두고 사유를 남긴다** — 지어내지
않는다.  사람은 검토 UI 에서 사유가 붙은 칸만 보면 된다.

---

## 2. 지금 상태 — 네 프로젝트

`python3 spike/regression_3p.py` (순차 · 약 22분) 하나로 전부 잰다.

| 프로젝트 | 도면 | 행 | 지문 | Q'ty | 축3 | 최대 RSS | 역할 |
| --- | ---: | ---: | --- | ---: | ---: | ---: | --- |
| **AL NOUF1** | 58장 A1 | **1037** | **`fb85b039`** | 1931 | **95.1** | 4.9G | **목표선.** 발주처 리스트가 있는 유일한 프로젝트 |
| SADARA | 9장 | 82 | `0767ba79` | 0 | 75.4 | 0.9G | 교차검증 (프로필 미지정) |
| TC2 | 60장 A3 | **684** | **`2ce3e724`** | **3991** | **90.4** | 7.2G | 다른 회사 양식 · 36회차 [D] Q'ty 3047→3167 · 37회차 도면번호 유도로 p25~28 4장 회수(607→684) · **38회차 [D] Typical 참조로 3783→3991** |
| UAD | 32장 A3 | 149 | **`eb1ee3ee`** | 149 | 83.1 | 3.2G | **1급(태그 인쇄) · SHX 글꼴** · 38회차 [F] p23 PDIT 2행 → SCT |

**AL NOUF1 만 정답지가 있다** — 축1(전량) **99.3 / 70.5** · 축2(SCT) **99.3 / 94.4**
· 발주처 양식 **885 / 76 / 23**.

**38회차** — 입찰/실행 선언 자리(스위치 하나) · UAD p11~17 은 다중 양식이 아니라 획 글자 · Typical 참조(TC2 +208) · TC2 MOV 0 의 원인(`act_box` 절대 pt) · 별표 방향 셋 이상.  AL NOUF1 불변.

**36회차** — `anchors` 룰셋 재바인딩 · 승수 낱말 문지기 제거 · 유닛 표기 꼴을 도면에서 배움(TC2 p33·p34 x4) · 삭제 24 적용.  AL NOUF1 불변.

**35회차(시험 · 엔진 0줄)** — "외워둔 값" 199개를 worktree(`/tmp/r35_strip`)에서 전부 지우고 유도만으로 다시 냈다.
**61개는 지워도 AL NOUF1 이 `fb85b039 · 1037 · 1931` 그대로**이고, 나머지 138개가 §5 의 다섯 목록이다.  본선 넷은 시작·끝 같다.

### 축3(식별률) — 지금 잃고 있는 점수 (지표별, 만점 대비)

| 프로젝트 | 도면번호 | 개정 | 태그→행 | **수량** | 심볼판정 | 파라미터 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| AL NOUF1 | 0 | 0 | 1.53 | 0 | 2.34 | 1.08 |
| SADARA | 0 | 0 | 0 | **16.67** | 5.82 | 2.15 |
| TC2 | 1.11 | 0 | 4.70 | 0 | 2.69 | 2.38 |
| UAD | 0.76 | **4.69** | 4.33 | 0 | 4.90 | 2.30 |

**★ 단일 최대 손실은 SADARA 의 수량 16.67점이고, 그것은 버그가 아니다** —
그 문서의 범례가 `PLANT×1` 만 정의하는데 도면 4장이 전부 유닛 `10` 이라
승수가 없다.  도면이 말하지 않으므로 **사람이 답할 자리**다 (§5 로드맵 1번).

### 사람 몫 — 검토 사유가 붙은 장 / 행

| 프로젝트 | 행 있는 장 | 검토 장 | 검토 행 | 가장 큰 사유 |
| --- | ---: | ---: | ---: | --- |
| AL NOUF1 | 45 | 27 | 152 | `DESCRIPTION_INCOMPLETE` 104 · `DESC_CCW_DIRECTION` 100 |
| SADARA | 4 | 4 | 82 | **`MULTIPLIER_UNDEFINED` 82 (전량)** |
| TC2 | 44 | 26 | 245 | **`MULTIPLIER_FROM_CONFIG` 203** |
| UAD | 14 | 14 | 149 | **`MULTIPLIER_FROM_CONFIG` 149 (전량)** |

**★ 세 프로젝트에서 사람 몫의 1위가 전부 승수다.**  검출을 더 고쳐도 이 숫자는
안 내려간다.

**★ 32회차 — 그 숫자가 화면까지 덮었다 → 33회차가 고쳤다.**  오버레이 색이
`needs_review` 를 SCOPE 보다 먼저 읽어(`pipeline._layers`) 검토 행이 **전량**인
SADARA·UAD 는 도면이 한 색(빨강)이었고 범례가 `SCT 공급 범위 0` 이라고 적었다.
33회차부터 **색은 SCOPE, 검토는 모서리 ● 표식**이고 범례는 `검토 필요 (그중) N` 이다.
**자리는 넷 다 맞다**(28장 캡처에서 어긋난 장 0 · A3 `/Rotate 270` 포함).
`out/round32/3_display.md` · `out/round33/4_색.md`.

**31회차가 그 434행을 세 번의 답으로 접었다** — 화면이 **유닛코드로 묶어**
묻는다 (SADARA `10` · TC2 `00` · UAD `00`).  AL NOUF1 은 범례가 답하므로
**묻지 않는다(0)**.  값은 사용자 몫이다.

---

## 3. 절대 불변 — 이것이 움직이면 되돌린다

```
AL NOUF1   지문 fb85b039 · 1037행 · Q'ty 1931
           축1 99.3/70.5 · 축2 99.3/94.4 · 양식 885/76/23
           Tag 칸 전부 `.....` · REV 58/58 · 글리프 H4/M32
SADARA     82행 · 0767ba79        (PID_PROJECT_CONFIG 지정하지 않는다)
TC2        607행 · c0a2d29d
UAD        149행 · db1a77d1
```

* 지문의 재료는 **행 아홉 칸**(`key` `tab` `type` `qty` `valve_type`
  `vendor_supply` `scope` `needs_review` `rect`) + `multipliers` + `legend`
  + 글리프 문자다.  `tag_no` · `description` · `evidence_tier` · `unit_notes`
  · `face_marks` 는 **지문 밖**이다.
* 지문이 움직였는데 검출을 안 바꿨으면 **원인을 칸 단위로 규명하기 전에 다음
  단계로 가지 않는다** (29회차 [B] 가 그 절차의 본보기).
* **측정 정의를 바꾸지 않는다** — 축1·축2 의 분모, 축3 여섯 지표와 가중(1/6).
  ⚠ 다만 **유도 항목이 늘면 축3 ⑥(파라미터)의 분자·분모가 같이 는다.**
  30회차에 AL NOUF1 95.0→95.1 · SADARA 75.3→75.4 가 그렇게 움직였고,
  **행은 한 칸도 안 움직였다.**  이것을 숨기는 쪽이 정의 변경이다.

---

## 4. 반복해서 나온 실패 유형 — **다음 회차가 가장 먼저 의심할 목록**

32회차까지 같은 모양이 계속 나왔다.  새 증상을 만나면 여기부터 맞춰 본다.
**32회차가 열 유형을 하나씩 대 봤다** (`out/round32/4_audit.md` C-3) — ④ 는 *이미 일어나고 있고*, ⑨ 는 *실제로 순서를 바꿔 돌려* 확인했다.

| # | 유형 | 신호 | 사례 |
| --- | --- | --- | --- |
| **①** | **찾고 있는데 안 쓴다** | "없는 기능" 처럼 보이는데 코드가 이미 그 값을 계산하고 버린다 | **11회** — 13(재분석 없는 로드) · 15 · 16(Block 규칙) · 18 · 22(measured_config) · 24(이력 표) · 25 · 27(노트 승수) · 28(SHX) · **30(버블 윤곽)** |
| **②** | **외워둔 값** | config·코드의 숫자·정규식이 특정 도면에서 잰 것 | 21(이력 표 좌표) · 26(승수 폴백) · **30(`formats.revision`)**.  목록은 `out/constants_by_source.md` |
| **③** | **검사기가 판정기와 같은 눈** | "전수 검사 0건" 인데 현장은 계속 지적 | 16→18→19 별표 SCOPE **세 번 반복** |
| **④** | **버킷이 줄을 섞는다** | 줄 간격이 버킷보다 촘촘한 문서에서 서로 다른 줄이 한 줄이 된다 | 27(`_notes_lines` 7pt) · 28~29(`_connector_labels` 5pt) — **둘 다 아직 남아 있다**.  ★ 32회차 실측: 줄 간격 중앙값 AL NOUF1 7.71 · SADARA 10.56 · TC2 5.53 · **UAD 3.50** — UAD 는 **72%** 가 5pt 미만이다 (소수가 아니다) |
| **⑤** | **"가장 가까운" ≠ "가까운"** | 거리로 이겼다는 것이 근거가 되지 않는다 | 10[G] · 17 C-4 |
| **⑥** | **높은 일치율 ≠ 인과** | 94% 맞으니 규칙으로 쓰자 | 29 [E-3] 계통코드 (§10-8) |
| **⑦** | **사각형은 심볼이 아니다** | bbox 모서리는 빈 자리인데 도면은 거기에 표시를 찍는다 | **30 [B]** (§10-10) |
| **⑧** | **글자가 글자가 아니다** | `get_text()` 가 비었는데 도면에는 글자가 있다 | 28(SHX 주석 10,729개) (§10-9).  32회차에 다른 경로를 셌다 — 네 문서 글꼴이 **전부 Type0**(Type3 는 표본에 없다) · **이미지는 UAD 36 · SADARA 45**(12장 기준)이고 **그 안에 글자가 있는지는 안 봤다** — 스캔 도면이 오면 미지수다 |
| **⑨** | **전역이 다음 문서로 샌다** | 섞어 분석하면 결과가 "그 전에 무엇을 분석했느냐" 에 달린다 | 21(`_HIST_ROWS`) · 22(`CFG`·`pidcache` 전역) |
| **⑩** | **시험이 어떤 데이터에서 도는지 시험이 말하지 않는다** | 침묵이 세 회차 이어진다 | 14(`UI 21`).  ⚠ 32회차 합성 도면 시험에는 그 장치가 없다 — `data/pid_total.pdf` 가 있는 기계에서만 만들어진다 |
| **⑪** | **표시에는 독립 감사가 없다** | 숫자는 다 맞는데 사람이 볼 화면이 틀렸다 | **32 [B]** — 별표 SCOPE 에는 19회차부터 독립 감사가 있는데, 오버레이는 이번에 사람 눈으로 **처음** 봤고 바로 결함이 나왔다 |

**절차로 굳은 것 넷**
1. **구현 전 실태 조사**를 회차의 첫 단계로 둔다 (①을 잡는다 — 다섯 회차 연속 범위가 줄었다).
2. **고치기 전에 네 문서를 전수로 재서** 무엇이 움직일 수 있는지 먼저 안다 (30 [B] 가 본보기).
3. **문턱은 분포의 빈 띠에서만** 정한다.  빈 띠가 없으면 그 문턱은 임의값이다.
4. **렌더로 눈으로 본다.**  숫자가 맞아도 도면이 아니라고 하면 도면이 옳다.

---

## 5. 로드맵 — 다음에 무엇을 할지 (근거 · 기대효과 · 게이트)

### ✔ 끝남 (38회차) — 입찰/실행 모드 · UAD 양식 실측 · Typical 참조 · TC2 MOV 원인 · SCOPE 별표

전문 `out/round38_result.zip`.  **AL NOUF1 · SADARA 불변.  TC2 만 [D] 로 `8a478417` → `2ce3e724` (Q'ty 3783 → **3991**) · UAD 만 [F] 로 `db1a77d1` → `eb1ee3ee` (p23 PDIT 2행 → SCT).**

| 항목 | 한 것 | 게이트 |
| --- | --- | --- |
| [B] 입찰/실행 | 선언 자리(장부 `mode` · 첫 화면 라디오 · 결과 띠) + 스위치 하나(`_attach_tags(declared_mode=)`).  실측은 언제나 돌고 다르면 `conflict` 로 말한다 | UAD `bid` 선언 → `db1a77d1` 불변 · 태그 0 · 후보 128 |
| [C] UAD p11~17 | **다중 양식이 아니다** — 한 양식 · 글자가 획뿐(p11~15·22) / 타이틀블록만 획(p16~18) / 꼬리 3자리(p32).  구현하지 않음 → 범위는 §8 7번 | — |
| [D] Typical | `app/engine/typical.py` — 원 표식(지름 < 그 장 버블) · 캡션(선이 안 닿는 원 + 글) · 상자(담는 후보 중 최소) · 참조(상자 밖 같은 id).  곱하는 곳 `_apply_typical` 하나 · `TYPICAL_AMBIGUOUS` | TC2 `qty` 10칸 · +208 = 예측 · 다른 셋 캡션 0 → 불변 |
| [E] TC2 MOV 0 | 원인 `act_box=(9.0,34.0)` 절대 pt (TC2 `M` Ø7.08 < 9).  코드 0줄 | — |
| [F] 별표 | 본문 별표 방향 **셋 이상** (`+` 획은 별표가 아니다) · p29 는 도면이 `*` 를 찍어 우리 값이 맞다 | UAD `scope` 2 · `vendor_supply` 5 (전부 렌더로 확인된 방향) · AL NOUF1·TC2 0 |
| [G] Nouf1 7 | 원인만 (`8_nouf1.md`) | — |

### ✔ 끝남 (37회차) — 도면번호 유도 · 창 상수 셋 실측 · 회사 PC 꾸러미 r37

* **[B]** `formats.drawing_no` 를 그 도면의 DWG NO. 칸에서 (두 장 이상 인쇄된 모양) — TC2 p25~28 회수 · 607→684 · 60/60 · 축3 90.4.
* **[C]** `ds.cap_span` · `dv.seg_span` · `tb.zoom` — **구현 전에 쟀다** (`out/round37_windows.md`).  범례가 버블을 그리지만 창의
  폭은 도면에 없고(돔 0.62배 ↔ 버블 0.67배 · 빈 띠 없음), 창을 해제한 회귀 두 벌로 **`cap_span` 은 어느 판정에도 안 닿는다**
  → 삭제.  `seg_span` 은 몸체 검출기 결함(높이 2pt 선)을 가리고 있어 유지.  `zoom` 은 실측 실패 0.  **유도하지 않은 것이 답이었다.**
* **[D]** `report.md`(반영 사항 + 다음 개발) · `out/PID_update_2026-09-13_r37.zip` (aac64ba 기준 130파일 · aac64ba worktree 에 풀어 HEAD 와 소스 대조 0).

### ✔ 끝남 (36회차) — `type_map` 재바인딩 · 승수 문지기 제거 · 유닛 표기 규칙 · 삭제 24

전문 `out/round36_result.zip`.  **AL NOUF1 불변 · TC2 만 [D] 로 `c0a2d29d` → `018315ef` (Q'ty 3047 → 3167 · p33·p34 40행 x1 → x4).**

| 항목 | 한 것 | 게이트 |
| --- | --- | --- |
| [B] `anchors` 표 | `ds.ruleset_v3(cfg)` 한 곳 · `_rebind_config` 이 다시 만든다 (+ `formats.unit_code_*`·`system_code_chars`·`text.dedup`).  35회차 "시험불가 25" → **전부 ㉣**.  import 때 굳는 값 37 중 17 이 아직 밖에 있다 (D·E 등급 · `3_type_map.md`) | 넷 불변 |
| [C] 승수 낱말 | IDENTICAL·SIMILAR·SAME·TYPICAL 을 판정에서 뺐다 — **번호 문단 + 유닛 표기 둘 이상** | 넷 Q'ty 불변 · 새로 잡힌 문단 0 |
| [D] 유닛 표기 꼴 | `learn_unit_forms` — 도면번호 유닛 자리와 NOTES 의 자기 코드 표기를 맞대 꼴을 배운다 (AL NOUF1 `#DD` 25장 · TC2 `D-D` 18장).  **맨 숫자는 배우지 않는다** | TC2 만 움직임 (예측과 같음) · 오검 0 |
| [E] 삭제 | 38 중 **24 적용** · 14 는 불가(3급 폴백 13 을 SADARA·TC2·UAD 가 읽는다 · `glyph_sizes` 는 낯선 경로에서만 ㉡) | E-1·E-2 각각 넷 불변 |

### ★ 남은 일 목록 — 36회차 갱신 (`out/round36/7_remaining.md`)

| 목록 | 개수 | 다음 회차가 하는 일 | 게이트 |
| --- | ---: | --- | --- |
| **유도 구현** | **21** | ㉣ — A 5 (`brk_*` 5) 부터.  ✔ `formats.drawing_no` 는 **37회차 [B]** 에 유도했다 (TC2 4장 회수 · 축3 89.1→90.4).  ✔ **37회차 [C]** 창 셋: `ds.cap_span` **삭제(적용)** · `dv.seg_span` → 결함 · `tb.zoom` → 조사 (`out/round37_windows.md`).  C 15 는 본문 실물 분포 | ★ AL NOUF1 불변 + TC2·UAD 축3 상승 |
| **사람 지정** | 129 | 91 (description 49 · excel 23 · valves 어휘 8 · 실무 판단 11) + **anchors 25** (`TT→TIT` 는 발주처 표기 — 키 집합은 범례 ISA 표가 감사할 수 있다) + **3급 폴백 13** (`actuator_stem`·`pneumatic`·`unit_multiplier_fallback` — 사람 지정 자리가 먼저 있어야 지운다) | — |
| **조사** | 23 | 미실측 19 · `formats.revision` · `glyph_sizes`(자기 프로필 문서에서도 설정에 없는 키를 얹을지) · **`tb.zoom`**(실측 실패 0 · 픽셀 문턱까지 19배) · **SADARA·TC2·UAD 범례 나비밸브 측정값**(`tick_reach_radii` 115/93/154 — 엉뚱한 원) | worktree `/tmp/r35_strip` |
| **삭제(열쇠)** | 1 | `project.code` — 지우지 않는다 | — |
| 결함 | **1** | **몸체 검출기가 높이 2pt 선을 BUTTERFLY 몸체로 받는다** (37회차 [C] — 기준선 TC2 p10 `[305.5,721.8,402.1,723.8]` 1행 · `seg_span` 을 열면 TC2 p18 77.6×2.0 · UAD p30 229.5×2.1 이 행이 된다).  `seg_span` 상한 60 은 이것을 가리는 우연이라 그 창은 결함을 고친 뒤 다시 잰다.  ⚠ 같은 모양의 값 17 이 `_rebind_config` 밖에 남아 있다 | ★ AL NOUF1 불변 |

㉮ 어휘·문형 **83** · ㉯ 범례·구조 **58** · ㉰ 도면에 없음 **33** (삭제를 뺀 174).  외워둔 값 199 → **174** (37회차 [C] `cap_span` 삭제).

**실무 판단 대기 (§8)**: SIMILAR 장(TC2 23장 + p33·p34)의 배수 — "SIMILAR P&ID" 가 같은 계기를 갖는가 · 3급 폴백 13 을 언제 어떻게 사람 지정으로 옮길지 · `glyph_sizes`.

### ✔ 끝남 (31 · 33회차) — 요지만

* **31회차** 승수 사람 지정(3급 경로) — `app/unit_multipliers.py` · `_note_factor(user=)`.  AL NOUF1 에 `x9` 를 넣어도 지문 불변(사람은 도면을 못 이긴다) ·
  SADARA 축3 75.4 → 92.0.  **값은 아직 사용자 몫** (§8).
* **33회차** 1급 밸브 태그 죽음(`_attach_tags` · `evidence["tag_no"]`) · SCOPE 색 = SCOPE 열 · 검토는 모서리 ● · 메모리 `release_ink`.
  전문 `out/round33_result.zip`.

### 2순위 — **경보 접미가 붙은 앵커 낱말** (UAD 미검출의 58%)

* **근거**: 30회차 [C].  UAD 미검출 36건 중 **21건**이 `LSA++`·`LS++,+,-,--`
  ·`PIA+,-` 처럼 수식자가 글자에 붙어 인쇄된 것이고, 앵커가 **낱말 통째
  일치**라 후보에도 못 든다 (미판정 목록에도 안 뜬다).
* **읽을 곳**: UAD p4 ISA 문자표 (§9 ①).  추측 어휘 확장 금지.
* **게이트**: ★ **AL NOUF1 1037행 · 축2 94.4% 불변**.
* **★ 31회차 [C] 가 먼저 쟀다** (`out/round31/5_alarm_suffix.md`) — 코드 0줄: 어휘는 UAD p4 가 정의한다 · AL NOUF1 의 위험 2건은
  사람이 쓴 검토 메모라 **두 규칙이 한 벌**(접미 제거 + 리뷰 메모 제외 — 후자가 없으면 축2 94.4 → 94.1) · (b) 갈래는 버린다.
* ⚠ 35회차 결함 목록(`anchors` 표)과 같은 자리다 — 표를 런타임에 조립하게 한 뒤에 하는 것이 순서다.

### 3순위 — **버킷 둘** (④ 유형 · 남아 있는 시한폭탄)

* `_connector_labels` **5pt** — TC2 지문이 28회차에 움직인 원인.  `_notes_lines` **7pt** — 별표 정의줄·공급자 이름을 아직 이것으로 읽는다.
* **읽을 곳**: 27회차가 수량 축에서 쓴 답 그대로 — **같은 y 끼리만** 묶는다.
* **게이트**: ★ AL NOUF1 커넥터 **218개 / `connector_reach` 70.2** 불변.

### 4순위 — 그 밖 (작지만 근거가 서 있다)

| 항목 | 근거 | 기대 |
| --- | --- | --- |
| 표지·목록 시트의 REV 를 `NONE` 으로 | 30 [D] §5 — UAD 9장이 `71` 이라는 없는 값을 낸다 | UAD 개정 23/32 → ?/32 |
| `formats.drawing_no` 6자 | TC2 56/60 · p25·p41 은 별표를 찾고도 행이 0 — **35회차 유도 구현 목록의 A** | TC2 1.11점 |
| 네모 상자 심볼(FLOW METER 8건) | 30 [C] ㉢ — 범례 확인 후 **등록 화면(18 [D])** 으로 | UAD 심볼판정 |
| 부개정(`A1` 꼴) | 30 [D] — 이력 표는 개정 표기의 **하한**이다 · 35회차 조사 목록 `formats.revision` | 다섯째 문서 대비 |
| 회사 PC 반영 꾸러미 | **10~35회차가 미반영**이다 (팀원은 824행 · 84.1% 도구를 쓴다) | — |
| `must_measure_by_hand` 를 여섯 → **하나**로 | 32 [C] — 24회차가 다섯을 유도로 바꿨는데 API·문서 둘이 아직 여섯을 적는다 | 새 양식을 받은 사람이 헛일을 안 한다 |
| 건너뛴 장의 사유를 바로잡기 | 32 [C] — 도면번호를 못 읽은 장을 `도면이 아님 (UNKNOWN)` 이라 적는다 (TC2 4장) | 조용한 것보다 나쁘다 |
| 폴백에 **누구의 값인지** 적기 | 32 [C] — 띠는 `유도 실패 N` 만 적는다 (SADARA 2 · TC2 2 · UAD 3) · 35회차 삭제 목록의 폴백 18 과 같은 자리 | 26회차 `MULTIPLIER_FROM_CONFIG` 규율을 범례 여덟에도 |

### 하지 않기로 한 것 (다시 제안하지 않는다)

`CLAUDE.md` §3 의 표 전체, 그리고:
* **태그로 SCOPE 판정** — 29회차가 근거 셋으로 닫았다 (§10-7).
* **전체 배관 그래프 순회** — 실측 세 번(13.1% · 4.9% · 10.4%).
* **형상 매칭으로 기기 검출** — 단일 축척이 없다.
* **깊이 문턱 / 낱말 위 / bbox 로 별표 거르기** — 30회차가 셋 다 버렸다.
* **35회차 삭제 목록을 지문 확인 없이 본선에 적용** — 목록은 58장 지문으로 세웠지만 적용은 회귀 넷을 다시 돌린 뒤다.

---

## 6. 구조 — 판정이 일어나는 **단 하나의 자리**들

같은 판정을 두 곳에서 하면 언젠가 갈린다.  아래는 "여기 하나" 로 못박아 둔 것들이다.

| 판정 | 한 곳 |
| --- | --- |
| 마크 **자리** | `detect_symbols.in_mark_window` |
| 마크 **소유자** | `detect_symbols._mark_gap` (유클리드) |
| 마크가 **심볼 글자인가** | `detect_symbols.on_bubble_face` (30회차) |
| SCOPE 한 값의 뜻 (화면) | `app.js` `scopeFacts` / (서버) `excel_out.in_client_scope` |
| 오버레이 색 | `pipeline._layers` — SCOPE 열과 **같은 값**을 읽는다.  ★ 33회차부터 **검토 사유는 여기서 읽지 않는다** — 항목의 `needs_review`·`reason` 에 따로 실리고 화면(`app.js drawOverlay`)이 모서리 ● 표식으로 그린다.  옛 저장의 `REVIEW` 는 `itemScope()` 가 그리드 SCOPE 열에서 되찾는다 |
| 1급 태그 붙이기 | `pipeline._attach_tags` **하나**.  근거는 `evidence["tag_no"]` (행 필드와 같은 이름).  `evidence["tag"]` 는 **다른 것** — 밸브의 버블 글자(문자열, `type_display` 가 읽는다).  33회차가 이 둘이 한 열쇠를 쓰다 죽던 것을 갈랐다 |
| 일괄 적용 사정거리 | `app.js` `sameSentence` |
| config 재바인딩 | `pipeline._rebind_config` (`_reconfigure` 와 되돌리기가 공유) |
| 측정축 정의 | `spike/accuracy.py` `AXES` (축1·축2) · `spike/identification.py` (축3) |
| 회귀 프로젝트 목록 | `spike/projects_3p.json` (**한 항목 추가가 프로젝트 추가**) |

**파이프라인 순서** (`pipeline.analyse`)
```
open_pdf → _fit_layout(도면에서 재고 낯선 프로필이면 얹는다) → titleblock_glyphs
        → titleblocks(못 읽으면 TitleBlockUnreadable) → legend_rules(없으면 LegendUnavailable)
        → layout(가장 비싸다 · 전체의 55~80%) → 장마다 instruments/valves
        → describe → rows → store
```
`analyse` 는 `_own_config` 로 감싸여 있어 **이 분석이 바꾼 config 가 다음
분석으로 새지 않는다** (22회차 · 실패해도 되돌린다).

---

## 7. 새 문서(다섯째 프로젝트)가 왔을 때

1. `docs/new_form_checklist.md` 절차대로 한 번 올려 `GET /jobs/{id}/measured_config`
   를 받는다.  `project_*.yaml` 을 만든다.
   **★ 32회차 정정 — 이력 표에서 손으로 잴 것은 `hist_row_inset` 하나다.**
   24회차가 나머지 다섯(`hist_rule_y`·`hist_rule_x0_max`·`hist_rule_x1_min`
   ·`hist_rev_col`·`hist_date_col`)을 유도로 바꿨고 세 프로젝트 전부에서
   `DERIVED` 로 나온다.  그런데 **API `must_measure_by_hand` 와
   `new_form_checklist.md` §3 이 아직 여섯을 적는다** — 고치는 것은
   다음 회차 몫이므로, 그때까지는 이 줄을 믿는다.
   `hist_row_inset` 만 사람 몫인 이유는 **도면이 그것을 그리지 않기**
   때문이다 (칸을 읽을 때 안쪽으로 물리는 여백).
2. `spike/projects_3p.json` 에 **한 항목**을 더하고 회귀를 돌린다.
3. **24회차가 남긴 재확인 목록 넷**을 본다: ① 노트 열 안쪽 괘선이 있는가
   ② 머리글 행이 개정 행보다 몇 % 높은가(빈 띠 유지) ③ 이력 띠가 둘인가
   ④ REV 칸에 개정 삼각형을 그리는가.
4. **30회차가 더한 것 둘**: ⑤ 개정 표기가 글자인가 숫자인가, 부개정(`A1`)이
   있는가 ⑥ 글자가 SHX(획)인가 — `page.get_text()` 가 비면 주석을 본다.
5. 등급을 판정한다 (`app/engine/tags.py`) — 태그가 인쇄돼 있으면 1급이다.
   **단 SCOPE 는 언제나 2급(별표·NOTES)이다** (§10-7).
   **★ 32회차 경고 — 1급인데 밸브 사각형 안에 태그가 인쇄돼 있으면 지금
   분석이 죽는다** (`AttributeError` · §5 1순위).  UAD 가 안 죽는 것은
   그 문서의 밸브 3행에 태그가 안 붙은 우연이다.
6. **도면 위 표시를 눈으로 본다** (32회차 [B]).  `spike/shot_overlay.py`
   가 장을 **고르지 않고**(균등 간격) 찍는다.  ⚠ 승수를 아직 안 답한
   프로젝트는 **전 행이 검토 필요라 도면이 한 색(빨강)이 된다** — 색이
   틀린 것이 아니라 한 색이 다른 색을 가리는 것이고, 지금은 그것이
   새 프로젝트의 **기본 상태**다 (§5 1순위).
7. **무엇이 조용히 폴백했는지 본다.**  범례 띠가 `유도 실패 N` 을 적는다
   (SADARA 2 · TC2 2 · UAD 3).  다만 *누구의 값으로 대신했는지* 는
   안 말하고, `brk_max_mark` 는 재고 나서 안 쓴 사실이 결과 json 에만
   있다 (32회차 [C]).

---

## 8. 실무 판단 대기 — 사용자가 답해야 진행되는 것

| # | 물음 | 무엇이 걸려 있나 |
| --- | --- | --- |
| 1 | **SADARA·TC2·UAD 의 유닛 승수** | 도면에 표가 없다.  SADARA 82행이 전부 빈칸이고 축3 16.67점이 여기 걸려 있다 |
| 2 | UAD Description 을 태그로 지을 것인가 | 태그만 답한 **102행**이 순증 · 갈린 19행은 렌더로 태그가 맞았다.  채점할 발주처 리스트가 없다 |
| 3 | UAD `VENDOR`(이름 없음) **30행** | 그 장 NOTES 가 그 표시를 정의하지 않는다 |
| 4 | 축3 ⑥ 분모를 "기록된 유도" 로 둘지 "적용된 유도" 로 좁힐지 | 30회차에 AL NOUF1 +0.1 · SADARA +0.1 이 이 정의에서 나왔다 |
| 5 | **오버레이에서 검토 사유와 SCOPE 를 어떻게 같이 보일 것인가** | 32 [B] — 지금은 검토가 SCOPE 를 덮어 두 프로젝트가 한 색이다.  후보 셋(테두리/해칭 · 층 분리 · 범례 겹쳐 적기) 중 고르는 것은 실무 판단 |
| 6 | `CLAUDE.md` §5 의 기존 6건 (PI 계상 관행 · 밸브 문형 · LS 문형 …) | 그대로 |
| 7 | **Typical 배수 × 유닛 승수** 를 곱할 것인가 (38회차 [D]) | 지금은 둘 다 곱한다(근거 패널에 두 인수 따로).  TC2 +208 이 전부 이 곱 |
| 8 | **UAD p11~17 범위** — ㉠ 획 사전 전체 / ㉡ 타이틀블록만(글리프 라이브러리 확장) / ㉢ 보류 (38회차 [C]) | 다중 양식이 아니라 글자가 획뿐.  권장 ㉡ — 30회차 [D] 와 같은 자리이고 도면번호가 지문 밖 |
| 9 | p8 MOV — 발주처 5 ↔ 우리 6 · AIT 행을 만들되 양식에서 뺄 것인가 (38회차 [G]) | 축2 게이트가 못 잰다 |
| 10 | [E] `act_box` 를 범례 원 지름의 비율로 — 다음 회차에 할 것인가 | TC2 MOV 0 → N · 게이트 AL NOUF1 불변 |

---

## 9. 한 줄로 남기는 원칙

> **도면이 말하는 것은 도면에서 읽는다.  외워두지 않는다.
> 도면이 말하지 않으면 사람에게 묻는다.  묻는 자리를 만들어 두는 것이
> 검출을 한 번 더 고치는 것보다 대개 낫다.**

30회차까지의 실측이 그 뒷문장을 가리키고 있다 — 사람 몫 1위가 세 프로젝트에서
전부 **도면이 말하지 않는 값**(승수)이다.
